# Line-ending normalization — cross-platform consistency (Windows/Unix/CI).
# The repository stores LF; each workstation checks out in its own convention.
* text=auto eol=lf

# Binary files: no normalization.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.ico binary
