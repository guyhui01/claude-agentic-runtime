# Changelog — claude-agentic-runtime

> Format [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) · Versioning [SemVer](https://semver.org/).
> Convention: each entry states the **Claude model** used.
> Note: the released version history below is kept **frozen in French** (audit integrity — published entries are never rewritten); new entries are in US English from now on.

---

## [Unreleased]

_Nothing yet._

---

## [0.11.0] - 2026-08-05 — A denial is not an answer: the param check stops reading negations as values 🚫
> Model: Claude Opus 5

### 🐛 Fixed
- **A denial is no longer read as an answer.** `paramFilled` had no general notion that a sentence denying its own subject is not a value: "No cloud provider has been chosen; AWS was ruled out" answered WF-003 `Cloud provider`, and ten of twelve probe sentences filled across eight manifests. The failure direction was the unsafe one — `paramsChecked: true` on a brief that denies the fact. A denial now governs a card value when both sit in the same **clause** and the denial does not follow the value. Measured before against after on the shipped code: **fifteen of seventeen probe denials filled, two do now — thirteen closed**; the fourteen-line over-reach control fills **14/14 before and after**; and **zero verdict moves over the 402 filled cells of the nineteen routed briefs**.
- **The label route carries the rule too.** A declared value that OPENS with a denial ("`Client: no name has been given`") is a refusal to answer, while one that merely contains a denial further along ("`Client: Northwind, ruled out last week`") is an answer with a caveat. `affirmativeString` only ever caught the bare sentinels, being anchored at `^`.
- **WF-005 `Audience` had two inverted verdicts.** It refused "the audience is our LinkedIn following" while accepting "there is no LinkedIn audience to address yet" — word order, not denial. A reverse-order branch repairs it; measured at **zero foreign fills**, so the cross-vocabulary matrix does not move.

### ✨ Added
- **`ParamSpec.absenceIsAnswer`** — the opt-out for the **five** specs whose card asks WHETHER rather than WHICH, where "no personal data is processed" *is* the answer: refusing them would leave the operator in a return loop they cannot exit, a worse defect than the one the guard fixes. Declared per spec, never inferred. Three (WF-003 `GDPR constraints (personal data)`, WF-007 `Sensitivities`, WF-010 `HR sensitivities`) were surfaced by the suite turning red; **two more only by the pre-push hardening pass, and both were already refusing a legitimate answer** — WF-006 `Competition`, whose fixture says "a sole source situation" and so cleared the guard by accident of vocabulary while "there is no competition on this deal" was refused; and WF-009 `Anti-fraud required`, whose detector carries an explicit `no (background\|reference) checks?` branch that the guard was cancelling.
- **Two versioned instruments** — `npm run measure:denials` (why a sentence fills: matched substring, route, owning clause, denial position) and `npm run verify:denials` (prototypes the rule to attack a policy before it reaches `src/`). Their predecessors lived in a scratchpad and died with the session.
- **Live-seed controls for the seven manifests that had none**, plus the closure of a truncation hole in the seed drift guard, which asserted presence and so passed a shortened copy (commits `f42b8ab`, `27f85c8`, previously undocumented here).

### ⬆️ Dependencies
- **Seven open Dependabot alerts down to one, four PRs merged** (`#51` postcss 8.5.25 · `#52` fast-uri 3.1.5 · `#49` ip-address 10.4.0 · `#50` hono 4.13.0). The triage was ordered by **reachability, not by advertised severity**: `src/` instantiates no HTTP or MCP server — the only `mcpServers` mention is a comment recording a deferred decision — so the five `hono` / `ip-address` / `@hono/node-server` advisories covered code that is never loaded. The one genuinely on the execution path was **`fast-uri`, reached through `ajv`**, a direct dependency that compiles the router's output schema. `#49` and `#50` were four commits behind `main`, so their green had been obtained on a stale base: both were rebased and re-checked before merging.
- **`@hono/node-server` (`#8`) stays open deliberately, and is now measured rather than reconducted.** The fix is `2.0.5`, a major jump from the `1.19.15` pinned upstream by `@modelcontextprotocol/sdk`; `npm audit fix` confirms the only route runs through that SDK. Forcing an `overrides` would pin a major transitive under a parent that never tested it, to close a Windows-only `serve-static` vector on a server this runtime never starts.

### 📝 Notes
- **Two denials stay open by decision, not omission**, and both are written into the probe. "AWS was ruled out" puts its denial *after* the value: closing it means admitting postposed participles, which gains one line and costs three false MISSING on legitimate prose — an argued judgement, **not a measurement**, since zero of nineteen briefs carry any such participle. "beginner is a guess" has no denial in its matched clause under any splitting tested; refusing it would require understanding the phrase.
- **The nineteen fixtures are not the repository's corpus.** The verification pass reported zero verdicts moved and the suite then turned five tests red, because the suite also builds AMENDED briefs that live only inside the test file. That gap is what surfaced both the `absenceIsAnswer` class and the comma boundary, without which one negation governed a whole enumeration.
- **A guard is only as good as the specs that opt out of it, and two of the five were invisible to the whole suite.** They were found by scanning the manifests' own comments for the policy they state ("the absence IS the fact the card asks for", "the card ends this line with yes/no"), not by any probe. A fixture can clear a new guard by accident of vocabulary and so hide the regression it causes.
- Falsified through `scripts/falsify.sh`: **ten cases, ten red, zero green** (seven on the guard, three on the opt-outs).

---

## [0.10.0] - 2026-08-02 — Dispatch V1: the ten parameter manifests, their end-of-project audit, and the defect class it left open 🔍
> Model: Claude Opus 5

### ✨ Added
- **Dispatch parameter manifests, second of ten: WF-002 "Delivery Agile SAFe".** Eight identity-card parameters derived from the card's `ART CONTEXT` block, pinned to catalog `v4.2.0`, and registered in `DEFAULT_MANIFESTS` — registration being what makes a manifest take effect, since an unregistered route reports an honest `paramsChecked: false` rather than failing. Taken first among the nine because its SAFe vocabulary (ART, PI, story points, cross-train dependencies) shares nothing with the WF-001 pilot: a detector that only worked on product-scoping prose fails loudly here rather than quietly. The coverage-matrix P02 brief is reused as the fixture and shows again that routing-qualified is not card-qualified: it identifies the train, its team count, its PI duration and the current PI, but states neither capacity nor dependencies, so it returns `PARAMS_MISSING` on those two.
- **Third of ten: WF-003 "AI Application Launch".** Eight `TECHNICAL CONTEXT` parameters, seven of them carrying an explicit enumeration on the card, so the detectors are the card's own families rather than a judgement about prose. Nothing here declares `defaultValue`: this card offers neither a sanctioned unknown nor an operator-profile constant, so every parameter is required. Checked against the dry-run oracle before its tests were written — the practice introduced with WF-002 — and it diverges in two places, both resolved at the card and both documented on the specification concerned. `Cloud provider` is stated in the brief's constraint list ("existing Azure tenancy"), which is why the technical detectors read `constraints` alongside the prose — the table lists it must-ask because it was computed on a thinner input, not because it missed the fact. `Target LLM` was classed as an operator-profile default, which conflates the model the **client's application** will run on — a sovereignty and cost decision the card lists "to fill in before starting" — with the model a live run of this runtime is routed to; it stays required. The result carries the same count the oracle predicted with a different composition. Its `GDPR constraints` line turned out to be the first case of a class, and produced the rule below.
- **Fourth of ten: WF-004 "AI Consulting Engagement".** Eight `ENGAGEMENT CONTEXT` lines become **ten specifications** — `Client [Name / Sector / Size]` is a conjunction and is split, so an operator who has already named the client is asked for the size, not for "Client". The hardest card so far, and not for its length: WF-002 and WF-003 describe technical objects whose vocabulary is unmistakable (an ART, a vector store), while this one describes a **commercial situation** in words any brief may use — a client, a duration, stakeholders, stakes. The exposure is therefore the false "filled", and two detectors were tightened against it before any test was written: `Engagement scope` no longer accepts a bare "support" ("decision support", "we support the team" are ordinary prose, not the card's Support engagement), and `Client AI maturity` no longer accepts a bare "advanced" (an "advanced RAG pipeline" says nothing about where the *client* stands; the word only counts beside the maturity vocabulary). Neither tightening is needed to make the fixture pass — both close a gap the neutral-prose probe cannot see, because these words are plausible domain vocabulary rather than absent vocabulary, and both are locked by a test that turns red when the detector is loosened back. `Expected deliverables` takes a detector rather than a `defaultValue`, unlike `Constraints`: intake guarantees the field is affirmative and non-empty, not that it names one of the card's four families. `submittedBy` is deliberately not mapped onto `Stakeholders` — filling a card parameter from the submitter's role would make the verdict depend on *who* submits the brief, the exact invariance the P19/P20 role probes proved live. The card's "— estimate" on maturity is not a sanctioned unknown: an estimate is a value. Against the dry-run checklist the divergences are the documented class — the qualified P04 fixture states the engagement window, the sponsor and two stakes that its sketch does not — and one prediction of our own was corrected by the check rather than the reverse: `Stakeholders` is answered by the constraint "executive readout expected", which names an audience.
- **Fifth of ten: WF-005 "Strategic Intelligence & Growth".** Seven `INTELLIGENCE CONTEXT` lines become seven specifications — the slash rule finds no conjunction here, and the one trap is typographic: in `[AI/LLM / AI consulting / …]` the slash of `AI/LLM` is internal to an item, not a separator. All seven are required, with no `defaultValue` and no sanctioned unknown, because each feeds a **backbone** step's input (STEP-01: scope, sources, horizon · STEP-02: audience · STEP-03: format, tone). Two card-level divergences are recorded rather than smoothed, both because they argue the other way: the dry-run table reads this row as four parameters covered by operator-profile defaults but never names which four, and it is not followed since the fixture brief carries a *client* — defaulting audience, tone or sources to the operator's own profile would assert the client's and invert the parameter's meaning; and the card's own quick-start block asks for only three of the seven, which is the strongest textual support that reading has, and is still not followed, a convenience prompt not being the card's contract. `Opportunity focus` carries the weakest **necessity** claim of the seven — its exact-match consumer is marked `[optional]` in the card's deliverables checklist — as distinct from `Horizon`, the weakest **detector**; if one specification here is ever demoted to optional-with-default it is that one and no other. The detectors were measured against the nineteen coverage-matrix briefs **before** any test was written, and that is what found two defects in the first draft: `Horizon` rejected the card's own word order (STEP-01 reads "Focus horizon: [short / medium / long term]", the horizon word *before* the value), and `Sources to prioritize` accepted "no monitored sources yet" — a false "filled" on a statement of absence. Both are locked by tests. `Horizon` requires the horizon word beside the value: accepting a bare quantity was measured to fill on the WF-004 fixture's "three-month engagement window" and the WF-010 fixture's "10-month project", a duration and a project length. The known cost is that the operator's natural phrasing *is* the bare quantity, so this line is often returned once — the safe direction, and the message names the card label, which designates the word to add. `LinkedIn` is refused as a prioritized source although the card lists it there, because the same token also serves the format and audience lines and would make three specifications non-independent; under-detecting "I watch LinkedIn" is again the safe direction.
- **Sixth of ten: WF-006 "Pre-sales / Commercial proposal".** Eleven `PRE-SALES CONTEXT` lines become **fourteen specifications**: `Prospect [Name / Sector / Size / AI maturity]` is a conjunction, and the split is not an interpretation — STEP-01's own input line reads "Prospect context: sector, size, AI maturity, history". `Known risks [Aggressive competition, floor price, hidden requirements]` uses commas rather than slashes, so it is one fact illustrated by examples and stays a single specification, the same shape as WF-001's `Constraints` line. This is the **first real use of `sanctionedUnknown`** in the codebase: the field was wired into route validation when the field-class policy was settled, and no manifest had exercised it until `Indicative budget [Estimated range / Not disclosed]`. Three judgements are settled at the card and recorded on the specification concerned. `Constraints` deliberately takes **no** `defaultValue`, unlike WF-001/002/004: their card line is generic (milestone, budget, GDPR, imposed stack), which intake's "constraints non-empty or justified" invariant genuinely satisfies, while this one is specifically about **hosting** (On-premise / Sovereign cloud / SecNumCloud / HDS) — the upstream invariant guarantees *a* constraint, never an infrastructure one, hence the name `infra_constraints`. `Competition [Firms competing / Sole source]` accepts a **negation as a filled value**, the exact inverse of the WF-005 sources guard: that card asks *which* sources, so "no monitored sources yet" is a false "filled", whereas this one asks *whether* there is competition, so an absence of competitors is the answer. And `Request type` refuses the bare token `competition` although the card lists it as a value, because the same word is the label of a **different line of the same card**; counting it bare would make two specifications non-independent, the WF-005 `LinkedIn` policy applied inside one card instead of across three lines. `Selection criteria` carries the weakest **necessity** claim of the fourteen, and it is recorded rather than smoothed: a search for "criteri" across the whole card returns exactly one hit, this parameter block itself, so no step names it as an input or an output; it stays required because the dry-run table classes it must-ask on **both** of this workflow's briefs, and it is the first candidate should one ever be demoted. `Decision-makers` applies a two-regime admission: **titles count bare** (a C-level or a named sponsor unambiguously answers who decides), **functions do not** — `procurement` names a department in "the procurement portal" as readily as a decision owner, so it counts only beside decision vocabulary. That is stricter than the shipped precedent, WF-004 `Stakeholders` admitting a bare `sponsor` and a bare `board` with no adjacency guard at all. The detectors were measured against the nineteen coverage-matrix briefs **before** any test existed, and that found one defect in the first draft: `Response deadline` accepted a deadline word beside a bare quantity and filled on the WF-004 fixture's "phase-one deadline in six weeks" — a delivery milestone inside an **already signed** engagement, which is the opposite of a bid response. That is the anchorless quantity WF-005 `Horizon` refused and the cross-vocabulary probe had to remove from WF-004 `Engagement duration`; letting a third manifest disagree with them would have been the same policy applied in two directions for the third time. The card's "RFP deadline — ISO 8601" annotation is a deliberate divergence: the ISO form is accepted but not required, since enforcing it would reject this workflow's own fixture ("the response is due in three weeks") — a format hint is a data-entry preference, not the card's contract.
- **WF-006 is the first workflow with TWO coverage-matrix briefs, so it gets a second independent measurement the previous five never had.** On P11 the manifest agrees with the dry-run table on all five of its must-ask lines, with no divergence at all; the single disagreement across both briefs is P06's `Decision-makers`, which the manifest reads as filled from "procurement-led process" — the requalification the table's own annotation describes, its row having been computed on a sketch that does not carry that phrase while the check runs on the qualified fixture that does. P11 also exercises the apposition that WF-004 `client_name` documents as an accepted miss ("Prospect Kestrel Mutual"): here the card's own field label licenses `Prospect` as an introducer, so the name is read — and WF-004 is **not** amended, its behaviour being reviewed, test-locked and shipped. Ten falsifications were run and every one turns a red test, four of them turning exactly one; the single mutation that reaches no dedicated case — admitting "mid-size" as a size class — is caught by the generic discrimination probe, which is what it is for. One of them is a correction of this batch's own test rather than of the code: the case named "refuses the bare token `competition`" initially left the P11 context intact, which says "first contact through **referral**" — another legitimate value of the same detector — so the line filled through a different branch and the case had never measured its subject at all.
- **Seventh of ten: WF-007 "Client Engagement Onboarding D1-D5".** Nine card lines become **eleven specifications** — `Client [Name / Sector / Size]` is the one conjunction here — and all eleven are required, with no `defaultValue` and no `sanctionedUnknown`, the first manifest since WF-005 with neither. The card carries no `Constraints` line, so the intake-guaranteed default of WF-001/002/004 has no subject. ⚠️ `D1 access [Badge, PC, VPN, tools, accounts — to validate]` is deliberately **not** a sanctioned unknown, which contradicts §1 of the dry-run listing it as this card's honest unknown: the accesses are enumerated ON the card, and what the annotation defers is validating them rather than stating them — the false-friend reading settled 2026-07-28, alongside WF-004's "— estimate". Three of this card's values are ordinary English words that the brief may use for anything, so the mapping is load-bearing and excludes two fields on purpose: `domain`, because this fixture's is "Management & Consulting" and `Consulting` is a value of the enumeration, and `expectedDeliverable`, because P07's reads "D5 scoping deliverables" where "scoping" names an output of this very workflow rather than the engagement bought. The bare word `team` is refused although the card lists `Team` among its values — it is the most common noun in the corpus, and counting it would leave that specification discriminating nothing, the WF-005 `LinkedIn` and WF-006 `competition` rule again. `Engagement location` carries the weakest necessity claim of the eleven and it is measured: searching the whole card for `location|on-site|remote|hybrid` returns exactly one hit, this parameter block, with no step naming it — and a second independent signal agrees, the card's quick-start asking for five of the nine lines and omitting this one. It stays required because §2 classes it must-ask, and it is the first candidate should one ever be demoted. Against that table the manifest reports two of its five must-ask parameters missing; the three divergences are the documented class (the qualified fixture states "medium duration", "sponsors identified" and "hybrid on-site engagement", none of which its sketch carries), and two run the other way — noting that §2 records only counts per class, never which parameter sits in which, so there is a total to compare against and no attribution. ⚠️ One phrase of that document pulls toward breaking an invariant: §3 reads "self-briefs (P07) make the submitter the answer source", and this fixture's `submittedBy` is "Consultant (self-brief)". Mapping it would make the verdict depend on WHO submitted the brief, the dependency the P19/P20 role probes falsified live; no specification maps it, and the policy suite asserts that for every manifest.
- **Measuring this manifest before writing a test for it found its first-draft defect, in the exact place the design claimed a guard.** `Engagement duration` filled on "engagement, sponsors identified, medium" — the class word anchored to an `engagement` sitting twenty-four characters earlier in an unrelated clause, rather than to the `duration` beside it. Proven rather than suspected: the same detector filled identically on "engagement, sponsors identified, medium-sized client", a brief stating a SIZE and no duration whatsoever. The reverse branch is now anchored on `duration` alone, the field noun that makes the order unambiguous, since the card writes the label first ("Engagement duration: medium") and that order is genuinely needed. Recorded on the specification because **nothing in the suite would have turned red** had the anchor been dropped: "medium" occurs exactly once in the nineteen briefs, "short" and "long" never, and the corpus states size as "mid-size"/"mid-cap" — so a bare class word passes the neutral-prose probe, the cross-vocabulary probe and this manifest's own fixture while filling on ordinary prose.
- **Two corrections to the shipped WF-004 manifest, both surfaced by writing its WF-007 sibling and neither visible to any existing guard.** `\bsponsor\b` carried no optional plural while its immediate neighbours in the same alternation did (`operational teams?`, `business units?`), so a brief saying "sponsors identified" was read as naming no stakeholder — an oversight rather than a policy, no card distinguishing the number and the WF-007 card writing `[Sponsors / …]` itself. Neither guard could see it: the cross-vocabulary probe records only the cells that ARE lit, and the policy table only pairs siblings that exist. Measured impact stated before the fix and confirmed by the snapshot diff afterwards: exactly one brief of the nineteen carries the plural, the matrix moves 38 → 39 cells, and nothing else moves. **The second correction completes a fix this repository already believed it had made.** The WF-006 test "does not read a delivery milestone of a SIGNED engagement as a response deadline" states in its own comment that the cross-vocabulary probe "removed this from WF-004 `engagement_duration`" — it removed the bare quantity, while the `phase` anchor kept the same string filling: P12 states no engagement duration anywhere ("Contract signed yesterday", "advisory engagement signed") and its only quantity is the constraint "phase-one deadline in six weeks", a delivery milestone. One card answered *no* on those words and the other answered *yes*, both green. `phase` is removed from both manifests, P12 joins the three foreign briefs that are not durations, and the sibling test asserting P12 "genuinely states an engagement window" — a premise the fixture falsifies — is corrected in the same lot, its reverse-order coverage moved to an amended brief rather than silently lost.
- **Eighth of ten: WF-008 "AI Act / GDPR Compliance Audit", the largest manifest — eleven card lines become NINETEEN specifications.** This card carries **four** conjunctions where the backlog announced three: `AI system audited [Name / Use case / Prod or project status]` has exactly the shape of `Client [Name / Sector / Size]` and is split on the same grounds, the backlog entry being a dated diagnosis that was tested at the card rather than trusted. `Suspected AI Act tier` is the third and last of the catalog's card-sanctioned unknowns, and **silence is not the unknown**: the annotation "— to confirm in STEP-01" licenses an explicit deferral, never the absence of any statement, so a brief saying nothing about the tier is returned. That is deliberately stricter than the dry-run table, which counts it covered — an intake that does not ask a compliance audit for its suspected tier is not doing its job. The slash/comma rule written on WF-007 was applied against three temptations that were dropped rather than added: `foundation model` and `RAG` on `AI model` (an architecture is not a model class), `ISO 27001` and `DPIA` on `Targeted frameworks` (a security standard and a deliverable are not frameworks this card lists).
- **Two structural calls, both recorded on the specification rather than left to be rediscovered.** `Client (geographic footprint)` is **the demotion candidate of the nineteen**, and the case is already instructed: searching the whole card for `geographic|footprint` returns exactly one hit, this parameter block, with no step consuming it, while the separate `Geography` line IS a verbatim step input — and §2 asks for it on neither of the two briefs, a weaker profile than WF-006 `Selection criteria`, which that table at least classed must-ask twice. It stays required because dropping a card fact silently is what splitting conjunctions exists to prevent, and because `defaultValue` is not honestly available here: the field means "can never be missing — an operator-profile constant or an intake-guaranteed value", and a client's footprint is neither, so a default would ASSERT a value. It must not read data-flow vocabulary or the split is decorative: `Geography` owns where the DATA goes, this one where the COMPANY operates, and a company present in France alone can still transfer to the US. Predicted before measuring and confirmed: MISSING on both briefs, since both state data geography and neither states a corporate footprint. Second call: `Data processed (sensitive)` and `(Art. 9 categories)` overlap by construction, Article 9 being the legal definition of sensitive data — and the second home brief is what proves they are nonetheless distinct, P08 filling both through "health data (GDPR art. 9)" while P20 says only "health data involved" and fills the category with the sensitivity statement still missing. Article 9 vocabulary also satisfies `Data processed (personal)`, a **one-way** implication stated on the spec: forcing the operator to restate "personal: yes" beside "health data" would be a redundant round trip.
- **Measuring this manifest before writing a test for it found FOUR first-draft defects, each qualified by the exact substring that fired.** `Client (geographic footprint)` filled on the NO_MATCH office-move brief through "office move of the Lyon site in" — a building read as a footprint. `AI system audited (use case)` filled on the WF-004 brief through "AI maturity diagnostic", a consulting deliverable rather than what a system does. `AI system audited (status)` filled on the WF-001 brief through "pilot budget capped for Q3", a budget line read as a deployment. And `Compliance deadline` filled on "deadline in three weeks" and "deadline in six weeks" — the **fifth** appearance of the anchorless-quantity policy in this repository, this time because the anchor list contained a bare `deadline`, the very word removed from WF-004 `engagement_duration` in this same lot. All four closed: the foreign matrix moves from 41 cells to 36, and eleven of the nineteen specifications fill on **zero** foreign brief. The client name is deliberately left missing on P08, which names its client in a genitive ("the triage chatbot OF Meridian Health"): adding `of` as an introducer was measured to read P08 and P17 correctly and also to fire on P05's "of GenAI", a technology read as a company, and a false name is the unsafe direction.
- **Ninth of ten: WF-009 "IT / AI Recruitment" — ten card lines, eleven specifications, and two slashes that mean opposite things.** `Role sought [Title / Level]` is a conjunction and is split; the slash in `Salary / day rate` sits in the LABEL — an alternative NAME depending on the contract type — and its value, `[Range or "to be defined"]`, carries none. Reading the second as a conjunction would have invented a twelfth specification for a fact stated once. This card has no `Expected deliverables` line, making it the first manifest that reads no `expectedDeliverable`. `Nice-to-have skills [Desired but non-blocking skills]` is the ONLY card-declared optional in the catalog, verified line by line across the ten cards: `Deliverables language` (WF-001/002) declares nothing on its card and is optional as an OPERATOR-PROFILE constant — two different justifications, one of each, where the tracker counted two of a kind, and that entry is annotated rather than rewritten.
- **This lot has ONE home brief where the previous two had two, and the gap is closed by an independent control rather than by a second brief written here.** It was the second brief that PROVED two overlapping WF-008 specifications distinct; inventing one now would be written by the same hand as the detectors. The replacement is the seed of the WF-009 live harness — authored 2026-07-12/13 for the spine and proven by two billed runs, months before any manifest existed — flattened into prose WITHOUT its field labels, the WF-005 method. **Eight of eleven fill**, and the expectation is deliberately NOT tuned upward: crafting the prose until it passes is the circular control WF-005 exists to refuse. All three misses are the seed's SHAPE — `Urgency` states "3 months", a bare quantity meaningful only under its field name, which reproduces the WF-005 `Horizon` finding on another card from a source that knows nothing about the policy; `Must-have skills` carries no requirement marker while the seed's nice-to-have list sits beside it; `Role sought (title)` names a title without saying anyone is being hired. ⚠️ And they are not all recoverable the same way: the first two are read by the label-declaration rule, the third is not, being half of a conjunction whose label carries its qualifier. A drift guard reads the harness file and turns red if the seed changes there.
- **Measuring before writing a test found three first-draft defects, all the same missing anchor.** `Role sought (title)` filled on the WF-004 brief through "CDO sponsor" — the client's own chief data officer, not a post being recruited. `Role sought (level)` filled through "upskilling of their STAFF" — the workforce, not the Staff Engineer grade. And `Urgency` accepted a bare quantity, the SIXTH appearance of that policy and the first time it was measured before being written rather than found afterwards. Closed, the manifest leaks **2 foreign cells over eighteen briefs**, the cleanest of the nine. Two detectors were deliberately NOT anchored, also on measurement: `Contract type` and `Assessment methods` fire on zero briefs bare, and tightening them would cost what over-tightening cost WF-008 `Geography` — refusing the card's own word for nothing.
- **`Must-have skills` requires a requirement marker for INDEPENDENCE rather than strictness**, since this card carries `Nice-to-have skills` too and a list of technologies says which is which only when something marks it. Measured: a naive technology detector fills on P09's "Kubernetes and MLflow stack" — the TEAM's stack, which is the subject of `Team context` two lines below, so the two specifications would answer each other. ⛔ And a falsification came back green on `required: false`, which turned out to be a real finding about the type rather than about this manifest: `required` and `defaultValue` MASK EACH OTHER. Measured — flipping the flag moves nothing, removing the default moves nothing, only removing BOTH reports the line missing. The redundancy predates this manifest and is kept, because the two fields say different things to a reader, but no falsification can guard either half alone and the file says so instead of implying otherwise.
- **A hardening pass on that manifest found the WF-007 defect class back: FIVE values admitted onto closed enumerations.** `portage` on `Contract type` — a distinct French arrangement the card does not list, and the OPERATOR's own employment situation, which has no business inside a card's values. `distributed` on `Location` — a TEAM property, and `Distribution` is a value of the WF-010 card, so it borrowed another card's vocabulary. `pair programming` and `assessment centre` on `Assessment methods`. And `head of` on `Role sought (level)`, where the comment argued in so many words that it "is not the same ladder" while the pattern accepted it — a fifth rung on a four-value enumeration, and the token whose only four occurrences in the corpus are `submittedBy` titles, the field mapped nowhere so that routing stays invariant to who submits. All five removed; the foreign matrix drops from 2 cells to 1 and the diff shows only that cell. ⚠️ **Two of the five came back GREEN under falsification and were not taken as validation**: `portage` appears in no brief and `head of` only inside `submittedBy`, so nothing in the corpus could measure them. A green falsification is a finding about the corpus, not a licence — a dedicated test now supplies the inputs the corpus lacks, and all three mutations turn it red. That test itself needed two corrections: P09's own `need` already answered `Role sought (level)`, the third time this repository has written an assertion whose subject was answered elsewhere in the same brief; and the first wording of the `head of` case did not even satisfy the mutated token's anchor, so it could not have failed. A case must be able to FAIL before it can pass.
- **The catalog teaches a form this runtime refused, and the fix is one rule rather than twenty-two patches.** Every card's quick-start block tells the operator to write `- Engagement type: [to fill in]`, and WF-006 and WF-008 inline the enumerations outright (`- Audit origin: [Preventive / Inspection / Due diligence]`). Card values are ordinary English — Scoping, Build, Training, Support, Team, Neutral, Demo — so every detector narrows them to a qualified form, which is right for PROSE and wrong for the form the card teaches. Measured across the eight manifests: **180 card values probed, 22 refused** when written the way the card writes them. `paramFilled` now also accepts a parameter that the brief ANSWERS BY NAMING IT, and all 180 read. Patching twenty-two regular expressions would have encoded the same rule twenty-two times; this reads the label the specification already carries. **Measured before and after on the nineteen fixture briefs: zero verdict flips** — the rule adds acceptance of a form the corpus does not use and changes no existing behaviour, which is why both snapshots are byte-identical. Two guards, both falsified red: the value must be AFFIRMATIVE, reusing the existing `NEGATIVE_SENTINEL` list rather than a second one, so "Engagement type: TBD" is not an answer; and a card-sanctioned unknown still passes, `sanctionedUnknown` being tested first, so the card's licence wins exactly where the card grants one. A conjunction's line label answers none of its halves — "Client: Acme" says which company, not which sector or size — and that exclusion is STRUCTURAL: the label matched is the spec's own `card` string, qualifier included. A conditional guard was drafted for it and **removed after falsification showed it changed nothing**, the case it claimed to stop being unreachable; the test that locks the property stays, and it turns red when the match is moved to the base label.
- **A re-verification pass ran a probe direction that had never been run, and it found two more defects before the push: does a detector read the value of its OWN card, written the way the card writes it?** No fixture can exercise that — a fixture is prose and a card is a form. `Geography` required the article in "outside THE EU" and therefore refused its card's own third value, "Outside EU with EU impact". `Audit origin` read `Preventive` in both word orders but `Incident` in only one, so "Audit origin: Incident" and "the audit follows a production incident" were both refused — an asymmetry inside one specification with no basis on the card, and the same class as WF-005 `Horizon` rejecting the word order of its own line. Both fixed, locked by a test and falsified red; the foreign matrix stays at 36 cells, so neither fix leaks. ⚠️ That test had to be ISOLATED before it could be believed: appending the phrase to P08's context left its own "EU only" in place, `Geography` filled through another branch, and the assertion held whether the fix was present or not — the context is now replaced. Extending the probe to all eight manifests returns 110 refusals out of 276, but it is dominated by artifacts (conjunction lines produce N² probes where "Client: Name." means nothing, and `Other` or `e.g. €200` are placeholders rather than values) and by DELIBERATE refusals that are documented and test-locked — bare `support`, bare `AI`, bare `competition`, bare `Team`. Whether a card label disambiguates a bare value is a policy to settle once across the ten manifests, not a fix to slip into a lot, and it is logged for the end-of-chantier audit rather than acted on here.
- **Eleven falsifications, all red, and one of them corrected a test instead of the code — twice over.** Restoring the bare `deadline` first came back GREEN, which read as "nothing measures this"; the diagnosis was that the mutation had targeted the quantity-first branch while the test exercised its mirror. Re-run on the branch the test does exercise, it turned red — and re-run on the quantity-first branch it stayed green, so that word order genuinely was measured by nothing. A third string was added to the test and the mutation now turns it red. A green falsification is a finding either way: here it named a real hole in the test rather than in the detector. Also dropped: a version-suffix branch on `AI system audited (name)` (`Triadex v2`), which made the policy audit's quantity marker fire on a digit sitting inside a proper noun — the class that marker's own definition excludes, and the same call as `one-?pager` on WF-007.
- **Tenth and last: WF-010 "Project Post-mortem / Lessons Learned" — nine card lines, twelve specifications, and the WF-009 slash trap in mirror image.** `Project / Incident : [Name + start/end dates]` puts its slash in the LABEL — a post-mortem covers a project OR an incident — and its conjunction is the `+` inside the values: a name AND its dates, two specifications. `Team involved : [Size / Distribution / Remote or on-site]` is a conjunction of the ordinary kind, three. `Available data : [KPIs, metrics, meeting minutes, logged incidents]` separates with commas, so those are examples of one fact and stay one specification — the WF-001 `Constraints` precedent. The block carries none of the three licence markers that exist in the catalog, so there is no sanctioned unknown. **All ten manifests are now registered in `DEFAULT_MANIFESTS`.**
- **`Expected format` is demoted to optional-with-default, a THIRD kind of default, and the measurement is what settled it rather than the argument.** It is neither card-declared like WF-009 `Nice-to-have skills` nor an operator-profile constant like WF-001 `Deliverables language`, but a WORKFLOW INVARIANT: STEP-06's `output_attendu` emits the complete report, the 1-page executive summary AND the presentation "if required" — the card defers that last choice to the step itself, so the run cannot lack the parameter. Measured as a required specification with a detector, it fills on **zero own brief and exactly one foreign one** (P08's "board presentation expected"), so its only cell in the whole corpus would have been a false one. ⛔ Mapping `expectedDeliverable` would not have fixed it: that field names WHAT is produced, not the FORM it takes, and the word "report" appears in none of the twenty briefs' mapped text.
- **The independent control is the live-harness seed, and it earned its keep by FALSIFYING a draft rather than by confirming one.** Written for the WF-010 spine months before any manifest, flattened without its field labels (the WF-005 method), it fills **10 of 12**; the two misses are the seed's own silence — it states no dates and has no HR field — and the expectation was not tuned upward. What it found: a second draft of `project_incident_name` required the determiner `the`, and the seed designates "Synthetic AI delivery project" with none. The anchor was a form assumption, not a property of how projects are named, and it was replaced by the house negation guard. A drift guard re-reads the harness file and turns red if the seed changes there.
- **Measuring before writing a test found three first-draft defects, each qualified by the substring that fired.** `project_incident_name` accepted any qualifier before a project noun and filled on P03's "before rollout" — a preposition promoted to a designation — and on P15's "…this is ongoing operations staffing, NOT A bounded project", reading a project name out of a sentence denying there is one; `programme`, `rollout` and `migration` went with it, being new members of my own invention on a line the card labels `Project / Incident`. `Client stakes` taken bare fills on **eight of the nineteen briefs that carry one** (P01, P06, P09, P11, P12, P14, P16, P17) — `deadline` is the token WF-006 `Response deadline` owns and `scope` is what a scoping mission is called — so a stake must now be MATERIALIZED ("four months late"), and three of the four card values are refused bare, which is stated on the specification rather than left silent. `Team involved (remote or on-site)` taken bare fills on two foreign briefs and no own one; P09 writes "platform team of eight, HYBRID Paris" eleven characters apart, so any window counted in characters would have caught it and the class separator is refused inside the window instead.
- **The card-value pass — does a detector read its OWN card's values, written as the card writes them — found three more, and one of them was structurally unreachable.** `\b[<>]` CAN NEVER MATCH, a word boundary cannot sit between a space and `<`, so "< 3 months" and "> 12 months", two of the three values of `Project duration`, were unreadable in the very draft whose comment claimed to read them. `Report audience` required "general public" and therefore refused its card's own `Public`. And `Closeout type` read the bare word `incident` while the card's own `Available data` line offers "logged incidents", so a brief listing its evidence would have stated its own closeout type — two specifications of the same manifest answering each other, found by this pass and not by any fixture. ⚠️ Widening `Public` does NOT cost nothing, and a first draft of that comment said it did: a scratch measurement had silently dropped the `context` of the three briefs whose value wraps to a second line (P01, P05, P19). The versioned instrument, which reads the fixture objects rather than their source text, showed the P05 cell immediately.
- **A defect in WF-007, shipped and test-locked, that no existing guard could see.** `engagement_location` carried `distributed team` while its card enumerates exactly `[On-site / Remote / Hybrid]` — a new member on a closed enumeration, and worse, borrowed from the WF-010 card where `Distribution` is a value. The identical token had been removed from WF-009 `Location` for that written reason; this copy survived the same correction. Nothing could catch it: the cross-vocabulary matrix RECORDS the cell it lit as a legitimate crossing, and the policy table pairs by name convention, where `location` and `engagement_location` share no stripped prefix. Impact measured before the edit and confirmed by the diff — the matrix drops from 41 cells to 40, the single removed cell is P10, and P07 still fills through "hybrid on-site engagement", so no verdict moves.
- **The policy audit's `negation-guard` marker was about to produce its first false positive, and it is corrected against a written definition rather than against a plausible count.** It tested for `(?<!` alone, which was exact only for as long as every lookbehind in the set happened to be a negation — it was, five times over — until WF-010 `closeout_type` used one for LEXICAL exclusion. The marker now requires a negation word; the five true positives are preserved and the false one is gone. Same class as the `one-?pager` reading removed from `quantity-vocabulary` on WF-007: a table that looks authoritative is worth less than one that is right.
- **Ten falsifications, all red, and each on a DEDICATED test.** One of them was not, at first: restoring a bare quantity on `Project duration` — the seventh appearance of that family — turned only the cross-vocabulary snapshot red, so a named assertion was added (P02 states "PI of 10 weeks" and "2-week sprints", a cadence and not a project duration) and the mutation now reddens it too. A falsification also corrected a test rather than the code: the first amended-brief case wrote "a team of nine, working on-site" and stayed `PARAMS_MISSING` because the comma guard refused it — the detector was right and the test prose was wrong, and the cost of that guard on ordinary phrasing is accepted deliberately. Both snapshots were regenerated one file at a time and read as diffs: the matrix adds a WF-010 section of 6 cells over 5 briefs and moves nothing else except the WF-007 cell above.
- **A pre-push verification pass ran a probe direction never run before, and it found the cost of a structural decision nobody had measured: the CARD'S OWN QUICK-START FORM, filled and sent through the check, fills 10 of 12.** The two misses share one cause. The label-declaration rule matches the SPEC's `card` string, qualifier included, so a conjunction's halves are never served by the card's BASE label — "Team involved:" answers none of its three. That exclusion was settled deliberately on 2026-07-31, where a guard written for it was removed as a measured no-op; what had never been measured is its cost on the form the catalog teaches an operator to use. The halves fall back to their prose detectors, and two of five are not read: `Team involved (remote or on-site)` refuses "9 people, distributed, hybrid" through the comma guard earned against P09, and `Project / Incident (name)` does not read "Atlas migration", only this card's own two nouns counting. Both are the safe direction and `PARAMS_MISSING` names the exact half to restate, so nothing is patched — widening either detector was measured and refused (`migration` is what filled on P03's "before rollout"; dropping the comma guard re-admits P09). A test now RECORDS the behaviour, and both mutations turn it red. **The systemic question — conjunctions versus the label rule across the six manifests that carry one — is logged for the end-of-project audit rather than answered in this lot.**
- **Two published figures were wrong and are corrected, both traced to a scratch corpus that was not merely incomplete but CONTAMINATED.** The ad-hoc flattener used during the analytical hardening dropped the `context` of the three briefs whose value wraps to a second line, AND spilled one brief's text into another at the tail — so `Client stakes` taken bare was reported filling "nine of the twenty briefs" when the true figure is **eight of the nineteen that carry a brief**, the phantom being a neighbour's words counted as P20's. The gap between "team" and "hybrid" in P09 was published as fifteen characters; it is **eleven**. Neither changes a decision — eight foreign fills still settle the anchor, and eleven characters still defeats a `{0,20}` window — but a precise-sounding number is what makes a comment authoritative, and both were re-derived against a wrapping-safe parser checked record by record against the source.
- **A second verification pass caught this manifest breaking, three times, the criterion the same lot had just applied to its sibling.** A card's COMMA list is the card giving examples ("Badge, PC, VPN, tools, accounts"; "Social context, restructuring, post-incident, etc."), so a detector may add neighbours of the same kind; a SLASH list is the card's own enumeration, where a synonym of a listed value is fair — WF-004 reads `upskilling` for Training and `GDPR` for Compliance — but a new member invents a value the card does not offer. `onboarding` was accepted as an `Engagement type` and as an `Expected deliverable` although the word appears on this card only in its TITLE and its filename, never as a value: it promoted the workflow's own name to a kind of engagement. `operational` was accepted as an `Identified stake`, a fifth category beside Business / Technical / Organizational / Political rather than a synonym of any. Measured before removing, as for every change in this lot: both words occur in the nineteen briefs (`onboarding` twice in the NO_MATCH legal brief, `operational` once) yet neither produced a fill, so the corpus stays at 41 foreign cells and no verdict moves — what goes is a latent false "filled". Checked for the same defect across the shipped siblings rather than assumed local: WF-004's `Priority stakes` and `Expected deliverables` add synonyms only, and its `Stakeholders` line is the one carrying genuinely new members, on a fact class the card leaves open — logged for the end-of-chantier audit rather than changed here.
- **Three further defects the audits caught on this lot, two of them in the new manifest and one in its own test.** The policy table showed `operational teams?` in the WF-004 sibling and absent here while THIS card is the one listing `Team`, so the qualified form was added — a measured no-op, no brief carrying the phrase, the `CAC ?40` precedent; `business units` was deliberately not carried over, sitting in no card at all. The same table flagged `Expected deliverables` as quantity vocabulary, which traced to the "one" inside a `one-?pager` synonym the card never uses — a false positive of exactly the class that marker's definition warns about, removed by dropping a word that was not the card's anyway. And falsification corrected a test rather than the code: "does not let the brief's DOMAIN state the engagement type" asserted against the fixture's own domain, which the detector never accepts bare, so it stayed green whether `domain` was mapped or not; it now qualifies the domain text so that the assertion measures its subject. Nine falsifications in total, every one turning a dedicated test red. A dead alternative was also removed from `Sensitivities`: `no (particular|known|specific) sensitivit\w*` never fires, `sensitivit\w*` already matching the same sentence — the explicit absence counts as a value because the card's own word is required, not because a branch grants it, and a detector carrying alternatives that never fire reads as a guard being applied.
- **The two instruments that found the last three manifest defects are now versioned, because both were living in a scratchpad and vanished with the session that wrote them.** `scripts/falsify.sh` mutates one literal in one tracked file, runs the suite, and reports **which** tests turn red. It exists as a script rather than as a command typed per lot because the first harness of this shape produced **six false greens**: its substitutions were routed through `sed`, the escaping was broken, the mutations never reached the files, and the suite ran on intact code while reporting "0 failing test(s)" — indistinguishable from six guards that genuinely fail to guard. Nothing downstream can catch that, since the suite is green whether the mutation bit or not, so the guard has to sit on the **mutation**: the literal must occur exactly once, `git diff` on the file must be **non-empty** before the suite is allowed to run, and the file is restored from the index afterwards — which is why a file carrying unstaged changes is refused outright rather than silently overwritten by the restore. A refused case is not a result and never counts as a pass. Substitution is a literal replace performed in Node, with cases travelling base64-encoded end to end and the replacement applied through a function rather than a string, since a raw replacement would let `$&` and `$1` fire — the escaping *is* the defect being prevented. The harness was falsified against itself across its six paths before being trusted: a real detector mutation returns **RED with 38 named tests**; an absent literal and an eight-occurrence literal are both refused; a no-op substitution is refused, which is the exact case the first harness passed; an untracked file and a file with unstaged changes are refused; and a comment word no test measures returns **GREEN**, which is how a finding is reported rather than a pass.
- **`scripts/measure-manifest.ts` is an instrument and not a guard, and the distinction is the point.** It asserts nothing and can never turn the suite red; its failure mode is not a false green but not being run. It earns its place because running it before writing tests has found, three times, a defect no later test would have looked for — each on a detector whose own fixture was about to make it green: WF-005 `Focus horizon` rejecting the word order of its own card, WF-005 `Sources to prioritize` filling on "no monitored sources yet", WF-006 `Response deadline` filling on a delivery milestone inside an already-signed engagement. A test written afterwards encodes whatever the detector already does; the measurement is what happens while it can still be wrong. It reads the shipped `paramFilled` verdict rather than reproducing it — a measurement that re-implements the rule it describes drifts from it silently, and would then describe a check that is not the one running, so that function is now exported for this purpose alone. For every foreign brief it prints the **exact substring that fired**, which is what the cross-vocabulary snapshot cannot give: that snapshot records which cells are lit, never why, and a foreign fill qualified by plausibility is how "budget capped for Q3" was read as a budget figure when the detector had matched the **3 of "Q3"**. It was calibrated against that snapshot before its output was used, and the calibration immediately disagreed — 51 cells against 17 on WF-001, 55 against 38 on WF-004 — resolving exactly to the `defaultValue` specifications, which fill everywhere by declaration and which the snapshot excludes as "measurable specs". Counting them inflates each manifest by a full column and buries the cells that carry information; the instrument now excludes them and states how many it dropped, and agrees with the shipped snapshot on **all six manifests**, cell for cell.
- **Per-family counts at the head of the policy-consistency snapshot, for visibility and explicitly not for proof.** They exist because a marker with **25% sensitivity** went unnoticed while its rows were on the page: three rows among thirty look exactly like three rows, whereas a count line moves visibly in a diff. No test asserts these numbers, deliberately — an expectation derived from the same predicate would only prove the marker equals itself. The first reading is worth two facts. `quantity-vocabulary` reports **12**, the ground truth the corrected marker was calibrated against. And `NO DETECTOR` and `default` both report **5** over the same five specifications, so every specification lacking a detector declares a default and none is structurally unfillable — an invariant of the fail-closed rule that was until now inferred from reading `paramFilled`, and is now measured.
- **A policy-consistency audit across every registered manifest, frozen as a reviewed table — built at six manifests rather than at ten, and deliberately.** Every other guard here judges ONE manifest against ITS OWN fixture, so a question of policy settled in opposite directions across two of them is invisible: both are green and each is locally right. That defect has occurred twice and was attempted a third time — "a bare quantity is not a value", refused by WF-005 `Horizon`, admitted by WF-004 `Engagement duration`, re-attempted by WF-006 `Response deadline`. The cross-vocabulary probe catches these only by luck, needing a foreign brief that happens to expose the divergence; this one reads the manifests themselves and puts the siblings side by side. It renders three tables — which brief fields each spec reads, which policy markers each carries (negation guard, quantity vocabulary, adjacency window, sanctioned unknown, declared default), and, for each fact carried by several cards, the vocabulary unique to each copy. Like the cross-vocabulary matrix it is **not** an assertion of correctness: cards genuinely differ, so a divergence is not mechanically a defect, and the value is in reading the diff. One hard assertion accompanies it, because it is an invariant rather than a judgement: `submittedBy` is mapped by no manifest — now a **measured** fact, obtained by calling each mapping with a distinct sentinel per field, rather than a sentence repeated in six comments. A second, "every required spec has a detector or a declared default", was drafted and then removed: it rebuilds machinery weighed and cut on 2026-07-28 as redundant, and the redundancy was re-falsified rather than taken on trust — deleting a required spec's `pattern` turns five existing tests red, the manifest's own happy-path among them. The table also records what it does **not** measure: it pairs specs by a name convention, so a divergence it cannot pair is a divergence it cannot see; its adjacency marker reads `{0,N}` only, so a quantity anchored by an immediately following unit noun shows no marker and the row is a prompt to read rather than a verdict; and it reads regex sources, not behaviour, which is the cross-vocabulary probe's job — neither subsumes the other. Timing argument, taken from the cross-vocabulary probe's own entry: at the end this would be a machine for *finding* divergences, meaning retrofits across ten files; here it is a machine for *preventing* them, since the four remaining manifests are written against a visible table.
- **What that audit found on its first read, including one asymmetry its author had not seen.** `CAC ?40` was present in WF-006 `Prospect (size)` and absent from its WF-004 sibling, with no basis on either card — a CAC 40 client is a size statement in any engagement; added to WF-004, and **measured as a no-op**, since no coverage-matrix brief contains the token. `from` was present as a name introducer in WF-006 only, and that one had been mistaken for card-licensed and lumped in with `Prospect`, which the WF-006 field label genuinely does license: `from` is generic, so WF-004 was simply failing to read names its own fixture carries ("Client brief received **from** Nordwind Insurance"). Added, and this one is **not** a no-op — it fills on three foreign briefs, moved the cross-vocabulary matrix from 35 cells to 38, and turned a shipped WF-004 test red, which is the guard working rather than a regression; the expectation was updated with its reason. And WF-006 `Prospect (AI maturity)` carried a comment claiming the same tightening as WF-004 while being materially wider — a legitimate divergence, since WF-004's card offers an enumeration to key on and this fact sits inside a conjunction with no list at all, but one that had to be written down rather than left to read as an accident. **The audit itself had to be made trustworthy before its output was acted on, and it took four corrections.** Its first two renderings raised false divergences by splitting alternations inside groups and by gluing the `b` of `\b` onto the literal following it, reporting one word as two divergences — a table that cries wolf gets ignored, which is worse than no table. Then its marker for the quantity family — the one policy question this audit exists for — was measured against a written definition rather than eyeballed, and found **25% sensitive**: it reported three specs where twelve carry quantity vocabulary, an authoritative-looking table with nine of twelve members invisible. Widening it to a bare `\d` swung it the other way to fifteen, pulling in a date literal and a model name, which would have been the harder error to notice. The definition is now fixed in the file and the implementation is checked against it, not against whichever count looked plausible. With the marker correct, the first full reading has a result worth stating: **no bare-quantity spec survives in any of the six manifests** — the five rows showing quantity vocabulary without an adjacency window all anchor the number to an adjacent literal or unit noun (`\d+\s+(developers|teams)`, `PI[-\s]?\d+`, `\d+\s*(employees|staff)`), verified spec by spec.
- **The honest `paramsChecked: false` path now has a permanent subject.** It was played by whichever real workflow had no manifest yet, which meant every manifest lot had to move the test forward — and the tenth would have left it with no subject at all. A synthetic resolvable route, deliberately absent from the registry, replaces it.
- **Positive controls written from the detector they test prove only that the detector is non-empty.** The first pass validated three WF-005 specifications on sentences derived from their own regular expressions — a check of two artifacts against each other, not against the world. Replaced by a source independent of the manifest: the seven values of the WF-005 live-harness seed, written months earlier for the spine and live-proven by its `completed` run, flattened into prose. Six of the seven fill, including the two the circular controls had "confirmed". The seventh is `Horizon`, and the reason is the seed's shape rather than a defect — it states `3 months` with the label carried by the field name, which flat prose does not have.
- **A cross-vocabulary probe over every registered manifest, frozen as a reviewed snapshot.** Each manifest is run against every *foreign* coverage-matrix brief — one whose expected route is another workflow, the four honest NO_MATCH briefs included — and the filled cells are rendered as a matrix committed alongside the test. It is deliberately **not** an assertion of correctness and must not become one: a filled foreign cell is not automatically a defect, since some facts legitimately cross cards (a brief stating GDPR *has* stated a compliance stake). What it refuses is an **unnoticed** change, so the value is in reading the diff when the matrix moves, never in the red/green — a new manifest necessarily changes it, and a reflexive regeneration would turn the file into a rubber stamp. Run at five manifests rather than ten on purpose: waiting for the tenth would let five more be written under a policy that may have drifted. One guard-the-guard assertion keeps a filtered fixture set or a shrunken registry from producing an empty matrix that reads as "no leak". Considered and dropped: a hard rule that no spec may fill on *every* foreign brief — measured against the defect that motivated the probe, it would not have caught it, the bare `AI` token filling on seven briefs and not on the office-move one.
- **First read of that matrix, and the reason it exists: four real defects in two already-shipped manifests, each qualified by the exact token that filled it rather than by inspection.** WF-003 `Monthly API budget` fills on the WF-001 brief through "budget capped for Q3" — the `budget … digit` branch matching the **3 of "Q3"**, a quarter label read as a monetary amount. WF-004 `Engagement scope` fills on a legal-drafting brief through the word "audit", whose only occurrence there is the **negation** "*not* an audit of an AI system" — the same class as the sources defect fixed in this batch — and on a model-pretraining brief through "training", taken from "research-grade *training* infrastructure", which is model training and not a client upskilling engagement. WF-004 `Engagement duration` fills on a **sprint length**, a **response deadline** and a **project length**, because its detector accepts a bare quantity and unit — exactly what was refused for WF-005 `Horizon` earlier in this batch, for exactly this reason. That last one is the finding that matters: not a typo, the same policy applied in two opposite directions across two manifests. Fixed in the next lot; the snapshot is committed **as found** so that the fix shows the cells closing in a diff rather than resting on a claim. Recorded and deliberately not narrowed: client name and sector fill nearly everywhere because every brief names a company, `AI app type` fills on the three chatbot briefs, `Stakeholders` on the briefs that do name a board, and WF-001 `Sector` fills on eleven of seventeen foreign briefs — it discriminates nothing, which is tolerable only because this check never claimed to validate the route.
- **A discrimination probe is blind to a detector keying on vocabulary its neutral brief happens to lack.** Widening WF-005's `Intelligence scope` to a bare `AI` token left the entire suite green: the probe's prose carries no AI vocabulary, and every other case involving that specification expects it filled. Corrected in the same lot with a case on the AI-dense WF-001 brief, which states not one intelligence parameter and must leave all seven missing. Seven falsifications were run against this manifest and each turns a **different** test red.
- **Slash rule: a bracketed `/` list means "or" on some card lines and "and" on others, and the manifest must state which.** `AWS / GCP / Azure` is an enumeration — pick one. `Personal data: YES/NO — Data location: EU/US` is a conjunction — supply both; so are `Name / Sector / Size` on WF-004 and WF-007, `Prospect` on WF-006, three lines on WF-008, and one each on WF-009 and WF-010. Reading one as the other inverts the semantics in one of two bad directions — an enumeration read as a conjunction never passes, a conjunction read as an enumeration passes on one fact out of three — and there is no safe default to fall back on, so the call is made from the card's wording and stated in a comment on the line concerned. A conjunction is now **split into one specification per fact** under a qualified label (`GDPR constraints (personal data)`, `GDPR constraints (data location)`), so `PARAMS_MISSING` names the half that is actually absent instead of answering "GDPR constraints" to an operator who has already supplied the location and leaving them nothing to act on. Consequence for planning: a manifest can hold more specifications than its card holds lines — WF-003 ships nine for eight — so the 81-parameter count read off the cards is a floor, not a total.
- **The 2026-07-19 dry-run table (§2 of the identity-card dry run) is consulted before every manifest — as a checklist of the parameters that are hard to obtain, not as an acceptance oracle.** It was computed against the coverage-matrix **sketches** (the raw stakeholder asks), while a manifest check runs against the **qualified fixture briefs**, which are richer: P02's sketch names no PI duration, P03's names no cloud provider, P04's names neither the sponsor role nor the engagement window. Those are two different questions on two different inputs, so a divergence is a signal to settle the point at the card — never a verdict on either side. An earlier version of this entry read those divergences as table errors and blamed a skipped `constraints` array; that diagnosis was wrong and is corrected in the document's own annotation rather than edited out of it. What the comparison does earn is the other direction: it caught a genuine defect in the code, where the first `ART name` detector required the proper noun to *precede* ART — encoding the word order of the card's example rather than what the field means — and would have asked the operator for a name the brief already carried.
- **A discrimination probe across every registered manifest.** Given prose that is grammatical, plausible and carries none of any card's vocabulary, every required parameter must come back missing; one that survives is keying on something other than its card. It covers each new manifest the moment it is registered, with no per-manifest fixture to write, and it measures the one failure the fail-closed detector cannot see — a pattern that is present but too permissive. Verified by falsification: replacing one WF-002 detector with a permissive `/.+/` turns the probe red.
- **A card's line label now answers its `(name)` half — settled once for the seven manifests carrying a conjunction, on measurement rather than argument.** The rule matched a spec's `card` verbatim, qualifier included, so no conjunction half could ever be served by the card's base label. That exclusion was deliberate and dated, but its **cost** had never been measured; measured on the quick-start form each card teaches, **9 of the 29 halves those forms answer came back MISSING anyway — 31%, and six of the nine were `(name)` halves**, because a proper-noun detector cannot read a name sitting immediately after the card's own label. Three options were measured: keeping the verbatim match (9 missing, no hollow pass); stripping any qualifier (0 missing, but "Client: Acme" then answers 3/3 halves on WF-004 and 4/4 on WF-008, and "Data processed: yes" answers all three **including** the Art. 9 category); stripping `(name)` only (3 missing, and a bare label answers exactly the name). The third is chosen, and it is a line rather than a heuristic: the label **designates** the identity, while sector, size, geographic footprint and Art. 9 category are further facts it does not state. The three residual misses are detector questions rather than label ones, one of them the comma guard earned against P09. The guard test is renamed and its old name is the lesson — it read "does NOT let a conjunction's line label answer ANY of its halves" while only ever asserting on sector and size, claiming a policy a third wider than its assertions. Also corrected: **seven** manifests carry a conjunction, not the six the tracker recorded — WF-003 `GDPR constraints` was missed. 31 halves across 108 specifications.
- **The policy table now pairs six specifications it could not see, closing a blind spot its own header describes.** It pairs by stripping an owner prefix, so `deliverable_format` (WF-005 Target, WF-006 Proposal, WF-010 Expected) and `stakes` (WF-004 Priority, WF-007 Identified, WF-010 Client) sat as six singletons, and section 3 skips singletons. ⛔ Not solved by a broader strip rule, which was tried first: stripping any leading qualifier collapses `team_size` onto `client_size`, and a team headcount is not a company size, so the table would begin raising divergences that are not ones — its own history records that a table which cries wolf gets ignored, which is worse than no table. Pairing is therefore a judgement, listed one specification at a time. The `stakes` group **reverses** a decision recorded on WF-007, and the reversal is the point: that spec read "deliberately NOT PAIRED, and the different name is the statement", which made the record of the decision the **absence of a row** in a table that pairs by name — a decision recorded as something invisible is one nobody re-examines. The three now sit side by side with disjoint vocabularies and the comment carries the reasoning instead: the card enumerations are disjoint, so the detectors must be, and nothing about pairing shares a detector. One hypothesis this raised was checked at source and was **wrong**, so no code moved for it — WF-007's `stakes|challenges|risks|drivers` are adjacency anchors requiring a card value beside them, not bare concept nouns accepted alone.
- **End-of-project audit of the ten manifests — every planned volet done, and one defect class left OPEN by it.** ⚠️ The word "closed" is deliberately not used: the audit's own method, applied once more to the audit's own lot, surfaced a class nothing here fixes — `paramFilled` has no general notion that **a denial is not an answer**, and on twelve sentences denying their own subject while naming a card value nearby, **ten fill**, across eight manifests ("No cloud provider has been chosen; AWS was ruled out", "this is not a partial failure and not an incident"). The failure direction is the unsafe one. It is **latent rather than active** — seven of twenty briefs carry a denial token, all in benign positions, and no current verdict is wrong — which is once again the corpus being unable to express the defect. Measured, recorded in the tracker as the next unit, and deliberately NOT patched spec by spec: two root-cause hypotheses were falsified within minutes (anchoring on one's own card-label word is shared by **48 of 108** specs, so it is the norm and not the defect; and proper-noun values are not immune, `cloud_provider` filling). What the planned volets did establish follows. The **join is total**: 108 specifications of 108 join to a line of their own card at the pinned tag, zero orphan — which also closes the sizing volet, since a line renamed or dropped under the pin would have failed it. No manifest has ever carried a tag other than `v4.2.0`, so no card could have changed underneath one already written. The cross-vocabulary snapshot's second pass adds no new cell class: 181 foreign cells over 42 specifications, 109 of them (60%) the documented identity class, and **59 of the 101 measurable specifications fill on zero foreign brief**. The dry run's §2 is confronted **at the card line** across the fifteen routed prompts — 27 agreements, 29 lines asked by §2 alone, 35 by the manifests alone — and the two directions are not symmetric. §2-only is the documented class, that table having been computed against the thinner coverage-matrix sketches. Manifest-only is dominated by one structural cause, stated once here instead of on twenty specifications: **`F` means "present *or directly derivable*"**, and deriving a value is exactly what a deterministic detector must never do — a detector inferring a sector from a company name is the false "filled" this layer exists to refuse. **One divergence survives that reading**: the legend classes `D` as a sanctioned unknown *or an operator-profile default*, naming three of the latter verbatim — deliverables language, tone, proposal format. Two are honoured; WF-006 `Proposal format` stays `required` with no default, settled at the card rather than by conforming to the table, because the brief carries a client and how the **prospect** wants to receive a proposal is not a constant of the operator's practice. The dry-run document is a dated snapshot, so this is an annotation under §2 and the table itself is untouched.

### 🔧 Fixed
- **The card-label rule declared on any dash, and English compounds the very nouns those labels use.** The separator class was `[:—-]` and the two dashes had never been measured on ordinary prose; they buy nothing and cost false **fills**, the unsafe direction. The bare hyphen is the serious one: "a client-facing chatbot rollout" parses as `Client` + `-` + `facing…` and answered WF-004 `Client (name)` on a brief naming no client, while WF-009 `Location` filled on "a location-based recommendation engine". The em-dash reads an apposition as a declaration — "the prospect — a regional insurer —" answered `Prospect (name)` while naming nobody. Narrowed to the colon, the only form the catalog's quick-start blocks ever teach. ⚠️ The defect **predates** the conjunction decision above, and that is measured rather than inferred: WF-009 `Location` is not a conjunction half and was never touched by it. What that decision did was widen the exposure, by making `Client` and `Prospect` — two of the most compounded nouns in business English — active bare labels. Invisible to the entire corpus, which is why nothing caught it: the nineteen briefs place no card label before a dash, so narrowing moves zero fixture verdict and both snapshots stay byte-identical, measured after the change rather than predicted before it. Falsified four ways — the full class, the hyphen alone and the em-dash alone each turn the new test red on their own, and breaking the colon itself turns six tests red including that test's own control, so the control is not vacuous.
- **The condition governing the label rule was wider in prose than in code, and nothing said the gap was deliberate.** The comment read "the label DESIGNATES the identity"; by that, WF-009 `Role sought [Title / Level]` qualifies — a title is exactly what the label designates — yet its half is `(title)` and stays excluded, which read as an accident of naming. Measured, it inverts into a reason: **"Role sought: senior" fills `role_level` and not `role_title`**, so a bare answer to that label is genuinely two-way, whereas "Client: Acme Corp" can be neither a sector nor a size. The operative condition is therefore **no sibling half can claim the same answer** — true of all six `(name)` halves, false here; serving `(title)` would let an answer stating only a grade fill both. The mechanism is written into the test rather than smoothed over, because it makes the point stronger: `role_level` anchors a grade word beside a role noun, and the noun it matches is `role`, taken from the line label itself. Falsified both ways — extending the rule to `(title)` turns the new case red, and breaking the level branch that fires here turns its control red, so the control is not inert.
- **`team_size` read one card fact through two disjoint vocabularies, and the recorded diagnosis was wrong.** The tracker described it as a coverage gap "on both sides", symmetric. Re-measured at the pinned cards it is one-sided, and the cards break the tie: WF-001 `Team size [Solo / 1 squad / Several SAFe teams]` is a **closed enumeration**, so reading "team of eight" would invent a new member — the class already corrected on WF-007 and WF-009 — and the probe agrees rather than merely permitting, widening it buying a foreign cell on P09 while moving no verdict of its own. WF-010 `Team involved [Size / Distribution / Remote or on-site]` enumerates nothing, so "3 squads" **is** a size and refusing it was the real gap. An agile-count branch is added to WF-010 alone, with a count required — a bare squad states no size on a line that lists none. Cost measured before the edit: foreign cells 6 → 7, the single new cell being P02 "3 squads", a genuine team-size statement about another card's team and so the recorded-crossing class. The corpus cannot see the gain in either direction (P10 states a distribution, P13 no team at all, both correctly missing before and after), so it is asserted on the form the card itself teaches.
- **README: two stale claims on a public surface, found by asking what this lot falsified.** The roadmap still listed "Dispatch V1 — the nine remaining parameter manifests" as open work while this lot shipped the last of them; the manifests become a ticked item and V1 keeps only what is genuinely unstarted. And the CI checklist still advertised "Node 20/22" — verified at source, the matrix has been **22 + 24** since 2026-07-30, when Node 20 was dropped for being end-of-life, with a third job running the suite against the real catalog at the pinned tag. ⛔ The status line's "276 tests green" is deliberately left alone: it is scoped to the `v0.9.1` release it names, and HEAD's 391 would match no released state.
- **Residual French in the repository's metadata files, on a public repository.** `.gitignore`, `.gitattributes`, `CODEOWNERS` and the `package.json` description were missed by the `v0.6.0` translation and are visible to anyone the showcase sends here — the same class as the `dependabot.yml` comments closed earlier, and folded into this lot rather than given a commit of its own, since this lot touches `.gitignore` anyway. The frozen French release history in this file is untouched by design (published entries are never rewritten), and so is the fixture name "Normand Alizé" in `spine-helpers.test.ts`: it is test **data**, one of the N-initial names that guard against `affirmativeString` false positives, not prose.
- **Half the CI matrix was spent on a dead Node runtime, and the Active LTS was never exercised.** `build-test` ran Node **20** and **22**: Node 20 reached **end of life on 2026-03-24**, four months before this entry, while Node **24** — the Active LTS, and a version `engines: ">=20"` claimed to support — was tested by nothing. Matrix moved to **22 (Maintenance LTS) + 24 (Active LTS)**, and `engines` to `">=22"` in the same lot, because claiming support for a runtime nothing tests is the same class of empty guarantee as a guard that never runs. Node 26 (Current) is the workstation's own version and is covered by every local run; that 24 sits between a tested 22 and a daily-used 26 is an inference, and the first CI run on it is the proof. Both jobs also gain `timeout-minutes: 10` against an observed duration of 23-28 s, so a hung job becomes a fast red instead of hours of silence. Measured while re-examining that job: the new integration job adds a ~1 s shallow clone to the same steps and runs **in parallel** with the matrix, so it costs runner minutes — free on a public repository — and no wall-clock. It also needs no secret, so pull requests from forks still pass.
- **CI verified none of the assertions that need the real catalog.** The `build-test` job checks out this repository alone — deliberate, since ADR-0002 keeps the runtime independent of the catalog — so every `real-sidecar` block skipped there. Measured: **28 assertions were unverified by CI** (316 passed / 23 skipped locally against 299 / 40 without a catalog; the 17-test delta is smaller only because eleven trivial fallbacks pass in their place). Those 28 are every backbone resolved against real agent prose, the plan built verbatim from the real WF-001 card, the route validated against the real sidecar, and the five manifest catalog-tag comparisons. A new `catalog-integration` job closes it, and three of its four decisions are corrections of a first design rather than choices made once:
  - **It clones the catalog at the tag the manifests PIN, not at the default branch.** `main` was the first design and reading ADR-0002 overturned it: that ADR rejects a live import precisely because "a catalog edit would break the runtime with no safety net", and accepts as a stated cost that "freshness is not immediate — you must bump to propagate". A job on `main` turns a catalog *release* into a red build here until the manifests are re-derived — converting a deliberately unhurried freshness into an urgency, and leaving a standing red that hides real regressions. At the pinned tag the tag comparison is tautological only for the manifest whose pin was read; for the other four it still catches an internal disagreement (falsified: pinning `wf-002` to `v4.1.0` turns its assertion red). Drift against the *latest* catalog tag is therefore answered as a session-start fact in `next_steps.md` — a difference there is freshness debt, not a failure.
  - **The tag is read from the code, never written in the workflow**, which would be a second source of truth for the pin, free to drift from the manifests it mirrors.
  - **The catalog is cloned anonymously rather than checked out with an action.** `actions/checkout`'s own README states `${{ github.token }}` is scoped to the current repository and says nothing about cross-repository *public* reads; the claim that the default token suffices was an inference, not a reading. A shallow HTTPS clone of a public repo needs no token, so the question does not arise, and it fails loudly on a tag that does not exist. Verified by running the exact command.
  - **It runs `npm test`, not a `real-sidecar` filter**, because a substring filter is itself a failure mode: rename a file out of the pattern and the job runs fewer tests while staying green.
  - **`REQUIRE_CATALOG` is what makes the job's green mean something.** The first version asserted that the cloned `sidecar.json` existed — which proves the clone, not that the tests read it: a mistyped `CATALOG_ROOT` in the step below would have skipped every real-sidecar block and passed, reproducing the defect the job exists to close. The demand now travels with the run instead: when that variable is set, a `no-catalog fallback` block firing is a **failure** rather than a skip, and both variables are declared once at job level so they cannot disagree. Falsified in three states: the pinned catalog with the flag → 316 passed / 23 skipped; a mistyped path with the flag → **11 failures**; the same absence *without* the flag → green, so a contributor with no catalog checkout is unaffected. And against a catalog extracted at `v4.1.0`, seven tests fail including all five tag comparisons.
  - **No billed call can fire there**, verified on three legs rather than assumed: the twelve live harnesses require `LIVE_RUN` *in addition* to a catalog, the one other `*live*` file is hermetic by construction (mocked `query()`, interim sidecar, stub resolver), and the runner refuses `ANTHROPIC_API_KEY`.
  - **Stated rather than implied, since this job's red is a signal and not a barrier:** `main` carries no branch protection, so no status check is required and nothing is mechanically blocked. The last CI conclusion therefore joins the session-start factual check beside the open-alerts count and the freshness comparison. Remaining limits, named: the job triggers on push and pull request to `main`; and a card edited in the catalog without a re-tag leaves the sidecar's tag unchanged and passes.
- **Ten test blocks announced "catalog not found" precisely because the catalog WAS found.** Each real-sidecar suite carried a complementary block, titled `"WF-00X spine — real sidecar (skip)"` with an assertion reading `"skipped: catalog not found (set CATALOG_ROOT or checkout sibling)"`, which runs *only* when no catalog is checked out. With a checkout present it is itself skipped, so the reporter printed ten lines stating the opposite of the truth — every word accurate, the reader's conclusion inverted. It cost a real misreading on 2026-07-29: the test summary was read as evidence that the real-sidecar proofs had not run. The blocks are now declared through one shared helper whose title names the **condition** rather than the effect and states the inference a skipped line licenses, so a wording correction lands once instead of eleven times. The same helper also becomes the single source of the `HAVE_CATALOG` gate, which each of the eleven suites used to recompute — the drift `catalog-root.ts` already exists to prevent. And the dispatch real-sidecar suite, which never had such a block, gains one: its eight assertions used to vanish with no explanation at all when the catalog was missing. Verified in **both** states, the second one never exercised before: with a checkout, the real blocks run and the fallbacks are skipped; with `CATALOG_ROOT` pointing nowhere, the real blocks skip and the fallbacks run and pass. The skipped count moves from 22 to 23 for that eleventh fallback alone. ⛔ Deleting these blocks would not be a simplification: they are what distinguishes *skipped for lack of a catalog* from *did not run*.
- **`.github/dependabot.yml` comments were still in French** in a repository translated to US English at `v0.6.0` and public since. Translated, with the two doc references it cites re-verified as live (`docs/NEXT_STEPS.md` §2.5, `docs/adr/0002`). The `open-pull-requests-limit: 5` line now carries what it actually does: it restates GitHub's own default, is stated explicitly so a future change of that default cannot silently change this repository's behaviour, and caps **version** updates only — security update pull requests are neither subject to it nor counted toward it, contrary to a note this repo's tracker had relied on. **No configuration line was changed**, only comments; the limit and the grouping were examined and deliberately left alone.
- **The four detectors the cross-vocabulary probe caught, in two manifests that had already shipped.** Each is closed by requiring the quantity or the word to sit beside what qualifies it, and the probe's snapshot — committed *as found* in the previous lot — now shows the six cells closing in a diff, with nothing else moving: WF-003 goes from 7 foreign fills to 6, WF-004 from 40 to 35.
  - **WF-003 `Monthly API budget` read a quarter label as a sum.** Its `budget … digit` branch filled on the WF-001 brief's "pilot budget capped for Q3", the digit being the **3 of "Q3"**. The branch is removed. Beyond that proven defect, and stated as a deliberate policy call rather than a fix: the card asks for a **monthly** budget, so a monetary amount now only counts beside a monthly qualifier — "€45k total project budget" answers a different question, and a brief omitting the word costs one return trip, the safe direction.
  - **WF-004 `Engagement scope` read a denial as a statement.** It filled on a legal-drafting brief whose only occurrence of the word is "*not* an audit of an AI system"; a negation guard now precedes the alternation, the same mechanism this batch already gave WF-005 `Sources to prioritize`. It also filled on "research-grade **training** infrastructure" — model training, not the card's training engagement — so `training` is narrowed positively (a training plan, programme, engagement, or the training *of* staff/teams), the form `Expected deliverables` already used, rather than a blacklist of the ML senses.
  - **WF-004 `Engagement duration` accepted a bare quantity, and so answered the card with a sprint length, a response deadline and a project length.** It now requires the quantity to sit within one sentence of what it measures (engagement, mission, assignment, phase, contract, intervention). This is the finding that mattered: **WF-005 `Horizon` had refused the bare quantity earlier in the same batch, for the same reason** — two manifests applying one policy in opposite directions, which no test could see because each was green on its own fixture. `programme`/`program` are deliberately **not** anchors: a "chatbot program" names the client's initiative as often as the engagement, and with them included the pre-sales brief escaped only because its gap measured 25 characters against a 24-character window — luck, not design. A companion test guards the opposite failure, checking that the two briefs which genuinely state an engagement window still fill, in either word order.
  - Six falsifications, each turning exactly one test red, including the over-tightening one. Suite **316 passed / 22 skipped**, strict typecheck green, no trace touched, no billed run.
- **The parameter detector could accept an entire card on one sentence of context.** `paramFilled` fell back to a non-emptiness test whenever a specification carried no `pattern`, and most identity-card parameters read the same shared brief fields — so a single non-empty `context` satisfied them all at once. That is a false "filled", the unsafe direction, on cards carrying up to eleven parameters. A specification with no detector is now reported missing. WF-001 `constraints` was the only one relying on that fallback: guaranteed by the intake completeness check, it now declares `defaultValue` — the existing mechanism for "never missing" — instead of depending on an implicit test. Behaviour on the existing manifest is unchanged.
- **`PARAMS_MISSING` returned internal identifiers to the human who has to act on them.** The decision listed `name` values (`team_size`, `project_method`) while the card label sitting beside them in the manifest (`Team size`, `Project method`) is business vocabulary by construction, copied verbatim from the catalog card. The label is now surfaced; `name` stays the internal key and is never shown. Traces recorded before this change remain dated evidence of what ran then — they are not a contract.

### ⬆️ Dependencies
- **Dependabot backlog triaged and cleared — 7 alerts down to 1, qualified at source rather than blind-bumped.** GitHub reported 7 vulnerabilities (2 high / 4 moderate / 1 low) on the default branch and 5 open Dependabot PRs (#42-#46) that nobody had mapped onto them. Resolved with `gh api …/dependabot/alerts` + `npm audit` + `npm ls`, splitting the execution path from the unreachable surface:
  - **`ajv` → `fast-uri` 3.1.2 → 3.1.4 — both `high` alerts, and the only ones on the execution path.** `ajv` validates every dispatch brief and every step handoff, so host-confusion in its URI resolver is the one finding that mattered here. Fixed.
  - **`@anthropic-ai/claude-agent-sdk` 0.3.207 → `^0.3.215`, lockfile resolved to 0.3.220** (the execution substrate) and, through its `@modelcontextprotocol/sdk` dependency, **`hono` 4.12.25 → 4.12.32** and **`body-parser` 2.2.2 → 2.3.0** — clearing 2 moderate and the low. The resolved versions are what the suite was verified against; they meet or exceed every open Dependabot target (#43/#44/#46/#47), so those close as satisfied.
  - **`actions/setup-node` v6 → v7** (CI only, no advisory attached).
  - **Left in place, deliberately: `@hono/node-server` (1 moderate, path traversal in `serve-static` on Windows).** The fix needs a major bump to `>=2.0.5`, pinned by `@modelcontextprotocol/sdk`; `npm audit`'s proposed remedy is a **semver-major downgrade of the Agent SDK to 0.2.85**, which would break the runtime and orphan the live proofs. The advisory is also **unreachable here**: this runtime never instantiates the Hono node server or serves static files — it calls the SDK's `query()` locally — and CI runs on Linux. Forcing a major override on a transitive dependency to silence a finding that cannot fire is a worse trade than carrying it. Revisit when `@modelcontextprotocol/sdk` widens the range upstream.
- Suite **276 passed / 22 skipped** and strict typecheck green after the bumps; the versioned live-run traces are untouched (verified by diff).

## [0.9.1] - 2026-07-27 — README realigned with the repo's own proofs 📄
> Model: Claude Opus 5

### 🔧 Fixed
- **`README.md` was frozen at `v0.5.0` and contradicted this repository's own evidence.** Untouched since the i18n batch of 2026-06-19, it still advertised *"3 workflows (WF-001/002/003) proven live end to end"* and *"126 tests green"*, and left the roadmap item `WF-002/003 live run end to end` unticked — while `v0.6.0`→`v0.9.0` had since live-proven **all ten** catalog workflows and added the **WF-000 dispatch** layer, which the file did not mention once. Since this repository is public and is the source cited for those proofs, the stalest surface was also the most exposed one. Every figure below was re-derived at source before writing:
  - **Status line** — `v0.5.0` → `v0.9.0`; *3 workflows* → **10** (WF-001 … WF-010), stated as **9 runs `completed` + 1 returned for rework** (WF-008, halted by its counter-review gate **by design** — a fail-closed proof, not a failed run); *126 tests* → **276 passed / 22 skipped**; the **WF-000 dispatch layer** named as what it is — a layer **upstream** of the spines, not an eleventh catalog workflow.
  - **POC scope** — the WF-001→002→003 delivery spine is recorded as reached and extended (ten spines on the same unchanged linear orchestrator); dispatch added as a fourth building block; the SDK-integration sentence widened from `runWf001` to `runWf001` … `runWf010`.
  - **Roadmap** — `WF-002/003 live run` ticked; entries added for the WF-004…WF-010 live proofs and for WF-000 dispatch V0; the two genuinely open items (dispatch V1, multi-workflow chaining) listed unticked, chaining stated as discovery-stage and blocked on inter-workflow context accumulation.
  - **Documentation** — direct pointers to `docs/audit/live-runs/` (the verbatim traces) and `docs/discovery/` (the dispatch deliverables), so a reader can check the claims rather than take them.
  - **Stack** — `Sonnet 4.6` → `Sonnet 5`, with the distinction made explicit: the per-workflow `modele_recommande` is a catalog artifact read verbatim, while the model a live proof is routed to is a runtime decision.
  - Model-provenance line updated (Opus 4.8 through `v0.9.0`, Opus 5 from `v0.9.1`).

## [0.9.0] - 2026-07-19 — WF-000 dispatch V0: intake → routing → execution plan, live-proven end-to-end 🚦
> Model: Claude Fable 5

### ✨ Added
- **WF-000 dispatch V0 — intake → routing → execution plan (`src/dispatch/`), upstream of the frozen spines.** Productizes the dispatch discovery (`docs/discovery/wf-000-dispatch-*.md`, 5 deliverables): a **validated need brief** (ajv-strict contract; the router input is never a raw stakeholder prompt) passes a **deterministic fail-closed completeness check** (affirmative substance via `affirmativeString`, word floor + versioned state-marker list incl. one generic qualified-request marker so off-catalog needs can reach an honest `NO_MATCH`, unjustified-empty-constraints rule, role-not-name guard) → a **single LLM router call** (system prompt built FROM the pinned sidecar — workflow table injected, never hardcoded — carrying the role-invariance rule `role ⊥ route` and the `NO_MATCH` sanction; one bounded re-ask on a shape-invalid answer, then fail-closed) → **deterministic validation** (id exists in the sidecar as a workflow, `dependsOn` resolvable, per-param manifest check; an invented id is rejected, never retried) → an **execution plan** read verbatim from the routed card (`duree_estimee`, `modele_recommande`; fail-closed if absent) — then **STOP: the go/no-go and the spine launch stay separate human commands** (no billed gate crossed). Outcomes are typed and all operator-facing: `REJECT_INCOMPLETE` / `REJECT_ROUTER_OUTPUT` / `NO_MATCH` / `PARAMS_MISSING` (route valid, missing params named — the returned-for-rework loop upstream of the go/no-go) / `ROUTED` (with `paramsChecked:false` stated honestly when no manifest covers the route). V0 ships the **WF-001 pilot manifest only** (7 params derived from the card's CLIENT CONTEXT block, pinned to catalog v4.2.0 — tag drift hard-fails in the real-sidecar test); the other nine manifests, assisted param filling and cost estimates are V1; ad-hoc composition and multi-WF chaining are V2. Tests: `test/dispatch-completeness.test.ts`, `test/dispatch-validate-route.test.ts`, `test/dispatch-run-dispatch.test.ts` (hermetic), `test/dispatch-matrix-oracle.test.ts` (**the 20 fictional coverage-matrix briefs as offline oracle** — proposer faked by design, no LLM in CI; incl. the two role probes P19/P20 and the four `NO_MATCH` miss classes), `test/dispatch-real-sidecar.test.ts` (skip-safe), `test/dispatch-run-live.test.ts` (billed router-accuracy harness, `LIVE_RUN=1`-gated, OAuth-only, tight caps; the score is **reported verbatim** — a mis-route is a finding, never a hidden failure). Fixtures: `test/fixtures/dispatch-briefs.ts` (all fictional). **Live router-accuracy run performed 2026-07-19** (billed, OAuth, explicit approval; `LIVE_MODEL=fable`, separate-quota counter, ~2.2 min): **score 19/20** — both role probes P19/P20 held (routing on state + deliverable, never on the submitter's role), the discriminating pairs resolved, all four off-catalog prompts got an honest `NO_MATCH`, P18 died deterministically at intake. The single mismatch is a **verbatim-reported finding**: P11 (pre-signature ask with no RFP document) → `NO_MATCH` with `nearestMiss: "WF-006"` — the router only sees the sidecar description arrow ("RFP received"), narrower than the card's "Direct solicitation" scope; the failure direction is the **safe** one (under-match, nearest miss named, never force-fit). V0 accepts it; a router-prompt hardening is consigned for V1. Trace versioned `docs/audit/live-runs/wf-000-router-live-result.json` (secret-scan clean, fictional briefs only, no live-model field — Fable attribution contextual).
- **WF-000 pilot P01 end-to-end — the V0 dispatch pipeline traversed live through the human gate to a spine delivery (2026-07-19, `LIVE_MODEL=claude-opus-4-8`, billed OAuth, explicit approval at each gate).** Phase 1, the **returned-for-rework loop live**: the UNAMENDED P01 brief (new fixture `P01_UNAMENDED` — the qualified brief before the operator collects the three must-ask facts) passed intake, routed to WF-001 and came back **`PARAMS_MISSING` exactly `[team_size, project_method, level_of_detail]`**, the identity-card dry run's prediction verbatim. Phase 2: the amended brief (fixture P01) returned **`ROUTED`, `paramsChecked: true`** with the execution plan read verbatim from the card ("45-90 min", `claude-sonnet-5` — a catalog artifact; the live routing stayed the operator's decision: Opus 4.8), then the pipeline **stopped before the go/no-go by construction**. Phase 3, on the human GO: the frozen WF-001 spine harness with a **P01-derived seed** (new `LIVE_BRIEF` env override; default seed unchanged; the seed is now captured in the trace payload) → **`completed` 3/3 steps, 9/9 blocking**, advisory 2/3 (the known-brittle INVEST regex, a criterion artifact) — content inspected substantive and P01-specific (claim-file-summarization epics for the adjusters, RAG over policy wordings, GDPR-by-design), not an echo of the default seed. A new hermetic regression locks the deterministic return loop (`PARAMS_MISSING` on `P01_UNAMENDED`, zero LLM). Traces versioned `docs/audit/live-runs/wf-000-pilot-p01-dispatch-live-result.json` + `wf-000-pilot-p01-wf001-live-result.json` (secret-scan clean, fictional briefs only, no live-model field — Opus 4.8 attribution contextual).

## [0.8.0] - 2026-07-13 — All 10 catalog workflows live-proven: WF-005/007/010 backbones, WF-009 end-to-end, decision-gate sentinel hardening 🏁
> Model: Claude Opus 4.8

### ✨ Added
- **WF-007 spine — "Client Engagement Onboarding D1-D5" (`Management & Consulting`).** New deterministic backbone sourced from `claude-agents/workflows/WF-007-onboarding-mission-j1.md` (v1.0): the sequential chain `CHEF-PROJET-IA → BUSINESS-ANALYST → CHANGE-MANAGER → REDACTEUR-IA` (STEP-01 → 02 → 03 → 05). Same `Management & Consulting` domain as WF-004/005/006 — adds a backbone, **not** a new domain. The source BPMN's **parallel fork** (STEP-02 BUSINESS-ANALYST ∥ STEP-03 CHANGE-MANAGER) is **linearized** (STEP-02 → STEP-03) on the **unchanged** linear orchestrator (WF-003 precedent); the AI-maturity gateway (STEP-04 CONSULTANT-IA) and the NDA/JURIDIQUE-IA branch stay optional, outside the backbone. Structurally a **linear twin of WF-004**: no fail-closed structural construct (STEP-01 opens as an ordinary blocking eval gate). Handoff idiom (one carried field per hop): `raci → clientContext → engagementPlan`; REDACTEUR (STEP-05) synthesizes all prior deliverables from its prose. DoD counts (D1-D5 plan, checklists) split into a relaxed **blocking floor** + an **advisory** at the exact spec number. **Live-run hardening applied from the start (WF-005 lesson):** output-schema numeric bounds pinned to the **blocking floor** (no `maxItems`, no `minItems` on optional fields) since `validate-handoff.ts` ajv-strict-validates the output at the handoff; the ideal is carried by advisory criteria + descriptions; a regression test locks it (a 1-entry-per-list run completes). Files: `src/spines/wf-007-onboarding.ts` (criteria + manifest), `src/spines/run-wf-007.ts` (wiring), `test/spine-wf-007.test.ts` (hermetic — happy path + hardening + empty-RACI halt + missing-D5-scoping halt + agent-missing fail-closed), `test/run-wf-007.test.ts` (real runner query() + budget/config guards), `test/run-wf-007-real-sidecar.test.ts` (real sidecar ≥ v4.1.0, the 4 agents resolve with real prose → `completed` 4/4), `test/wf-007-run-live.test.ts` (billed live harness, `LIVE_RUN=1`-gated, OAuth-only; live proof routed to a user-designated performant model), `test/fixtures/wf-007-outputs.ts`. Requires the catalog sidecar **≥ v4.1.0** (all four agents present). Offline proof (hermetic + real-sidecar) green. **Live run performed 2026-07-13** (billed, OAuth, explicit approval; `LIVE_MODEL=claude-opus-4-8` per the 2026-07-13 model-routing decision): **`completed`, 4/4 steps `pass`** — all blocking **and** advisory checks (**10/10 blocking + 8/8 advisory**) on a synthetic mid-cap European industrial engagement (3-month AI scoping, hybrid, post-reorg sensitivity, two incumbent SSII): a full D1-D5 kickoff plan, stakeholder RACI, client-context sheet, change-management reading, and the REDACTEUR-IA onboarding kit (intro email + D1 report template + D5 scoping note). ~4.1 min, $4 cap respected, **clean audit trace** (no plan file written to `~/.claude/plans/`, consistent with the Opus WF-004/006 runs). Verbatim trace versioned `docs/audit/live-runs/wf-007-live-result.json` (secret-scan clean — no third-party PII, no path, no key; no model field); live-runs README updated. A fourth live-proven backbone in the Management & Consulting domain (adds a backbone, not a new domain).
- **WF-010 spine — "Project Post-mortem / Lessons Learned" (`Management & Consulting`) — the TENTH and last workflow to receive a spine.** New deterministic backbone sourced from `claude-agents/workflows/WF-010-post-mortem-projet.md` (v1.0): the sequential chain `CHEF-PROJET-IA → QA-AGILE → CHANGE-MANAGER → REDACTEUR-IA` (STEP-01 → 02 → 03 → 06). Same `Management & Consulting` domain as WF-004/005/006/007 — adds a backbone, **not** a new domain; with it **all 10 catalog workflows now have a spine**. The source BPMN's **parallel fork** (STEP-02 QA-AGILE ∥ STEP-03 CHANGE-MANAGER ∥ optional STEP-04) is **linearized** (STEP-02 → STEP-03) on the **unchanged** linear orchestrator (WF-003 precedent). Every gateway is optional and stays outside the backbone — including, **unlike WF-008 where the counter-review gate is mandatory in the backbone**, the AUDIT-METHODO-IA counter-review here (STEP-05B), which the source marks OPTIONAL (high-stakes/disputed post-mortems only). Structurally a **linear twin of WF-004**: no fail-closed structural construct (STEP-01 opens as an ordinary blocking eval gate). Handoff idiom (one carried field per hop): `improvementPlan → qualityReview → teamReview`; REDACTEUR (STEP-06) synthesizes all prior deliverables from its prose. DoD counts ("5 Whys on the 3 main problems", "5-10 improvement actions") split into a relaxed **blocking floor** + an **advisory** at the exact spec number. **Live-run hardening applied from the start (WF-005 lesson):** output-schema numeric bounds pinned to the **blocking floor** (no `maxItems`, no `minItems` on optional fields) since `validate-handoff.ts` ajv-strict-validates the output at the handoff; the ideal is carried by advisory criteria + descriptions; a regression test locks it (a 1-entry-per-list run completes). Files: `src/spines/wf-010-post-mortem.ts` (criteria + manifest), `src/spines/run-wf-010.ts` (wiring), `test/spine-wf-010.test.ts` (hermetic — happy path + hardening + empty-root-causes halt + missing-capitalization halt + agent-missing fail-closed), `test/run-wf-010.test.ts` (real runner query() + budget/config guards), `test/run-wf-010-real-sidecar.test.ts` (real sidecar ≥ v4.1.0, the 4 agents resolve with real prose → `completed` 4/4), `test/wf-010-run-live.test.ts` (billed live harness, `LIVE_RUN=1`-gated, OAuth-only; live proof routed to a user-designated performant model), `test/fixtures/wf-010-outputs.ts`. Requires the catalog sidecar **≥ v4.1.0** (all four agents present, incl. `AGENT-QA-AGILE`). Offline proof (hermetic + real-sidecar) green. **Live run performed 2026-07-13** (billed, OAuth, explicit approval; `LIVE_MODEL=claude-opus-4-8` per the 2026-07-13 model-routing decision): **`completed`, 4/4 steps `pass`** — all blocking **and** advisory checks (**10/10 blocking + 11/11 advisory**) on a synthetic AI-product go-live REX that shipped off-baseline (+2 weeks, +8% cost, 3 P1 defects resolved in hypercare, zero data-integrity loss): a 5-Whys root-cause analysis on the 3 main problems, an improvement plan, a QA-AGILE quality/test/tech-debt review, a change-management team/adoption reading, and the REDACTEUR-IA lessons report. ~5.5 min, $4 cap respected. **With this run all 10 catalog workflows are live-proven** (fifth backbone in the Management & Consulting domain, adds a backbone not a new domain). ⚠️ **Process-hygiene note (honest correction):** this Opus run **DID write a plan file** to the run environment (`~/.claude/plans/`, under the forced `permissionMode:"plan"`), which **falsifies** the earlier WF-005 reading that only Sonnet writes plan files and Opus does not — the behavior is not cleanly model-determined. The artifact carried no PII/secret (a legitimate REDACTEUR task plan, synthetic REX content) and was **deleted** after review; the versioned trace is clean (no meta-commentary leak, no `internalNote` field on this workflow). Verbatim trace versioned `docs/audit/live-runs/wf-010-live-result.json` (secret-scan clean, no model field); live-runs README updated.

- **WF-005 spine — "Strategic Intelligence & Growth" (lightest backbone, `Management & Consulting`).** New deterministic backbone sourced from `claude-agents/workflows/WF-005-veille-growth.md` (v1.1): the sequential chain of the 3 core agents `VEILLE-STRATEGIQUE → GROWTH-IA → REDACTEUR-IA` (STEP-01 → STEP-02 → STEP-03). Same `Management & Consulting` domain as WF-004/006 — it **adds a backbone, not a new domain** — and is the runtime's **lightest** chain (3 agents; the workflow's `modele_recommande` is `claude-sonnet-5`). Structurally a **linear twin of WF-004**: no fail-closed *structural* construct — STEP-01's `condition_passage` ("signals qualified **and ranked** before writing") is modeled as an ordinary blocking eval gate (`veille-highlights-ranked` requires every highlight to carry a title + an impact rating, so an unranked radar halts before the content plan), and both source gateways (the FINANCIAL-ANALYST opportunity-scoring branch STEP-01B, the JURIDIQUE-IA compliance branch STEP-04) stay optional, outside the backbone (WF-003/004 simplification). Handoff = the one-field-per-hop idiom: `highlights` → `topics`. Numeric DoD counts (Top-5 highlights, 1-3 topics, 1-3 LinkedIn posts) are split into a relaxed **blocking floor** + an **advisory** at the exact spec number. **Live-run hardening (anti-false-KO):** the output JSON schema is ajv-strict-validated at the handoff (`validate-handoff.ts`), so its numeric bounds are pinned to the **blocking floor** — `minItems` = the floor (highlights ≥ 3), **no `maxItems`** (topics / LinkedIn posts), and no `minItems` on the *optional* `toolsToWatch` — while the *ideal* (Top-5, 1-3) is carried by the advisory criteria and the field descriptions; this prevents a modest-but-valid Sonnet-5 output (e.g. 4 highlights, a 4th topic/post, an empty optional array) that every blocking gate passes from being hard-failed at the strict handoff. A regression test (`spine-wf-005.test.ts`) locks it in (a 3-highlight / 4-topic / 4-post run **completes**). The live seed also instructs an offline, from-knowledge synthesis (no browsing) to avoid a spurious refusal under the read-only cap. The orchestrator is **unchanged** (ADR-0007, deterministic eval gate, no LLM-as-judge). Files: `src/spines/wf-005-veille.ts` (criteria + manifest), `src/spines/run-wf-005.ts` (wiring), `test/spine-wf-005.test.ts` (hermetic — happy path + unranked-highlights halt + empty-LinkedIn halt + agent-missing fail-closed), `test/run-wf-005.test.ts` (real runner query() + budget/config guards), `test/run-wf-005-real-sidecar.test.ts` (real sidecar ≥ v4.1.0, the 3 agents resolve with real prose → `completed` 3/3), `test/wf-005-run-live.test.ts` (billed live harness, `LIVE_RUN=1`-gated, OAuth-only; recommended model `claude-sonnet-5`), `test/fixtures/wf-005-outputs.ts`. Requires the catalog sidecar **≥ v4.1.0** (all three agents present). Offline proof (hermetic + real-sidecar) green. **Live run performed 2026-07-13** (billed, OAuth, explicit approval): first routed to `claude-sonnet-5` (the catalog's `modele_recommande`) → `completed` 3/3 with substantive content, but the run **wrote a plan file** to the environment (despite the runner's forced `permissionMode:"plan"`) and leaked **plan-mode meta-commentary** (a real name + local path) into the *optional*, non-gated `internalNote` — **process-hygiene** defects, not deliverable-quality ones, and **model-dependent** (the two prior Opus 4.8 runs, WF-004/006, left no plan file; both Sonnet 5 runs did). **Re-routed to `claude-opus-4-8`** for a clean audit trace → **`completed`, 3/3 steps `pass`** (all blocking + advisory: 7 ranked highlights, 3 topics, a 3,750-char synthesis, 3 LinkedIn posts), ~3.1 min, $4 cap respected, **no disk write**, a legitimate `internalNote` (a pre-publish factual-check note). Verbatim trace versioned `docs/audit/live-runs/wf-005-live-result.json` (secret-scan clean — no third-party PII, no path, no key; no model field); live-runs README updated. The catalog's `modele_recommande` stays `claude-sonnet-5`; the **live proof is routed to Opus 4.8** for trace cleanliness (a runtime model-routing decision, not a catalog change).

- **Decision-gate sentinel rejection helpers (`affirmativeString`, `countAffirmativeField`).** A DECISION gateway (e.g. "a candidate is selected") must reject an agent's honest in-band refusal — `"None"`, `"N/A"`, `"cannot…"`, `"not applicable…"` — which a bare presence check (`nonEmptyString`) accepts as a non-empty string. Anchored, word-boundary sentinel matcher so a real value (including names starting with "no"/"na" such as `Noah`, `Nadia`) is never misread as a refusal (`src/spines/spine-helpers.ts`, `test/spine-helpers.test.ts`).

- **WF-009 spine — "IT/AI Recruitment" (fifth domain: HR & Talent).** New deterministic backbone sourced from `claude-agents/workflows/WF-009-recrutement-it-ia.md` (v1.0): the sequential chain of the 4 core agents `BUSINESS-ANALYST → CONSULTANT-IA → REDACTEUR-IA → RH-IA` (STEP-01 → STEP-02A → STEP-03 → STEP-04 → STEP-05 → STEP-06), where `AGENT-RH-IA` carries the last three backbone steps (sourcing/scoring, selection, offer). Opens **HR & Talent**, a domain untouched by WF-001/002 (Agile & Product), WF-003 (Dev & Engineering), WF-004/006 (Management & Consulting) and WF-008 (Compliance & Governance) — the fifth live-provable domain. Structurally a **linear twin of WF-004**: no fail-closed *structural* construct in the orchestrator, which stays **unchanged** (ADR-0007, deterministic eval gate, no LLM-as-judge). The two BPMN **loop-back gateways** of the source (`<shortlist validated?>` after STEP-04, `<candidate selected?>` after STEP-05, both `NO → back to sourcing`) are modeled as **ordinary blocking eval gates** — reopening a live-proven orchestrator to add a bounded resume loop is out of scope — so an insufficient shortlist halts at STEP-04 (`rh-shortlist-validated`, floor ≥ 3) and a missing selected candidate halts at STEP-05 (`sel-candidate-selected`), the same halt semantics as every other spine (a halt here reads "shortlist insufficient, broaden sourcing", an operational iteration state, not a compliance defect). The FINANCIAL-ANALYST hire-vs-freelance TCO fork (STEP-02B) and the CHANGE-MANAGER onboarding branch stay optional, outside the backbone (WF-003/004 simplification). Numeric DoD counts ("6-10 grid criteria", "10-15 interview questions", "shortlist of 3-5", "2-3 references") are split into a relaxed **blocking floor** + an **advisory** at the exact spec number, while the JSON schema communicates the ideal to the agent. Files: `src/spines/wf-009-recrutement.ts` (criteria + manifest), `src/spines/run-wf-009.ts` (wiring), `test/spine-wf-009.test.ts` (hermetic — happy path + **both gateway halts** + missing-MoSCoW at STEP-01 + agent-missing fail-closed), `test/run-wf-009.test.ts` (real runner query() — call-order fake since RH-IA repeats — + budget/config guards), `test/run-wf-009-real-sidecar.test.ts` (real sidecar ≥ v4.1.0, the 4 agents resolve with real prose), `test/wf-009-run-live.test.ts` (billed live harness, `LIVE_RUN=1`-gated, OAuth-only; recommended model `claude-sonnet-5`), `test/fixtures/wf-009-outputs.ts`. Requires the catalog sidecar **≥ v4.1.0** (all four agents present, incl. `AGENT-RH-IA`). Offline proof (hermetic + real-sidecar) green. **A first billed live run (Sonnet 5) was performed and exposed a hollow-pass finding** (no CVs in the seed → the two decision gateways passed on the agent's honest in-band refusal); the gateways were hardened and the hollow trace is **not versioned** — see Fixed. WF-009 stays **offline-proven**; its showcase surface presents it as a defined backbone in a new domain, not a live proof.

- **WF-009 — `candidatePool` passthrough (enables a `completed` HR run without changing the orchestrator).** The BPMN sourcing step STEP-04 scores candidate CVs, but CVs are **exogenous** to the workflow (they arrive after the ad is published) and no upstream backbone step produces them; the first `completed` attempt was impossible because the linear orchestrator only routes data via the previous step's output (`run-spine.ts`: `input = output`), so STEP-04 only ever received `jobAd`. Added a `candidatePool` field carried **unchanged** through STEP-01→03 (a "recruitment dossier that accumulates") so the RH sourcing step (STEP-04) scores **real** profiles → a real shortlist (`rh-shortlist-validated` passes on substance) → a selected candidate (`sel-candidate-selected`) → the offer, i.e. a genuine `completed` 6/6 rather than a hollow pass. A **deliberate, documented deviation** from the one-field-per-hop idiom (the orchestrator stays **unchanged**, ADR-0007). ⚠️ **RGPD / demonstrator scope:** the pool is **synthetic and fictional** by design — this repo is public and the live trace is versioned, so a real CV would be personal data published with no legal basis or retention limit. For a **real** deployment this passthrough would be the wrong pattern (it threads candidate data through steps that do not need it, against data minimization); production would inject the pool at STEP-04 via an ATS side-channel (an orchestrator evolution), never echo it upstream — the passthrough is a demo affordance, not a blueprint for real data. Files: `src/spines/wf-009-recrutement.ts` (candidate item + pool schema, threaded through the STEP-01/02A/03 output contracts and STEP-02A/03/04 input contracts), `test/fixtures/wf-009-outputs.ts` (shared synthetic pool of 5, scored into a real shortlist), `test/wf-009-run-live.test.ts` (live seed carries the pool). Offline proof green: hermetic + **real-sidecar (≥ v4.1.0) `completed` 6/6 with real prose**. **Billed live run performed 2026-07-13** (Sonnet 5, OAuth, explicit approval): **`completed`, 6/6 steps `pass`**, ~6.5 min, $4 cap respected — the fifth domain (HR & Talent) proven end-to-end. Content inspected (not just verdicts): the 5-candidate synthetic pool flowed unchanged STEP-01→03, STEP-04 scored it with real discrimination (the seeded weak profile CAND-04 → 20/100, excluded), shortlisted 4 real candidates, and selected CAND-05 (91/100) — a *different* pick than the fixture, i.e. genuine reasoning, not an echo. The SAME `rh-shortlist-validated` gateway that halts on a no-candidate refusal here passes on real substance: the two WF-009 traces (`wf-009-live-result.json` completed + `wf-009-live-result-gate-halt.json` returned) together prove the gate **discriminates**. ⚠️ Framed honestly as `completed` **on a synthetic scenario with a provided candidate pool**, NOT autonomous sourcing (RGPD: synthetic by design). Trace versioned (secret-scan clean, no model field); live-runs README updated with both entries.

### 🐛 Fixed
- **WF-009 — the two decision gateways were fooled by an honest in-band refusal (hardened after the first live run).** The first billed live run (Sonnet 5) returned `completed` 6/6, but a **content** inspection (not just verdicts) exposed a **hollow pass**: the seed carried a recruitment brief but no candidate CVs, so STEP-04's active sourcing had nothing to work on — the agent honestly returned a 3-entry **placeholder** shortlist ("… only to satisfy `minItems:3`") and `selectedCandidate = "None — no candidate can be selected"`. The gateways `rh-shortlist-validated` (`minArrayLen ≥ 3`) and `sel-candidate-selected` (`nonEmptyString`) **passed on that content** — the exact `NO` branch they exist to halt on — cascading to a hollow offer letter (`"CANNOT BE ISSUED"`). Hardened: `rh-shortlist-validated` now counts REAL candidates (`countAffirmativeField(shortlist, "candidate") ≥ 3`) and `sel-candidate-selected` requires an `affirmativeString`, so both fail closed on the refusal (regression tests reproduce the live outputs in `test/spine-wf-009.test.ts`). Hardening the two gateways suffices — once the valves fail closed, the downstream completeness criteria only ever see real content. The hollow live trace is **not versioned** (it would be theater). **Live-verified 2026-07-12** (billed, Sonnet 5): re-running the same no-CV seed now **halts fail-closed at STEP-04** (`rh-shortlist-validated`) on the agent's honest refusal ("No candidate data available") — the identical content that produced the hollow pass before hardening — a "gate says no" proof on the candidate-selection decision; anonymized trace versioned `docs/audit/live-runs/wf-009-live-result-gate-halt.json` (renamed to preserve it alongside the later end-to-end `completed` run — see Added; secret-scan clean, no model field).

## [0.7.0] - 2026-07-12 — Three live-proven backbones: Consulting, Pre-sales, and the compliance-audit counter-review gate 🧱
> Model: Claude Opus 4.8

### ✨ Added
- **WF-006 spine — "Pre-sales / Commercial proposal" (GO/NO-GO qualification gateway).** New deterministic backbone sourced from `claude-agents/workflows/WF-006-avant-vente-proposition-commerciale.md` (v1.0): the sequential chain of the 6 core agents `CONSULTANT-IA → BUSINESS-ANALYST → AI-ARCHITECT → CHEF-PROJET-IA → FINANCIAL-ANALYST → REDACTEUR-IA` (STEP-01→05,07). Same `Management & Consulting` domain as WF-004 (does not open a new domain); its value is the **GO/NO-GO qualification gateway on STEP-01**: the blocking `presales-go` criterion reads a short `verdictCode` token (`GO | NO-GO | CONDITIONAL`) — **normalized** for case/whitespace (WF-001 lesson) so a verbosely phrased GO is not false-negated, kept a plain string rather than a schema `enum` so a stray value halts cleanly at the eval gate (documented no-bid) instead of failing earlier at ajv handoff validation — separate from a free-text `rationale`; a `NO-GO` or `CONDITIONAL` (conditional GO, which a live run cannot auto-resolve) halts the whole workflow at STEP-01 with a documented no-bid decision — the first **opening** business decision gate in the runtime, the same value-equality gate family as WF-003's `fa-decision-go` and WF-008's tier gateway, on the **unchanged** linear fail-closed orchestrator (ADR-0007, no LLM-as-judge). The `verdictCode`/`rationale` split hardens against the first live run's finding: seeded with a €250k full build vs two incumbent SSII, the agent (Opus 4.8) returned a reasoned **conditional-GO / no-bid** (win probability 35%) and wrote its whole rationale into the verdict field — the exact-match gate would have false-negated a GO phrased that way. The competitive-benchmark fork (STEP-03B VEILLE-STRATEGIQUE) and the AI-Act/GDPR contractual gateway (STEP-06 JURIDIQUE-IA) stay optional, outside the backbone (WF-003/004 simplification). Numeric DoD counts (4 commercial scenarios, 10-15 pitch slides) split into a relaxed blocking floor + an advisory at the exact spec number; the JSON schema communicates the ideal to the agent. Files: `src/spines/wf-006-avant-vente.ts` (criteria + manifest), `src/spines/run-wf-006.ts` (wiring), `test/spine-wf-006.test.ts` (hermetic — happy path + **NO-GO gateway halt** + conditional-GO halt + missing price), `test/run-wf-006.test.ts` (real runner query() + NO-GO + budget/config guards), `test/run-wf-006-real-sidecar.test.ts` (real sidecar ≥ v4.1.0, the 6 agents resolve with real prose), `test/wf-006-run-live.test.ts` (billed live harness, `LIVE_RUN=1`-gated, OAuth-only; recommended model `claude-opus-4-8`; Option-A seed = qualified opportunity → GO → completed), `test/fixtures/wf-006-outputs.ts`. The NO-GO no-bid halt is proven **deterministically offline** (the hermetic tests) — the billed live run is reserved for the additive proof: the full pre-sales chain running to `completed` and producing the commercial proposal (the live "a gate can say no" is already banked by WF-008). Requires the catalog sidecar **≥ v4.1.0**. **Live run performed 2026-07-11** (billed, OAuth, explicit approval) with `LIVE_MODEL=claude-opus-4-8` (the workflow's recommended model): **`completed`, 6/6 steps `pass`** (16/16 blocking + 14/14 advisory), the hardened **`presales-go` gateway returning GO** in the *proceed* direction and the full pre-sales chain running through to the commercial proposal (STEP-07). Seeded with a freelance-fit opportunity (≈€45k advisory/design scope, ~30-40 person-days via portage) so the qualification gate legitimately clears — the **additive** proof complementing WF-008's returned-for-rework halt: WF-008 banked "a gate can say *no*", WF-006 banks the hardened opening gate saying *yes* on a legitimate GO and the chain completing to the deliverable. Anonymized trace versioned `docs/audit/live-runs/wf-006-live-result.json` (secret-scan clean, no model field); live-runs README + `docs/NEXT_STEPS.md` snapshot updated.
- **WF-004 spine — "AI Consulting Engagement" (fourth domain: Management & Consulting).** New deterministic backbone sourced from `claude-agents/workflows/WF-004-mission-conseil-ia.md` (v1.1): the sequential chain of the 6 core agents `CONSULTANT-IA → FINANCIAL-ANALYST → CDO-DIRECTEUR-IA → CHANGE-MANAGER → FORMATEUR-IA → REDACTEUR-IA` (STEP-01→05, 07). Opens **Management & Consulting**, a domain untouched by WF-001/002 (Agile & Product), WF-003 (Dev & Engineering) and WF-008 (Compliance & Governance). Structurally a **linear twin of WF-003**: no fail-closed structural construct — STEP-01's `condition_passage` ("diagnostic validated before the roadmap") is modeled as an ordinary blocking eval gate, and the GDPR/AI-Act gateway (STEP-06 JURIDIQUE-IA) plus the competitive-benchmark fork (STEP-02B VEILLE-STRATEGIQUE) stay optional, outside the backbone (same simplification as WF-003). Numeric DoD counts (Top-5 use cases, 4 training levels, 3 ADKAR populations, 10-15 slides) are split into a relaxed **blocking floor** + an **advisory** at the exact spec number, while the JSON schema communicates the ideal to the agent. The orchestrator is **unchanged** (ADR-0007, deterministic ISO 19011 eval gate, no LLM-as-judge). Files: `src/spines/wf-004-conseil.ts` (criteria + manifest), `src/spines/run-wf-004.ts` (wiring), `test/spine-wf-004.test.ts` (hermetic — happy path + out-of-range maturity + empty ComEx deck), `test/run-wf-004.test.ts` (real runner query() + budget/config guards), `test/run-wf-004-real-sidecar.test.ts` (real sidecar ≥ v4.1.0, the 6 agents resolve with real prose), `test/wf-004-run-live.test.ts` (billed live harness, `LIVE_RUN=1`-gated, OAuth-only; recommended model `claude-opus-4-8`), `test/fixtures/wf-004-outputs.ts`. Requires the catalog sidecar **≥ v4.1.0**. **Live run performed 2026-07-11** (billed, OAuth, explicit approval) with `LIVE_MODEL=claude-opus-4-8` (explicit Opus 4.8 routing): **`completed`, 6/6 steps `pass`**, ~8.1 min, budget cap ($4.0) respected — first billed run routed to Opus 4.8, opening the fourth live-proven domain. Anonymized trace versioned `docs/audit/live-runs/wf-004-live-result.json` (secret-scan clean, no model field); live-runs README + `docs/NEXT_STEPS.md` snapshot updated.
- **WF-008 spine — "AI Act / GDPR Compliance Audit" (third domain, counter-review gate).** New deterministic backbone sourced from `claude-agents/workflows/WF-008-audit-conformite-ia-act-rgpd.md` (v1.0): the sequential chain of the 7 core agents **plus** the independent methodology counter-review — `JURIDIQUE-IA → AI-ARCHITECT → SECURITE-IA → DATA-ENGINEER → CDO-DIRECTEUR-IA → CHANGE-MANAGER → AUDIT-METHODO-IA → REDACTEUR-IA`. Opens **Compliance & Governance**, a domain untouched by WF-001/002 (Agile & Product) and WF-003 (Dev & Engineering). Two deterministic constructs new to this spine, both enforced by the existing fail-closed eval gate (ADR-0007, no LLM-as-judge — the agent emits the verdict, a rule enforces it): (1) a **counter-review clearance gate** on STEP-06C (`audit-verdict-cleared` requires `verdict === "cleared"`) — a `"returned"` verdict halts the spine *before* the final report (STEP-07), the first mid-spine return-for-rework gate in the runtime; (2) an **"Unacceptable" AI Act tier gateway** on STEP-01 (`jur-tier-not-unacceptable`) — an unacceptable system halts the backbone (recommend cessation), fail-closed. The parallel fork (STEP-02/03/04) is linearized and the two "if required" branches (STEP-04B DATA-SCIENTIST, STEP-06B FINANCIAL-ANALYST) stay optional, outside the backbone — same simplification as the WF-003 spine. The orchestrator is **unchanged**: the whole design fits the existing linear fail-closed executor. Files: `src/spines/wf-008-audit.ts` (criteria + manifest), `src/spines/run-wf-008.ts` (wiring), `test/spine-wf-008.test.ts` (hermetic — happy path + tier gateway + returned-verdict gate + anti-theater empty bias log), `test/run-wf-008-real-sidecar.test.ts` (real sidecar ≥ v4.1.0, the 8 agents resolve with real prose), `test/wf-008-run-live.test.ts` (billed live harness, `LIVE_RUN=1`-gated, OAuth-only), `test/fixtures/wf-008-outputs.ts`. Requires the catalog sidecar **≥ v4.1.0** (the release that widened the sidecar to WF-008's agents, incl. `AGENT-AUDIT-METHODO-IA`). `npm test` **139/8** + `typecheck` green. **Live run performed 2026-07-11** (billed, OAuth, explicit approval) with `LIVE_MODEL=fable`: **`failed` — fail-closed at STEP-06C by design** — STEP-01→06 all `pass`, then the counter-review gate `AUDIT-METHODO-IA` **returned the audit** (`verdict="returned"`, 5 documented reservations + 4-entry bias log + 6 ISTQB exit criteria; only the blocking `audit-verdict-cleared` did not clear), halting the report (STEP-07). First live trace of the fail-closed halt **blocking** a deliverable (a stronger signal than an all-green run: the gate can say *no*). Anonymized trace versioned `docs/audit/live-runs/wf-008-live-result.json` (secret-scan clean, no model field); live-runs README + `docs/NEXT_STEPS.md` snapshot updated.

### 🔄 Changed
- **Ultrareview follow-ups (no behavior change).** Two cosmetic/wording cleanups surfaced by a cloud code review of PR #22: (1) renamed the leftover French local `gabarit` → `template` in the WF-001 INVEST check (`src/spines/wf-001-cadrage.ts`); (2) aligned the WF-002 `voteConfiance` criterion/schema descriptions to the actual gate semantics — they advertised a strict `> 3.5` while the check uses `numberAtLeast` (`≥ 3.5`), so both descriptions now read `≥ 3.5` (`src/spines/wf-002-delivery.ts`). The gate behavior is unchanged; `npm test` 126/6 + `typecheck` green.

### 🐛 Fixed
- **WF-001 INVEST criterion — determiner-agnostic (gate-semantics fix).** The advisory `po-us-format-invest` regex only accepted `As a`/`As an`, so valid stories opening with `As the …` (or a role with no determiner) were wrongly flagged off-template. Reproduced on the **WF-001 Fable live run** (`docs/audit/live-runs/wf-001-live-result.json`): 2 of 13 real stories failed, e.g. *"As the Head of B2B (sponsor), I want … so that …"*. Broadened `/as an? .+i want.+so that/i` → `/\bas\b .+\bi want\b.+\bso that\b/i` (`src/spines/wf-001-cadrage.ts`): only the three narrative anchors (`as …` / `i want …` / `so that …`) are enforced — determiner-agnostic — and a word boundary stops `was a` from being misread as the `as a` opener. Regression cases added (`test/wf-001-output-contract.test.ts`: `As the …`, no-determiner, `was a`). The fixed criterion passes all 13 real stories of the live trace; `npm test` **130/6** + `typecheck` green.

### ⬆️ Dependencies
- **`@anthropic-ai/claude-agent-sdk`** 0.3.195 → 0.3.201 (Dependabot #30).

### Notes
- Offline suite green at release: **165 passed / 12 skipped** + strict `typecheck` (live tests skipped by default → no token cost in CI).
- **Three backbones proven live (billed runs, OAuth subscription only), 2026-07-11:** **WF-004** `completed` 6/6 on Opus 4.8 (opens the fourth domain, Management & Consulting); **WF-006** `completed` 6/6 on Opus 4.8 (the hardened opening `presales-go` gate returning GO on a legitimate opportunity, the pre-sales chain completing to the commercial proposal); **WF-008** `failed` **fail-closed at STEP-06C by design** on Fable (the counter-review gate returned the audit, `verdict="returned"`, halting the report — the first live trace of a gate blocking a deliverable). Verdicts judged per-criterion (`gate.results[].passed`), not on final status. Anonymized traces versioned under `docs/audit/live-runs/`.
- The orchestrator is **unchanged** across all three backbones (ADR-0007, deterministic fail-closed eval gate, no LLM-as-judge).
- The released version history below stays frozen in French (audit integrity).

## [0.6.0] - 2026-06-21 — Full FR → US English transition, live-proven in English 🌐
> Model: Claude Opus 4.8

### 🎯 Context
Milestone: the runtime is now **100% US English** (source, tests, schema, docs) **and proven to work live in English** — the FR → US English internationalization of the runtime is complete. Unlike a pure documentation pass, this release **changes runtime behavior** (R3): the runtime now expects English deliverables and judges them with English criteria, coherent with the now-English `claude-agents` catalog. The three workflows were re-run live in English to refute the "green offline ≠ works live" risk.

### 🔄 Changed — behavior (R3, `34a847c` · `fdafe5f`)
- **The runtime now expects English deliverables.** The deliberate decision (taken with Guy) is to align the runtime with the English catalog agents. Concretely:
  - WF-001 INVEST criterion regex `/en tant que .+ je veux .+ afin (de|d')/` → **`/as an? .+i want.+so that/`**; the statement schema hint becomes « As a … I want … so that … ».
  - Data enum `erreur`/`limite` → **`error`/`boundary`** (predicate + schema hints across WF-001/WF-003).
  - `query-runner` `formatInstruction()` → "ENFORCED OUTPUT FORMAT…", and the no-result message → "stream ended with no result message".
  - All coupled test fixtures and assertions (`wf-001-output-contract`, `wf-00x-outputs`, `test/fixtures/catalog/*`, query-runner/sidecar.schema tests, sdk-adapter) were synced in the same commits. Category-C tokens (criterion/agent/`WF-*`/`STEP-*` IDs, schema keys, enums kept as data, model IDs, paths, env vars) left intact.

### 📝 Changed — internationalization, no behavior change
- **R1 (`d57f058`, PR #20) — documentation** to US English: `README.md`, `CONTRIBUTING.md`, `COMMERCIAL.md`, the PR template, `docs/ARCHITECTURE.md`, `docs/NEXT_STEPS.md`, `docs/note_cadrage_poc.md`, the 7 ADRs (`docs/adr/0001`→`0007`), and the audit reports (`docs/audit/`).
- **R2 (`b4fcc4a`, PR #21) — source** comments, docstrings and non-asserted `Criterion.description` audit labels across `src/**/*.ts`. Executed code untouched.
- **R3b (`c5f99d2`, PR #23) — decorative test prose**: `it()`/`describe()` names, comments, docstrings and non-asserted fixture prose across all `test/` files. Asserted strings left as-is (already English).
- **R4a (`7ca48c7`, PR #24) — schema descriptions**: the 11 `description` fields of the SSOT `schema/sidecar.schema.json` (ajv ignores descriptions → no validation impact). The vendored copy in `claude-agents` was resynced canonically identical (catalog PR #12); `check:schema-drift` green both sides.

### 🐛 Fixed (`97d1a9e`, PR #25)
- **WF-003 live-run caps raised** so the 7-agent chain completes: `maxTurns` 6 → 15 and `maxBudgetUsd` 1.0 → 4.0 in `wf-003-run-live.test.ts`. The earlier live failure was a genuine `error_max_turns` at STEP-03 (code-gen agent), **not** a translation defect.

### ✨ Added (`97d1a9e`, PR #25)
- **Archived English live-run traces** under `docs/audit/live-runs/` from the English re-proof (anonymized; fictional client, public author name only).

### ⬆️ Dependencies
- **`hono`** 4.12.23 → 4.12.25 (Dependabot #19).
- **`@anthropic-ai/claude-agent-sdk`** 0.3.176 → 0.3.177 (Dependabot #18).

### 🔒 License
- Aligned the ISO 5230 license audit (`docs/audit/conformite_licences_iso5230.md`) with the PolyForm Noncommercial relicensing (`c936070`).

### Notes
- Offline suite green throughout: **126 passed / 6 skipped** + strict `typecheck` (live tests skipped by default → no token cost in CI).
- **Live re-proof in English (billed runs, observed, OAuth subscription only):** **WF-001 12/12 criteria** (incl. the 2 translation-coupled advisories — INVEST regex matched a real English PO output, `error`/`boundary` covered), **WF-002 15/15**, **WF-003 26/26** (`completed`, 7/7 steps, 762s). Verdicts judged per-criterion (`gate.results[].passed`), not on final status. → the "it works in English" claim is now **certain**, not inferred.
- The released version history below stays frozen in French (audit integrity).

## [0.5.0] - 2026-06-17
> Modèle : Claude Opus 4.8

### 🔄 Changed
- **Relicensing : Business Source License 1.1 → PolyForm Noncommercial License 1.0.0** (`LICENSE`). Abandon de la bascule automatique vers Apache 2.0 (*Change Date* à 4 ans) au profit d'une **protection non-commerciale permanente** : tout usage commercial requiert désormais une licence séparée, sans expiration. Texte standard PolyForm (identifiant SPDX `PolyForm-Noncommercial-1.0.0`) + deux `Required Notice` (copyright Guy HUI-BON-HOA + contact commercial). Mises en cohérence : badge et section « Licence » du `README.md`, champ `license` de `package.json`, et `COMMERCIAL.md` (libre = usage non-commercial ; payant = tout usage commercial ; section « bascule » remplacée par « protection permanente »). **Bump mineur** : pré-1.0 (POC / asset de portfolio), le changement de termes de licence est tracé comme évolution notable sans signaler une GA.

## [0.4.0] - 2026-06-13
> Modèle : Claude Opus 4.8

### ✨ Added
- **Première trace live versionnée — WF-003 `completed`** (`docs/audit/live-runs/wf-003-live-result.json`) : run live « Lancement Application IA » du 2026-06-13, **7/7 étapes `pass`** (STEP-00 → STEP-06), cap budget respecté. Revue P3 OK (cas synthétique, aucune donnée réelle/secret) → commit `git add -f`. **Clôt l'artefact P3** (mécanisme **et** preuve par exécution réelle). Valide en live le correctif « structured output natif » à STEP-03.
- **Câblage run live WF-002 / WF-003** (`src/spines/run-wf-002.ts`, `run-wf-003.ts`) : `assembleWf00X` + `runWf00X`, strictement calqués sur `run-wf-001.ts` (manifeste réel + registre + résolveur d'agent + `createQueryRunner`, mêmes gardes fail-closed).
- **Schémas de sortie RESSERRÉS** des spines WF-002/003 (`wf-002-delivery.ts`, `wf-003-lancement.ts`) — application de la leçon WF-001 : le schéma injecté **==** le contrat vérifié par la gate. Bornes de comptage bloquantes → `minItems/maxItems` ; champs imbriqués bloquants → `required` ; critères **advisory** → `description` seulement (jamais de contrainte dure). Le contrat reste compatible avec les sidecars intérimaires existants.
- **Preuve hors-ligne** : tests hermétiques de câblage (`run-wf-00X.test.ts` : mock conforme → `completed` ; eval gate → `failed` fail-closed ; gardes budget/config) **et** tests « sidecar réel » (`run-wf-00X-real-sidecar.test.ts` : `loadSidecar` du vrai `sidecar.json` → prose réelle → `assembleWf00X` → `runSpine` → `completed`, skip propre si catalogue absent). Sorties conformes factorisées en fixtures partagées (`test/fixtures/wf-00X-outputs.ts`, DRY).
- **Harnais de run live gardés** (`wf-002-run-live.test.ts`, `wf-003-run-live.test.ts`, `LIVE_RUN=1`) : capture persistante dans `docs/audit/live-runs/` (artefact P3), skip par défaut → CI verte, aucun appel facturé.
- **Observabilité par étape** (`runSpine` : hook `onStep?(StepProgressEvent)`, propagé par `runWf001/002/003`) : émet `start`/`done` (+ verdict) à chaque étape. Les harnais live écrivent une **trace de progression incrémentale** (`live-progress.json`, helper partagé `test/live-progress.ts`) → un run long est sondable EN DIRECT et exploitable même s'il est interrompu. Sans effet quand le hook est absent.
- **Sortie structurée NATIVE du SDK** (`query-runner`) : quand un `outputSchema` est fourni, on passe `outputFormat: { type:"json_schema", schema }` à `query()` → le SDK contraint la sortie au schéma et remplit `structured_output` (avec ses retries natifs). Bien plus robuste que de parser un texte rédigé par le modèle — corrige l'échec du run live WF-003 à STEP-03 (DEV-PYTHON-IA : code volumineux → JSON cassé). Le parsing texte reste un filet de sécurité (`structured_output` absent).

### 📝 Changed
- **Tests « sidecar réel »** : le sidecar du catalogue `claude-agents` passe à **14 assets** (indexation des backbones WF-002/003, `claude-agents` v3.26.2). `run-wf-001-real-sidecar.test.ts` — assertion de périmètre passée d'une **égalité stricte** (3 ids figés) à une **inclusion** (backbone WF-001 ⊂ assets) : le runtime ne dépend que de ce qu'il consomme ; l'inventaire exact est la propriété du générateur catalogue + son `--check` CI (ADR-0002/0003). Un nouvel agent indexé ne casse plus ce test.
- **Défaut `CATALOG_ROOT`** réaligné sur le repo **réel** `claude-agents` via un point de vérité unique `test/catalog-root.ts` (le défaut figé `claude-catalogue` était un vestige de renommage qui consommait un clone stale en silence). Fin de la duplication du défaut dans `run-wf-001-real-sidecar` + `wf-001-run-live`.

### ⬆️ Dependencies
- **`@anthropic-ai/claude-agent-sdk`** 0.3.162 → **0.3.176** (Dependabot #4, rebasé et re-validé : `query()`/types OK).
- **`typescript`** ^5.6 → **^6.0** (6.0.3) et **`@types/node`** ^22 → **^25** (25.9.3) (Dependabot #3, bump majeur TS validé sur l'artefact réel : `tsc --noEmit` strict OK + suite verte).
- **`actions/checkout`** v4 → **v6** dans la CI (Dependabot #2).

### Notes
- Suite verte, `typecheck` strict OK. Décompte selon l'environnement : **126 passed / 6 skipped** catalogue `claude-agents` présent (les 6 skips = **3 harnais live facturés** `LIVE_RUN` + **3 marqueurs `runIf`** des blocs « catalogue absent », inactifs car le catalogue est là ; les validations « sidecar réel » tournent et passent). En CI sans catalogue, la combinaison est symétrique (validations sidecar skippées, marqueurs actifs). **Runs live observés et probants : WF-003 (`completed`, 7/7) et WF-002 (`completed`, 5/5)** → artefact P3 clos, les deux traces versionnées dans `docs/audit/live-runs/`.

## [0.3.0] — 2026-06-11 — POC exécutable de bout en bout (run live WF-001) + audit qualité ISO v1
> Modèle : Claude Opus 4.8

### Fixed
- **Remédiation audit ISO v1 — P1 + P2 (2026-06-11, Opus 4.8)**. Lot **distinct** et **postérieur** au constat figé (commit d'audit `0f981b7`), conformément à l'intégrité d'audit (on corrige après avoir constaté).
  - **P1 — décomposition ISO 25012 corrigée** : la formule « 5 schéma + 2 intégrité » était **inexacte** (elle omettait `conformité`, pourtant portée par l'enum `type` au schéma, et comptait `exactitude`/`accessibilité` comme purement « intégrité » alors qu'elles sont déjà bien-formées au schéma). Énoncé rectifié partout en **7 caractéristiques avec leur lieu d'encodage exact** : 5 pleinement au schéma (complétude, cohérence, crédibilité, actualité, **conformité**) + `exactitude`/`accessibilité` **réparties** schéma (bien-formé) ↔ intégrité (réel). Le **total de 7 était juste**, seule la ventilation était fausse. Fichiers : `schema/sidecar.schema.json` (`description` du `$id`), `docs/NEXT_STEPS.md` (§1 tableau + §3.2). **Aucun changement de comportement** (descriptions/doc uniquement, validation ajv inchangée).
  - **P2 — nomenclature alignée (balayage exhaustif)** : « spine WF-001→003 » → « **backbone d'un workflow (ex. WF-001)** » dans **tous les documents vivants** où elle apparaissait — `docs/ARCHITECTURE.md` (point de vue « Exécution », ligne citée par l'audit), `docs/NEXT_STEPS.md` §3.4 (déclencheur), `README.md` (roadmap). Cohérent avec [NEXT_STEPS §2.4-B.3](docs/NEXT_STEPS.md) : l'unité réellement exécutable est le backbone d'UN workflow, pas une macro-chaîne. **Reliquat de README** corrigé au passage : case §2.4-B cochée (exécuteur live **fait**) ; case « PR de bump catalogue » laissée décochée (process manuel tracé, pas une PR automatisée — cf. §2.5). **Exclu volontairement** : `docs/note_cadrage_poc.md` (« Document fondateur — VALIDÉE par Guy 2026-06-03 ») laissé **intact** — instantané figé, comme un ADR ; sa divergence est déjà annotée dans NEXT_STEPS (« nommage placeholder »).
  - Périmètre : **docs/schéma uniquement**, zéro code de production touché ; suite de tests inchangée verte.
- **Remédiation audit ISO v1 — P3 : observabilité du run live (2026-06-11, Opus 4.8)**. La capture du résultat de run live ne va plus dans `/tmp` éphémère mais dans une **trace persistante du repo** : `test/wf-001-run-live.test.ts` écrit désormais par défaut dans `docs/audit/live-runs/wf-001-live-result.json` (+ `mkdirSync` récursif), surchargeable par `LIVE_RESULT_FILE`. Le `.json` brut est **gitignoré** (`docs/audit/live-runs/.gitignore`) ; la trace **anonymisée** d'un run probant sera committée sur décision après revue (`docs/audit/live-runs/README.md`). **Honnêteté assumée** : la trace du run « plein pot » du 2026-06-09 n'est PAS reconstituée (capture `/tmp` perdue ; reconstruction manuelle = artefact fabriqué, **refusée**) — seul le **mécanisme** est corrigé ; l'**artefact** suivra le **prochain run live** (Piste A, facturé, sur accord). Harness gardé `LIVE_RUN=1` (skip par défaut → CI verte), aucun appel facturé. Typecheck strict OK, 111 tests verts.
- **Remédiation audit ISO v1 — P4 : conformité licences ISO 5230 / OpenChain (2026-06-11, Opus 4.8)** (`docs/audit/conformite_licences_iso5230.md`). Déclencheur ISO 5230 atteint (licence MIT fixée) → inventaire **factuel** des licences (149 paquets prod+dev+transitif). **Verdict global 🟢 Conforme** : aucun copyleft fort (0 GPL/AGPL/LGPL/EUPL/CDDL), deps de production permissives (MIT/ISC/BSD/Apache-2.0), MPL-2.0 (`lightningcss`) **dev-only** (absent du sous-arbre production). **Découverte factuelle** : `@anthropic-ai/claude-agent-sdk` est **propriétaire** (« © Anthropic PBC », Commercial Terms), pas OSI — compatible avec la publication MIT du code propre, mais à **divulguer**. **Action** : section « Licence » du `README.md` complétée pour expliciter cette dépendance d'exécution propriétaire (abonnement requis). **ISO 23894 et 25024 laissées différées** (déclencheurs non atteints — pas de sur-ingénierie sur un POC *asset portfolio*).

### Added
- **§3 — Audit qualité ISO v1 livré (2026-06-11, Opus 4.8)** (`docs/audit/audit_qualite_iso_v1.md`) : auto-évaluation interne (*gap analysis*, PAS une certification — ADR-0006), déclencheur §3.4 atteint (run live « plein pot » 2026-06-09). Posture ISO 19011 (preuves factuelles `fichier:ligne` + sorties de tests + run live réel, verdict nuancé assumé). Grille critère→preuve→verdict sur les 4 référentiels retenus. **Verdicts** : Données (25012) 🟡 Conforme avec réserve · Architecture (42010) 🟡 Conforme avec réserve · Logiciel/runtime (25010) 🟢 Conforme · Gouvernance IA (42001) 🟢 Conforme. Aucune non-conformité majeure, aucun faux signal. **2 réserves documentaires** identifiées (P1 : décomposition ISO 25012 « 5+2 » inexacte — `conformité` omise, `exactitude`/`accessibilité` réparties schéma↔intégrité, total 7 OK ; P2 : nomenclature obsolète « spine WF-001→003 » dans `ARCHITECTURE.md:67`) + **révision des normes différées** (ISO 5230 **déclenchée** : licence MIT fixée ; ISO 23894 en approche ; ISO 25024 reste différée). Plan de remédiation priorisé P1→P5. **Constat figé** : audit committé AVANT toute remédiation (intégrité d'audit — on ne corrige pas avant de constater) ; corrections P1/P2 traitées en lot distinct ultérieur référençant cet audit. Audit en lecture/analyse seule, zéro modification de code de production.
- **§2.4-B.3 — RUN LIVE WF-001 EXÉCUTÉ (2026-06-09, observé, OAuth abonnement) + resserrement des contrats**. Premier run de bout en bout avec le vrai `query()` (caps 1 $/étape, `maxTurns` 6, `permissionMode:"plan"`, sidecar réel v3.26.0, prose réelle). **Déclencheur d'audit ISO §3.4 atteint.** Résultat : `failed` fail-closed à STEP-03 — **STEP-01 (BUSINESS-ANALYST) passé en live** (JSON conforme produit par un vrai agent), arrêt à STEP-03 (PO-SCRUM). Cause = **écart de contrat** (24 US / 9 épics vs DoD 8–15 / 3–5 ; champ `userStory` ≠ `statement`), le schéma de sortie étant trop lâche pour communiquer la forme attendue. **Correctif** : schémas de sortie STEP-03/04 resserrés — forme exacte des items (US : `statement`/`priorite`/`estimation`/`dod` ; Gherkin : `given`/`when`/`then`) + bornes `minItems`/`maxItems` — via le nouvel helper `arrOf` (`src/spines/spine-helpers.ts`). Le format injecté au prompt = désormais le contrat vérifié par la gate (source unique). Harness reproductible `test/wf-001-run-live.test.ts` (gardé `LIVE_RUN=1`, **skip par défaut** → aucun appel facturé en CI) + régression `test/wf-001-output-contract.test.ts`. Fixtures hors-ligne inchangées (rétrocompatible). Suite **108 tests verts** (+2 skip), `typecheck` strict OK. ✅ **Re-run live après correctif : `completed` (2026-06-09)** — 3 agents en live, **toutes les gates *blocking* passées** (STEP-01 ✓ · STEP-03 ✓ : 13 US ∈ 8–15, champs `statement/priorite/estimation/dod`, 5 épics ∈ 3–5 · STEP-04 ✓ : 7 scénarios `given/when/then` + plan de test). ✅ **Cadrage « plein pot »** : les 2 *advisory* traités → re-run `completed` avec **0 critère en échec** (blocking + advisory). (a) `qa-cas-erreur-et-limite` : nudge par description de schéma (`type: nominal|erreur|limite`) → l'agent couvre les 3. (b) `po-us-format-invest` : **bug de critère** (regex sans élision française) corrigé pour accepter « En tant qu'… afin d'… », validé 12/12 sur la sortie live réelle + fixture (régression dans `test/wf-001-output-contract.test.ts`). **Thèse du POC empiriquement validée** (gates blocking ET advisory vertes en live).
- **§2.4-B.3 — injection du format de sortie (rend la spine aboutissable au live)** (`src/sdk/query-runner.ts`, `src/orchestrator/{types,run-spine}.ts`) : le `StepRunCall` porte désormais `outputSchema` (= `contract.output`), transmis par `runSpine`. Quand il est présent, `createQueryRunner` (a) **ajoute au prompt** une instruction « réponds UNIQUEMENT par un JSON conforme à ce schéma », et (b) **parse la réponse en objet** (`parseJsonObject` : JSON nu, clôture ```json``` ou objet noyé dans du bruit) ; une réponse non parsable en objet ⇒ **fail-closed** (`QueryRunnerError`). C'est la brique qui permet aux eval gates / handoffs (qui attendent des objets) de s'appliquer au run live — sans elle, un agent renvoie de la prose et la spine échouait dès STEP-01. Sans `outputSchema` : comportement inchangé (sortie texte brute). **Rétrocompatible** (les 9 tests runner d'origine inchangés). +7 tests runner (injection · prompt inchangé sans schéma · parse objet · clôture markdown · prose→levée · tableau→levée · `structured_output` prioritaire) + `test/wf-001-live-path.test.ts` (2 tests **bout-en-bout via le vrai `createQueryRunner`** : `query` JSON conforme→`completed` 3 verdicts pass · `query` prose→fail-closed). Suite **103 tests verts**, `typecheck` strict OK. Aucun appel réseau/facturé. **Reste pour le run live** : auth OAuth abonnement + lancement sur accord explicite + run observé.
- **§2.4-B.3 (étape 2) — câblage du run WF-001** (`src/spines/run-wf-001.ts`) : `runWf001(opts)` assemble le manifeste réel via `loadSpine` (+ registre + résolveur d'agent) puis le déroule avec `runSpine` branché sur `createQueryRunner` — point d'entrée du run de bout en bout, l'orchestrateur restant pur/inchangé. Résolveur et `runnerDeps` injectables (prod = `toAgentDefinition(catalogRoot)` + `query()` SDK ; test = stub + faux `query`). 4 tests **bout-en-bout hermétiques** passant par le VRAI `createQueryRunner` (donc ses 4 gardes) : DoD conforme→`completed` · sous-seuil DoD→`failed` eval-gate · garde budget `ANTHROPIC_API_KEY`→levée · config insuffisante→`Wf001ConfigError`. Aucun appel réseau/facturé. **Reste pour le run live réel** : sidecar réel (générateur §2.3, repo `claude-agents`) + lancement sur accord explicite + run observé.
- **§2.4-B.4 — spines réelles WF-002 et WF-003** (`src/spines/wf-002-delivery.ts`, `src/spines/wf-003-lancement.ts`) : deux nouveaux backbones déterministes sourcés des vrais workflows du catalogue, sur le modèle de WF-001 (1 fichier + tests hermétiques dédiés par spine).
  - **WF-002 « Delivery Agile SAFe »** (v1.1) : backbone séquentiel **STEP-01 (PRODUCT-MANAGER-SAFE) → STEP-02 (RELEASE-TRAIN-ENGINEER) → STEP-03 (PO-SAFE) → STEP-04 (SCRUM-MASTER) → STEP-06 (CHEF-PROJET-IA)** ; STEP-05 (QA, parallèle) et STEP-07 (CHANGE, conditionnel) hors backbone. 14 critères déterministes tracés aux DoD réels (dont conditions chiffrées : vote de confiance > 3.5, 3-5 PI Objectives, 5-10 US sprint). 5 tests.
  - **WF-003 « Lancement Application IA »** (v1.2) : backbone séquentiel complet des **7 agents core** STEP-00 (FINANCIAL-ANALYST) → STEP-01 (PROMPT-ENGINEER) → STEP-02 (AI-ARCHITECT) → STEP-03 (DEV-PYTHON-IA) → STEP-04 (QA-AGILE) → STEP-05 (DEVOPS-CLOUD) → STEP-06 (SECURITE-IA) ; le fork DEV-TYPESCRIPT-IA (optionnel) hors backbone. 18 critères déterministes tracés aux DoD réels (dont `condition_passage` chiffrées : Go financier, coverage ≥ 80 %, taux réussite ≥ 90 %, golden dataset 20-50, 0 vulnérabilité Critical OWASP LLM, < 2 High). 6 tests.
  - Décision actée : chaque workflow a SA propre spine (pas de macro-chaîne WF-001→003). Contrats de handoff conçus linéaires fail-closed (sortie amont promet le champ requis par l'entrée aval immédiat).
- **Helpers de spine partagés** (`src/spines/spine-helpers.ts`) : fabriques de schémas JSON (`objSchema`/`arr`/`str`/`num`/`obj`) + prédicats de lecture défensive (`asRecord`/`nonEmptyArray`/`arrayLenBetween`/`minArrayLen`/`nonEmptyString`/`isNumber`/`numberAtLeast`), factorisés depuis `wf-001-cadrage.ts` (DRY, partagés par les 3 spines). `wf-001-cadrage.ts` refactoré pour les importer (zéro changement de comportement, tests WF-001 inchangés).
- **§2.5 — Pipeline CI** (`.github/workflows/ci.yml`) : workflow GitHub Actions sur `push`/`pull_request` vers `main` — `npm ci` + `typecheck` strict + `npm test` sur matrice Node 20/22 (la suite couvre la validation des schémas ajv + les eval gates, fail-closed). `concurrency` annule les runs obsolètes ; `permissions: contents read`. **Dependabot** (`.github/dependabot.yml`) : bumps npm + github-actions hebdomadaires en **PR tracées** (cohérent ADR-0002 — propagation explicite, jamais implicite) ; le catalogue `claude-agents` (non-dépendance npm, épinglé par `catalogTag`) reste un bump manuel tracé.
- **§2.4-B.3 (étape 2) — adaptateur `query()` → `StepRunner`** (`src/sdk/query-runner.ts`) : `createQueryRunner(deps)` enveloppe la fonction `query()` du Claude Agent SDK derrière l'interface `StepRunner` de l'orchestrateur (§2.4-B.1), permettant le run LIVE sans toucher au cœur pur de `runSpine`. `query` est **injectable** (défaut = SDK), d'où des tests **hors-ligne** (faux `query` → `AsyncIterable` de messages, zéro réseau). Mapping `StepRunCall`→`query` : `input` sérialisé → `prompt` · `agent.prompt` → `systemPrompt` · `agent.tools`/`disallowedTools`/`model`/`maxTurns` → options. **4 gardes fail-closed non négociables** : (1) `ANTHROPIC_API_KEY` défini ⇒ refus AVANT tout appel (OAuth abonnement uniquement, jamais de clé métrée) ; (2) `permissionMode:"plan"` **forcé** (read-only, ADR-0001) ; (3) caps DURS par étape (`maxBudgetUsd` 1.0 + `maxTurns` 8 par défaut, jamais dépassés même si l'agent en demande plus) ; (4) tout résultat non-`success` (budget/tours/erreur) ou flux sans résultat ⇒ on lève. Extraction sortie : `structured_output ?? result`. 9 tests hermétiques (mapping + plan forcé · sérialisation JSON · string brute · plafonnement maxTurns · garde clé-API · succès→output · structured_output prioritaire · erreur→levée · flux vide→levée) ; suite portée à **79 tests verts**, `typecheck` strict OK. Aucun appel réseau/facturé. **Reste pour le run live §2.4-B.3** : sidecar réel via générateur §2.3 (chantier `claude-agents`) + lancement gardé sur accord explicite + run observé.
- **§2.4-B.3 (prép hors-ligne) — première spine RÉELLE : backbone de WF-001** (`src/spines/wf-001-cadrage.ts`) : manifeste `WF_001_CADRAGE_MANIFEST` sourcé du vrai workflow du catalogue (`workflows/WF-001-cadrage-produit-ia.md`, v1.2) — backbone séquentiel **STEP-01 (BUSINESS-ANALYST) → STEP-03 (PO-SCRUM) → STEP-04 (QA-AGILE)**, contrats I/O conçus cohérents amont/aval (handoff fail-closed). **11 critères déterministes** tracés aux DoD réels des fiches d'étape (8 *blocking* + 3 *advisory*), exposés via `buildWf001CadrageRegistry()`. Étapes conditionnelles (UX-DESIGNER / CHANGE-MANAGER / PRODUCT-MANAGER-SAFE) hors backbone (YAGNI). 6 tests hermétiques (assemblage 3 étapes · fail-closed si agent absent du sidecar · run conforme→`completed` · sous-seuil DoD 5 US→`failed` eval-gate · *advisory* non bloquant) ; suite portée à **70 tests verts**, `typecheck` strict OK. Aucun appel réseau/facturé. **Reste pour le run live §2.4-B.3** : sidecar réel via générateur §2.3 (chantier `claude-agents`) + branchement `query()` gardé (caps + OAuth).
- **§2.4-B.2 — implémentation ADR-0007 : manifeste de spine + registre de critères (hors-ligne)** (`src/manifest/{types,load-manifest}.ts`, `src/eval/criteria-registry.ts`) : `loadSpine(manifest, sidecar, registry, resolveAgent)` assemble un `SpineManifest` en `SpineStep[]` prêt pour `runSpine`, en **croisant fail-closed** le sidecar (asset présent ET de type *agent*) et le registre (critère résolu par `id`), provenance (`assetId`/`catalogTag`) tirée de l'asset, agent résolu via un `AgentResolver` injecté (`toAgentDefinition` en prod). Contrats portés en JSON Schema (donnée) dans le manifeste ; critères restés du code, référencés par `id` via `CriterionRegistry` (refus des doublons, `resolve` fail-closed). `ManifestValidationError` agrège toutes les incohérences (asset inconnu, non-agent, critère inconnu, stepId dupliqué, spine vide). Orchestrateur `runSpine` **inchangé** (assembleur en amont). 7 tests hermétiques (dont bout-en-bout manifeste→runSpine) ; suite portée à **64 tests verts**.
- **§2.4-B.1 — exécuteur de la spine (hors-ligne, sans réseau)** (`src/orchestrator/{types,run-spine}.ts`) : `runSpine(steps, runner, initialInput)` déroule WF-001→002→003 en branchant, par étape, un **runner injectable** (abstraction de `query()`, mocké en B.1), l'**eval gate** (brique 2, fail-closed) sur la sortie, puis les **contrats de handoff** (brique 1, fail-closed) vers l'aval. **Pré-vol statique** (`checkContractCompatibility`) des contrats adjacents AVANT tout appel runner. Orchestrateur **pur** (aucun disque/réseau, seul effet via le runner). **Provenance** (`assetId` + `catalogTag`) et **`GateReport`** consignés dans une trace d'audit conservée jusqu'à l'étape atteinte, y compris en échec (posture ISO 19011). 6 tests hermétiques (happy path · provenance · fail-closed gate · fail-closed handoff · pré-vol statique · cohérence stepId) ; suite portée à **57 tests verts**. Source des contrats/critères encore en fixtures (décision de source = §2.4-B.2, à trancher).
- **§2.4-A — adaptateur catalogue → SDK** (`src/sdk/to-agent-definition.ts`) : transforme un `Asset` de type *agent* (+ sa prose `.md`) en `AgentDefinition` du Claude Agent SDK (`prompt` = prose, `description` = description du sidecar). Lecture seule (ADR-0001), garde anti-traversal, défauts read-only (`Read`/`Grep`/`Glob`) + `overrides` explicites en attendant que le sidecar porte `tools`/`model`/`mcp` (*data gap*, cf. `NEXT_STEPS §2.1`). 5 tests ; suite portée à **51 tests verts**.
- **Dépendance `@anthropic-ai/claude-agent-sdk`** (`^0.3.162`) : package vérifié (API `query({prompt, options})`, type `AgentDefinition`). Première moitié de l'intégration SDK, **sans réseau ni appel facturé**.

- **ADR-0007 — source des contrats/critères tranchée (§2.4-B.2)** (`docs/adr/0007-...md`) : **manifeste de spine propriété du runtime** — contrats en JSON Schema (donnée) + critères référencés par `id` depuis un registre TS (code déterministe, pas de DSL prématuré), croisés avec le sidecar (descriptif, inchangé — ADR-0003). Fait décisif : un `Criterion.check` est du code, non sérialisable en sidecar. Référencé dans README + NEXT_STEPS §2.4-B.2.

### Changed
- **§2.6 — montée `vitest` 2 → 4** (`vitest@^4.1.8`) : chantier de dette technique soldé. Les 5 vulnérabilités `npm audit` provenaient en réalité de la chaîne **vitest 2 → vite → esbuild** (serveur de dev, non exposée), PAS du SDK comme indiqué précédemment. La montée v4 les purge intégralement. **94 tests verts** sur vitest 4, `typecheck` strict OK — aucune régression, aucun changement de config requis.
- **`docs/NEXT_STEPS.md`** : §2.4-B.3 (câblage glue), §2.4-B.4 (spines WF-002/003), §2.5 (CI), §2.6 (vitest) actées ; briques 1 & 2 et §2.4-A déjà actées ; prérequis §2.4-B précisé — **auth OAuth abonnement uniquement**, jamais `ANTHROPIC_API_KEY` (clé métrée = facturation au token + priorité sur l'OAuth → risque de dépassement budget), standby *fail-closed* à la limite de quota + caps par run (`maxBudgetUsd`/`maxTurns`/`permissionMode:"plan"`).

### Security
- **§2.6 — `npm audit` : 5 vulnérabilités → 0** après montée `vitest` 4 (chaîne esbuild/vite/vite-node/@vitest/mocker). **Correction factuelle** : ces vulnérabilités étaient des deps transitives de **vitest** (dev), non du SDK `@anthropic-ai/claude-agent-sdk` (mention erronée d'une entrée [Unreleased] antérieure). `found 0 vulnerabilities` confirmé.
- **Posture de sécurité du dépôt activée (2026-06-08)** : alertes de vulnérabilité Dependabot **ON** + correctifs de sécurité automatiques **ON** (`automated-security-fixes` : `{enabled:true, paused:false}`). Toute future faille sera détectée et corrigée en PR tracée (cohérent ADR-0002). Réglages GitHub côté dépôt (hors fichiers versionnés) ; le `dependabot.yml` (bumps de version) reste le pendant versionné.

### Notes
- Règle d'auth/budget consignée (mémoire projet) : OAuth abonnement, jamais de clé métrée. Vérifié 2026-06-04 : `ANTHROPIC_API_KEY` absente aux 3 scopes (session/User/Machine) — règle déjà structurellement tenue.

---

## [0.2.0] — 2026-06-04 — Socle d'exécution : Loader + contrats + eval gate
> Modèle : Claude Opus 4.8
>
> Premier socle de **code** du runtime (la 0.1.0 était documentaire). Briques 0, 1 et 2 du POC. **46 tests verts**, `typecheck` strict OK.

### Added
- **Brique 0 — Loader sidecar (fail-closed)** :
  - **`schema/sidecar.schema.json`** (JSON Schema 2020-12) : index machine-lisible du catalogue, encodant de façon **exécutable** 5 des 7 caractéristiques ISO/IEC 25012 retenues (ADR-0006) — complétude, cohérence (`if/then` par type), crédibilité (`source` = `{file, catalogTag}`), actualité (tag épinglé + horodatage), exactitude/accessibilité bien-formées. `path`/`source.file` anti-traversal (pas de `/` initial ni `..`).
  - **`src/sidecar/types.ts`** : miroir TypeScript du sidecar.
  - **`src/sidecar/integrity.ts`** : les 2 caractéristiques ISO 25012 hors portée d'un JSON Schema — `checkReferentialIntegrity` (`dependsOn`→`id`), `checkAccessibility` (joignabilité disque, refus défensif des chemins absolus/`..`) — plus `checkUniqueIds` (unicité globale des `id`) et l'agrégat `checkIntegrity`.
  - **`src/loader/load-sidecar.ts`** : pipeline ordonné à court-circuit `parse` → `schema` (ajv) → `integrity`, retournant un `Sidecar` typé ou levant une `SidecarValidationError` qui agrège **tous** les problèmes (`LoadIssue[]` par étape). Schéma compilé une seule fois (cache module) ; `catalogRoot` par défaut = dossier du sidecar ; intégrité non lancée si le schéma échoue.
  - Tests : `sidecar.schema.test.ts` (18), `sidecar.integrity.test.ts` (8) + fixture hermétique `test/fixtures/catalog/`, `loader.test.ts` (7).
- **Brique 1 — contrats de handoff typés** (`src/handoff/{types,validate-handoff}.ts`) : `checkContractCompatibility` (statique *shallow* : champs `required` de l'aval promis par la sortie amont) + `validateHandoff` (runtime fail-closed : payload conforme à la sortie amont ET à l'entrée aval), `HandoffValidationError` agrégée, ajv2020/strict réutilisé du loader. Comble le vide que le SDK ne couvre pas — la cohérence I/O inter-étapes. 7 tests.
- **Brique 2 — eval gate déterministe** (`src/eval/{types,eval-gate}.ts`) : critères `blocking`/`advisory` (check défensif), `runEvalGate` (ÉVALUE — ne lève jamais, produit le rapport-preuve même en succès) + `assertGatePassed` (APPLIQUE — fail-closed). Pas de LLM-as-judge (POC ; extension ultérieure, même interface `Criterion`). Fixture = DoD du cadrage WF-001. 6 tests.
- **`package.json`** : version 0.1.0 → 0.2.0 ; `package-lock.json` (premier `npm install`, 52 paquets).

### Notes
- Choix d'encodage sidecar : pas de champ `format` redondant, pas de checksum/score (ISO 25024 **différé**, ADR-0006), validation par `pattern` plutôt que `format` → aucune dépendance `ajv-formats`.
- Le sidecar de production n'est pas dans ce repo (ADR-0003) : importé épinglé depuis `claude-agents` ; `checkAccessibility` résout les chemins via un `catalogRoot` injecté (jamais hardcodé).
- vitest maintenu en `^2.1` (montée v4 = chantier dédié ; faille esbuild côté serveur de dev non exposée).

---

## [0.1.0] — 2026-06-03 — Fondation documentaire & gouvernance (pré-code)
> Modèle : Claude Opus 4.8

### Added
- **Note de cadrage** du POC (`docs/note_cadrage_poc.md`) — objectif portfolio, périmètre spine WF-001→003, 3 briques, invariants, risques.
- **6 ADR fondateurs** (`docs/adr/`) : consommateur read-only · import épinglé versionné · sidecar propriété du catalogue · propagation gardée par eval gates · feedback par PR humaine · **référentiels qualité (42010/25012/25010/42001 retenus ; 25024/23894/5230 différés ; 27001/12207/15288/26511+/19770 écartés)**.
- **`docs/ARCHITECTURE.md`** : couches, diagramme Mermaid, description d'architecture ISO/IEC/IEEE 42010 (parties prenantes / préoccupations / points de vue), modèle de propagation Dependabot-like.
- **Base projet GitHub** : `README.md`, ce `CHANGELOG.md`, `CONTRIBUTING.md`, `CODEOWNERS`, `.gitignore`, gabarit de PR.
- **Licence MIT** (`LICENSE`) © 2026 Guy HUI-BON-HOA.

### Notes
- Aucun code à ce stade. `claude-agents` (catalogue SSOT) reste **intact** ; ce repo n'a aucun droit d'écriture dessus.

---

[Unreleased]: https://github.com/guyhui01/claude-agentic-runtime/compare/v0.11.0...HEAD
[0.11.0]: https://github.com/guyhui01/claude-agentic-runtime/compare/v0.10.0...v0.11.0
[0.10.0]: https://github.com/guyhui01/claude-agentic-runtime/compare/v0.9.1...v0.10.0
[0.9.1]: https://github.com/guyhui01/claude-agentic-runtime/compare/v0.9.0...v0.9.1
[0.9.0]: https://github.com/guyhui01/claude-agentic-runtime/compare/v0.8.0...v0.9.0
[0.8.0]: https://github.com/guyhui01/claude-agentic-runtime/compare/v0.7.0...v0.8.0
[0.7.0]: https://github.com/guyhui01/claude-agentic-runtime/compare/v0.6.0...v0.7.0
[0.6.0]: https://github.com/guyhui01/claude-agentic-runtime/compare/v0.5.0...v0.6.0
[0.5.0]: https://github.com/guyhui01/claude-agentic-runtime/compare/v0.4.0...v0.5.0
[0.4.0]: https://github.com/guyhui01/claude-agentic-runtime/compare/v0.3.0...v0.4.0
[0.3.0]: https://github.com/guyhui01/claude-agentic-runtime/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/guyhui01/claude-agentic-runtime/releases/tag/v0.2.0

<!-- 0.1.0 carries no link: it was never tagged. Verified against `git tag`
     rather than assumed from the heading list — the earliest tag is v0.2.0. -->

