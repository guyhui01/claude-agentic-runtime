# Next steps — `claude-agentic-runtime` (session resume tracker)

> **Purpose:** per-repo session resume point + paste-ready restart prompt.
> This file is the **session tracker** (where we stopped, what to do next).
> The detailed POC roadmap lives in [`docs/NEXT_STEPS.md`](docs/NEXT_STEPS.md) — do not merge the two.
> Convention: one dedicated `next_steps.md` per subject (catalog, agentic runtime, projects, RAWLY).

---

## ⏸ PRIORITÉ INTER-CHANTIERS — ⛔ ANNULÉE PAR GUY LE 2026-08-16

> **⛔ ANNULÉE — 2026-08-16.** Guy a annulé la question de priorité inter-chantiers. Ce repo
> continue sur SON prochain lot (ÉTAPE ① audit éval, voir le `▶▶ NEXT UNIT` plus bas), sans
> arbitrage vitrine/RAWLY préalable. Tout le raisonnement historique 2026-08-04→08-12 ci-dessous
> est conservé pour mémoire, il n'est plus une désignation active.
>
> ▫ **PENDING hors de ce repo** : la section vitrine **« How to use the Catalog »** (Atelier 1,
> analysée + validée implémentable le 2026-08-16 : structure 5 blocs, copy EN, compteur dérivé de
> `sidecar.json` et non figé, disclaimer « simulated expert personas » de `README.md` repris,
> commandes ancrées re-vérifiées à la source contre `START.md`). ⛔ **À consigner dans le tracker
> de `guyhui-showcase`, PAS ici** — c'est un chantier vitrine, repo distinct.

> **⚠️ LIRE CECI AVANT LE BLOC DU 2026-08-05 CI-DESSOUS, qui argumente sur une prémisse
> désormais FAUSSE.** Ce bloc désignait `guyhui-showcase` comme chantier actif au motif que
> « le seul défaut **factuel** ouvert est côté vitrine — elle affiche `v0.10.0` ». **Mesuré le
> 2026-08-12 : la vitrine affiche `v0.11.0`** (`docs/stack/agentic_runtime/index.md`), et Guy
> a re-cliqué et validé les deux liens de snapshots le même jour. Le défaut qui fondait cette
> priorité **n'existe plus** — le bloc ci-dessous est conservé pour son raisonnement, il n'est
> plus une désignation valide.
>
> **Ce qui a changé de l'autre côté** : les 12 commits accumulés depuis `v0.11.0` — dont **deux
> advisories fermées** (`GHSA-frvp-7c67-39w9`, `GHSA-2v37-7h3g-55p8`) et le re-pin du catalogue
> sur `v4.3.0` — sont **RELEASÉS le 2026-08-12 en `v0.12.0`** (tag + GitHub Release `Latest`,
> CI verte). ⟹ **La vitrine a désormais un retard de version À NOUVEAU FACTUEL** : elle affiche
> `v0.11.0`, et c'est le même type de défaut que celui qui lui donnait la priorité le
> 2026-08-05. La passe vitrine a maintenant trois choses à surfacer, pas une : la version, les
> deux advisories, et le re-pin.
>
> ⛔ **Ne pas déduire un nouvel ordre de ces deux faits — c'est une décision de Guy**, et un
> tracker qui l'invente reproduit exactement le défaut que ce bloc vient de subir. Ce qui est
> établi : l'argument « la vitrine d'abord » est éteint ; ce qui ne l'est pas : ce qui passe
> devant.

> **Ce repo N'EST PLUS le premier.** Nouvel ordre : **(1) `guyhui-showcase` →
> (2) `claude-agentic-runtime` (run live) → (3) RAWLY.**
>
> **Pourquoi l'ordre s'est inversé — le motif d'origine est ÉTEINT, pas contredit.**
> La priorité du 2026-08-04 tenait à une chaîne bloquée par *la classe de démenti* :
> pas de release, donc rien de nouveau à surfacer. **Elle est fermée et `v0.11.0`
> est publiée** ⟹ le runtime a livré ce que la vitrine attendait, y compris les deux
> commits de sondes de seeds que seule l'absence de version publiée tenait hors du
> site.
>
> **Ce qui départage maintenant** : le seul défaut **factuel** ouvert est côté
> vitrine — elle affiche `v0.10.0`, faux depuis le 2026-08-05. Ce qui reste ici est
> une **montée en gamme de la preuve** (le run live), pas une correction : le
> drill-down de la vitrine dit déjà noir sur blanc qu'elle est prouvée par le
> registre et ses tests, **pas** par des runs facturés.
>
> ⚠️ **Ne pas se raconter d'urgence dans l'autre sens non plus** : la vitrine
> périmée d'une version est un écart mineur et visible, pas un incident.
>
> ▫ **Bloc d'origine du 2026-08-04, conservé pour son raisonnement** : la chaîne
> qu'il décrit (release → run live → preuve vitrine) reste exacte ; c'est son
> premier maillon qui est désormais franchi. Les lots RAWLY n'ont toujours
> **aucune dépendance en aval**.

---

## ▶ RESUME HERE — paste-ready restart prompt

```
Resume session — runtime /Users/guyhui/CLAUDE/claude-agentic-runtime (repo guyhui01/claude-agentic-runtime).
Apply the startup ritual (prompt_demarrage.md). Factual check FIRST: `git status -sb`, `gh pr list`, `gh api repos/guyhui01/claude-agentic-runtime/dependabot/alerts --jq '[.[]|select(.state=="open")]|length'` (expected: **0** since 2026-08-12 — the Hono/Windows alert this line used to expect is CLOSED: `GHSA-frvp-7c67-39w9` was fixed by `8a2ca11` (`@hono/node-server` 1.19.15 → 2.1.0), which is the upstream widening the previous expectation was waiting for. Anything ABOVE 0 is untriaged. ⚠️ This endpoint is NOT the whole picture: on 2026-08-12 `npm audit` reported a HIGH that has **no** Dependabot alert at all — run `npm audit` too, and read the two as independent sources), `gh run list --branch main --limit 5 --json name,conclusion,headSha --jq '.[]|select(.name=="CI")|"\(.conclusion) \(.headSha[0:7])"'` (expected `success` at HEAD; `main` is NOT branch-protected, so a red CI blocks nothing mechanically — it is only seen if someone looks, which is why it is checked here). ⚠️ **This line used to read `--limit 1` without a name filter, and it did NOT measure the CI**: on 2026-08-12 it returned a *Dependabot* run (`npm_and_yarn in /. - Update`) whose `success` says nothing about the test suite. Filter on the workflow NAME, or the check is theatre), `git ls-remote --tags https://github.com/guyhui01/claude-agents.git | grep -oE 'v[0-9]+\.[0-9]+\.[0-9]+$' | sort -V | tail -1` **vs** the tag the manifests pin (`grep -ho 'catalogTag: "v[0-9.]*"' src/dispatch/manifests/*.ts | sort -u`) — **a difference is not a CI failure, it is freshness debt**: CI pins the manifests' own tag by design (ADR-0002 accepts that freshness is not immediate), so drift against the latest catalog release is answered HERE, at session start, and never by a red build. ⚠️ **CORRECTED 2026-08-12 — "not a failure" is true of CI and FALSE of your own machine.** If the sibling `claude-agents` checkout sits on `main` (or on any tag newer than the pin), `test/dispatch-real-sidecar.test.ts` turns **10 tests red** — `expected 'v4.2.0' to be 'v4.3.0'` — because that guard reads the LOCAL sidecar while CI clones the catalog AT the pinned tag. Measured that day: local suite 388 passed / **10 failed**, the same 398 tests CI sees green. To get a clean local baseline without touching the catalog checkout, reproduce what CI does: `git clone --depth 1 --branch <pinned tag> https://github.com/guyhui01/claude-agents.git <tmp>` then `CATALOG_ROOT=<tmp> npx vitest run` → 398 passed / 23 skipped. **Do not read a red local suite as a regression before checking the sibling's tag.** ✅ **The v4.2.0 → v4.3.0 drift that produced this measurement was CLOSED the same day (commit `c43abc4`)** — the ten manifests now pin **v4.3.0** and the local suite is green with no override. This paragraph stays because the SHAPE recurs at every catalog release, not because that drift is still open: re-read the pin at source rather than trusting this sentence. docs/NEXT_STEPS.md — never from memory.

Then FIND THE SINGLE LIVE `▶▶ NEXT UNIT` MARKER in this file and work from it. Do not take a unit name from anywhere else — not from this prompt, not from a framing block, not from a memory: every one of those has already gone stale here while still reading as current.

▫ **[CADRAGE DU 2026-07-27 — conservé pour son raisonnement, PLUS pour son décompte. ⚠ Ce bloc n'est PLUS l'unité suivante : le seul marqueur `▶▶ NEXT UNIT` vivant est plus bas — le CHERCHER dans le fichier, ne jamais se fier à un nom d'unité ou à un décompte écrits ici, qui périment à chaque lot (cette phrase a déjà annoncé « WF-007 » et « six des dix » longtemps après que ce fût faux).** Il annonçait « the nine remaining » et en laissait quatre — un cold start qui lit le haut du fichier dimensionne le chantier sur un chiffre périmé. Ce qui reste valable ici : le choix de l'arc contre le multi-WF, le modèle à copier, les quatre décisions de politique, et le fait que le chiffrage à 81 est un **plancher** (WF-006 a livré 14 specs pour 11 lignes de carte).**]** Cadrage d'origine : les neuf manifests de paramètres restants (Dispatch V1), choisis par Guy le 2026-07-27 contre l'arc multi-WF (see the requalified entry below for why). Deliberately NOT started at the end of that session: it is 12× the WF-001 pilot, and half-rushed regex is the textbook cohérent-faux ([[feedback-gros-morceaux-debut-de-session]], [[feedback-pas-de-mode-degrade]]). Sizing measured at source 2026-07-27 by reading each card's `## Contextual parameters` block:
  - **81 param specs across 9 files** — WF-002: 8 · WF-003: 8 · WF-004: 8 · WF-005: 7 · WF-006: **11** · WF-007: 9 · WF-008: **11** · WF-009: 10 · WF-010: 9. (WF-001, the shipped pilot, has 7.)
  - **Model to copy:** `src/dispatch/manifests/wf-001.ts` — `ParamManifest` (`src/dispatch/types.ts:109`) = `{workflow, catalogTag, params[]}`, each param `{name, card, required, mapping(brief), pattern?, defaultValue?}`. **Register each new manifest in `DEFAULT_MANIFESTS` (`src/dispatch/run-dispatch.ts:28`)** — a route with no manifest currently reports an honest `paramsChecked:false`, so a half-registered set degrades silently rather than failing.
  - **The volume is not the risk — the policy repeated 81 times is.** Four decisions to settle ONCE, before writing file two: (1) `required` vs optional-with-default (the pilot made `deliverables_language` optional); (2) **card-sanctioned unknowns must be ACCEPTED by the param check, not rejected** (dry-run §2 / `fact`-style field-class policy) — they exist literally in the cards and are easy to miss: WF-006 `Indicative budget : [… / Not disclosed]`, WF-009 `Salary / day rate : [Range or "to be defined"]`; getting these wrong inverts `PARAMS_MISSING` semantics; (3) `pattern` detectors are deterministic regex whose SAFE failure direction is a false "missing" — never a false "filled"; (4) **`PARAMS_MISSING` must surface the `card` label, not the `name`.** Verified at source 2026-07-27: `validate-route.ts:136` is `.map((p) => p.name)`, so the live P01 proof returned `[team_size, project_method, level_of_detail]` — snake_case identifiers — while the human label sitting right next to it in the manifest (`card: "Team size"`) is discarded at the exact moment a human has to read the message. The card labels are business vocabulary by construction (copied verbatim from the catalog card: WF-006 reads like a bid/no-bid qualification sheet, WF-009 like a recruitment brief), so the fix is free and the current behaviour throws that away. `name` stays the internal key. **Settle it before file one** — retrofitting means reworking 81 specs and regenerating the traces. NB doctrine: this does NOT open multi-profile; the V0 user is still the accountable operator. It makes the operator's own return loop legible now, and leaves the V1 QUALIFY step nothing to undo.
  - **Suggested slicing (Guy's option (b), deferred):** do **WF-002 alone and complete first** (8 specs + registration + tag-drift real-sidecar test) as the prototype — its SAFe vocabulary is far from WF-001's, so it stresses the shape; the other eight become mechanical afterwards.
  - **Pin `catalogTag` to the sidecar's real tag and let the real-sidecar test hard-fail on drift** (currently `v4.2.0`) — re-verify the tag at source, do not copy it from this tracker.

  ✅ **PREP DONE 2026-07-28 — the four policy decisions are settled AT SOURCE, and the fail-closed socle is shipped (commit `dba2949`, local). Read this before writing manifest one; three of the entries above were WRONG and are corrected here.**
  - **Sizing re-verified card by card: exact.** 8+8+8+7+11+9+11+10+9 = **81 specs**. Sidecar tag re-read at source = **`v4.2.0`** (matches `claude-agents` HEAD tag).
  - **Decision (1), CORRECTED — puis RE-CORRIGÉE le 2026-08-01, voir l'annotation en fin de puce.** `Deliverables language` is NOT the general case: among the nine it exists **only in WF-002**. A second card-declared optional the entry above misses: **WF-009 `Nice-to-have skills`** ("Desired but non-blocking skills"). Rule: `required: true` by default; `false` + `defaultValue` only where the card declares it non-blocking or it is an operator-profile constant. → **2 cases out of 81.** ⚠️ **ANNOTATION 2026-08-01 — « card-declared optional » est imprécis pour l'un des deux, vérifié ligne à ligne sur les DIX cartes : il n'y en a qu'UN.** `Nice-to-have skills` (WF-009) écrit littéralement « Desired but **non-blocking** skills ». `Deliverables language` (WF-001/002) ne déclare **rien** — sa ligne est `[French / English / Bilingual]` — et il est optionnel comme **constante de profil opérateur** (`defaultValue: "English", // operator-profile default`). Deux justifications **différentes**, une de chaque, non deux du même genre. Le décompte de 2 reste juste ; c'est leur nature qui ne l'était pas.
  - **Decision (2), CORRECTED — there are at least THREE sanctioned unknowns, not two.** WF-006 `Indicative budget [… / Not disclosed]`, WF-009 `Salary / day rate [Range or "to be defined"]`, **+ WF-008 `Suspected AI Act tier [… — to confirm in STEP-01]`** (the card defers the value to a run step). All stay `required`, satisfied via `sanctionedUnknown`. **False friends deliberately EXCLUDED** — do not "fix" them later without reading this: WF-007 `D1 access [… — to validate]` (the list exists; its validation is what is deferred) and WF-004 `Client AI maturity [… — estimate]` (an estimate IS a value).
  - **Decision (3), SHIPPED as code rather than as discipline.** `paramFilled` no longer falls back to `affirmativeString` on the mapped text: a spec with neither `pattern` nor `defaultValue` is now reported MISSING. Rationale — most card params map onto the same shared brief fields (`context`, `need`), so an emptiness test passed a whole card on one sentence; WF-006 and WF-008 (11 specs each) would have become hollow passes. WF-001 `constraints` was the only spec relying on that fallback and now declares `defaultValue` (satisfied upstream: intake rejects an unjustified empty `constraints`). Suite **276/22** + strict typecheck green, P01 return-loop assertions and versioned traces untouched. ⚠️ **The guard is installed but NOT yet exercised** — no manifest currently carries a required spec without a detector. WF-002 is what exercises it.
  - **Decision (4), DONE 2026-07-28 — a REPLACE, not an additive field.** `validate-route.ts` `.map((p) => p.name)` → `.map((p) => p.card)`. Grep-verified 2026-07-28: `missingParams` has **zero programmatic consumer** (2 test assertions only), so carrying a second parallel array of labels would be a dead key kept for the sake of a dated trace. The 2026-07-19 P01 trace stays an honest record of what ran that day; it is evidence, not a contract. Update the 2 assertions in the same edit.
  - **Do NOT rebuild the machinery that was considered and dropped** (each was weighed and cut on 2026-07-28): a `satisfiedUpstream` flag on `ParamSpec` (duplicates `defaultValue`, whose doc comment already means "never missing" and was widened to cover upstream invariants); a shape-invariant test asserting every required spec has a detector (redundant — the fail-closed behaviour denounces a forgotten `pattern` at the manifest's own happy-path test); per-manifest positive probe fixtures (that IS the happy-path test, counted twice).
  - **KEEP one guard: the generic negative probe** (~10 lines, one test over `DEFAULT_MANIFESTS`). A brief of neutral prose carrying no card vocabulary must leave **every `required` spec without a `defaultValue`** reported missing. It is the only thing that measures whether a `pattern` DISCRIMINATES rather than merely exists — the fail-closed change catches a missing regex, not a lazy one like `/.+/`, and the hard params (WF-008 `Volumes`, `Geography`) are exactly where a permissive regex is tempting. Note the `defaultValue` carve-out in the assertion: WF-001 `constraints` can never be missing by design.
  - **WF-002 and WF-003 SHIPPED 2026-07-28.** Both follow the same lot shape, which is the template for the seven remaining: manifest pinned `v4.2.0` · registered in `DEFAULT_MANIFESTS` · tests = gap → `PARAMS_MISSING` in card labels, amended brief → `ROUTED paramsChecked:true`, real-sidecar entry hard-failing on tag drift · every new guard falsified before being trusted. Zero trace touched, zero billed run.

  ✅ **WF-004 SHIPPED 2026-07-28 (commit `46adf02`, poussé).** 8 lignes de carte → **10 specs** (conjonction `Client` éclatée). RULE 3 appliquée pour la première fois : table de specs validée par Guy AVANT tout code, et elle a payé — deux détecteurs resserrés à la revue (`Engagement scope` refuse un « support » nu, `Client AI maturity` refuse un « advanced » nu), tous deux inutiles pour faire passer la fixture et tous deux verrouillés par un test qui rougit si on les relâche. **Quatre falsifications** exécutées (support nu · advanced nu · détecteur de taille paresseux `mid-size` → attrapé par la sonde générique · drift de tag) : chacune rougit un test DIFFÉRENT. Une prédiction à moi corrigée par le contrôle, pas l'inverse : `Stakeholders` est rempli par la contrainte « executive readout expected » — attendu corrigé, détecteur intact. Point faible assumé et documenté dans le fichier : `client_name` ne reconnaît qu'une FORME syntaxique (nom propre après with/for/at, ou forme juridique) — un brief en apposition « Marlowe Foods, a mid-cap, signed… » est lu comme sans nom ; échec dans la bonne direction, test dédié qui enregistre ce comportement. Suite **298/22** + typecheck strict verts.

  ✅ **WF-005 SHIPPED 2026-07-29 (commit `a6282c8`, poussé).** 7 lignes de carte → **7 specs**, aucune conjonction (le slash de `AI/LLM` est INTERNE à un item, pas un séparateur). Les 7 required, aucun default, aucun sanctioned unknown : chacune alimente l'input d'une étape DU BACKBONE (STEP-01 scope/sources/horizon · STEP-02 audience · STEP-03 format/tone). **Deux divergences tranchées à la carte et consignées parce qu'elles plaident l'inverse** : §2 lit `D=4` sans nommer ses quatre cellules (non suivi — le brief P05 porte un CLIENT, un default de profil opérateur affirmerait l'audience/le ton/les sources DU CLIENT) ; et le bloc **Quick-start de la carte ne demande que 3 des 7** — le meilleur appui textuel qu'ait ce `D=4`, non suivi car les inputs de STEP-01 et STEP-03 exigent explicitement `Focus horizon` et `Tone`. `Opportunity focus` porte la **revendication de nécessité** la plus faible (son consommateur en correspondance exacte est `[optional]` dans la DoD) — distinct de `Horizon`, le **détecteur** le plus faible ; si une spec doit un jour être dégradée en optional-with-default, c'est celle-là et aucune autre.
  - **Détecteurs MESURÉS sur les 19 briefs de la matrice avant qu'un test n'existe — c'est ça qui a trouvé deux défauts du premier jet** : `Horizon` refusait l'ordre des mots de la carte elle-même (« Focus horizon: medium term », mot d'horizon AVANT la valeur), et `Sources to prioritize` se remplissait sur « no monitored sources yet » — faux « rempli » sur un énoncé d'absence. Les deux verrouillés par test. `Horizon` exige le mot d'horizon à côté de la valeur : accepter la quantité nue a été mesuré et remplit sur « three-month engagement window » (P04) et « 10-month project » (P10). Coût assumé : le phrasé naturel de l'opérateur EST la quantité nue, donc cette ligne revient souvent une fois — bonne direction, et le message nomme le libellé de carte, donc il désigne le mot à ajouter. `LinkedIn` est REFUSÉ comme source bien que la carte l'y liste : le token sert aussi les lignes format et audience, le compter rendrait trois specs non indépendantes.
  - **Deux corrections de MÉTHODE, qui dépassent WF-005** (question de Guy : « satisfaisable, est-ce un mode dégradé ? » — oui, ça l'était) : (1) **un contrôle positif dérivé de la regex qu'il teste ne prouve que la non-vacuité de la regex.** Remplacé par une source indépendante déjà dans le repo — les 7 valeurs du seed du harnais live WF-005 (écrit 2026-07-13 pour la spine, prouvé par son run `completed`), aplaties en prose : **6/7 remplissent**, dont les deux que les contrôles circulaires avaient « confirmées ». La 7ᵉ est `Horizon`, et c'est la FORME du seed, pas un défaut (il dit `3 months`, le libellé étant porté par le NOM DU CHAMP, ce que la prose plate n'a pas). (2) **la sonde de discrimination générique est aveugle à un détecteur qui s'accroche à du vocabulaire que son brief neutre n'a pas** : élargir `intelligence_scope` à un `\bAI\b` nu laissait TOUTE la suite verte. Corrigé dans le même lot par un cas sur le brief WF-001, dense en vocabulaire IA et qui ne dit pas un seul paramètre WF-005.
  - **7 falsifications, chacune rouge sur un test DIFFÉRENT** — branche d'ordre inverse retirée · quantité nue acceptée · gardes de négation retirées · `LinkedIn` nu accepté comme source · `/.+/` sur `opportunity_focus` · drift de tag · `\bAI\b` nu. La 7ᵉ était **verte** = le seul vrai KO de la batterie, corrigé avant le commit puis rejouée rouge. Suite **308/22** + typecheck strict verts, zéro trace touchée, zéro run facturé.

  ✅ **WF-006 SHIPPED 2026-07-30 (commit `3f881ad` + correctif pré-push `93ddb8c`, poussés).** 11 lignes de carte → **14 specs** (conjonction `Prospect` éclatée en 4 ; l'input de STEP-01 dit littéralement « Prospect context: sector, size, AI maturity, history », donc le découpage n'est pas une lecture). `Known risks` reste UNE spec : ses séparateurs sont des VIRGULES, donc des exemples, précédent `Constraints` de WF-001. **Première utilisation réelle de `sanctionedUnknown`** de tout le repo — le champ était câblé dans `validate-route.ts:75` depuis le réglage de la politique et aucun manifest ne l'avait exercé.
  - **Trois appels tranchés à la carte**, chacun documenté sur la spec : (1) `Constraints` ne prend **PAS** de `defaultValue` contrairement à WF-001/002/004 — leur ligne est générique (jalon, budget, RGPD, stack imposée), que l'invariant d'intake satisfait ; celle-ci est spécifiquement **d'hébergement** (On-premise / Sovereign cloud / SecNumCloud / HDS), et l'invariant amont garantit *une* contrainte, jamais une contrainte d'infra → renommée `infra_constraints`. (2) `Competition` accepte une **négation comme valeur** (« sole source ») — l'inverse exact du garde de WF-005 `Sources to prioritize`, et la différence est ce que demande chaque carte : WF-005 demande QUELLES sources, celle-ci demande S'IL Y A concurrence. (3) `Request type` refuse le token `competition` nu bien que la carte le liste : c'est aussi le libellé d'une AUTRE ligne de la même carte, donc le compter rendrait deux specs non indépendantes.
  - **`Selection criteria` porte la revendication de nécessité la plus faible des 14, et c'est mesuré** : `grep -i criteri` sur toute la carte renvoie **exactement un hit — le bloc de paramètres lui-même**. Aucune étape ne le consomme, ni en input ni en output. Il reste `required` parce que §2 le classe must-ask sur les DEUX briefs ; si une spec doit un jour être dégradée en optional-with-default, c'est celle-là. `Competition` est la 2ᵉ plus faible (son seul consommateur, STEP-03B, est optionnel et hors backbone).
  - **`Decision-makers` applique deux régimes d'admission** : les **titres** comptent nus (un C-level répond sans ambiguïté à « qui décide »), les **fonctions** non — `procurement` nomme un service dans « the procurement portal » aussi bien qu'un décideur, donc il ne compte qu'à côté du vocabulaire de décision. C'est **plus strict que le précédent shippé** : WF-004 `Stakeholders` admet `sponsor` et `board` nus sans aucune garde d'adjacence.
  - **Détecteurs MESURÉS sur les 19 briefs avant qu'un test n'existe — un défaut du premier jet trouvé ainsi** : `Response deadline` acceptait un mot d'échéance à côté d'une quantité nue et se remplissait sur P12 « phase-one deadline in six weeks », un jalon de livraison dans une mission **déjà signée**. C'est le détecteur à quantité nue que WF-005 `Horizon` a refusé et que la sonde croisée a retiré de WF-004 `Engagement duration` — **la même politique aurait divergé pour la 3ᵉ fois**. Divergence assumée vis-à-vis de la carte : elle annote « RFP deadline — ISO 8601 », l'ISO est **accepté mais pas exigé**, sinon la fixture P06 de ce workflow même (« due in three weeks ») serait rejetée.
  - **WF-006 est le premier workflow à avoir DEUX briefs de la matrice (P06 + P11)** → une 2ᵉ mesure indépendante que les cinq précédents n'avaient pas. Sur P11 : accord **total** avec §2 sur ses cinq must-ask. La seule divergence des deux briefs est `Decision-makers` sur P06, lu **rempli** depuis « procurement-led process » — la requalification que décrit l'annotation de §2 elle-même. P11 exerce aussi l'apposition que WF-004 `client_name` documente comme raté assumé (« Prospect Kestrel Mutual ») : ici le libellé de la carte autorise `Prospect` comme introducteur, donc le nom EST lu. ⛔ **WF-004 n'est PAS amendé** — comportement revu, verrouillé par test, shippé.
  - **10 falsifications, toutes rouges, 4 sur exactement 1 test** ; **une seule** n'a pas de cas dédié (`mid-size` admis comme classe de taille) et c'est la sonde générique qui l'attrape, ce pour quoi elle existe. Le `/.+/` sur `selection_criteria`, lui, rougit bien deux cas dédiés (les listes de gaps de P06 ET de P11) — vérifié sur la sortie de la batterie, pas de mémoire. **Une a corrigé mon propre test et pas le code** : le cas « refuse le token `competition` nu » laissait le contexte P11 intact, qui dit « first contact through **referral** » — autre valeur légitime du même détecteur — donc la ligne se remplissait par une autre branche et le cas ne mesurait pas son sujet. ⚠️ Et le **premier harnais de falsification a produit 6 faux verts** (échappement cassé : la mutation ne s'appliquait pas et la suite tournait quand même sur du code intact) — refait avec un garde qui refuse de lancer si `git diff` ne prouve pas la mutation. Réutiliser CE harnais, pas le premier.
  - **Sentinelle `paramsChecked:false`** : elle était jouée par le workflow réel encore sans manifest (WF-006 jusqu'ici), donc chaque lot devait la déplacer — et le 10ᵉ l'aurait laissée sans sujet. Remplacée par un id synthétique `WF-UNMANIFESTED`, résolvable et délibérément hors registre. **Plus rien à déplacer aux lots WF-007…010.**
  - **Snapshot de la sonde croisée régénéré délibérément et lu en diff** : addition pure de 23 lignes, une section WF-006, **les cinq sections existantes inchangées** (prédit avant, prouvé par le diff). 32 cellules étrangères, toutes sur l'identité prospect + `Decision-makers` + `Constraints` sur P08 (« EU only » = résidence de données, croisement légitime) ; les **8 specs propres à l'avant-vente remplissent sur ZÉRO brief étranger**. Suite **329/23** + typecheck strict verts, zéro trace touchée, zéro run facturé.

  ✅ **LIVRÉ 2026-07-29 — conception d'origine conservée telle quelle ci-dessous, elle a tenu.** ⤵ **CONSIGNÉ 2026-07-28 (question de Guy : audit de fin de chantier ?) — OUI, mais sous forme de SONDE CROISÉE déterministe, et à faire APRÈS WF-005, pas à la fin.** Ce qu'elle attrape et qu'aucun garde actuel ne voit : un détecteur qui s'accroche à du **vocabulaire générique inter-domaines**. La sonde négative existante teste une prose SANS vocabulaire de carte — elle est donc aveugle à une spec WF-004 qui se remplirait sur le brief WF-008. Format : chaque manifest × les fixtures étrangères, **matrice figée en snapshot et relue une fois**, jamais une assertion dure — une case remplie n'est pas mécaniquement un défaut (le GDPR traverse légitimement plusieurs cartes ; c'est déjà enregistré tel quel dans le test inter-vocabulaire WF-004, où `Priority stakes` se remplit sur un brief WF-001). Faire tourner à 5 manifests plutôt qu'à 10 : sinon cinq manifests de plus s'écrivent sous une politique qui a pu dériver. Zéro quota, zéro LLM. ⛔ Une relecture humaine finale des ~90 specs n'est PAS l'audit : elle ne trouverait rien de falsifiable.

  ✅ **SONDE CROISÉE LIVRÉE 2026-07-29 (commit `e95882e`) + LES 4 DÉFAUTS QU'ELLE A TROUVÉS CORRIGÉS (commit `2112a26`).** `test/dispatch-cross-vocabulary-probe.test.ts` + son snapshot `test/__snapshots__/cross-vocabulary-matrix.md`. Elle a trouvé, **dès la première lecture, 4 défauts réels dans deux manifests déjà livrés**, chacun qualifié par le token exact qui faisait feu (mesuré, jamais supposé) :
  - **WF-003 `Monthly API budget`** se remplissait sur « pilot budget capped for Q3 » — la branche `budget … chiffre` attrapait le **3 de « Q3 »**, un label de trimestre lu comme un montant.
  - **WF-004 `Engagement scope`** se remplissait sur une **négation** (« *not* an audit of an AI system », brief juridique P14) et sur « research-grade **training** infrastructure » (entraînement de modèle, pas une mission de formation).
  - **WF-004 `Engagement duration`** se remplissait sur une durée de sprint, un délai de réponse et une durée de projet — **le détecteur à quantité nue que `Horizon` de WF-005 avait refusé le MÊME JOUR, pour la même raison.** C'est LE finding : une politique appliquée dans deux directions opposées selon le manifest, invisible à tout test parce que chacun était vert sur sa propre fixture.
  - **Ordre de livraison délibéré** : snapshot commité **tel que trouvé** AVANT tout correctif, pour que la correction montre les cellules se fermer **en diff** (WF-003 7→6, WF-004 40→35, rien d'autre ne bouge) au lieu de reposer sur mon affirmation. À réutiliser tel quel au prochain audit.
  - **Choix tranché par la mesure, contre mon premier réflexe** : les deux variantes d'ancrage de la durée étaient indistinguables sur le corpus, mais celle qui gardait `program` ne ratait P06 que de **25 caractères contre une fenêtre de 24**. Retenue : celle qui EXCLUT `program`/`programme` (un « chatbot program » nomme l'initiative du client). Un garde qui tient par un caractère de marge ne tient pas.
  - **7 falsifications, chacune rouge sur exactement 1 test**, dont une dans l'autre sens (**sur-resserrer** l'ancrage rougit le garde qui protège P04/P12). Suite **316/22** + typecheck strict verts.
  - ⚠️ **Les 35 cellules restantes de WF-004 sont la classe légitime enregistrée** (nom/secteur du client, `Priority stakes`, `Stakeholders`, `Expected deliverables`) : tout brief nomme une entreprise et son secteur. Ne PAS les « corriger » sans lire ceci. WF-001 `Sector` se remplit sur 11 briefs étrangers sur 17 — il ne discrimine rien, ce qui n'est tolérable que parce que ce check n'a jamais prétendu valider la route. La valeur de la sonde est désormais qu'une cellule **nouvelle** apparaîtra en diff.
  - ▫ **Rejouer la sonde au 10ᵉ manifest** (2ᵉ passage), et régénérer le snapshot **délibérément** en lisant le diff — une régénération réflexe (`-u`) transforme le fichier en tampon. NB : le snapshot bouge **à chaque** manifest enregistré (la sonde itère `DEFAULT_MANIFESTS`), donc « lire le diff » est une obligation de CHAQUE lot, pas seulement du 10ᵉ.

  🔍 **AUDIT COMPLET DE FIN DE CHANTIER — demandé par Guy le 2026-07-30, à exécuter APRÈS WF-010, en unité dédiée et à froid.** ⛔ Ce n'est **pas** une relecture humaine des specs (85 au 2026-08-01, ~110 attendues à dix — chiffre à re-mesurer, jamais à recopier d'ici) : la note du 2026-07-28 tient toujours, elle ne trouverait rien de falsifiable. L'audit est **déterministe, offline, zéro quota**, et chaque volet doit pouvoir rendre un verdict qu'on n'a pas écrit d'avance. Volets, dans cet ordre — **deux sont désormais CLOS (2 et 5), un est requalifié (3) et un est largement couvert (4)** ; lire leur statut avant de planifier :
  ⚠️ **RESTRUCTURÉ 2026-07-30 : le volet 2 est SORTI de la fin de chantier et LIVRÉ (`test/dispatch-policy-consistency.test.ts` + snapshot `policy-consistency.md`).** Raison : le tracker se contredisait — l'entrée de la sonde croisée dit noir sur blanc « *run at five manifests rather than ten, on purpose: waiting for the tenth would let five more be written under a policy that may have drifted* », et trois lignes plus bas je repoussais après WF-010 le volet couvrant exactement cette dérive. À la fin c'est une machine à **trouver** (retrofits sur 10 fichiers) ; maintenant c'est une machine à **prévenir** (WF-007…010 s'écrivent contre une table visible). **Ce qu'il a trouvé dès la première lecture, dont une dette que je n'avais PAS vue :** (a) `CAC ?40` présent dans `prospect_size` seul → ajouté à `client_size`, **no-op mesuré** (le token n'est dans aucun brief) ; (b) **`from` présent dans `prospect_name` seul** — introducteur générique sans base de carte, contrairement à `Prospect` que le libellé de WF-006 licencie → ajouté à `client_name`, **PAS un no-op** : matrice croisée 35 → **38** cellules, exactement les 3 briefs disant « from <Nom> » (P01/P06/P19), et un test WF-004 shippé a rougi (le garde a fonctionné) puis été mis à jour avec sa raison ; (c) mon commentaire de `prospect_ai_maturity` disait « same tightening as WF-004 » alors que la table le falsifie — divergence **légitime** (la carte WF-004 énumère, celle de WF-006 non) mais qui devait être écrite. ⚠️ **La sonde a dû être fiabilisée avant d'être crue — QUATRE corrections, et les deux dernières sont les plus instructives.** (i) et (ii) : ses deux premières sorties signalaient de fausses divergences (`bank\w*`, `beginner`, `badvanced`) par découpage naïf des alternations et collage du `\b` sur le littéral — une table qui crie au loup finit ignorée. (iii) **son marqueur de la famille « quantité » — la seule question de politique qui justifie cet audit — était sensible à 25 %** : il signalait 3 specs là où **12** en portent. Table d'apparence autoritaire, 9 membres sur 12 invisibles. Trouvé en le mesurant contre une définition écrite au lieu de le regarder. (iv) L'élargir à un `\d` nu l'a fait passer à 15 en attrapant un littéral de date et un nom de modèle — l'erreur la plus difficile à voir des deux. **Leçon de méthode : écrire la DÉFINITION de la famille avant le détecteur, puis vérifier l'implémentation contre elle, jamais contre le compte qui paraît plausible.** ✅ **Résultat du 1ᵉʳ passage complet, une fois le marqueur juste : AUCUNE spec à quantité nue ne subsiste dans les 6 manifests** — les 5 lignes « quantité sans fenêtre d'adjacence » ancrent toutes le nombre sur un littéral ou un nom d'unité adjacent, vérifiées une par une. **UNE seule assertion dure** l'accompagne (`submittedBy` mappé nulle part, désormais un fait MESURÉ et non une phrase répétée dans six commentaires) : une seconde, sur « toute spec required a un détecteur », a été écrite puis **retirée** car elle reconstruit la machinerie écartée le 2026-07-28 — redondance **re-falsifiée** avant retrait (supprimer un `pattern` rougit 5 tests existants). ⚠️ **Ce que la table NE mesure PAS est écrit dans le fichier** : elle apparie les specs par convention de nommage (une divergence qu'elle ne peut pas apparier, elle ne la voit pas) ; son marqueur d'adjacence ne lit que `{0,N}` ; et elle lit des SOURCES de regex, pas du comportement — c'est le rôle de la sonde croisée, aucune des deux n'absorbe l'autre.
  1. **Sonde croisée, 2ᵉ passage**, à la fin, snapshot relu en diff contre le 1ᵉʳ passage. ⚠️ **Ne pas se fier au chiffrage** : cette ligne annonçait « ~140 specs », un chiffre écrit quand le plancher de 81 lignes de carte servait de proxy. Réel mesuré au 2026-08-01 : **85 specs sur 8 manifests** (WF-008 seul en pèse 19 pour 11 lignes). Re-compter à ce moment-là, ne pas reprendre un nombre écrit ici.
  2. ✅ **LIVRÉ — Audit de COHÉRENCE DE POLITIQUE inter-manifests. Le relire à chaque nouveau manifest (il bouge à chaque enregistrement), pas seulement à la fin.** C'est la classe de défaut qui s'est produite **deux fois et a failli se produire une troisième** : la quantité nue, refusée par WF-005 `Horizon`, admise par WF-004 `Engagement duration` (trouvée par la sonde croisée), et re-tentée par WF-006 `Response deadline` (trouvée par la mesure préalable). Aucun test ne peut la voir, parce que chaque manifest est vert sur SA fixture. Forme attendue : un script qui, pour chaque **famille de politique** (quantité ancrée vs nue · garde de négation · admission d'un token nu vs adjacence · mapping de `expectedDeliverable` · mapping de `submittedBy` — qui doit être vide partout), liste quelles specs l'implémentent et lesquelles non, et **rend une table**. Une divergence n'est pas mécaniquement un défaut (les cartes diffèrent), mais chacune doit être **nommée et tranchée**, pas découverte deux mois plus tard.
  3. **Confrontation systématique au §2 du dry-run — REQUALIFIÉE 2026-07-31 : elle se fait À LA LIGNE DE CARTE, pas à la spec.** Le §2 n'a jamais connu que les lignes de carte ; comparer sa colonne must-ask à un ensemble de `missingParams` qui compte des FAITS produit, sur une carte à conjonctions, des différences qui sont de l'**arithmétique** et non des jugements — sur WF-008, 6 divergences réelles au lieu d'une dizaine mêlées d'artefacts. Procédure : agréger les specs à leur ligne (une ligne est manquante si **au moins un** de ses faits l'est), puis comparer. La jointure est mesurée et totale : **85 specs sur 85 se joignent à leur ligne de carte, zéro orpheline** (2026-08-01, chemins lus dans le sidecar). Chaque divergence restante doit déjà porter une justification sur la spec ; celles qui n'en portent pas sont le livrable de ce volet. ⚠️ **Cette agrégation ne vaut que pour comparer au §2** — jamais pour rendre `PARAMS_MISSING` à l'opérateur, où nommer la moitié manquante est tout l'intérêt du découpage.
  4. **Re-dérivation du chiffrage à la carte, au tag épinglé — LARGEMENT COUVERT depuis le 2026-08-01, mais PAS entièrement ; ne pas le rayer.** Ce que la jointure du volet 3 couvre déjà : une ligne de carte **renommée ou disparue** sous le pin ferait échouer la jointure, or elle est à 85/85. Ce qu'elle ne couvre **pas** : le nombre de VALEURS sur une ligne inchangée, ni le nombre de lignes du bloc. Reste donc à re-compter lignes-de-carte et specs à la source pour les 10 — et à ne pas conclure « trivial » d'avance : le pin est le même depuis `v4.2.0`, mais c'est justement ce qui rendrait un écart significatif.
  5. ✅ **CLOS 2026-07-31 — « le libellé de carte désambiguïse-t-il la valeur nue ? »** Ce volet a été ouvert puis **résolu et livré dans la foulée**, il ne reste rien à auditer ici. Fait décisif vérifié à la source : le quick-start de CHAQUE carte enseigne `- Libellé: valeur`, WF-006/WF-008 y inlinant les énumérations. `paramFilled` accepte désormais un paramètre que le brief **répond en le nommant** — **180 valeurs de carte sondées, 0 refusée**, et **zéro basculement** de verdict sur les 19 briefs (snapshots byte-identiques). Les 22 cas nommés qui motivaient ce volet sont donc **tous fermés**.
  ⚠️ **Ce que l'audit NE mesure PAS, à écrire dans son rapport** : il ne dit rien de la JUSTESSE d'un détecteur sur de la prose réelle non fixturée (le corpus reste les 19 briefs, tous écrits par moi — contrôle positif circulaire au niveau du corpus), ni de la pertinence métier d'un paramètre. La seule source indépendante disponible reste les seeds des harnais live, écrits pour les spines — l'appui utilisé pour WF-005 ; l'étendre aux 10 est le seul moyen connu de sortir de la circularité, et c'est un 5ᵉ volet **optionnel** à chiffrer avant de le promettre.

  ✅ **UNITÉ ZÉRO — LIVRÉE 2026-07-31 (commit local, non poussé). Les trois items sont faits ; le cadrage d'origine est conservé ci-dessous, il a tenu.** Ce que la livraison a appris en plus du plan :
  - **`scripts/falsify.sh` est falsifié contre lui-même sur ses SIX chemins**, parce qu'un harnais de falsification qui rend un faux vert est pire que pas de harnais : mutation réelle → **RED avec 38 tests nommés** · littéral absent → refusé · littéral à 8 occurrences → refusé · **substitution no-op → refusée** (le cas exact que le premier harnais passait) · fichier non suivi → refusé · fichier à modifications non stagées → refusé · mot de commentaire que rien ne mesure → **GREEN**. L'arbre est revérifié propre après chaque cas. ⚠️ Un `git status` vérifié à la main ne suffit pas : le garde qui compte est celui sur la MUTATION, pas sur les tests.
  - **`scripts/measure-manifest.ts` a été CALIBRÉ contre la matrice croisée shippée avant qu'on lise sa sortie, et il a divergé** — 51 cellules contre 17 sur WF-001, 55 contre 38 sur WF-004. L'écart se résout exactement aux specs à `defaultValue`, que la matrice exclut sous le nom « measurable specs » : elles s'allument partout par déclaration, gonflent chaque manifest d'une colonne pleine et noient les cellules informatives. Aligné, puis **accord exact sur les six manifests**. C'est la leçon [[feedback-calibrer-une-metrique-contre-verite-terrain]] appliquée à un instrument neuf, et elle a payé au premier essai.
  - **`paramFilled` est désormais EXPORTÉ** (`validate-route.ts`) et l'instrument le lit au lieu de le ré-implémenter — une mesure qui reproduit la règle qu'elle décrit en dérive en silence, et décrirait alors un check qui n'est pas celui qui tourne. Seul usage de cet export.
  - **Compteurs de familles : la première lecture donne deux faits.** `quantity-vocabulary` = **12**, la vérité terrain contre laquelle le marqueur avait été corrigé. Et `NO DETECTOR` = `default` = **5 sur les mêmes cinq specs** — donc aucune spec n'est structurellement impossible à remplir ; l'invariant fail-closed était jusqu'ici inféré de la lecture de `paramFilled`, il est maintenant mesuré.
  - **Outillage :** `npm run measure -- WF-00X` (compile via `scripts/tsconfig.json` → `.tools-build/`, gitignoré ; la compilation EST le typecheck de l'instrument, à chaque appel). Node ne résout pas les specifiers `.js` vers `.ts`, d'où le passage par `tsc` plutôt qu'une exécution directe — ne pas « simplifier » en `node scripts/measure-manifest.ts`, c'est mesuré et ça échoue.
  - ▫ **Dette de traduction repliée dans le même lot** ([[feedback-corriger-dette-avant-livraison]]) : `.gitignore`, `.gitattributes`, `CODEOWNERS` et la `description` de `package.json` étaient restés en français sur un repo PUBLIC — même classe que `dependabot.yml`, replié ici parce que ce lot touche `.gitignore` de toute façon. ⛔ **NE PAS « corriger » deux choses** : l'historique de versions figé en français dans le CHANGELOG (intégrité d'audit, jamais réécrit) et le nom de fixture « Normand Alizé » dans `spine-helpers.test.ts`, qui est une DONNÉE de test (nom en N- gardant contre les faux positifs d'`affirmativeString`), pas de la prose. `next_steps.md` reste bilingue à dessein — c'est un tracker de session, pas un livrable.

  🔧 **Cadrage d'origine — UNITÉ ZÉRO, OUTILLER AVANT WF-007, décidé 2026-07-30. Petite, mais PRÉREQUISE : les deux outils concernés vivaient dans le scratchpad et ont disparu avec la session.** Même argument que pour la sonde de cohérence : l'outil avant le lot qui s'en sert.
  1. **`scripts/falsify.sh` — versionner le harnais de falsification.** ⚠️ **Le premier harnais du 2026-07-30 a produit SIX FAUX VERTS** : l'échappement des mutations était cassé, la mutation ne s'appliquait pas, et la suite tournait quand même sur du code intact en affichant « 0 failing test(s) ». **Rien d'autre ne peut attraper ça** — la suite est verte que la mutation ait mordu ou non. **Le garde à reproduire, c'est le cœur de l'entrée :** (a) la substitution assert `count(old) == 1` et sort en erreur sinon ; (b) **avant de lancer la suite, `git diff --quiet <fichier>` doit être FAUX** — si le fichier est inchangé, le run est REFUSÉ et non compté ; (c) `git checkout -- <fichier>` après chaque cas, le fichier ayant été `git add` au préalable ([[feedback-git-add-avant-mutation-fichier-neuf]]). Sortie utile = le NOM des tests rougis, pas seulement le compte.
  2. **`scripts/measure-manifest.ts <WF-00X>` — versionner la mesure pré-test.** Ce n'est pas un garde mais un INSTRUMENT, et son mode de défaillance est le non-usage. C'est lui qui a trouvé les deux défauts du premier jet de WF-005 et celui de `response_deadline` sur WF-006, **chaque fois avant qu'un test n'existe**. Il fait deux choses : (a) pour chaque brief de `DISPATCH_FIXTURES`, quelles specs se remplissent (via `mapping` + `sanctionedUnknown` + `pattern`, exactement comme `paramFilled`) ; (b) pour chaque brief ÉTRANGER, le **sous-chaîne exacte qui fait feu** (`pattern.exec()`), sans quoi on qualifie un remplissage par supposition.
  3. **Compteurs par famille en tête du snapshot `policy-consistency.md`** (~3 lignes). Aujourd'hui un marqueur à **25 % de sensibilité** (3 specs signalées sur 12 réelles) passait inaperçu : 3 lignes parmi 30 ressemblent à 3 lignes. Une ligne « `quantity-vocabulary` : 12 specs » bouge visiblement en diff. ⛔ **NE PAS écrire d'auto-test sur ces marqueurs** : dérivé du même prédicat il prouverait que le marqueur est égal à lui-même ([[feedback-controle-positif-circulaire]]). Le compteur donne de la VISIBILITÉ, pas une preuve — la distinction est le point. ⛔ Rien à faire sur `audit_memory.py` (repo `claude-config`) : défaut corrigé, documenté dans le fichier, deux contrôles bloquants falsifiés.

  ✅ **WF-007 SHIPPÉ 2026-07-31.** 9 lignes de carte → **11 specs** (conjonction `Client` éclatée). 11 required, **aucun `defaultValue`, aucun `sanctionedUnknown`** — le 1ᵉʳ depuis WF-005 sans ni l'un ni l'autre (la carte n'a pas de ligne `Constraints`). RULE 3 appliquée : table validée par Guy avant tout code, **et un blindage analytique demandé par Guy AVANT l'Unit B, qui a payé plus que la table elle-même**.
  - **Le blindage a converti 6 prédictions sur 11 en MESURES** (WF-004 lit la même ligne `Client`, byte pour byte au tag, et P07 lui est étranger) — et **a cassé deux de mes affirmations** : j'avais justifié `stakeholders` sur un précédent WF-004 « qui admet `sponsor` nu » alors que la mesure disait le contraire, et j'avais présenté l'attribution F/D du §2 comme un fait alors que le doc ne donne que des COMPTES par classe, jamais quel paramètre est dans laquelle.
  - **⚠️ DEUX DÉFAUTS DANS WF-004, SHIPPÉ ET VERROUILLÉ PAR TESTS, qu'aucun garde existant ne pouvait voir.** (1) `\bsponsor\b` sans `s?` entre des voisins qui l'ont (`operational teams?`, `business units?`) : la sonde croisée n'enregistre que les cellules ALLUMÉES, et la table de politique n'apparie que des frères — or le frère n'existait pas. Impact chiffré avant correction, confirmé par le diff : matrice **38 → 39**, une seule cellule (P07), rien d'autre. (2) **L'ancre `phase` complétait un correctif que le repo se croyait déjà appliqué** : le test WF-006 « delivery milestone ≠ response deadline » affirme dans son commentaire que la sonde croisée « a retiré ça de WF-004 `engagement_duration` » — elle a retiré la quantité NUE, `phase` gardait la même chaîne remplissante. P12 n'énonce **aucune** durée d'engagement (« Contract signed yesterday », « advisory engagement signed ») ; sa seule quantité est `constraints: ["phase-one deadline in six weeks"]`, un jalon. Une carte répondait *non* sur ces mots, l'autre *oui*, **les deux vertes**. `phase` retiré des deux, P12 déplacé vers les briefs « pas des durées », et le test frère qui affirmait que P12 « énonce sa propre durée » — prémisse falsifiée par la fixture — corrigé, sa couverture d'ordre inverse reportée sur un brief amendé plutôt que perdue en silence.
  - **La mesure préalable a trouvé le défaut de premier jet de WF-007 exactement là où ma conception prétendait avoir un garde** : `Engagement duration` remplissait sur « engagement, sponsors identified, medium » — le mot de classe ancré sur un `engagement` situé 24 caractères plus tôt dans une clause sans rapport. **Prouvé et non supposé** : le même détecteur remplissait à l'identique sur « …, medium-sized client », un brief qui n'énonce qu'une TAILLE. Branche inverse ré-ancrée sur `duration` seul. ⚠️ **Aucun test de la suite n'aurait rougi** si l'ancre avait sauté : « medium » n'apparaît qu'une fois dans les 19 briefs, « short »/« long » jamais, et le corpus dit `mid-size`/`mid-cap` — donc classe nue = vert partout et faux « rempli » sur de la vraie prose. Argumenté sur la spec, pas délégué à un test.
  - **Trois autres défauts attrapés par les audits, dont un dans MON test** : la table de politique a montré `operational teams?` chez le frère et absent ici alors que c'est CETTE carte qui liste `Team` (ajouté, no-op mesuré, précédent `CAC ?40`) ; elle a marqué `Expected deliverables` en `quantity-vocabulary` à cause du « one » de `one-?pager`, un mot que la carte n'emploie pas et un faux positif de la classe même que la définition du marqueur annonce (retiré) ; et la falsification a corrigé **mon test** — « does not let the brief's DOMAIN state the engagement type » assertait sur le domaine de la fixture, que le détecteur n'accepte jamais nu, donc il restait vert que `domain` soit mappé ou non. Une **alternative morte** a aussi été retirée de `Sensitivities` (`no (particular|known|specific) sensitivit\w*` ne tire jamais, `sensitivit\w*` matchant déjà la phrase) : une alternative qui ne tire jamais se lit comme un garde qu'on applique.
  - **9 falsifications, toutes rouges sur un test DÉDIÉ**, via `scripts/falsify.sh` (versionné à l'unité zéro, et ses trois refus ont servi : un littéral mal écrit a été REFUSÉ au lieu d'être compté vert). Trois specs propres à l'onboarding — `D1 access`, `Identified stakes`, `Expected deliverables` — remplissent sur **ZÉRO** brief étranger. Snapshots régénérés délibérément et lus en diff. ⚠️ **Piège d'outil consigné** : `npx vitest run -u fileA fileB` n'a mis à jour QU'UN des deux snapshots ; le second est resté rouge et je l'ai cru régénéré. Régénérer **un fichier à la fois**.
  - ⚠️ **UNE 2ᵉ PASSE DE VÉRIFICATION (demandée par Guy) a trouvé QUE CE MANIFEST ENFREIGNAIT, TROIS FOIS, LE CRITÈRE QUE LE MÊME LOT VENAIT D'APPLIQUER AU FRÈRE.** Règle à retenir : **liste à VIRGULES = exemples** (widening par voisins licencié), **liste à SLASH = énumération fermée** — un **synonyme** d'une valeur listée est légitime (WF-004 lit `upskilling` pour Training, `GDPR` pour Compliance), un **nouveau membre** invente une valeur que la carte n'offre pas. Retirés : `onboarding` comme `Engagement type` ET comme `Expected deliverable` — le mot n'est sur cette carte que dans le TITRE et le nom de fichier, jamais comme valeur, donc je promouvais le nom du workflow au rang de type d'engagement ; et `operational` comme `Identified stake`, 5ᵉ catégorie et non synonyme des quatre. Mesuré avant retrait : les deux mots existent dans les 19 briefs (`onboarding` ×2 dans le brief légal NO_MATCH, `operational` ×1) mais **aucun ne produisait de remplissage** — 41 cellules avant/après, aucun verdict déplacé, ce qui part est un faux « rempli » latent. **Contrôlé chez les frères plutôt que supposé local** : `Priority stakes` et `Expected deliverables` de WF-004 n'ajoutent que des synonymes ; seul son `Stakeholders` porte de vrais nouveaux membres, sur une classe de fait que la carte laisse ouverte → **consigné pour l'audit de fin de chantier (volet 3), pas modifié ici**. ⛔ Ne pas « nettoyer » les synonymes en croyant appliquer cette règle : la distinction joue dans les DEUX sens.
  - **`Engagement location` porte la revendication de nécessité la plus faible des onze, mesurée** : `location|on-site|remote|hybrid` = **1 seul hit sur toute la carte**, le bloc de paramètres ; aucune étape ne la consomme ; et le quick-start de la carte demande 5 des 9 lignes en l'omettant. Elle reste `required` (§2 la classe must-ask) mais c'est LE candidat à la dégradation. À l'inverse `Sensitivities` est la plus forte : input littéral de STEP-03, étape du backbone.

  ✅ **WF-008 SHIPPÉ 2026-07-31 (commit local).** 11 lignes → **19 specs**, le plus gros manifest. **QUATRE conjonctions, pas trois** : `AI system audited [Name / Use case / Prod or project status]` a exactement la forme de `Client [Name / Sector / Size]` — la liste RULE 2 du tracker était un diagnostic daté, testé à la carte et corrigé. Dernière inconnue sanctionnée du catalogue (`Suspected AI Act tier`), avec la règle tranchée : **le SILENCE n'est pas l'inconnue** — « to confirm in STEP-01 » licencie un report EXPLICITE, pas l'absence d'énoncé. Plus strict que le §2, à dessein.
  - **La mesure préalable a trouvé QUATRE défauts de premier jet**, chacun qualifié par la sous-chaîne exacte : `Client (geographic footprint)` ← « office move of the Lyon **site in** » (un bâtiment lu comme une implantation) · `AI system (use case)` ← « AI maturity **diagnostic** » (livrable de conseil) · `AI system (status)` ← « **pilot** budget capped for Q3 » (un budget) · `Compliance deadline` ← « **deadline** in three/six weeks » — **5ᵉ occurrence de la quantité mal ancrée**, et j'avais ancré sur `deadline`, le mot faible retiré de WF-004 dans ce même lot. Fermés : matrice **41 → 36** cellules, et **11 des 19 specs remplissent sur ZÉRO brief étranger**.
  - **`Client (geographic footprint)` = LE candidat à la dégradation, dossier instruit d'avance** : 1 seul hit sur la carte (le bloc de paramètres), aucune étape ne la consomme alors que `Geography` est un input d'étape verbatim, et le §2 ne la demande sur aucun des deux briefs — profil plus faible que WF-006 `Selection criteria`. Gardée `required` car `defaultValue` n'est **pas honnêtement disponible** (le champ signifie « ne peut jamais manquer » ; une implantation client n'est ni une constante d'opérateur ni un invariant d'intake — un default l'AFFIRMERAIT). ⛔ Elle ne doit jamais lire le vocabulaire de flux de données, sinon l'éclatement est décoratif.
  - **`Data processed (sensitive)` vs `(Art. 9)` : le 2ᵉ brief est ce qui PROUVE qu'elles sont distinctes** — P08 remplit les deux via « health data (GDPR art. 9) », P20 ne dit que « health data involved » et laisse `sensitive` manquante. `personal` est satisfaite par le vocabulaire Art. 9, implication **à sens unique** écrite sur la spec.
  - **11 falsifications, toutes rouges — et une a corrigé MON TEST, en deux temps.** Restaurer `deadline` nu est d'abord revenu **VERT** : diagnostic, ma mutation visait la branche « quantité puis ancre » alors que le test exerçait le miroir. Rejouée sur la bonne branche → rouge ; rejouée sur l'autre → **verte pour de bon**, donc cet ordre de mots n'était mesuré par **rien**. Troisième chaîne ajoutée au test, mutation désormais rouge. ⚠️ **Un vert de falsification est un constat dans les deux cas** : ici il désignait un trou du test, pas du détecteur.
  - **Confrontation au §2 faite À LA LIGNE, pas à la spec** (décision du 2026-07-31) : le §2 n'a jamais connu que 11 lignes, comparer à 19 faits produit de l'arithmétique et non des jugements. Agrégé, P08 laisse **6 divergences réelles** — accord sur `Volumes` et `Compliance deadline` ; nous demandons en plus `Client`, `AI system audited`, `Audit origin`, `Suspected AI Act tier` ; le §2 demande en plus `Geography` et `AI model` (classe documentée : la fixture énonce ce que l'esquisse n'a pas). ⚠️ **Cette agrégation ne vaut que pour comparer au §2** — jamais pour rendre `PARAMS_MISSING` à l'opérateur, où nommer la MOITIÉ manquante est tout l'intérêt. **⤵ Requalifie le volet 3 de l'audit de fin de chantier**, aujourd'hui formulé au niveau item.
  - ⛔ **AUDIT CARTE↔DÉTECTEUR : ÉTUDIÉ ET ABANDONNÉ 2026-07-31, ne pas le reproposer sans lire ceci.** Objectif visé : attraper mécaniquement un détecteur acceptant une valeur que sa carte n'offre pas (le défaut `onboarding`/`operational` de WF-007). **Trois variantes prototypées et MESURÉES, toutes rejetées** : (a) mots du détecteur non rattachés à une valeur de carte → **670 mots, 10,2/spec** — le défaut y serait 1 mot sur 670 ; (b) groupes d'alternance « majoritairement valeurs » → **26 signalements, ~0 vrai positif** (`novice`≈beginner, `cost`≈price, `deck`≈kit) **et n'attrape pas `onboarding`**, l'extraction fusionnant valeurs et ancres ; (c) valeurs de carte non lues par leur propre détecteur, mesuré comportementalement → **42 %**, dominé par les **refus délibérés** (`LinkedIn`, `Training` nu, quantités nues) — **signal inversé**, il accuserait le meilleur travail du repo. La distinction « synonyme d'une valeur listée » vs « nouveau membre » **exige de lire la carte**. ✅ **Ce qui traite le défaut à la place** : la table d'Unit A porte désormais une colonne **valeurs de carte VERBATIM** en regard de ce que le détecteur accepte en plus — le contrôle redevient humain au moment où il coûte le moins. Prototypes en scratchpad, **zéro machinerie livrée**.

  ✅ **DETTE DES 22 VALEURS DE CARTE — CORRIGÉE 2026-07-31, génériquement.** Le fait qui tranche, vérifié à la source : **le bloc quick-start de CHAQUE carte enseigne `- Libellé: valeur`**, et WF-006/WF-008 y inlinent les énumérations (`- Audit origin: [Preventive / Inspection / Due diligence]`). Le catalogue enseigne donc la forme que le runtime refusait — mismatch réel entre les deux repos, pas artefact de sonde. `paramFilled` accepte désormais un paramètre que le brief **répond en le NOMMANT** : **180 valeurs de carte sondées, 0 encore refusée**. ⛔ **Ne pas rustiner les regex une par une** : 22 patches auraient encodé la même règle 22 fois. **Mesuré avant/après sur les 19 briefs : ZÉRO basculement de verdict** — les deux snapshots sont byte-identiques, la règle n'ajoute que l'acceptation d'une forme que le corpus n'emploie pas. Gardes : valeur **affirmative** (réutilise `NEGATIVE_SENTINEL`, donc « Engagement type: TBD » n'est pas une réponse) et `sanctionedUnknown` testé AVANT, donc la licence de la carte gagne là où la carte en donne une. ⚠️ **Un garde de conjonction a été écrit puis RETIRÉ** : la falsification a montré qu'il ne changeait rien — l'exclusion est **structurelle** (le libellé matché est le `card` de la spec, qualificatif compris, donc « Client: Acme » ne répond à aucune des trois moitiés). Code mort retiré, test conservé : il rougit si le match passe au libellé de base.

  ⚠️ **2ᵉ PASSE DE VÉRIFICATION (2026-07-31) — une direction de sonde JAMAIS TOURNÉE, et elle a trouvé deux défauts dans WF-008 avant le push.** La question : *le détecteur lit-il la valeur de sa PROPRE carte, écrite comme la carte l'écrit ?* Aucune fixture ne peut l'exercer — une fixture est de la prose, une carte est un formulaire. Trouvés et corrigés : `Geography` exigeait l'article de « outside **the** EU » et refusait donc la 3ᵉ valeur de sa carte, « Outside EU with EU impact » ; et `Audit origin` lisait `Preventive` dans les DEUX ordres de mots mais `Incident` dans un seul — asymétrie interne à une spec, sans base sur la carte. Les deux verrouillés par un test, les deux falsifiés rouges. ⚠️ **Le test a dû être ISOLÉ avant d'être cru** : en ajoutant la phrase au contexte de P08, qui dit déjà « EU only », `Geography` se remplissait par une autre branche et l'assertion tenait avec ou sans le correctif — le contexte est désormais REMPLACÉ. Leçon WF-006 `competition`, deuxième fois.
  - ▫ **CONSIGNÉ, NON TRAITÉ — nouveau volet pour l'audit de fin de chantier : « le libellé de carte désambiguïse-t-il la valeur nue ? »** **⚠️ LE CHIFFRE BRUT DE CETTE SONDE A ÉTÉ RETIRÉ D'ICI À DESSEIN.** Une 1ʳᵉ rédaction disait « 110 refus sur 276, dominée par des artefacts » : proportion AFFIRMÉE et non mesurée, et un nombre alarmant que personne ne peut actionner. Mesuré ensuite : **276 sondes = 79 artefacts de conjonction** (les moitiés d'une conjonction sont des FAITS, pas des valeurs — « Client: Name. » ne veut rien dire) **+ 9 placeholders** (`Other`, `e.g. €200`, `PI`) **+ 22 refus réels**. Et sur ces 22, **21 sont NOMMÉS dans le commentaire de leur propre spec** : ce sont des décisions examinées et verrouillées — `support` nu (WF-004), `AI` nu (WF-005), `competition` nu (WF-006), `Team` nu (WF-007) — que les inverser réintroduirait les faux remplissages qu'elles existent pour arrêter. **Un seul était silencieux** (`WF-004 expected_deliverables` refusant `Training`), et sa raison existait sur la spec VOISINE qui avait copié sa forme ; commentaire ajouté sur l'originale, 2026-07-31. **Le périmètre ouvert est donc 22 cas nommés, pas 110** — dont **WF-007 `Engagement type: Scoping`**, refusé parce que la spec exige `scoping engagement`. ⛔ **Ne pas corriger au fil de l'eau** : « le libellé rend la valeur nue non ambiguë » est une POLITIQUE à trancher UNE fois pour les dix manifests, pas un correctif de lot — sinon on inverse cinq décisions verrouillées en croyant corriger une faute de frappe. Les deux corrections de WF-008 ci-dessus n'en relèvent pas : l'une est un article que la carte n'écrit pas, l'autre une asymétrie interne ; aucune ne renverse un refus délibéré.

  ✅ **WF-009 SHIPPÉ 2026-08-01.** 10 lignes → **11 specs**. ⚠️ **Deux slashes de sens opposé, le piège de cette carte** : `Role sought [Title / Level]` est une conjonction (éclatée) ; celui de `Salary / day rate` est dans le **LIBELLÉ** — un nom alternatif selon le contrat — et sa valeur `[Range or "to be defined"]` n'en porte aucun. Le lire comme une conjonction aurait inventé une 12ᵉ spec. Carte **sans ligne `Expected deliverables`** : premier manifest des dix qui ne lit pas ce champ.
  - **La faiblesse structurelle du lot est traitée par un CONTRÔLE INDÉPENDANT, pas par un 2ᵉ brief que j'aurais écrit** (il aurait été circulaire). WF-009 n'a qu'un brief propre là où WF-006/WF-008 en ont deux — or c'est le second qui *prouvait* deux specs distinctes. Remplacement : le **seed du harnais live WF-009**, écrit le 2026-07-12/13 **pour la spine** et prouvé par deux runs facturés, aplati en prose **sans ses libellés de champ** (méthode WF-005). Résultat **8/11**. ⛔ **Expectation NON ajustée jusqu'à obtenir un meilleur chiffre** — retoucher la prose jusqu'à ce qu'elle passe est le contrôle circulaire que WF-005 a appris à refuser. Les 3 manquantes sont la **forme du seed** : `Urgency` (« 3 months » nu — WF-005 `Horizon` reproduit sur une autre carte, corroboration venue d'une source qui ignore la politique), `Must-have skills` (aucun marqueur, et la liste *nice-to-have* est juste à côté dans le même seed), `Role sought (title)`. ⚠️ **Et elles ne se rattrapent PAS toutes pareil** : les deux premières se lisent via la règle `Libellé: valeur` ; la troisième non, car c'est une moitié de conjonction dont le libellé porte le qualificatif. Garde anti-dérive : le test **relit le fichier du harnais** et rougit si le seed y change.
  - **Trois défauts de premier jet trouvés par la mesure, tous par ancrage manquant** : `role_title` ← « **CDO** sponsor » (le CDO du client, pas un poste recherché) · `role_level` ← « upskilling of their **staff** » (le personnel, pas le grade Staff Engineer) · et la 6ᵉ occurrence de la quantité nue sur `urgency`, **mesurée avant d'être écrite** pour la première fois. Fermés : **2 cellules étrangères sur 18 briefs**, le manifest le plus propre des neuf.
  - **`Must-have skills` exige un marqueur d'exigence, pour l'INDÉPENDANCE et non par sévérité** : la carte porte AUSSI `Nice-to-have skills`, et une liste de technos ne dit laquelle est laquelle que si quelque chose la marque. Mesuré : un détecteur naïf remplit sur « Kubernetes and MLflow **stack** » de P09 — le stack de l'ÉQUIPE, sujet de `Team context` deux lignes plus bas. Avec le marqueur, P09 la rend manquante, **d'accord avec le §2**.
  - **`contract_type` et `assessment_methods` gardés en valeurs NUES, sur mesure et non par habitude** : 0 remplissage sur les 19 briefs. Sur-ancrer aurait coûté ce que le sur-ancrage a coûté à WF-008 `Geography` — refuser le mot de la carte pour rien.
  - ⛔ **`required: false` et `defaultValue` SE MASQUENT MUTUELLEMENT — mesuré** : basculer `required` ne bouge rien, retirer `defaultValue` ne bouge rien, **seul le retrait des DEUX** rend la ligne manquante. Redondance antérieure à ce manifest (WF-001/002 `deliverables_language`). **Conservée** — les deux disent des choses différentes à un LECTEUR — mais il ne faut **pas** prétendre qu'une falsification garde ce drapeau : aucune ne le peut seule.
  - **8 falsifications rouges** + 1 verte requalifiée (le masquage ci-dessus). Snapshots régénérés un fichier à la fois : **seule la section WF-009 s'ajoute**, aucune section existante n'a bougé.
  - ⚠️ **BLINDAGE APRÈS COUP (2026-08-01) — la classe de défaut de WF-007 était de retour : CINQ valeurs ajoutées à des énumérations fermées.** `portage` sur `Contract type` (arrangement français que la carte ne liste pas — **et c'est ma propre situation d'opérateur importée dans les valeurs d'une carte**) · `distributed` sur `Location` (propriété d'ÉQUIPE, et `Distribution` est une valeur de la carte **WF-010** — vocabulaire emprunté à une autre carte) · `pair programming` et `assessment centre` sur `Assessment methods` · **`head of` sur `Role sought (level)`, dont mon propre commentaire disait qu'il « n'est pas du même barème » pendant que le pattern l'acceptait.** Les cinq retirés. Matrice **2 → 1** cellule étrangère, et le diff ne montre que cette cellule.
  - ⚠️ **DEUX falsifications sont revenues VERTES et je ne les ai pas prises pour une validation** : `portage` n'existe dans aucun brief, `head of` **uniquement dans `submittedBy`** (mappé nulle part) — donc rien ne pouvait les mesurer. Un vert de falsification est un **constat sur le corpus**, pas un permis. Test dédié ajouté qui fournit les entrées que le corpus n'a pas ; les trois rougissent désormais. ⚠️ Et ce test a dû être corrigé **deux fois** : d'abord parce que le `need` de P09 répondait déjà à `Role sought (level)` (**3ᵉ fois** que ce repo écrit une assertion dont le sujet est déjà rempli ailleurs dans le même brief), puis parce que « Head of Data Science » ne satisfait même pas l'ancre du token muté — **un cas doit pouvoir ÉCHOUER avant de pouvoir passer**.

  ✅ **WF-010 SHIPPÉ 2026-08-01 — LES DIX MANIFESTS SONT LIVRÉS.** 9 lignes → **12 specs**, la pré-dérivation ci-dessous a tenu au chiffre près (2 conjonctions, `+` pour la ligne 1, slash pour `Team involved`). **Un optionnel, d'un TROISIÈME genre** : `Expected format` n'est ni déclaré par la carte (WF-009) ni une constante de profil opérateur (WF-001) mais un **invariant de workflow** — le `output_attendu` de STEP-06 émet les trois formats et défère la présentation à l'étape (« if required »). Tranché par la mesure et non par l'argument : en required avec détecteur, il remplissait sur **zéro brief propre et un seul brief étranger** (P08 « board presentation expected »).
  - **Le contrôle indépendant a FALSIFIÉ un jet au lieu de le confirmer** — c'est sa première fois dans ce repo. Seed du harnais live WF-010, aplati sans ses libellés (méthode WF-005/WF-009) : **10/12**, les 2 manquantes étant le silence du seed (aucune date, aucun champ RH), attendu **non ajusté**. Ce qu'il a cassé : mon 2ᵉ jet de `project_incident_name` exigeait le déterminant `the`, or le seed désigne « Synthetic AI delivery project » sans déterminant. L'ancre était une hypothèse de FORME à moi, pas une propriété de la façon dont un projet se nomme.
  - **Mesure avant test : 3 défauts de premier jet.** `project_incident_name` acceptait n'importe quel qualifiant → P03 « before rollout » et **P15 « …NOT A bounded project », un nom de projet lu dans une phrase qui en nie l'existence** ; `programme|rollout|migration` retirés avec (nouveaux membres inventés sur une ligne que la carte appelle `Project / Incident`). `Client stakes` en valeurs nues remplit sur **8 briefs sur 19** (`deadline` appartient à WF-006, `scope` nomme une mission de cadrage) → l'enjeu doit être MATÉRIALISÉ, et **3 des 4 valeurs de carte sont refusées nues**, écrit sur la spec. `team_work_mode` nu : 2 briefs étrangers, 0 propre — P09 écrit « team of eight, **hybrid** Paris » à 11 caractères, donc toute fenêtre comptée en caractères l'attrapait ; c'est le **séparateur de classe** qui est refusé dans la fenêtre, pas un compte de caractères.
  - **La passe valeurs-de-carte en a trouvé 3 autres, dont une structurellement inatteignable** : **`\b[<>]` NE PEUT JAMAIS MATCHER** (une frontière de mot ne tient pas entre une espace et `<`) — « < 3 months » et « > 12 months », 2 des 3 valeurs de `Project duration`, étaient illisibles dans le jet même dont le commentaire prétendait les lire. `Report audience` exigeait « general public » et refusait donc son propre `Public`. Et `Closeout type` lisait `incident` nu alors que la ligne `Available data` de la MÊME carte offre « logged incidents » : la liste des preuves énonçait le type de clôture — deux specs du même manifest qui se répondent, trouvé par cette passe et par aucune fixture.
  - ⚠️ **Ma sonde de blindage avait un trou et c'est l'instrument versionné qui l'a rattrapé** : mon aplatissement de scratch perdait le `context` des 3 briefs dont la valeur passe à la ligne (P01, P05, P19). Conséquence réelle : élargir `Public` **coûte** une cellule (P05 « public LinkedIn audience »), là où j'avais écrit « ça ne coûte rien ». Leçon : une mesure de scratch qui lit du TEXTE SOURCE n'est pas l'instrument qui lit les OBJETS.
  - ⚠️ **UN DÉFAUT DANS WF-007, SHIPPÉ ET VERROUILLÉ, QU'AUCUN GARDE NE POUVAIT VOIR.** `engagement_location` portait `distributed team` alors que sa carte énumère exactement `[On-site / Remote / Hybrid]` — nouveau membre sur énumération fermée **et** vocabulaire emprunté à la carte WF-010 (`Distribution`). Le token identique avait été retiré de WF-009 le 2026-08-01 pour ce motif écrit ; cette copie a survécu à la correction. Invisible parce que la matrice croisée **enregistre** la cellule comme légitime et que la table de politique n'apparie pas `location` avec `engagement_location` (strip = `client_|prospect_|infra_`). Retiré : matrice **41 → 40**, la seule cellule qui tombe est P10, P07 remplit toujours par ses propres valeurs.
  - **Le marqueur `negation-guard` de l'audit de politique allait produire son 1ᵉʳ faux positif** : il testait `(?<!` nu, exact tant que TOUS les lookbehind du jeu étaient des négations — ils l'étaient, 5 fois — jusqu'à mon `(?<!logged )`, une exclusion LEXICALE. Resserré contre une définition écrite ; les 5 vrais positifs conservés. Même classe que le `one-?pager` retiré de `quantity-vocabulary` sur WF-007.
  - ⚠️ **PASSE DE VÉRIFICATION PRÉ-PUSH (demandée par Guy) — une direction de sonde JAMAIS TOURNÉE : le bloc QUICK-START DE LA CARTE, rempli et passé au check. Résultat 10/12, et les 2 manquantes ont UNE seule cause.** La règle de libellé matche le `card` de la SPEC, qualificatif compris, donc les moitiés d'une conjonction ne sont **jamais** servies par le libellé de BASE de la carte (« Team involved: » ne répond à aucune des trois). Cette exclusion a été tranchée à dessein le 2026-07-31 (un garde écrit puis retiré, no-op mesuré) — **ce qui n'avait jamais été mesuré, c'est son COÛT sur la forme que la carte enseigne à l'opérateur**. Les moitiés retombent sur leurs détecteurs de prose et 2 sur 5 ne sont pas lues : `Team involved (remote or on-site)` refuse « 9 people, distributed, hybrid » par le garde de virgule gagné contre P09, et `Project / Incident (name)` ne lit pas « Atlas migration » (seuls les deux noms de la carte comptent). Direction sûre, `PARAMS_MISSING` nomme la moitié à reformuler ⟹ **rien n'est rustiné** : élargir a été mesuré et refusé (`migration` remplissait sur « before rollout » de P03 ; retirer le garde de virgule ré-admet P09). Un test ENREGISTRE le comportement, les deux mutations le rougissent. ⤵ **La question systémique — conjonctions vs règle de libellé sur les SIX manifests qui en portent — va au volet 3 de l'audit, pas à un correctif de lot.**
  - ⚠️ **Deux chiffres publiés étaient FAUX, et le corpus fautif n'était pas seulement incomplet : il était CONTAMINÉ.** Mon aplatissement de scratch perdait le `context` des 3 briefs à valeur multi-lignes **et** déversait le texte d'un brief dans un autre en fin de fichier — d'où « 9 briefs sur 20 » pour les enjeux nus, alors que le vrai chiffre est **8 sur 19**, le fantôme étant les mots du voisin comptés comme ceux de P20. Et l'écart « team » → « hybrid » de P09 était publié à 15 caractères ; il est de **11**. Aucun des deux ne déplace une décision (8 remplissages étrangers tranchent toujours l'ancre, 11 caractères battent toujours une fenêtre `{0,20}`) — mais un chiffre d'apparence précise est ce qui rend un commentaire autoritaire. Re-dérivés contre un parseur robuste aux retours à la ligne, vérifié enregistrement par enregistrement contre la source. [[feedback-mesure-scratch-vs-instrument-versionne]]
  - **12 falsifications, toutes rouges, 0 refusée, rejouées sur le code FINAL** (les 10 du lot + 2 pour le nouveau test quick-start). Une ne l'était pas au départ (la quantité nue sur `Project duration`, **7ᵉ occurrence de la famille**, n'était rougie que par le snapshot) → assertion nommée ajoutée sur P02. Et une falsification a corrigé **mon test** : l'amendement écrivait « a team of nine, working on-site » et restait `PARAMS_MISSING` — le garde de virgule refusait, à raison. Snapshots régénérés **un fichier à la fois** et lus en diff. Suite **390/23** + typecheck strict verts.

  ▫ **[HISTORIQUE] Pré-dérivation WF-010 du 2026-08-01 — conservée, elle a tenu.**
  - **9 lignes → 12 specs**, et **AUCUN optionnel, AUCUNE inconnue sanctionnée** (bloc scanné : zéro marqueur `non-blocking|optional|to be|to confirm`). Les 12 sont donc required.
  - **Deux conjonctions, et la première a un piège de forme** : `Project / Incident : [Name + start/end dates]` — le slash est dans le **LIBELLÉ** (Projet OU Incident, comme `Salary / day rate` de WF-009), la conjonction est le **`+`** : Name ET dates → **2 specs**. `Team involved : [Size / Distribution / Remote or on-site]` → **3 specs**. Les sept autres lignes → 1 spec chacune.
  - ⚠️ **Trois risques d'indépendance repérés d'avance :** (a) `Closeout type` porte la valeur **`Incident`**, qui est aussi la moitié du LIBELLÉ de la ligne 1 — le motif exact de WF-006 `Request type`/`competition` ; (b) `Team involved (remote or on-site)` recoupe `Location` de WF-009 et `Engagement location` de WF-007 — et `distributed` vient d'être RETIRÉ de WF-009 précisément parce que `Distribution` appartient à CETTE carte, donc c'est ici qu'il doit vivre ; (c) `HR sensitivities` s'appariera avec `sensitivities` de WF-007 dans la table de politique si on le nomme pareil — le nommer ainsi est un CHOIX pour rendre la comparaison visible (précédent `stakeholders` de WF-007).
  - **`Project duration [< 3 months / 3-12 months / > 12 months]` = 7ᵉ occurrence de la famille quantité.** Classes nommées comme WF-007 `Engagement duration` : ancrer, jamais de quantité nue, et **mesurer avant d'écrire** (sur WF-009 c'est ce qui a évité le défaut).
  - **`Available data [KPIs, metrics, meeting minutes, logged incidents]` = VIRGULES** → exemples → **une** spec (précédents `Constraints` WF-001, `Known risks` WF-006, `Team context` WF-009).
  - ✅ **DEUX briefs propres, P10 et P13** — la 2ᵉ mesure indépendante qui manquait à WF-009. §2 : P10 → must-ask `Project duration, Team involved, Available data` ; P13 → `Duration, Team involved, Available data, HR sensitivities`. Confronter **à la ligne**, pas à la spec.
  - **Protocole inchangé** : Unit A avec la colonne **valeurs-de-carte verbatim**, blindage analytique entre A et B, `npm run measure -- WF-010` **avant tout test**, falsifications via `scripts/falsify.sh`, snapshots régénérés **un fichier à la fois**. ⛔ Vérifier chaque valeur ajoutée contre l'énumération de la carte : **synonyme = oui, nouveau membre = non** — le défaut s'est reproduit sur WF-007 ET WF-009.
  - ⤵ **Après WF-010 : l'audit de fin de chantier** — volet 2 livré, volet 5 clos, volet 3 requalifié à la ligne, volet 4 annoté.

  ⚖️ **DÉCISION DE GUY (2026-08-01) : la release vient APRÈS l'audit, pas avant.** Le `[Unreleased]` accumulé depuis `v0.9.1` (les dix manifests, l'audit de cohérence de politique, la sonde croisée, le lot CI) est prêt à être figé — mais l'audit peut encore déplacer des chiffres et des verdicts, et une release qui les épingle d'avance serait à réécrire. Ne pas re-poser la question au démarrage : elle est tranchée.

  ✅ **AUDIT DE FIN DE CHANTIER — CLOS LE 2026-08-02. Les quatre volets restants et les trois dettes sont faits ; le cadrage d'origine est conservé ci-dessous, il a tenu.** Ce que l'audit a appris **en plus** du plan (⛔ **aucun décompte de commits ici** : cette ligne en portait un, écrit juste avant qu'une 2ᵉ passe n'en ajoute deux — un compte périssable dans un tracker se périme à l'instant où on l'écrit, cf. [[feedback-tracker-ne-pas-epingler-head]] ; le lot se lit au `git log`) :
  - **Volet 4 — clos par un fait, pas par une relecture.** Les 88 lignes de carte re-comptées au pin (81 hors pilote) retrouvent **exactement** le chiffrage du 2026-07-27. Et le risque réel n'était pas la dérive du pin, immuable, mais un manifest **re-épinglé sans re-lecture de sa carte** : mesuré sur l'historique complet des dix fichiers, **aucun n'a jamais porté un autre `catalogTag` que `v4.2.0`**, donc aucune carte n'a pu changer sous un manifest déjà écrit.
  - **Volet 1 — 2ᵉ passage : aucune classe de cellule nouvelle.** Sonde re-jouée, snapshot **propre** avant toute régénération. Dix sections relues et agrégées par spec : **181 cellules sur 42 specs**, dont **109 (60 %) sont la classe identité documentée** ; **59 des 101 specs mesurables remplissent sur ZÉRO brief étranger**. ⚠️ **Le chiffre de specs du tracker était faux** : il annonçait « à re-mesurer à 97 », le réel est **108** (le 97 valait `85 + 12`, WF-009 oublié dans l'addition).
  - **Dette (a) — le diagnostic du tracker s'est INVERSÉ à la mesure.** Il la décrivait comme un trou « des deux côtés » et symétrique. Elle est **unilatérale**, et ce sont les cartes qui tranchent : `Team size` de WF-001 est une **énumération fermée** (Solo / 1 squad / Several SAFe teams), donc y lire « team of eight » inventerait un nouveau membre — la classe corrigée sur WF-007 et WF-009 ; `Team involved [Size / …]` de WF-010 **n'énumère rien**, donc « 3 squads » EST une taille et la refuser était le vrai trou. Branche ajoutée à WF-010 seul, compte exigé. Coût mesuré avant l'édition : cellules 6 → 7, la seule nouvelle étant P02 ← « 3 squads », un énoncé de taille authentique. Le corpus ne voit le gain d'aucun côté (P10 énonce une distribution, P13 aucune équipe) ⟹ **asserté sur le formulaire que la carte enseigne**.
  - **Dette (b) — l'appariement a été élargi, et une décision antérieure RENVERSÉE à dessein.** ⛔ **Pas par une règle de strip plus large, et c'est le premier essai qui l'a montré** : elle écrase `team_size` sur `client_size`, deux faits distincts, et la table se mettrait à crier au loup. Carte d'alias explicite, une spec à la fois. Le groupe `stakes` renverse un « DELIBERATELY NOT PAIRED » écrit sur WF-004/WF-007 : le porteur de la décision **était l'absence d'une ligne dans la table**, donc quelque chose que personne ne ré-examine — exactement l'angle mort que la table existe pour fermer. Les trois sont désormais affichés côte à côte, et le commentaire porte le raisonnement. ⚠️ **Une hypothèse à moi, vérifiée à la source, était FAUSSE et rien n'a bougé pour elle** : `stakes|challenges|risks|drivers` de WF-007 ne sont pas des noms de concept admis nus mais des **ancres d'adjacence**. La colonne « vocabulaire unique » mêle ancres et valeurs — elle lit des sources de regex, pas du comportement.
  - **Dette (c) — TRANCHÉE PAR GUY LE 2026-08-02, une fois et pour les sept manifests concernés.** ⚠️ **Sept, pas six** : WF-003 `GDPR constraints` manquait au décompte du tracker — **31 demi-specs sur 108**. Coût du statu quo mesuré sur le formulaire quick-start de chaque carte : **9 des 29 moitiés répondues revenaient MANQUANTES (31 %), et six des neuf étaient des `(name)`**. Trois voies mesurées, pas argumentées : garder le verbatim → 9 manquantes ; strip de tout qualificatif → 0 manquante mais « Client: Acme » répond 3/3 sur WF-004 et 4/4 sur WF-008, et « Data processed: yes » répond **y compris à la catégorie Art. 9** ; **strip de `(name)` seul → 3 manquantes et un libellé nu répond exactement au nom (1/3, 0/3). RETENUE.** C'est une ligne et non une heuristique : le libellé **désigne** l'identité. ⚠️ **Le coût de l'option écartée est INVISIBLE au corpus** — les 19 briefs sont en prose, aucun n'emploie le format `Libellé: valeur`, donc seul le garde dédié l'attrape ; et le garde en question **a été renommé, son ancien nom étant la leçon** : il disait « does NOT let a conjunction's line label answer ANY of its halves » en n'assertant que sur `sector` et `size`, donc son titre revendiquait une politique un tiers plus large que ses assertions.
  - **Volet 3 — la jointure est totale et le §2 est confronté À LA LIGNE sur les dix. 108 specs sur 108 se joignent à une ligne de leur propre carte, zéro orpheline** (ce qui ferme aussi le résidu du volet 4). Sur les quinze prompts routés : **27 accords, 29 lignes demandées par le §2 seul, 35 par les manifests seuls**, 26 lignes distinctes. Contrôle passé AVANT toute lecture : la ligne P08 reproduit **exactement** les six divergences mesurées indépendamment le 2026-07-31. **Les deux directions ne sont pas symétriques et la légende explique les deux** : le §2-seul est la classe documentée (esquisses vs fixtures) ; le manifests-seul est dominé par une cause structurelle écrite **une fois** et non répétée sur vingt specs — **`F` signifie « présent OU DIRECTEMENT DÉRIVABLE »**, et dériver est précisément ce qu'un détecteur déterministe ne doit jamais faire. **Une seule divergence survit à cette lecture** : la légende classe `D` comme inconnue sanctionnée **ou constante de profil opérateur**, en nommant trois de ces dernières — « deliverables language, tone, **proposal format** ». Les deux premières sont honorées ; **WF-006 `Proposal format` reste `required` sans default**, tranché à la carte comme l'exige RULE 1 : le brief porte un CLIENT, et la façon dont le PROSPECT veut recevoir une proposition n'est pas une constante de la pratique de l'opérateur. Le §2 est un instantané daté ⟹ **annoté, jamais réécrit**.
  - ⚠️ **2ᵉ PASSE SUR LE LOT D'AUDIT LUI-MÊME (2026-08-02, demandée par Guy) — ELLE A TROUVÉ UN DÉFAUT RÉEL, donc la clôture ci-dessus ne se lit PAS comme « l'audit s'auto-suffisait ».** La classe de séparateur de la règle de libellé était `[:—-]`. Le **tiret nu** faisait lire un mot composé comme une déclaration — « a client-**facing** chatbot » remplissait `Client (name)` de WF-004 sur un brief qui ne nomme aucun client — et le cadratin faisait lire une apposition comme une réponse (« the prospect — a regional insurer — » remplissait `Prospect (name)` sans nommer personne). Resserré au **deux-points**, seule forme que les quick-start enseignent. **Le défaut est ANTÉRIEUR à la dette (c), et c'est mesuré et non inféré** : `Location` de WF-009 remplissait sur « location-based recommendation engine », or ce libellé n'est pas une moitié de conjonction et la dette (c) ne l'a jamais touché — elle a **élargi l'exposition**, pas créé le trou, en rendant `Client` et `Prospect` actifs comme libellés nus. ⚠️ **Invisible à la TOTALITÉ du corpus** : aucun des 19 briefs ne place un libellé de carte devant un tiret ⟹ zéro verdict déplacé et **les deux snapshots byte-identiques** (mesuré APRÈS le changement, pas prédit avant). Falsifié dans quatre directions, dont une qui casse le deux-points lui-même pour prouver que le contrôle du test n'est pas vide. **Leçon transverse : une 2ᵉ passe qui ne fait que rejouer des sondes déjà vertes ne vaut rien — celle-ci a payé parce qu'elle a attaqué une classe de caractères que personne n'avait jamais mesurée.**
  - ⚠️ **BLINDAGE ANALYTIQUE (3ᵉ passe, 2026-08-02) — la règle `(name)` était JUSTE, mais le principe écrit à côté était plus large que le code, et rien ne disait pourquoi l'écart était voulu.** Le principe disait « le libellé DÉSIGNE l'identité » ; à ce compte `Role sought [Title / Level]` de WF-009 y a droit, or sa moitié est `(title)` et reste exclue — ce qui se lisait comme un accident de nommage. **Mesuré, ça s'inverse en raison** : « Role sought: **senior** » remplit `role_level` et **pas** `role_title`, donc une réponse nue à CE libellé est réellement à deux voies, là où « Client: Acme » ne peut être ni un secteur ni une taille. **La condition opératoire n'est donc pas « le libellé désigne cette moitié » mais AUCUNE MOITIÉ SŒUR NE PEUT REVENDIQUER LA MÊME RÉPONSE** — vraie pour les six `(name)`, fausse ici. Servir `(title)` laisserait une réponse n'énonçant qu'un grade remplir les DEUX moitiés. ⚠️ Le mécanisme est écrit dans le test plutôt que lissé, parce qu'il renforce le finding : `role_level` s'ancre sur le nom `role`, pris **dans le libellé lui-même**. **Trois autres claims de la session sont passés d'affirmation à mesure, sans qu'aucun ne déplace de code** : « invisible au corpus » reposait sur l'invariance des snapshots, mesuré directement = **0 des 20 fixtures ne place l'un des 79 libellés devant un tiret** ; le défaut du tiret **n'est pas une classe qui se répète** (espace de recherche nommé : classes-tiret des dix manifests + `validate-route` + les trois `sanctionedUnknown` — tous les hits sont `[-\s]`/`[\w&-]` en connecteur intra-composé, position inverse d'un séparateur) ; et l'alias `client_stakes` est **à la fois** no-op aujourd'hui **et** porteur dès qu'on resserre le strip, exactement ce que son commentaire revendique.
  - ▫ **Dettes de PROCESSUS du lot d'audit, repliées dans le même lot** ([[feedback-corriger-dette-avant-livraison]]) : (1) les commits d'audit n'avaient **rien** écrit dans `[Unreleased]` alors que chaque lot précédent le fait — et l'unité suivante étant la release, qui lit cette section, un audit entier serait sorti non documenté ; (2) cette entrée portait un décompte de commits périssable ; (3) `client_stakes` dans la carte d'alias est **redondant par construction** (le strip de préfixe produit déjà `stakes`) — conservé mais **documenté comme tel**, parce qu'il épingle le nom de rôle indépendamment de la règle de strip, ce qu'une alternative morte ne fait pas.
  - ▫ **Ce que l'audit NE mesure PAS, et qui reste vrai** : rien sur la JUSTESSE d'un détecteur sur de la prose réelle non fixturée (les 19 briefs sont tous écrits par moi — contrôle positif circulaire au niveau du corpus), ni sur la pertinence métier d'un paramètre. La seule source indépendante connue reste les seeds des harnais live ; l'étendre aux dix est un volet **optionnel** à chiffrer avant de le promettre.

  ✅ **CLASSE DE DÉMENTI — LIVRÉE 2026-08-05 (commit local, non poussé). Le cadrage d'origine est conservé ci-dessous ; il a tenu sur le fond, et la METHODE qu'il imposait a cassé trois de mes propres décisions avant qu'elles n'atteignent `src/`.**
  - **Résultat mesuré AVANT/APRÈS sur le code shippé** (et non par prototype) : **15 démentis sur 17 remplissaient, 2 restent ⟹ 13 fermés** · contrôle de sur-portée **14/14 avant ET après** · **0 verdict déplacé** sur les **402 cellules remplies** des 19 briefs · suite **398/23** + typecheck strict verts · **10 falsifications, 10 rouges, 0 verte, 0 refusée**.
  - ⚠️ **« 15 fermés » était FAUX et a survécu au commit — le blindage pré-push l'a corrigé.** Le snapshot dit combien remplissent AUJOURD'HUI ; cinq des dix-sept lignes ont été ajoutées AVEC le garde et n'avaient jamais été mesurées sans lui, et deux autres étaient déjà refusées pour des raisons étrangères. La mesure honnête exige un avant/après, désormais outillé : `absenceIsAnswer` route une spec sur le chemin non gardé, donc un clone qui le porte EST l'ancien `paramFilled` — même appui pour le « 0 déplacé ».
  - ⛔ **LA DÉCOUVERTE QUI DÉPASSE CE LOT : les 19 fixtures NE SONT PAS le corpus du repo.** L'instrument de vérification a rendu « **0 verdict déplacé** » et la suite a ensuite rougi **5 tests** — parce que la suite construit aussi des **briefs AMENDÉS** qui ne vivent que dans le fichier de test. Toute sonde de ce repo lit `DISPATCH_FIXTURES` ; aucune ne lit les amendés. C'est l'angle mort partagé, un cran au-dessus de celui déjà connu ([[feedback-corpus-angle-mort-partage]]), et il a caché **deux** défauts : la classe `absenceIsAnswer` et la frontière de virgule.
  - **Une classe que le garde ne doit PAS toucher, trouvée par la suite qui rougit à l'arrivée** : sur certaines cartes **un démenti EST la réponse** (la carte demande S'IL Y A, pas LEQUEL). Trois specs shippées l'assertent déjà, chacune argumentée à sa carte — refuser les enfermerait l'opérateur dans **une boucle de retour dont il ne peut pas sortir**, défaut pire que celui que le garde corrige. → champ `absenceIsAnswer` **déclaré par spec, jamais inféré**. **CINQ specs, pas trois.**
  - ⛔ **« WF-006 `Competition` n'en a pas besoin — vérifié, non supposé » : le claim était juste sur son objet et FAUX dans sa portée.** J'avais vérifié la FIXTURE (« a sole source situation », aucun token de démenti) et conclu sur la CLASSE. Mesuré au blindage : « there is no competition on this deal » et « no competitors have been identified » **remplissaient avant et étaient REFUSÉS après**. Une fixture peut franchir un garde neuf **par accident de vocabulaire** et masquer la régression qu'il cause ([[feedback-portee-du-claim-depasse-le-lot-livre]]).
  - ⛔ **WF-009 `Anti-fraud required` — le cas le plus net, invisible à toute la suite** : son détecteur porte une branche explicite `no (background|reference) checks?`, donc le garde **annulait une alternative que la spec avait délibérément écrite**, le token de démenti étant À L'INTÉRIEUR du match qu'il devait licencier.
  - **Ce qui les a trouvées n'est aucune sonde** : un grep des COMMENTAIRES des manifests sur la politique qu'ils énoncent (« the absence IS the fact the card asks for », « the card ends this line with yes/no »). Les trois premières venaient de tests rouges ; ces deux-là n'avaient aucun test. Réutiliser ce grep à chaque garde transverse.
  - **Trois décisions de ma table initiale étaient FAUSSES, et chacune l'était parce que le corpus qui la « confirmait » ne pouvait pas exprimer son contre-exemple** : (1) « le garde ne concerne que la route `pattern` » — aucune phrase-sonde n'employait le format `Libellé: valeur`, et « `Client: no name has been given` » remplissait ; (2) « la virgule n'est pas une frontière — 0 divergence sur 2052 cellules » — mesure **vide**, aucun corpus ne plaçait une négation devant une énumération, alors que les briefs amendés le font ; (3) « la portée est le levier » — c'est le **VOCABULAIRE** : la liste ratait **6 formes courantes sur 8** (`isn't`, `lack`, `absent`, `far from`, `yet to be`…).
  - **Collision invisible sans son cas de test** : `yet` était à la fois conjonction de coordination (frontière de clause) et moitié du démenti « has **yet to be** put in production » — le découpage coupait le token en deux et la phrase remplissait pendant que toutes ses sœurs se fermaient. `yet` retiré de la liste des frontières, avec sa raison.
  - **Deux gardes n'étaient gardés par RIEN** avant la passe de falsification : retirer le vocabulaire élargi ou le garde de la route `label` ne rougissait aucun test. Comblés par 5 lignes de corpus dédiées — sans quoi la politique était **déclarée et non gardée** ([[feedback-garde-vert-sans-avoir-garde]]).
  - ▫ **Dette OUVERTE, délibérément non repliée** : **les 90 fenêtres d'adjacence des dix manifests sont `[^.]{0,N}` et autorisent toutes le `;`**, donc n'importe laquelle peut apparier deux clauses (mesuré sur WF-009 `role_level`). La règle « une clause traversée qui nie suffit » ferme le cas **sans y toucher**. Les resserrer en `[^.;]{0,N}` est un lot séparé dont le coût n'est **pas** mesuré — ne pas l'ouvrir au fil de l'eau.
  - ▫ **Deux limites ÉCRITES, pas des oublis** : « AWS was ruled out » (démenti postposé — le fermer coûte 3 faux MISSING pour 1 gain ; ⚠️ **jugement argumenté, PAS une mesure** : 0 des 19 briefs ne porte de participe postposé, les deux plateaux sont vides) et « beginner is a guess » (aucun démenti dans la clause du match, sous aucun découpage testé).
  - ▫ **Instruments versionnés** : `npm run measure:denials` (pourquoi une phrase remplit : sous-chaîne, route, clause propriétaire, position du démenti) et `npm run verify:denials` (prototype la règle pour l'attaquer avant `src/`). Le second **porte l'aveu de son propre angle mort** dans son en-tête. ⚠️ Son `SHIPPED` doit rester aligné sur `validate-route.ts` — un prototype qui dérive décrit un check qui n'est pas celui qui tourne.

  ✅ **RELEASE `v0.13.0` FAITE 2026-08-12 (tag) / Release GitHub publiée le 2026-08-13 — « Clause and field boundaries close, and the dispatch loop is proven past the pilot ».** Tag annoté + GitHub Release **non-draft, non-prerelease**, CI verte sur le commit de release `a568868` (3 jobs). Contenu : les **91** fenêtres d'adjacence qui n'admettent plus le `;`, les **20** jonctions de champ rendues explicites, et le run live P04. `[Unreleased]` remis à `_Nothing yet._`. ▫ **Aval FAIT** : la vitrine surface `v0.13.0` + la trace P04 depuis `v1.17.7`, prod-vérifiée 9/9. ▫ ⚠️ **Le catalogue est passé `v4.4.0` le 2026-08-13** alors que les dix manifests épinglent `v4.3.0` : dérive de fraîcheur **par conception** (ADR-0002), CI verte car elle clone au tag épinglé — mais **la suite LOCALE redeviendra rouge à 10 tests** dès que le checkout `claude-agents` passera sur `v4.4.0`. Parade dans le bloc de reprise en tête.

  ✅ **RELEASE `v0.12.0` FAITE 2026-08-12 — « Two advisories closed, and the catalog pin catches up ».** Tag annoté + GitHub Release **non-draft, non-prerelease, `Latest` confirmé par l'API**, CI verte sur le commit de release `3ce4e7c` (les 3 jobs, dont `real-sidecar`), `[Unreleased]` remis à `_Nothing yet._`. Contenu : les deux advisories (`GHSA-2v37-7h3g-55p8` nanoid dev-only, `GHSA-frvp-7c67-39w9` hono), le re-pin du catalogue sur `v4.3.0`, et les surfaces liées rendues cohérentes. ▫ **Le lot a failli partir SANS entrée de CHANGELOG** : trois changements avaient été poussés sans y figurer, alors que le commit juste avant portait la sienne — le CHANGELOG documentait donc une advisory transitive fermée et pas l'autre, à six jours d'écart. **C'est Guy qui l'a vu**, pas mon process ([[feedback-corriger-dette-avant-livraison]]). ▫ Version bumpée dans `package.json` **et dans les deux champs du lock** (`version` + `packages[""]`) — la dérive exacte contre laquelle le catalogue a dû poser un garde après que cinq de ses tags soient sortis avec un lock périmé.

  ✅ **RELEASE `v0.11.0` FAITE 2026-08-05 — « A denial is not an answer ».** Tag annoté + GitHub Release **non-draft, non-prerelease, `latest` confirmé par l'API** (`releases/latest` → `v0.11.0`), CI verte sur le commit de release `6326a4f`, `[Unreleased]` remis à vide. ▫ **Dette du CHANGELOG repliée dans le même lot** : onze versions publiées ne portaient **qu'une seule** link ref ; toutes rétablies, et `0.1.0` reste **sans lien** parce qu'elle n'a jamais été taguée (vérifié par `git tag`, pas déduit de la liste des titres) — un lien cassé aurait été pire que l'absence.
  - ⟹ **Les deux commits de sondes de seeds (`f42b8ab`, `27f85c8`) sont désormais DANS une version publiée** : le seul motif qui les écartait de la vitrine le 2026-08-03 tombe. Ils portent le résultat le plus vendeur du chantier (41 specs sur 78 se remplissent ; contrôle « nearly powerless » sur WF-001/002/003).

  ⏸ **CE REPO N'EST PLUS LE CHANTIER ACTIF — décision de Guy, 2026-08-05 : le chantier passe à `guyhui-showcase`.** Motif : la release `v0.11.0` ayant été publiée, le **seul défaut FACTUEL ouvert est côté vitrine** (le site affiche encore `v0.10.0`, faux depuis ce jour) tandis que ce qui reste ici est une **montée en gamme de la preuve**, pas la correction d'un défaut. ⚠️ **L'argument des « deux passes de surfaçage » a été pesé puis ÉCARTÉ** : la 2ᵉ passe serait ciblée (insérer une trace), alors que subordonner la vitrine à une décision facturée laisse une information fausse en ligne pour une durée indéterminée. **Ne pas re-plaider ; l'unité ci-dessous reste la prochaine DE CE REPO, elle n'est pas la prochaine SESSION.** ▫ **ANNOTATION 2026-08-06** : Guy a rouvert ce repo pour **une** unité ciblée (les deux en-têtes périmés, livrée), parce qu'elle était **amont de la release vitrine** — le repo qui produit l'artefact lié porte sa correction ([[feedback-tache-amont-porte-maj-aval]]). Cela ne redonne pas la priorité à ce repo : la vitrine reste le chantier actif, et elle attend maintenant le **push** de `ff62c72`.

  ✅ **LIVRÉE 2026-08-06 (commit local `ff62c72`, NON POUSSÉ) — LES DEUX EN-TÊTES SONT CORRIGÉS DANS LEURS GÉNÉRATEURS, snapshots régénérés un fichier à la fois et lus en diff : prose seule, aucune ligne de table ne bouge. Suite 398/23 + typecheck strict verts. Le cadrage d'origine est conservé ci-dessous, il a tenu.** Ce que la livraison a appris en plus du plan :
  - **UN TROISIÈME EN-TÊTE PÉRIMÉ, de la même classe, que la recette n'avait pas vu** : le ⚠️ du §2 de `denial-probe.md` avertissait que WF-005 `audience` avait « ses deux verdicts inversés » et que le contrôle « mesure une ligne de moins ». Or `wf-005.ts:101` porte la branche d'ordre inverse **ajoutée le 2026-08-05**, et le corps du snapshot le prouve (§1 `refused`, §2 `FILL`) : l'avertissement affirmait **l'inverse** de la table placée trois lignes plus bas. Trouvé en lisant le fichier entier plutôt que les deux points nommés.
  - ⚠️ **MA PROPRE PROPOSITION ÉTAIT FAUSSE SUR LE §1, corrigée avant d'écrire.** J'avais rédigé la nouvelle légende en disant que les 2 lignes qui remplissent encore sont les specs `absenceIsAnswer` — **non** : celles-là (WF-006 `competition`, WF-009 `anti_fraud_required`) sont dans le corpus de SUR-PORTÉE, où elles doivent remplir. Les 2 du §1 sont **WF-003 et WF-004**, deux décisions argumentées et chiffrées dans le header (« WHAT THE GUARD DOES NOT CLOSE »), dont celle de WF-003 est **argumentée et non mesurée** (les deux plateaux de sa balance sont vides sur le corpus réel) — la légende le dit désormais.
  - **Le hors-périmètre corrigé sur ordre de Guy** : le §3 publiait `7 of 20 briefs` alors que sa boucle **saute P18**, qui n'a aucun brief. Dénominateur désormais dérivé de la boucle (`briefsProbed`) et non de `DISPATCH_FIXTURES.length`. Re-dérivé par **deux voies indépendantes** qui concordent (parseur ad hoc + compteur du test) : 20 fixtures → **19 sondées, 15 routées, 4 `NO_MATCH`**. ⚠️ Mon premier parseur de scratch a rendu « 1 fixture » — rejeté, pas ignoré ([[feedback-mesure-scratch-vs-instrument-versionne]]).
  - ⛔ **CE QUI N'A PAS ÉTÉ TOUCHÉ, à ne pas « corriger » plus tard sans lire ceci** : (a) le bloc `HISTORICAL FRAMING` du fichier de test, déjà ré-annoté le 2026-08-05 (« it is history, not status ») — c'est la RAISON D'ÊTRE, pas l'état ; (b) l'entrée `v0.11.0` du CHANGELOG, qui porte la même tournure « nineteen routed briefs » : **version publiée, figée, jamais réécrite**. Elle est imprécise et le reste sciemment.
  - ▫ **La leçon générale a une cause mécanique** : la vitrine LIE ces fichiers, mais rien dans ce repo ne sait qu'un fichier est lié depuis l'extérieur. Les deux snapshots sont restés verts pendant que leur titre mentait — **aucun test ne lit un en-tête**.
  - ⤵ **Aval, non fait ici** : la vitrine doit **re-cliquer les 2 liens** puis releaser. Ces snapshots ne sont à jour sur GitHub qu'après un push de ce repo (⚠ commit LOCAL à ce stade).

  ▫ **[HISTORIQUE] Cadrage d'origine du 2026-08-06 — conservé, il a tenu.** La vitrine `v0.11.0` (livrée en local, recette passée) **lie ces deux snapshots** — repo public, précédent `v1.6.1` — pour rendre ses chiffres vérifiables d'un clic. La recette du 2026-08-06 a trouvé que **la cible contredit la phrase qui la porte** :
  - **(a) `test/__snapshots__/denial-probe.md` — L'EN-TÊTE DIT L'INVERSE DU CORPS.** Titre : « *Denial probe — a frozen record of an **UNFIXED** defect class* » ; intro : « *It fixes nothing… the guard can **later** be written* ». Or le corps du **même fichier** dit « *2 of 17 denials fill today* », et le garde est livré depuis `v0.11.0`. L'en-tête date d'avant le garde et n'a jamais été repris. La vitrine affirme « *fifteen are correctly refused; two still get through — **named, one by one*** » et envoie sur un document dont le titre dit que rien n'est corrigé. **Un lecteur lit le titre en premier.**
  - **(b) `test/__snapshots__/live-seed-controls.md` — UNE QUANTITÉ NON VÉRIFIÉE.** « *written for the spine, **weeks** before any parameter check* ». Mesuré : seed WF-005 daté **2026-07-13** (commentaire de `dispatch-validate-route.test.ts`), premier manifest de paramètres **2026-07-19** ⟹ **6 jours**, pas des semaines. C'est vrai pour les dix manifests (2026-08-02), faux pour le premier. **La vitrine a retiré cette quantité de sa propre copie ; la source qu'elle cite la porte encore.** ⟹ retirer le quantificateur plutôt que le ré-estimer.
  - **⟹ Corriger les deux chaînes d'en-tête DANS LES GÉNÉRATEURS** (`test/dispatch-denial-probe.test.ts`, `test/dispatch-live-seed-controls.test.ts`), régénérer les snapshots, suite + typecheck verts. ⚠️ **Ne pas réécrire l'HISTOIRE du fichier** : le probe reste le relevé d'une classe, on corrige l'état qu'il déclare, pas sa raison d'être ([[feedback-audit-fige-annoter-pas-reecrire]]). **Puis la vitrine re-clique les 2 liens et release.**
  - ▫ **Leçon générale, au-delà de ces deux fichiers** : un artefact qu'on LIE devient une surface de lecture publique. Son en-tête vieillit indépendamment de son corps, et **aucun test ne le voit** — les deux snapshots sont verts.

  ✅ **LOT DU 2026-08-12 — TROIS COMMITS LOCAUX, NON POUSSÉS (`ab8745e`, `fe02673`, `c43abc4`).** Aucun run facturé, aucune trace touchée.
  - **`ab8745e` — `GHSA-2v37-7h3g-55p8` fermée** : `nanoid` 3.3.16 → 3.3.18, dev-only (prouvé au lock ET par `npm audit --omit=dev` à 0). **Ce qui a tranché le remède est la plage du PARENT** : `postcss@8.5.25` déclare `^3.3.16`, donc le correctif tombe DEDANS, sans `overrides` et sans faire bouger `postcss` — cas exactement inverse de `@hono/node-server`, où le correctif sortait de la plage du SDK MCP et où forcer avait été refusé. L'audit ISO figé est **annoté sous son tableau §1, verdict « 0 vulnerability » laissé intact** (addition pure de 8 lignes, zéro suppression). ⚠️ **Fait enregistré sans être expliqué** : cette advisory est vue par `npm audit` et par **aucune** alerte Dependabot (18 entrées, toutes `fixed`). Les deux sources ne sont pas interchangeables.
  - **`fe02673` — quatre faits périmés du bloc de reprise corrigés**, dont deux qui rendaient un contrôle décoratif (l'attendu Dependabot, et un `gh run list --limit 1` qui mesurait un run Dependabot au lieu de la CI).
  - **`c43abc4` — les dix manifests re-épinglés sur `v4.3.0`**, ce qui supprime les 10 rouges locaux permanents. **Le risque de re-dérivation a été MESURÉ nul avant d'éditer** : `workflows/` byte-identique entre les deux tags, assets du sidecar strictement identiques une fois les deux champs de version neutralisés, 85 assets inchangés, v4.3.0 étant une release d'outillage du catalogue. 69 occurrences / 17 fichiers, 69+/69−. **Trié, pas balayé** : les 5 docs `discovery/` et le CHANGELOG gardent leur `v4.2.0` (constat daté, pas un pin), et les 56 `v4.1.0` sont des fixtures synthétiques. Garde re-falsifié APRÈS le changement (wf-002 sur un `v9.9.9` inexistant ⟹ exactement 1 test rouge, le bon). ⚠️ **Mon premier harnais de remplacement a rendu un faux vert** (`for` non splitté en zsh : 0 fichier modifié, « incohérences: 0 » affiché) — refait en bash avec un garde global sur le total attendu, qui aurait refusé de tourner. Encore un vert sans avoir gardé.

  ✅ **RUN LIVE DE DISPATCH — FAIT LE 2026-08-12, sur ordre explicite de Guy, abonnement OAuth (aucune clé API, vérifié avant de lancer).** `LIVE_MODEL=claude-opus-4-8` — **le même modèle que le pilote P01**, choisi par Guy. ⚠️ **Ma formulation d'origine disait « pour que le workflow soit la SEULE variable qui change » : c'est FAUX, mesuré à la passe qualité pré-push.** Tenir le modèle constant retire **une** variable, pas toutes : entre les deux traces, le pin du catalogue est passé de `v4.2.0` à `v4.3.0` et `validate-route` est passé de `p.name` à `p.card`. Comparer les deux traces est donc une comparaison de runs, **pas une expérience contrôlée**. 2 appels routeur, **16,2 s**. Résultat : le routeur a pris **WF-004 aux deux phases** ; non amendé ⟹ `PARAMS_MISSING [Engagement duration, Client AI maturity]`, amendé ⟹ `ROUTED` + `paramsChecked:true` + plan attaché, arrêt avant le go/no-go humain. Trace versionnée (`git add -f`, convention P3, briefs fictifs par construction) : `docs/audit/live-runs/wf-000-p04-dispatch-live-result.json`.
  - **Le refus a été PRÉDIT OFFLINE avant toute dépense**, et il est désormais verrouillé là (`P04_UNAMENDED` + deux cas dans `dispatch-validate-route.test.ts`, assertion en ENSEMBLE EXACT et non en sur-ensemble, falsifiée : remettre la fenêtre d'engagement retirée rougit le bon test). Le check de paramètres n'appelle aucun LLM ⟹ sa sortie était connaissable gratuitement, et ce qu'un run facturé pouvait encore trancher est **PLUS ÉTROIT que je ne l'ai d'abord écrit — trouvé à la 2ᵉ passe qualité, dans les preuves de ce repo même**. Le run d'exactitude du routeur du 2026-07-19 (`wf-000-router-live-result.json`, 19/20) **portait DÉJÀ P04 et l'a DÉJÀ routé en live vers WF-004**, avec `paramsChecked:false` (WF-004 n'avait pas encore de manifest). « Un vrai routeur choisit-il WF-004 » était donc répondu depuis trois semaines. Ce que ce run achète réellement : **la BOUCLE COMPLÈTE sur un workflow non-pilote désormais doté d'un manifest** — refus live, amendement, puis route vérifiée. Plus étroit, et c'était bien ce qui manquait.
  - ⚠️ **UNE ERREUR DE MON ANALYSE, TROUVÉE SUR DEMANDE DE GUY (« revérifie ton analyse ») AVANT LA DÉPENSE, et corrigée dans les commentaires versionnés** : j'avais écrit que la valeur du 2ᵉ run était le refus **en libellés de carte**. **Faux, mesuré contre le code livré** : `validate-route` mappe `p.card` aujourd'hui, donc **rejouer le pilote P01 inchangé rendrait déjà `[Team size, Project method, Level of detail]`** au lieu du jeu snake_case de la trace 2026-07-19. Les libellés sont un effet visible sur n'importe quel run ; **le seul mérite du coût est que le workflow n'est pas le pilote**. La justification fausse était partie dans deux commentaires de fichiers avant d'être rattrapée.
  - **Ce que le run prouve, et rien de plus** : le mécanisme **généralise au-delà de WF-001**. C'est **UN** chemin de code de plus — **pas** « les dix prouvés live », et il ne dit rien des neuf autres manifests. ⛔ Ne pas laisser la formule enfler à l'écriture.
  - ▫ **Aval, non fait ici** : la vitrine peut désormais surfacer cette trace (elle est portée par CE repo, [[feedback-tache-amont-porte-maj-aval]]). Elle sort **après** une release, la trace étant postérieure à `v0.12.0`.

  ✅ **DETTE DES FENÊTRES D'ADJACENCE — FERMÉE LE 2026-08-12 (commits `c5fd237` instrument + `d37b146` correctif + un rattrapage, voir ci-dessous).** ⚠️⚠️ **LE COMPTE : DEUX CHIFFRES FAUX AVANT LE BON, DONT LE MIEN.** Le tracker portait « 90 ». Ma re-dérivation a répondu « **87** » et **était fausse aussi** : elle grepait UNE seule forme, `[^.]{0,N}`. L'inventaire complet donne **91** — 87 `[^.;]`, **plus trois `[^,.]{0,N}` dans wf-010** qui excluent la virgule mais **pas** le point-virgule (donc porteuses du même défaut, non corrigées au premier passage), plus une `[^.,;]` déjà correcte. Les trois resserrées en `[^,.;]`, en gardant leur exclusion délibérée de la virgule. ⟹ **Après ce lot, plus aucune fenêtre n'admet le `;`**, vérifié par un inventaire de TOUTES les formes et non d'une seule.
  - ⚠️⚠️ **LA FRONTIÈRE DE CHAMP ÉTAIT PLUS LÂCHE QUE LA FRONTIÈRE DE CLAUSE, trouvée à la 4ᵉ passe de vérification.** Un mapping concatène `need`, `context` et les `constraints` jointes — ils étaient collés par un **espace nu**. La seule chose qui empêchait une fenêtre d'apparier la fin d'un champ avec le début du suivant, c'était que **tous les briefs du corpus finissent leurs champs par un point** : protection par la ponctuation des DONNÉES, pas par le CODE. **Mesuré tant que c'était ouvert** : retirer le point final du `context` de P02 fait matcher `WF-008.compliance_deadline` sur `« 10 weeks compliance »` — `10 weeks` vient du contexte, `compliance` des contraintes. **Un vrai faux positif, à un point près.** ⟹ les **20** jonctions passent en `; `, y compris dans les mappings **composés** que mes deux premiers `sed` avaient ratés (trois vagues successives avant le contrôle exhaustif — même travers que les 3 fenêtres ci-dessus : traiter un motif et croire avoir traité la classe). Coût mesuré AVANT : **0 changement de verdict sur 1919 paires spec×brief**. Garde ajouté (retirer tous les points finaux ne doit déplacer aucun verdict) et falsifié : re-coller une jonction à l'espace rougit ce test.
  - ⚠️ **LA SONDE NE POUVAIT PAS TROUVER CES TROIS-LÀ, et elle affichait 0 après le correctif principal** : les specs concernées ne produisent aucun appariement injectable sur le corpus — c'est exactement la limite de population écrite dans son en-tête, qui a mordu sur le lot même qui la livrait. **C'est un grep de FORME qui les a trouvées, pas la mesure de COMPORTEMENT.** Les deux méthodes sont complémentaires, aucune n'absorbe l'autre ; consigné dans l'instrument. **Le coût était connu AVANT d'éditer et il est nul** : zéro des **269** appariements réels sur les vingt briefs ne traversait un `;`, donc aucun verdict ne pouvait bouger — confirmé, le resserrage rougit **exactement un** test (le snapshot de la sonde) et laisse 399 verts, aucun autre snapshot ne bouge.
  - **Instrument livré AVANT le correctif, snapshot figé tel que trouvé** (motif de la sonde croisée) : 11 fenêtres traversaient un `;` injecté, sur 9 specs et 7 manifests ⟹ **0** après, dénominateur inchangé à 103, aucune ligne ajoutée, diff LU et non régénéré à l'aveugle.
  - **Son corpus est DÉRIVÉ, pas écrit** : on injecte un `;` dans les appariements que les détecteurs font réellement. ⚠️ Ma première version utilisait **huit phrases inventées** : elle trouvait le défaut sur UNE spec et en ratait huit — elle mesurait mon imagination ([[feedback-controle-positif-circulaire]]).
  - ⚠️ **Deux limites écrites dans l'instrument, pas lissées** : (a) seules les specs qui remplissent déjà sont sondables ⟹ tout chiffre est un **plancher** ; (b) l'injection tombe souvent **au milieu d'un groupe nominal** (`three-month; engagement`), que personne n'écrit ⟹ une ligne prouve que la fenêtre **traverserait** un `;`, **pas** qu'un brief réaliste déclenche le faux positif. Le cas réaliste est prouvé à part et **assertion dure** : `role_level` refuse « the budget is senior-approved; we still need to hire a developer » et **remplit toujours** « we are hiring a senior MLOps engineer » — le contrôle qui rend le garde discriminant et non simplement strict.
  - **Falsifié dans les DEUX sens.** Relâcher une fenêtre ⟹ 2 tests rouges. Resserrer davantage (exclure aussi la virgule) ⟹ **VERT, donc un finding** : la ligne « `;` oui, `,` non » est **argumentée, pas mesurée**. Consignée dans l'en-tête de la sonde, **sans garde ajouté** : aucun appariement `role_level` du corpus ne contient de virgule, un test assertirait donc contre un cas vide.
  - **Politique appliquée aux 91 et pas aux 9 prouvées**, sinon le correctif créait lui-même la divergence inter-manifests que la table de cohérence existe pour attraper.

  ✅ **AUDIT DE FIN DE CHANTIER — PASSE DE VÉRIFICATION FRAÎCHE DU 2026-08-14 : RE-VÉRIFIÉ PROPRE, zéro nouveau défaut, zéro changement de `src/`, rien au CHANGELOG.** Rejouée à froid sur l'état ACTUEL des manifests (post démenti + re-pin `v4.3.0` + fenêtres d'adjacence), au tag épinglé (clone `v4.3.0` + `CATALOG_ROOT` ⟹ baseline **402 passed / 24 skipped** ; la suite locale par défaut rougit à **10 tests** de drift de tag, checkout voisin sur `v4.4.0` — attendu, parade = clone au pin, bloc de reprise en tête). Les trois volets confirment la clôture du 2026-08-02, chacun RE-MESURÉ à la source (aucun chiffre repris du tracker) :
  - **Volet 4** — **88 lignes de carte** (81 hors pilote) · **108 specs** (101 hors pilote), comptées par trois champs concordants (`card`/`name`/`required` ; deux sur-comptes de `grep` dus à des commentaires, écartés). Cartes byte-identiques `v4.2.0↔v4.3.0`. Match exact.
  - **Volet 1** — snapshot de la sonde croisée **byte-gelé depuis `66079fb`** (dette a, 2026-08-02) et **vert au pin** ⟹ les lots démenti/adjacence n'ont retiré aucun remplissage étranger. **182 cellules** (deux comptes indépendants concordent) = 181 + la cellule de dette a. Aucune classe nouvelle.
  - **Volet 3** — jointure **totale** : 108 specs → **88 labels de base distincts** = 88 lignes, zéro orpheline. Nombres §2 (27/29/35) invariants : leurs trois entrées sont mesurées-inchangées (§2 figé, cartes identiques, verdicts de remplissage gardés par « 0/402 » démenti + « 0/1919 » adjacence, verts au pin).
  - Seule divergence non-adjugée, inchangée : **WF-006 `Proposal format`** (`required`, sans default), argumentée sur la spec.
  ▫ *[HISTORIQUE] L'ancien marqueur pointait cet audit (« volets 1·3·4 ») ; sa consigne « re-compter, ne rien reprendre » a été honorée ci-dessus.*

  ✅ **ADR-0008 — LIVRÉ ET POUSSÉ le 2026-08-14. Le cadrage d'origine est conservé ci-dessous, il a tenu.** Fichier : `docs/adr/0008-dispatch-gate-route-or-refuse.md`, record honnête ([[feedback-adr-retrospectif-record-honnete]] : Status « locked with the PO 2026-07-18 / recorded 2026-08-14 », alternatives (a)/(b) en voix analytique avec citations de provenance, honest-record note). Commits `bd7109e` (ADR + audit frais) + `1c7be06` (entrée CHANGELOG `[Unreleased]`, docs-only) ; **`origin/main == HEAD`, 0 ahead**. ⚠️ **CE N'EST PLUS UN `▶▶ NEXT UNIT`** — le marqueur avait pointé cette unité APRÈS sa livraison dans le même commit `bd7109e`, exactement le défaut de marqueur périmé que le haut du fichier dénonce ; corrigé le 2026-08-16 par le check factuel de démarrage. **Aucun `▶▶ NEXT UNIT` vivant ne subsiste : ce repo a tout livré** (10 manifests · audit re-vérifié propre · release `v0.13.0` · ADR-0008). Reliquat = l'entrée `[Unreleased]` docs-only (ADR-0008 + ADR-0009, release non urgente). ⤵ *Cadrage d'origine conservé pour son raisonnement :* ADR-0008 : « toute demande est routée ou refusée à la porte d'entrée ; rien ne s'exécute sur un brief non routé ou trop mince ». Arbitrage RÉEL verrouillé avec le PO le **2026-07-18** (brief-contract §1 « it never force-fits » + check fail-closed §4 ; router-draft §1 « LLM propose, code déterministe dispose, humain décide »), jamais consigné en ADR — la couche WF-000 (juillet) est postérieure aux ADR 0001-0007 (juin). Alternatives réelles à documenter : **(a)** workflow explicite obligatoire (l'opérateur nomme le WF, pas de routeur) ; **(b)** LLM choisit librement et exécute sans garde (force-fit). ⚠ Record honnête vérifié : c'est une décision, pas de l'implémentation habillée. ▫ **Aval indépendant, hors unité** : la passe vitrine de la trace P04 — la dernière entrée (v0.13.0) la donne déjà FAITE (vitrine `v0.13.0` + trace P04, prod-vérifiée 9/9) ; à re-confirmer côté vitrine, jamais ici.

  ✅ **ADR-0009 — LIVRÉ ET POUSSÉ le 2026-08-16 (commit `09e560e`, `origin/main == HEAD`).** Fichier : `docs/adr/0009-open-core-boundary.md` + entrée CHANGELOG `[Unreleased]`. Frontière open-core décidée EN SESSION (record forward-looking, pas rétrospectif) : restent PUBLICS sous PolyForm Noncommercial le catalogue, la méthode `src/dispatch/` (route-or-refuse), les spines-démo et les **traces** (funnel de preuve) ; un **control plane privé futur** (repo NEUF, pas une extraction) possède gouvernance/RBAC/permissions, metering coût, audit store persistant et un moteur éval durci issu de la graine `src/eval/`. Déclencheur de privatisation = un client payant **en BYO-key** faisant tourner `policy → approval → execution → evaluation → audit`. Alternatives rejetées consignées (privatiser le moteur maintenant ; MIT à vie ; SaaS complet d'emblée). ⚠️ **Fait corrigé en séance** : `src/eval/` n'est PAS une graine vide — les 10 spines déclarent déjà **~241 critères déterministes** (`check:`), la DoD de chaque workflow encodée en règles auditables. Ce qui manque = persistance, score composite, et **preuve de discrimination** des critères, pas les critères.

  ✅ **ÉTAPE ① — PROTOTYPE WF-001 LIVRÉ 2026-08-19 (commit `36933ed`, local, NON POUSSÉ ; entrée CHANGELOG `[Unreleased]` §Tests).** Fichiers : `test/spine-wf-001-discrimination.test.ts` + snapshot + fixture partagé `test/fixtures/wf-001-spine.ts` (sidecar intérim extrait, `spine-wf-001.test.ts` refactoré dessus). Baseline propre en conditions CI (clone du tag épinglé) : **425 passed / 24 skipped, 0 échec** ; les 10 rouges en local sont le seul drift catalogue (sibling sur `main` v4.4.0 vs pin v4.3.0), hors lot.
  - **Ce que la passe analytique a retourné, et c'est le vrai apport : la question n'est PAS « chaque check discrimine-t-il ? » (ils le font tous) mais « lequel est PORTEUR vs lequel double brick 1 ? ».** Ordre runtime lu à la source (`run-spine.ts`) : runner → eval gate (critères, output BRUT) → handoff (`validateHandoff` valide `producer.output`, donc le schéma tightened min/max/types). Un critère dont la clause est aussi dans le schéma est rattrapé UNE étape plus loin même retiré. **Mesuré par RETRAIT : 6/9 blocking sont PORTEURS (les checks de VIDE que JSON Schema ne sait pas exprimer), 3 sont REDONDANTS** — `po-backlog-8-15` + `po-epics-3-5` (schéma), `qa-gherkin-non-vide` (subsumé par `qa-given-when-then`). Aucune sous-clause morte (17 clauses rougissent). Pôle positif ancré sur la trace réelle commitée du 2-juillet (anti-circularité). Chaque garde falsifié dans les deux sens avant d'être cru.
  - ⚖️ **DÉCISION TRANCHÉE 2026-08-19 → ADR-0010 (`docs/adr/0010-schema-owns-structure-criteria-own-dod.md`), et le change-set WF-001 appliqué (commit ci-dessous).** Réponse : **ne rien élaguer** — l'eval-gate est un RELEVÉ D'AUDIT, pas qu'un enforcer ; « redondant pour l'enforcement » ≠ « redondant pour l'audit ». Principe : le schéma possède la STRUCTURE, les critères possèdent la SÉMANTIQUE de DoD + la trace de conformité. Appliqué à WF-001 : bornes de comptage en **source unique** (constantes partagées schéma↔critère), **un point de DoD par critère** (`qa-given-when-then` = bonne-formation seule, existence à `qa-gherkin-non-vide`), et **le seul vrai trou fermé** — `priorite !== undefined` → `nonEmptyString` (une `priorite: ""` passait les deux gates). Reste ouvert pour un lot ultérieur (détail ci-dessous, NON tranché ici).
  - ▫ **OUVERT — couverture DoD carte vs critères WF-001 (F4/F5), lot séparé, LOW, chaque point à trancher À LA CARTE (certains resteront volontairement non gardés).** Trouvés par lecture de la carte v1.2 à la source, tous défendables aujourd'hui — aucun verdict actuel n'est faux : **(F4a)** STEP-01 la carte demande les stakeholders « **with roles** », le critère ne garde que la non-vacuité de la liste (rôles non vérifiés) ; **(F4b)** STEP-03 « backlog **ordered** by value/risk » non gardé (difficile en déterministe) ; **(F4c)** STEP-04 « **for each selected US** » — couverture Gherkin par US sélectionnée non gardée ; **(F5)** `ba-questions-ouvertes-presentes` (advisory) passe sur un tableau **vide** — présence-en-tant-que-tableau seulement, jamais le contenu. Détail source : en-tête de `test/spine-wf-001-discrimination.test.ts` (F4/F5). ⛔ À traiter APRÈS avoir décliné le patron sur les 9 spines, ou en même temps que chacune (chaque carte a ses propres écarts).

  ✅ **WF-002 LIVRÉ 2026-08-20 (commit `f8ff94e`, local, NON POUSSÉ ; entrée CHANGELOG `[Unreleased]` §Tests).** Fichiers : `test/spine-wf-002-discrimination.test.ts` + snapshot + fixture partagée `test/fixtures/wf-002-spine.ts` (sidecar/resolver/mockRunner extraits, `spine-wf-002.test.ts` refactoré dessus). Baseline propre conditions CI (clone tag épinglé v4.3.0) : **449 passed / 24 skipped, 0 échec**, typecheck strict vert.
  - **La répartition schéma/critère RE-MESURÉE, et elle DIFFÈRE de WF-001 — ce qui prouve la consigne « mesurer par spine, ne jamais supposer ».** Mesuré par retrait : **4/10 blocking PORTEURS** (3 checks de vide que `type:string` n'exprime pas + le SEUIL `voteConfiance ≥3.5` absent du schéma), **6/10 REDONDANTS** avec le schéma d'output (bornes de comptage, types, présence de clés `required`, appliqués au handoff par ajv2020 strict). Hypothèse porteuse VÉRIFIÉE à la source par une passe analytique (demandée par Guy) : `validateHandoff` valide `producer.output` à CHAQUE étape, terminale incluse via `terminalSink` — la mesure a confirmé l'analyse, pas l'inverse. Pôle positif ancré sur la trace live WF-002 réelle. Chaque garde falsifié dans les deux sens (classe par retrait, rougeur par clause, garde de couverture).
  - ▫ **OUVERT — findings WF-002, lot séparé, LOW.** **F1** : `cp-dashboard-avancement-risques` teste `!== undefined` → `avancement:""`/`risques:[]` passent critère ET schéma (forme exacte du trou F3 de WF-001 fermé par ADR-0010) ; il est l'un des 6 redondants, flaggé à part pour ce trou. **F2** : les 5 autres critères schéma-redondants. **F3** : advisory quasi-vides (`rte-roam-risks-present`, `sm-impediments-listes` passent sur `[]`). Détail : en-tête de `test/spine-wf-002-discrimination.test.ts`. ⛔ Traiter avec F4/F5 de WF-001, à la carte.

  ✅ **WF-003 LIVRÉ 2026-08-22 (commit local, entrée CHANGELOG `[Unreleased]` §Tests).** Fichiers : `test/spine-wf-003-discrimination.test.ts` + snapshot + fixture partagée `test/fixtures/wf-003-spine.ts` (sidecar/resolver/mockRunner extraits, `spine-wf-003.test.ts` refactoré dessus ; `happyOutputs` inline gardé, même périmètre que WF-002). Baseline propre conditions CI (clone tag épinglé v4.3.0) : **477 passed / 24 skipped, 0 échec**, typecheck strict vert (+28 tests vs baseline WF-002 449).
  - **La répartition RE-MESURÉE par retrait BOUGE encore : 12 PORTEURS / 6 REDONDANTS sur 18 blocking** (WF-001 = 6/9, WF-002 = 4/10). Les 12 uniques = ce que JSON Schema ne sait pas dire : non-vacuité de string (7), valeur exacte (`fa-decision-go` = « Go », `sec-zero-critical` = 0), seuil numérique (`dev-coverage-min-80`, `qa-taux-reussite-90`, `sec-high-sous-2`). Les 6 redondants doublent une borne de tableau / un type appliqué au handoff. **Hypothèse de classe posée par confrontation critère↔schéma AVANT le run, puis mesurée par retrait — le run l'a confirmée, pas l'inverse.** Garde de classe falsifié dans les deux sens (bascule d'une classe ⟹ le test de retrait rougit `expected completed to be failed`). Pôle positif ancré sur la trace live réelle `wf-003-live-result.json` (`completed`, passe les 18).
  - ▫ **OUVERT — findings WF-003, lot séparé, LOW, confrontés à la carte v1.2 au pin v4.3.0.** **F1** content-blind (`sec-owasp-llm-10` compte 10 sans vérifier LLM01-LLM10 distincts ; advisory passent sur `[]`). **F2** 6 critères schéma-redondants. **F3** divergences DoD borne/sémantique : `dev-coverage-min-80` = `≥80` vs carte « >80 » strict ; `sec-high-sous-2` compte tous les High vs « non-residual » ; STEP-04 moitié « 0 Critical bug on nominal » du gate non gardée. **F4** trous de couverture DoD (few-shot/RAG prompts, integration plan, stack au-delà de `llm`, `.env.example`, typage Gherkin, docker-compose/monitoring, go-live checklist). Détail : en-tête de `test/spine-wf-003-discrimination.test.ts`. ⛔ Traiter avec F4/F5 de WF-001 et F1-F3 de WF-002, à la carte.

  ▫ **CORRECTION FACTUELLE 2026-08-22 : WF-001 (`36933ed`) et WF-002 (`f8ff94e`) sont POUSSÉS** (mesuré : `HEAD == origin/main`, 0/0) — les entrées ci-dessus qui les disent « local, NON POUSSÉ » étaient périmées (poussés dans un lot ultérieur non consigné). WF-003 reste en commit local ce jour, non poussé (push sur ordre de Guy).

  ✅ **WF-004 LIVRÉ 2026-08-22 (commit local, entrée CHANGELOG `[Unreleased]` §Tests).** Fichiers : `test/spine-wf-004-discrimination.test.ts` + snapshot + fixture partagée `test/fixtures/wf-004-spine.ts` (sidecar/resolver/mockRunner extraits, `spine-wf-004.test.ts` refactoré dessus ; catalogVersion v4.1.0 préservée). Baseline propre au pin v4.3.0 : **500 passed / 24 skipped, 0 échec**, typecheck strict vert (+23 vs WF-003 477).
  - **Répartition RE-MESURÉE par retrait : 6 PORTEURS / 9 REDONDANTS sur 15 blocking** (WF-001 = 6/9, WF-002 = 4/10, WF-003 = 12/18). 6 uniques = non-vacuité de string (5) + **plage numérique** (`consultant-maturity-score` 1-10). Garde de classe falsifié **dans les deux sens** (flip→unique rougit « removal must open a hole », flip→redundant rougit « removal must still fail »). Pôle positif ancré sur `wf-004-live-result.json` (`completed`, passe les 15).
  - ▫ **OUVERT — findings WF-004, lot séparé, LOW, carte v1.1 au pin v4.3.0.** **F1** advisories content-blind (`Array.isArray` passe sur `[]`). **F2** 9 critères schéma-redondants. **F3 (spécifique et notable)** : le design « plancher BLOCKING assoupli + advisory au chiffre exact » (en-tête de la spine) est **contredit par le schéma**, qui utilise `{ min: <idéal> }` (useCases 5, adkar 3, catalog 4, deck 10) et **impose donc l'idéal au handoff** — le plancher assoupli ne peut jamais se déclencher, et l'advisory-à-l'idéal est en fait hard-gated. Si l'intention « plancher souple » est réelle, le levier est le `min` du schéma, pas les critères. **F4** trous de couverture DoD (maturity per-dimension, sector benchmark, target data architecture, AI champions network, technical appendices). Détail : en-tête de `test/spine-wf-004-discrimination.test.ts`. ⛔ Traiter avec les findings WF-001/002/003, à la carte.

  ✅ **WF-005 LIVRÉ 2026-08-22 (commit local, entrée CHANGELOG `[Unreleased]` §Tests).** Fichiers : `test/spine-wf-005-discrimination.test.ts` + snapshot + fixture partagée `test/fixtures/wf-005-spine.ts` (sidecar/resolver/mockRunner extraits, `spine-wf-005.test.ts` refactoré dessus). Baseline propre au pin v4.3.0 : **515 passed / 24 skipped, 0 échec**, typecheck strict vert (+15 vs WF-004 500).
  - **Répartition RE-MESURÉE par retrait : 3 PORTEURS / 4 REDONDANTS sur 7 blocking** (WF-001 = 6/9, WF-002 = 4/10, WF-003 = 12/18, WF-004 = 6/15). Nouveauté : les 2 porteurs de contenu **par-item** (`everyItemHasStrings` sur title/impact et format/angle) que le schéma `type:string` ne peut pas exprimer (non-vacuité par item). Détail honnête : `growth-topics-floor` est redondant **via un sibling** (`growth-topics-actionable` rougit sur `[]`), pas via le schéma — la matrice le distingue. Garde de classe falsifié **dans les deux sens**. Pôle positif ancré sur `wf-005-live-result.json` (`completed`, passe les 7).
  - ▫ **OUVERT — findings WF-005, lot séparé, LOW, carte v1.1 au pin v4.3.0.** **F1** content-blindness partielle (les checks par-item vérifient la non-vacuité, pas le domaine — `impact` accepte n'importe quelle string, pas seulement High/Medium/Low). **F2** 4 floors schéma-redondants. **F3 (contraste POSITIF avec WF-004 F3, pas un défaut)** : l'auteur de cette spine a corrigé le problème WF-004 — le schéma épingle son `min` au PLANCHER assoupli (pas à l'idéal advisory) et **supprime `maxItems`**, donc un run modeste-mais-valide n'est pas hard-failed et l'idéal reste advisory-only. **F4** trous de couverture DoD (qualified engagement opportunities, hashtags/timing, internal note, quote of the week). Détail : en-tête de `test/spine-wf-005-discrimination.test.ts`. ⛔ Traiter avec les findings WF-001→004, à la carte.

  ✅ **WF-006 LIVRÉ 2026-08-22 (commit local, entrée CHANGELOG `[Unreleased]` §Tests).** Fichiers : `test/spine-wf-006-discrimination.test.ts` + snapshot + fixture partagée `test/fixtures/wf-006-spine.ts`. Baseline propre au pin v4.3.0 : **541 passed / 24 skipped, 0 échec**, typecheck strict vert (+26 vs WF-005 515).
  - **Répartition RE-MESURÉE par retrait : 8 PORTEURS / 8 REDONDANTS sur 16 blocking** (série : 6/9, 4/10, 12/18, 6/15, 3/7). Porteurs notables : le **gateway GO/NO-GO** `presales-go` (value-equality — retirer le critère laisse un « NO-GO » aller à `completed`, trou réel de no-bid), la **non-vacuité par-clé** du BANT `presales-bant`, la **plage** `presales-win-probability` 0-100. Garde de classe falsifié **dans les deux sens**. Pôle positif ancré sur `wf-006-live-result.json` (`completed`, passe les 16).
  - ▫ **OUVERT — findings WF-006, lot séparé, LOW, carte v1.0 au pin v4.3.0.** **F1** content-blindness (`red-proposal` accepte n'importe quelle string non vide, pas 20-40 pages). **F2** 8 critères schéma-redondants. **F3** le défaut WF-004 (plancher assoupli battu par le schéma) **RÉCIDIVE ici** (WF-005 l'avait corrigé, WF-006 v1.0 non) : `fin-scenarios` est un floor non-vide mais le schéma épingle `commercialScenarios` à `min 3`. **F4** trous de couverture DoD. Détail : en-tête de `test/spine-wf-006-discrimination.test.ts`. ⛔ Traiter avec les findings WF-001→005, à la carte.

  ✅ **WF-007 LIVRÉ 2026-08-22 (commit local, entrée CHANGELOG `[Unreleased]` §Tests).** Fichiers : `test/spine-wf-007-discrimination.test.ts` + snapshot + fixture partagée `test/fixtures/wf-007-spine.ts`. Baseline propre au pin v4.3.0 : **557 passed / 24 skipped, 0 échec**, typecheck strict vert (+16 vs WF-006 541).
  - **Répartition RE-MESURÉE par retrait : 5 PORTEURS / 5 REDONDANTS sur 10 blocking** (série : 6/9, 4/10, 12/18, 6/15, 3/7, 8/16). Les 5 uniques = non-vacuités de string (`ba-client-context` + les 4 livrables STEP-05) ; les 5 redondants = floors de tableau rattrapés au handoff. Garde de classe falsifié **dans les deux sens**. Pôle positif ancré sur `wf-007-live-result.json` (`completed`, passe les 10).
  - ▫ **OUVERT — findings WF-007, lot séparé, LOW, carte v1.0 au pin v4.3.0.** **F1** content-blindness. **F2** 5 critères schéma-redondants. **F3 POSITIF** : cette spine applique la parade WF-005 (schéma min = plancher, pas de maxItems) → **ne reproduit PAS** le défaut F3 de WF-004/006. **F4** trous de couverture DoD. Détail : en-tête de `test/spine-wf-007-discrimination.test.ts`. ⛔ Traiter avec les findings WF-001→006, à la carte.

  ✅ **WF-008 LIVRÉ 2026-08-22 (commit local, entrée CHANGELOG `[Unreleased]` §Tests) — LE PLUS GROS, avec blindage analytique CORRECTIF avant livraison (demandé par Guy).** Fichiers : `test/spine-wf-008-discrimination.test.ts` + snapshot + fixture partagée `test/fixtures/wf-008-spine.ts`. Baseline propre au pin v4.3.0 : **587 passed / 24 skipped, 0 échec**, typecheck strict vert (+30 vs WF-007 557).
  - **Répartition RE-MESURÉE par retrait : 14 PORTEURS / 8 REDONDANTS sur 22 blocking** (série : 6/9, 4/10, 12/18, 6/15, 3/7, 8/16, 5/10). **DEUX gateways value-equality** mesurés UNIQUE (trous réels) : `jur-tier-not-unacceptable` (retirer → un « Unacceptable » va jusqu'à `completed`) et `audit-verdict-cleared` (retirer → un audit « returned » atteint le rapport final). Garde de classe falsifié **dans les deux sens** (dont un gateway).
  - **⚠️ CORRECTIF appliqué par le blindage** : la trace live WF-008 est `status:"failed"` (halt fail-closed au STEP-06C sur un vrai verdict « returned »), **PAS `completed`** — le pôle positif du patron aurait asserté `completed` à tort. Réécrit : STEP-01→06 = pôle positif réel, `audit-verdict-cleared` échouant sur l'output réel STEP-06C = **pôle NÉGATIF réel** (anti-circularité renforcée), STEP-07 (jamais lancé live) couvert par la fixture happy. **Leçon transverse : ne jamais copier le bloc pôle-positif sans vérifier `status` de la trace — WF-009 a aussi une variante `gate-halt`.**
  - ▫ **OUVERT — findings WF-008, lot séparé, LOW, carte v1.0 au pin v4.3.0.** **F1** content-blind (`sec-owasp-llm-10` compte 10 sans vérifier LLM01-LLM10). **F2** 8 schéma-redondants. **F3** pas de défaut plancher ici (comptes durs DoD, schéma min = critère). **F4** trous de couverture DoD. Détail : en-tête de `test/spine-wf-008-discrimination.test.ts`. ⛔ Traiter avec WF-001→007, à la carte.

  ✅ **WF-009 LIVRÉ 2026-08-22 (commit local, entrée CHANGELOG `[Unreleased]` §Tests) — avec blindage analytique CORRECTIF avant livraison (demandé par Guy).** Fichiers : `test/spine-wf-009-discrimination.test.ts` + snapshot + fixture partagée `test/fixtures/wf-009-spine.ts`. Baseline propre au pin v4.3.0 : **612 passed / 24 skipped, 0 échec**, typecheck strict vert (+25 vs WF-008 587).
  - **Répartition RE-MESURÉE par retrait : 9 PORTEURS / 7 REDONDANTS sur 16 blocking** (série : 6/9, 4/10, 12/18, 6/15, 3/7, 8/16, 5/10, 14/22). **DEUX gateways DURCIS** mesurés UNIQUE : `rh-shortlist-validated` (`countAffirmativeField>=3` — 3 placeholders passent le schéma min 3 mais échouent le gate) et `sel-candidate-selected` (`affirmativeString` — rejette « None — no candidate… »). Garde de classe falsifié **dans les deux sens** (dont le gateway durci).
  - **CORRECTIF / double ancrage réel** : WF-009 a DEUX traces live — `wf-009-live-result.json` (completed → pôle positif) ET `wf-009-live-result-gate-halt.json` (failed au STEP-04 sur `rh-shortlist-validated` → **pôle NÉGATIF réel**). Les deux ensemble prouvent que le gate DISCRIMINE. Le blindage a vérifié la sémantique de `affirmativeString`/`countAffirmativeField` à la source AVANT de fabriquer les witnesses (sinon witnesses non fiables).
  - ▫ **OUVERT — findings WF-009, lot séparé, LOW, carte v1.0 au pin v4.3.0.** **F1** content-blindness (sauf les 2 gates, qui SONT le durcissement). **F2** 7 schéma-redondants. **F3** le défaut plancher WF-004/006 **RÉCIDIVE sur STEP-02A** (`assessmentGrid` schéma min 6, `interviewQuestions` min 10 = les idéaux advisory, donc les floors ≥3 ne se déclenchent jamais). **F4** trous de couverture DoD. Détail : en-tête de `test/spine-wf-009-discrimination.test.ts`. ⛔ Traiter avec WF-001→008, à la carte.

  ✅ **WF-010 LIVRÉ 2026-08-22 (commit local, entrée CHANGELOG `[Unreleased]` §Tests) — DERNIÈRE spine, blindage analytique systématique avant livraison. ÉTAPE ① COMPLÈTE (10/10).** Fichiers : `test/spine-wf-010-discrimination.test.ts` + snapshot + fixture partagée `test/fixtures/wf-010-spine.ts`. Baseline propre au pin v4.3.0 : **628 passed / 24 skipped, 0 échec**, typecheck strict vert (+16 vs WF-009 612).
  - **Répartition RE-MESURÉE par retrait : 5 PORTEURS / 5 REDONDANTS sur 10 blocking** (spine plate, aucun gateway). 5 uniques = non-vacuités de string ; 5 redondants = floors de tableau (schéma min 1). Garde de classe falsifié **dans les deux sens**. Pôle positif sur `wf-010-live-result.json` (`completed`, ses 4 traces backbone couvrent les 10 blocking).
  - ▫ **OUVERT — findings WF-010, lot séparé, LOW.** **F1** content-blindness. **F2** 5 schéma-redondants. **F3 POSITIF** : parade WF-005 appliquée → ne reproduit pas le défaut F3. **F4** trous de couverture DoD. Détail : en-tête de `test/spine-wf-010-discrimination.test.ts`. ⛔ Traiter avec WF-001→009, à la carte.

  ═══════════════════════════════════════════════════════════════════════════════
  ✅✅ **ÉTAPE ① COMPLÈTE — les 10 spines portent un audit de discrimination éval** (WF-001→010, l'équivalent éval des sondes dispatch). Série de la répartition PORTEURS/REDONDANTS, mesurée par retrait et JAMAIS supposée, qui a bougé à chaque spine : **uniques = 6·4·12·6·3·8·5·14·9·5**. Instruments : `test/spine-wf-0NN-discrimination.test.ts` (×10) + snapshots + fixtures partagées `test/fixtures/wf-0NN-spine.ts` (×10). Suite **628/24/0** au pin, typecheck vert.

  ✅ **RELEASE v0.14.0 FAITE 2026-08-22 (piste (b)) — tag annoté + GitHub Release `Latest`, non-draft/non-prerelease, CI verte sur le commit de release `d6a430d`.** Titre : « The eval gates are measured spine by spine: load-bearing criteria told from schema echoes, and one empty-value hole closed ». Contenu : les 10 audits de discrimination + ADR-0008/0009/0010 + le trou WF-001 empty-priority fermé. `package.json`/`package-lock.json` bumpés 0.13.0→0.14.0, `[Unreleased]` remis à vide. **Tout est poussé sur `origin/main`, 0 ahead.** ▫ Aval (hors ce repo) : la vitrine pourra surfacer `v0.14.0` — porté par CE repo, [[feedback-tache-amont-porte-maj-aval]], à faire côté `guyhui-showcase`.

  ⛔ **AUCUN `▶▶ NEXT UNIT` VIVANT AUTO-DÉSIGNÉ NE SUBSISTE.** Restent deux pistes ÉLIGIBLES, N'ÉTANT PAS des ordres — **décision de Guy requise** (ne pas inventer d'ordre) :
  - **(a) Lot findings « à la carte »** (F1-F4 accumulés sur WF-001→010) : corriger les défauts réels consignés — notamment **F3 (plancher assoupli battu par le schéma) sur WF-004, WF-006, WF-009** (STEP-02A), et **F3(a) WF-003** (`dev-coverage-min-80` = `≥80` vs carte « >80 » strict). Chaque point à trancher À LA CARTE, certains resteront volontairement non gardés. C'est le SEUL lot qui TOUCHE `src/` (élargir des critères) — hors périmètre de l'audit qui ne corrigeait rien. Les en-têtes des 10 `test/spine-wf-0NN-discrimination.test.ts` portent le détail des findings.
  - **(c) Re-pin catalogue v4.3.0 → v4.4.0** (freshness debt, décision `src/` distincte ; byte-diff des cartes + suite verte au nouveau pin avant de couper).
  ═══════════════════════════════════════════════════════════════════════════════

  ▫ **[HISTORIQUE — cadrage d'origine de l'ÉTAPE ①, conservé pour son raisonnement, LIVRÉ le 2026-08-19] — AUDIT DE DISCRIMINATION DES CRITÈRES ÉVAL, prototype WF-001.** L'équivalent éval des sondes dispatch (cross-vocabulary, denial-probe) : **mesurer si chaque `check:` rougit quand il le doit**, jamais le supposer — un critère qui ne peut pas échouer est un `/.+/` présent-inutile. Livrable : pour chaque critère `blocking`, une sortie « bonne » (passe) + une « mauvaise » (rougit), **FALSIFIÉE avant d'être crue** ([[feedback-un-vert-de-falsification-nest-pas-un-permis]]). Périmètre du prototype : **WF-001 (12 critères) d'abord** — lire sa DoD À LA SOURCE (carte catalogue), falsifier chaque `blocking`, le lot devient le patron des neuf autres (même méthode que WF-002 pour les manifests). Chantier **PUBLIC, déterministe, zéro run facturé** (aligné ADR-0007 : jamais LLM-juge-LLM ; le juge LLM reste l'extension gardée de dernier recours, cf. `src/eval/types.ts`). ⛔ Ne PAS élargir un critère dans le même geste que construire l'instrument (le travers qui a échoué 2× le 2026-08-02 côté dispatch). ▫ **À re-mesurer à froid, jamais recopié d'ici** : le décompte 12/15/26/26/14/30/18/30/29/21 (≈241) a été relevé le 2026-08-16 par `grep -c 'check:' src/spines/*.ts` — le re-compter à la source au démarrage.
  - **Prompt de reprise (paste-ready, session fraîche)** : `Resume — runtime /Users/guyhui/CLAUDE/claude-agentic-runtime (guyhui01/claude-agentic-runtime). Applique prompt_demarrage.md + check factuel (git status -sb ; gh pr list ; dependabot=0 ; npm audit ; CI@HEAD=success ; drift tag catalogue vs pin). Puis TROUVE le seul ▶▶ NEXT UNIT vivant dans next_steps.md = ÉTAPE ① SUITE (décliner sur WF-003…010, 8 spines restantes ; WF-001 livré 2026-08-19 commit 36933ed, WF-002 livré 2026-08-20 commit f8ff94e). Modèle le plus frais : test/spine-wf-002-discrimination.test.ts (table AUDIT + 2 axes + garde de couverture + fixture partagée extraite). Re-mesure les critères par spine à la source (grep -c 'check:' src/spines/wf-*-*.ts) ET relis la DoD de chaque carte au tag épinglé AVANT de dimensionner ; la répartition schéma/critère se MESURE par spine (retrait), jamais ne se suppose. Falsifie chaque garde dans les deux sens. Applique ADR-0010 (schéma=structure / critères=sémantique DoD + audit ; source unique des bornes ; un point de DoD par critère).`

  ▫ **[HISTORIQUE — FERMÉ] L'ancien marqueur : LA DETTE DES 90 FENÊTRES D'ADJACENCE `[^.]{0,N}` QUI AUTORISENT LE `;`** (voir la puce « Dette OUVERTE, délibérément non repliée » plus haut, et le rappel « Reste runtime » plus bas : coût **non mesuré**, lot séparé, ne pas l'ouvrir au fil de l'eau). ⛔ **Le mesurer AVANT de le chiffrer** : n'importe laquelle de ces fenêtres peut apparier deux clauses, ce qui a été prouvé sur WF-009 `role_level` et sur aucune autre. ⛔ Ce marqueur n'est pas un ordre de démarrage : la priorité inter-chantiers est à re-trancher par Guy (voir le ⏸ en tête, dont le motif d'origine est éteint).
  - ⚠️ **DIMENSIONNEMENT VÉRIFIÉ À LA SOURCE LE 2026-08-05, et il corrige une lecture trop rapide : le harnais de la BOUCLE n'existe que pour P01/WF-001.** `test/dispatch-pilot-p01-live.test.ts` fait exactement ce qu'on veut (phase 1 : brief non amendé ⟹ `PARAMS_MISSING` live ; phase 2 : brief amendé ⟹ `ROUTED` + plan verbatim ; **stop avant le go/no-go humain par construction**) — mais **sur P01 seulement**. `test/wf-004-run-live.test.ts` existe aussi et **n'est PAS ce qu'il faut** : c'est le run d'une **spine**, pas une boucle de dispatch. ⟹ l'unité consiste à **décliner le patron P01 sur un second brief**, pas à partir de zéro ni à réutiliser tel quel.
  - **Gate d'exécution** : `LIVE_RUN=1 LIVE_MODEL=<id complet> npx vitest run <fichier>` ; sans `LIVE_RUN`, la suite normale saute ces fichiers (c'est ce que montrent les 3 skips « billed, observed »).
  - **Ce que le run prouve exactement** : que le mécanisme **généralise au-delà du pilote** — la trace de 2026-07-19 montre `team_size`, un identifiant snake_case, alors que le refus porte désormais les **libellés de carte**. **PAS** « les dix prouvés live » : c'est **un seul chemin de code**, pas dix. Ne pas laisser la formule enfler à l'écriture.
  - **Choix du brief** : un workflow **≠ WF-001**, de préférence un dont le refus nomme un libellé parlant (p. ex. WF-004 `Engagement duration`). À trancher sur les fixtures existantes plutôt qu'en écrivant un brief neuf.
  - ▫ **Aval** : la mise à jour de la vitrine est portée par CE repo ([[feedback-tache-amont-porte-maj-aval]]) — donc **une seule** passe vitrine ensuite, qui surface `v0.11.0` + les sondes de seeds + la trace.
  - ▫ **[HISTORIQUE] Les deux voies telles qu'elles étaient posées avant l'arbitrage :**
  - **Voie A — le run live d'abord** (cadrage du 2026-08-03, étape (3), inchangé) : **UNE** boucle de dispatch sur un workflow **≠ WF-001**, pour que `PARAMS_MISSING` porte un libellé de carte que la trace de 2026-07-19 ne montre pas. Ce qu'il prouve exactement : que le mécanisme **généralise au-delà du pilote** — **PAS** « les dix prouvés live ». Puis **une seule** passe vitrine qui surface la version ET la trace.
  - **Voie B — la vitrine directement** : elle reste honnête sans le run (son drill-down dit noir sur blanc « *proven by the registry and its test suite, not by ten billed runs* »). Le run live est une **montée en gamme de la preuve, pas la correction d'un défaut** — ne pas se raconter une urgence.
  - ⚠️ **Le coût de l'ordre inverse est le seul argument dur** : ouvrir la vitrine avant le run live impose **deux** passes de surfaçage au lieu d'une.
  - ▫ **Ce que la vitrine devra surfacer dans tous les cas** : `v0.10.0` → `v0.11.0` (le site affiche la version courante), et le garde de démenti côté preuve. ⚠️ Appliquer la méthode éprouvée du showcase : **TRIER version-courante vs fait-daté, ne jamais `sed`** — et `v0.11.0` n'est **pas** docs-only, donc la question « faut-il une phrase *`v0.11.0` adds…* ? » se re-pose.
  - ▫ **Reste runtime, hors chemin critique** : la dette des **90 fenêtres d'adjacence en `[^.]{0,N}`** qui autorisent le `;` (coût non mesuré, lot séparé — ne pas l'ouvrir au fil de l'eau). Elle redevient l'unité suivante, la classe de démenti étant fermée, et elle est mécanique : ordre [[feedback-release-tag-et-github-release]] (CHANGELOG → bump → commit → tag → push → `gh release`), `[Unreleased]` désormais rédigé et couvrant aussi les deux commits de sondes de seeds qui n'y figuraient pas. ⚠️ **Re-vérifier tout statut à la source avant de couper.**

  ✅ **DEPENDABOT TRIÉ ET REFERMÉ 2026-08-05, avant la release — 7 alertes ouvertes → 1, 4 PRs mergées, 0 PR ouverte.** Le triage que ce tracker réclamait depuis le 2026-07-27 (« personne n'a mappé les PRs sur les alertes ») est fait, et c'est la **joignabilité** qui a commandé l'ordre, pas la sévérité affichée :
  - **`src/` n'instancie AUCUN serveur HTTP ni MCP** (grep vérifié : les seuls hits sont des mots dans des commentaires). Les cinq alertes `hono` / `ip-address` / `@hono/node-server` portaient donc sur du code **jamais chargé**. Le SEUL réellement sur le chemin d'exécution était **`fast-uri` ← `ajv`**, dépendance directe qui compile le schéma du routeur.
  - **Mergées** : `#51` postcss (dev) · `#52` fast-uri 3.1.5 (ferme la high joignable) · `#49` ip-address 10.4.0 (ferme 3 alertes) · `#50` hono 4.13.0. ⚠️ **`#49` et `#50` étaient en retard de 4 commits** : leur vert avait été obtenu sur une base périmée ⟹ `@dependabot rebase` puis attente de la CI avant chaque merge ([[feedback-merge-branche-a-jour-et-validee]]). Vérifié après coup en local : `npm ci` + suite **398/23** + typecheck verts.
  - ⛔ **`#8 @hono/node-server` RESTE OUVERTE, à dessein et désormais MESURÉE** (elle ne l'était pas). Correctif = `2.0.5`, saut **majeur** depuis la `1.19.15` qu'épingle `@modelcontextprotocol/sdk` — `npm audit fix` le confirme : la seule voie passerait par le SDK MCP lui-même. **Ne pas forcer d'`overrides`** : épingler un transitif majeur sous un parent qui ne l'a pas testé casserait le SDK pour un vecteur non joignable et **Windows-only** (`serve-static`). Attendu du prompt de reprise = **1**, on y est.
  - ▫ **Les runs « Dependabot Updates » en échec n'étaient PAS un défaut du repo** : `pull_request_exists_for_latest_version`, une tentative de recréer une PR déjà ouverte. Le merge les a fait disparaître. Ne pas repartir en chasse.

  ⚠️ **Ce marqueur disait « LA RELEASE » et c'était FAUX** — écrit le 2026-08-02 quelques heures avant que la classe ci-dessous ne soit trouvée, il est resté à l'air courant. C'est le défaut contre lequel le haut de ce fichier met en garde depuis des semaines, commis par moi dans la même session. ⛔ **La release NE PEUT PAS être l'unité suivante** : elle figerait un `[Unreleased]` annonçant un audit clos alors qu'une classe trouvée **par la méthode de cet audit** est ouverte. Elle redevient l'unité suivante une fois celle-ci fermée, et elle est mécanique à ce moment-là (ordre [[feedback-release-tag-et-github-release]] : CHANGELOG → bump → commit → tag → push → `gh release` ; `[Unreleased]` accumulé depuis `v0.9.1`, à rédiger depuis `git log` et jamais depuis ce tracker).

  **LE FAIT, mesuré le 2026-08-02, jamais corrigé** : `paramFilled` n'a aucune notion générale qu'un **démenti n'est pas une réponse**. Sur 12 phrases niant leur propre sujet tout en nommant une valeur de carte à proximité, **10 REMPLISSENT**, sur 8 manifests différents — « **No** cloud provider has been chosen; **AWS** was ruled out » (WF-003) · « the client has **no** AI maturity assessment; **beginner** is a guess » (WF-004) · « **No** selection criteria on **price** or **expertise** were shared » (WF-006) · « the system is **not** in **production** and **no pilot** is planned » (WF-008) · « this is **not a partial failure** and **not an incident** » (WF-010). Direction d'échec **UNSAFE** : un faux `FILL` rend `paramsChecked:true` sur un brief qui nie le fait.
  - **Les 5 gardes de négation existantes sont des lookbehinds à UN token** (`(?<!\bno )`, `(?<!\bwithout )`, `(?<!\bnot a )`) : elles n'attrapent que l'adjacence immédiate, jamais « the role is **not yet defined**; **senior** hires are paused ».
  - ⚠️ **LATENT, PAS ACTIF — et c'est ce qui borne l'urgence sans l'annuler** : 7 des 20 briefs portent un token de démenti, tous en position bénigne, les 3 `NO_MATCH` ne touchant aucun manifest. **Aucun verdict actuel n'est faux.** Encore le corpus qui ne sait pas exprimer le défaut ([[feedback-corpus-angle-mort-partage]]).
  - ⛔ **NE PAS retrofiter spec par spec.** Deux hypothèses de cause racine ont été posées puis **falsifiées en minutes** : (i) « l'ancre est le nom du propre libellé » → **48 specs sur 108** partagent cette forme, c'est la norme et non le défaut ; (ii) « les valeurs en noms propres sont à l'abri » → `cloud_provider` remplit. Ne pas re-poser ces deux-là.
  - **Le contrôle qui MANQUAIT à la proposition d'origine, et sans lequel l'unité est ingérable : un TROISIÈME corpus de sur-portée** — des phrases où un token de démenti est présent **sans nier la valeur** (« GDPR applies, **no** exceptions; personal data is processed »), qui doivent continuer à remplir. Les deux autres corpus sont les 12 démentis (à refuser) et les 19 briefs (aucun verdict ne bouge). Une proposition qui nomme le risque sans nommer la mesure qui le retire est un mode dégradé ([[feedback-pas-de-mode-degrade]]).
  - ✅ **INSTRUMENT LIVRÉ 2026-08-02 — `test/dispatch-denial-probe.test.ts` + snapshot `denial-probe.md`. Il ne corrige RIEN, à dessein** (précédent « unité zéro » : l'outil avant le lot qui s'en sert). La définition de portée est écrite **dans l'en-tête du fichier**, avant tout détecteur : *un démenti gouverne une valeur quand les deux sont dans la MÊME CLAUSE et que le démenti précède*. Le garde devra être vérifié **contre ce texte**, jamais contre un compte plausible. Les trois corpus sont figés en snapshot (pas en test rouge : un rouge permanent masque les vraies régressions) ⟹ à la livraison du garde, le diff montre les 10 remplissages se fermer et le corpus de sur-portée rester ouvert. ⚠️ **`affirmativeString`/`NEGATIVE_SENTINEL` NE SONT PAS réutilisables** — ancrés sur `^`, ils classent une valeur de champ entière, pas de la prose ; je l'avais supposé en proposant le travail, c'est faux.
    - ⚠️ **Le corpus de sur-portée a trouvé un défaut dès sa première lecture** : WF-005 `audience` refuse « the audience is our LinkedIn following » (ordre des mots) tout en **acceptant** « there is no LinkedIn audience » — ses deux verdicts sont inversés, et la cause n'a rien à voir avec les démentis. **Non corrigé** (élargir un détecteur dans le même geste que construire l'instrument est la motion qui a échoué deux fois le 2026-08-02) ⟹ le contrôle mesure **7 lignes, pas 8**, tant que ce détecteur n'est pas réparé. À traiter dans l'unité du garde.
    - ⛔ Une assertion de taille du registre a été écrite puis **retirée, falsification à l'appui** : ce fichier résout les specs par nom au lieu d'itérer le registre, et mésinscrire WF-003 rougit le test de résolvabilité pendant qu'un `length >= 10` reste vert.
  - **Méthode imposée** : écrire la **DÉFINITION de la portée AVANT le détecteur**, puis vérifier l'implémentation contre elle — jamais contre le compte qui paraît plausible (leçon du marqueur `quantity-vocabulary` à 25 % de sensibilité). La primitive existe déjà (`NEGATIVE_SENTINEL`) et la discipline d'adjacence aussi (`[^.]{0,N}`) : le garde se définit sur la **région appariée**, pas sur la phrase — sinon il devient sémantique, et le régler par regex est hors de portée. Falsifier dans les deux sens.

  ▫ **Reste optionnel, jamais promis** : le 5ᵉ volet d'audit — sortir de la circularité du corpus en confrontant les dix manifests aux **seeds des harnais live** (écrits pour les spines, prouvés par des runs facturés). À chiffrer avant de l'annoncer.
    - ✅ **FAIT depuis, et NON RELEASÉ** — commits `f42b8ab` + `27f85c8`, postérieurs au tag `v0.10.0`. Écartés de l'affichage vitrine le 2026-08-03 pour cette seule raison : **hors périmètre d'une version publiée**. Ils portent le résultat le plus inconfortable et le plus vendeur du chantier : **41 specs sur 78 se remplissent**, et le contrôle est **« nearly POWERLESS on WF-001/002/003 »** — dit par le lot lui-même.

  ▫ **[CADRAGE CONSIGNÉ 2026-08-03, question de Guy : « devons-nous tester la mécanique du manifeste en run live ? » — RÉPONSE : NON pour le mécanisme, OUI pour la boucle, UNE seule fois. ⛔ Ce n'est PAS le `▶▶ NEXT UNIT` ; il vient APRÈS la classe de démenti.]**
    - **Fait qui tranche, vérifié à la source le 2026-08-03** : le check de paramètres est **déterministe, zéro LLM** (`grep -n "query\|runner\|llm" src/dispatch/validate-route.ts` → **vide**). Les mêmes entrées donnent les mêmes sorties hors ligne ⟹ **un run facturé n'apprend RIEN sur le mécanisme**. Ce qu'il testerait, c'est la **boucle** autour : routeur LLM → refus → amendement humain → spine.
    - **Ce qui manque au mécanisme n'est pas la vivacité, c'est une ENTRÉE INDÉPENDANTE** ([[feedback-controle-positif-circulaire]]). Un run live n'y change rien : il exécute le même code sur un brief que j'écris encore. **Les seeds des harnais live cassent la circularité, pas un run de plus** — ils sont antérieurs au check et écrits pour un autre usage.
    - **⟹ Geste le plus fort = GRATUIT et DÉJÀ ÉCRIT : releaser les sondes de seeds** (voir la puce ci-dessus). La vitrine passerait de « prouvé par le registre et ses tests » à « corroboré par une source qui ne sait rien des politiques qu'elle confirme ».
    - **Le SEUL run live justifié, et son périmètre exact** : **une** boucle de dispatch sur un workflow **≠ WF-001**. Raison : la vitrine affirme que le refus nomme désormais les **libellés de carte**, mais sa seule trace live (2026-07-19) montre `team_size` — la prose doit *expliquer* le contraste. Un run sur, p. ex., WF-004 produirait une trace où `PARAMS_MISSING` porte `Engagement duration` : **une pièce au lieu d'une explication**. Changement de CLASSE de preuve, pas de volume.
    - **⚠ Ce qu'un tel run prouve, exactement** : que le mécanisme **généralise au-delà du pilote**. **PAS** « les dix prouvés live ». ⛔ Ne pas laisser la formule enfler à l'écriture — dix runs pour dix manifests serait disproportionné : c'est **un seul chemin de code**, pas dix.
    - **ORDRE IMPOSÉ** : (1) fermer d'abord **la classe de démenti** (`▶▶ NEXT UNIT` ci-dessus) — un run facturé sur une version dont on connaît déjà un défaut de détecteur grave une **preuve périmée** ; (2) releaser les sondes de seeds ; (3) **puis** le run live, **porte facturée, sur accord explicite de Guy**.
    - **Contre-argument posé d'emblée, à ne pas oublier** : la vitrine est **honnête aujourd'hui sans ce run** — son drill-down dit noir sur blanc « *proven by the registry and its test suite, **not** by ten billed runs* ». Ce n'est **pas un défaut à corriger**, c'est une montée en gamme de la preuve. Ne pas se raconter une urgence.
    - **Aval** : la mise à jour de la vitrine est portée par CE repo, celui qui produit la preuve ([[feedback-tache-amont-porte-maj-aval]]) — pas par une session showcase qui irait la chercher.

  ▫ **[HISTORIQUE] Cadrage d'origine de l'audit — conservé, il a tenu.** ⤵ **NEXT UNIT — L'AUDIT DE FIN DE CHANTIER, en unité dédiée et à froid** (son cadrage complet est plus haut, section 🔍). Ce qui reste après ce lot, et rien d'autre : **volet 1** (sonde croisée, 2ᵉ passage — la section WF-010 vient d'être ajoutée et lue en diff, mais la RELECTURE COMPLÈTE des dix sections n'est pas faite ; re-compter les specs à ce moment-là, ne pas reprendre un chiffre écrit ici) · **volet 3** (confrontation §2 **à la ligne** sur les dix, l'agrégation étant mesurée totale à 85/85 avant ce lot — à re-mesurer à 97) · **volet 4** (re-dérivation du chiffrage à la carte, partiellement couvert par la jointure). Volets 2 et 5 clos. ⚠️ Deux dettes ouvertes par CE lot, à traiter dans le volet 3 et pas au fil de l'eau : (a) `team_size` de WF-001 (`squads`) et de WF-010 (`team of N`) lisent le même fait de carte avec des vocabulaires **disjoints** — trou de couverture des deux côtés, direction sûre, aucun verdict déplacé, mais nommé ; (b) trois specs de format (`target_format` WF-005, `proposal_format` WF-006, `expected_format` WF-010) et trois specs d'enjeux (`priority_stakes`, `identified_stakes`, `client_stakes`) que la table de politique **n'apparie pas** faute de préfixe commun — elle ne peut pas voir une divergence qu'elle ne peut pas apparier, et c'est écrit dans son propre fichier. **(c) NOUVEAU, trouvé à la passe pré-push du 2026-08-01 : les moitiés de conjonction ne sont jamais servies par la règle de libellé** (elle matche le `card` qualifié de la spec), donc sur le bloc quick-start que la carte enseigne, une moitié répondue peut être rendue manquante — mesuré 2/5 sur WF-010. **SIX manifests portent au moins une conjonction** ; la question est de politique (faut-il que « Team involved: 9 people, distributed, hybrid » réponde aux trois ?) et se tranche UNE fois, jamais au fil de l'eau — précédent exact des 22 valeurs de carte du 2026-07-31.

  ▫ **[HISTORIQUE] Cadrage d'origine — WF-009 (10 lignes, conjonction `Role sought [Title / Level]`), puis WF-010 (9, conjonctions `Project / Incident [Name + dates]` et `Team involved [Size / Distribution / Remote]`).** **Huit des dix manifests existent.** RULE 3 (Unit A = table AVEC la colonne valeurs-de-carte verbatim, Unit B = exécution) + **blindage analytique entre A et B** : sur WF-007 il a trouvé deux défauts shippés dans WF-004, sur WF-008 la mesure préalable en a trouvé quatre. `npm run measure -- WF-009` dès le manifest registré, AVANT tout test. Rappel : WF-009 porte un `defaultValue` déclaré par la carte (`Nice-to-have skills`) et une inconnue sanctionnée (`Salary / day rate — to be defined`).

  ▫ **[HISTORIQUE] Cadrage WF-008 d'origine — (11 lignes, la plus grosse, 3 conjonctions : `Client [… / Geographic footprint]`, `Data processed [Personal / Sensitive / Art. 9]`, `Volumes [# individuals / training volume]`), puis WF-009 / WF-010.** **Sept des dix manifests existent.** Appliquer RULE 3 (Unit A = table, Unit B = exécution) ET demander le blindage analytique entre les deux : sur WF-007 il a trouvé deux défauts shippés dans WF-004 que la table seule n'aurait jamais vus. `npm run measure -- WF-008` dès le manifest registré, AVANT d'écrire un test. Appliquer RULE 3 (Unit A = table, Unit B = exécution) : elle a produit deux corrections réelles sur WF-004 avant qu'une ligne de code n'existe, et sur WF-006 elle a fait tomber trois erreurs de ma table AVANT le code (un consommateur d'étape inventé, une erreur de compte, une convention de libellé fausse).
  - ⚖️ **DÉCISION CLOSE 2026-07-30 : NE PAS factoriser `client/prospect_name|sector|size`. Ne pas rouvrir à WF-007/008/009/010.** Mon critère initial (« factoriser si les trois textes ont convergé à l'identique ») était mauvais : l'identité textuelle ne mesure pas ce que la factorisation achète. L'argument qui tranche est autre — **factoriser `name` aurait propagé l'introducteur `Prospect` de WF-006 dans WF-004, c'est-à-dire modifié un comportement livré et verrouillé par test EN EFFET DE BORD d'un refactor**, ce qu'on refuse explicitement par ailleurs. Et le seul qui avait convergé (`sector`) est le spec le moins porteur du lot (non-discriminant, documenté), donc celui où factoriser rapporte le moins. **La réponse au risque de duplication n'est pas de factoriser, c'est de détecter** — d'où le volet 2 ci-dessous, qui couvre les 14 specs et pas seulement les 3 partagées. Two procedure rules were learned the hard way and are NOT optional; skipping either is what produced the corrections of 2026-07-28.
  - **RULE 1 — read `docs/discovery/wf-000-dispatch-identity-card-dry-run.md` §2 BEFORE writing the manifest, as a CHECKLIST, not as an oracle.** Its M column names the parameters that must be asked of the stakeholder (F = present or derivable, D = card-sanctioned unknown or operator-profile default). **It was computed against the coverage-matrix SKETCHES — the raw asks — while the manifest check runs against the qualified FIXTURE briefs, which are richer.** Verified 2026-07-28 on the three divergences seen so far: P02's sketch states no PI duration, P03's states no cloud provider, P04's states neither the sponsor role nor the engagement window; every one of those facts exists only in the fixture. The table was right about its own input each time. So a divergence is **not** an error on either side — it is a signal to settle the point **at the card**, then document the call on the spec. ⚠️ An earlier version of this rule called §2 "the acceptance oracle" and said it had "been wrong twice, because it skipped the `constraints` array": that diagnosis was **wrong** (and doubly so on P02, where the fact sits in `context`), it reached four texts before being caught, and it is corrected in the document's own annotation rather than deleted. What the comparison genuinely earns is the other direction — it caught the `ART name` word-order defect in the code. WF-004's row is P04 → 3 must-ask: Engagement duration, Stakeholders, Priority stakes.
  - **RULE 3 — SPLIT EACH MANIFEST IN TWO (Guy, 2026-07-28).** **Unit A: the spec table, zero code.** Read the card + its §2 row, classify every line (enumeration or conjunction, per RULE 2), decide required / default / sanctioned unknown, and present the table for review — card label · internal name · required · what the detector looks for · any divergence from §2. **Unit B: execution, mechanical** — manifest, registration, tests, falsification, CHANGELOG, commit, on a table already agreed. Why: every substantive error of 2026-07-28 (`Target LLM` misclassified, the slash rule missed, the `ART name` shape) was a **judgement**, and each was buried inside a single green lot of ~10 simultaneous decisions where it could not be seen until after the fact. The split puts the judgements in front of the human before they are encoded.
  - **RULE 2 — the slash rule.** A bracketed `/` list means "or" on some lines and "and" on others. `AWS / GCP / Azure` = enumeration; `Name / Sector / Size` = conjunction. Getting it wrong inverts the semantics in one of two bad directions and **there is no safe default**. A conjunction is SPLIT into one spec per fact under a qualified label (`GDPR constraints (personal data)`), so `PARAMS_MISSING` names the half that is missing. Known conjunctions still to handle: **WF-004 `Client [Name / Sector / Size]`**, WF-006 `Prospect [Name / Sector / Size / AI maturity]`, WF-007 `Client [Name / Sector / Size]`, WF-008 `Client [… / Geographic footprint]` + `Data processed [Personal / Sensitive / Art. 9]` + `Volumes [# individuals / training volume]`, WF-009 `Role sought [Title / Level]`, WF-010 `Project / Incident [Name + dates]` + `Team involved [Size / Distribution / Remote]`. **Consequence: the 81-spec sizing above is a FLOOR, not a total** — WF-003 ships 9 specs for 8 card lines.
  - **Reminder on mapping:** technical and commercial facts land in the `constraints` array as readily as in the prose. A detector that reads only `need`+`context` reproduces the oracle's own blind spot.
  - **Reminder on falsification:** stage before mutating. `git checkout` restores the INDEX — an unstaged correction is destroyed by it, which cost one rework on 2026-07-28.
  - **Repo hygiene re-verified 2026-07-28:** `gh pr list` = **0 open PR** (the five Dependabot PRs closed themselves as predicted), `dependabot/alerts` = **1 open** = the Hono/Windows one left in place on purpose. Suite 276/22 + typecheck green at `v0.9.1`.
  - **Re-vérifié 2026-07-31 :** PR **#48** (dev-deps `@types/node` 26.1.1→26.1.2) mergée en squash sur ordre de Guy — **elle était 8 commits en retard sur `main`** et son vert avait donc été mesuré sur un état périmé, incluant le manifest WF-006 et `run-dispatch.ts` ; branche mise à jour (`gh pr update-branch`), CI **re-vérifiée sur le NOUVEAU head SHA** avant merge (un rollup vert revenu en 12 s doit faire vérifier le SHA, pas conclure), puis `npm ci` + suite + typecheck locaux. `gh pr list` = **0**, alertes = **1** (Hono/Windows, délibérée), CI verte 3 jobs dont `real-sidecar integration`, **aucune dérive de tag** (catalogue `v4.2.0` = tag épinglé). Suite **331/23**.

✅ **CHANTIER CI — CLOS 2026-07-30 (4 commits : `f1888e6` → `e8cd277` → `3c46627` → `b8c8a5a`).** ⚠️ **Dérive de périmètre assumée et consignée** : parti comme une « unité de clôture » de la dette d'hygiène, c'est devenu un chantier CI complet. La cause est une question de Guy (« un reste à corriger avec les deux états ? ») qui a révélé un vrai défaut ; le périmètre a suivi le défaut, pas le plan.
  - **Le défaut trouvé** : `build-test` ne clone pas le catalogue (voulu, ADR-0002), donc **28 assertions n'étaient vérifiées par AUCUNE CI** — toutes les résolutions de backbone sur la vraie prose, le plan bâti sur la vraie carte WF-001, la route validée sur le vrai sidecar, et les 5 comparaisons de `catalogTag`. Le garde dont 5 manifests disent « hard-fails on drift » ne rougissait que sur le poste de Guy. (Le delta de suite n'est que de 17 parce que 11 fallbacks triviaux passent à la place.)
  - **Job `catalog-integration`** : clone anonyme et superficiel du catalogue **au tag que les manifests épinglent** — tag **lu depuis le code**, jamais écrit dans le YAML (ce serait une 2ᵉ source de vérité pour l'épinglage).
  - **⛔ NE PAS le repointer sur `main` : c'est le premier design, et ADR-0002 l'a renversé.** L'ADR rejette l'import live « *because a catalog edit would break the runtime with no safety net* » et **assume** que « *freshness is not immediate — you must bump to propagate* ». Sur `main`, une **release** du catalogue rougit ce repo jusqu'à re-dérivation : une fraîcheur volontairement non urgente devenue urgence, plus un rouge permanent qui masque les vraies régressions. La dérive vis-à-vis du **dernier** tag publié est traitée comme un **fait de démarrage** (bloc ▶ RESUME ci-dessus), où un écart se lit comme de la dette de fraîcheur, jamais comme un échec.
  - **`REQUIRE_CATALOG` est ce qui donne un sens au vert de ce job.** Mon premier garde (`test -f …/sidecar.json`) prouvait que le clone existe, **pas que les tests l'ont lu** : un `CATALOG_ROOT` typé de travers aurait tout sauté en restant vert — le défaut même que le job ferme. Le drapeau transforme un `no-catalog fallback` qui se déclenche en **échec**. Falsifié en 3 états : épinglé + drapeau → 316/23 · chemin typé de travers + drapeau → **11 échecs** · même absence **sans** drapeau → **vert** (un contributeur sans clone n'est pas puni, ce qu'ADR-0002 exige).
  - **Aucun run facturé possible en CI** — vérifié sur 3 jambes, pas supposé : les 12 harnais exigent `LIVE_RUN` EN PLUS du catalogue · le 13ᵉ fichier `*live*` (`wf-001-live-path.test.ts`) est hermétique par construction · le runner refuse `ANTHROPIC_API_KEY`.
  - **`build-test` réexaminé** : Node **20 était EOL depuis le 2026-03-24** (moitié de matrice sur un runtime mort) et Node **24, l'Active LTS**, n'était testé par rien alors qu'`engines` le déclarait supporté → matrice **22 + 24**, `engines` **>=22**, `timeout-minutes: 10` sur les deux jobs (durée observée 23–28 s). Durées mesurées : le nouveau job ajoute ~1 s de clone, tourne **en parallèle** ⟹ minutes de runner (gratuites, repo public), zéro latence ; aucun secret ⟹ les PR de forks passent.
  - ✅ **PROUVÉ SUR RUNNER 2026-07-30, dès le premier run (run `30506034137`)** — cette entrée disait « reste à prouver » pendant quelques minutes ; les trois réserves sont tombées ensemble, et la preuve est le DÉCOMPTE, pas le vert :
    - `catalog-integration` → **316 passed / 23 skipped** contre `build-test` → **299 / 40**. C'est l'écart exact mesuré en local : **les 28 assertions ont réellement tourné**. Avec `REQUIRE_CATALOG=1`, un catalogue absent aurait donné 11 échecs — donc ce vert ne peut plus vouloir dire « rien n'a été vérifié ».
    - **Node 24 vert en 21 s** : l'inférence par encadrement (22 testé / 26 local) est devenue un fait. `timeout-minutes: 10` face à 21–27 s observés.
    - Le log confirme les deux étapes sensibles : tag **extrait du code** (`--branch "v4.2.0"`) et clone **anonyme** réussi. Un run qui s'exécute vaut validation du schéma Actions, que `js-yaml` ne donnait pas.
    - ▫ **Bruit connu, ne pas le « corriger »** : le log porte `warning: refs/tags/v4.2.0 … is not a commit!` — un `clone --depth 1 --branch <tag ANNOTÉ>` avertit tout en récupérant le bon commit. L'éteindre coûterait un `init` + `fetch --depth 1 <tag>^{}`, soit deux lignes pour un avertissement cosmétique. Non recommandé.
    - **Contrôle de reprise** (déjà dans le bloc ▶ RESUME) : `gh run list --limit 3` doit montrer **trois** checks verts, dont `real-sidecar integration (catalog at the pinned tag)`.

✅ **CLOS 2026-07-29 (commit `39f938f`, lot d'hygiène « two signals that read the opposite of the truth ») — les DEUX dettes ci-dessous sont traitées ensemble, comme prévu.** Ce qui a été livré au-delà de la simple correction : un **helper partagé** (`test/catalog-sentinel.ts`) dont le titre nomme la CONDITION du bloc et non son effet ; ce helper devient aussi la **source unique de la porte `HAVE_CATALOG`**, que 11 suites recalculaient chacune de leur côté (même classe de dérive que celle que `catalog-root.ts` existe pour éviter) ; et la suite `dispatch-real-sidecar` reçoit un fallback qu'elle **n'avait jamais eu** — ses 8 assertions disparaissaient sans explication quand le catalogue manquait, d'où **23 skipped au lieu de 22**. Vérifié dans **les DEUX états**, dont le second jamais exercé jusqu'ici (`CATALOG_ROOT` pointant dans le vide : les blocs réels sautent, les fallbacks tournent et passent). Côté `dependabot.yml` : **aucune ligne de configuration modifiée**, prouvé par un diff filtré sur les lignes non-commentaire (vide) ; les deux références de doc citées re-vérifiées vivantes. Suite **316/23** + typecheck strict verts. ⛔ Ne pas « simplifier » en supprimant ces blocs : ils sont ce qui distingue *sauté faute de catalogue* de *n'a pas tourné*.

▫ **[HISTORIQUE, conservé pour le raisonnement] FOLD INTO A NEXT LOT (trivial, no dedicated commit) — les 10 titres-sentinelles `describe.runIf(!HAVE_CATALOG)` disent l'inverse de ce qu'ils signifient.** Repéré 2026-07-29 en répondant à une question de Guy sur le mot « skipped ». Chaque `run-wf-00X-real-sidecar.test.ts` porte un bloc complémentaire qui ne tourne QUE si le catalogue est absent, intitulé `"skipped: catalog not found (set CATALOG_ROOT or checkout sibling)"`. Catalogue présent ⟹ ce bloc est sauté, et le rapport affiche donc 10 lignes « catalog not found » **précisément parce que le catalogue A été trouvé**. Chaque mot est vrai, la conclusion du lecteur est l'inverse — j'ai moi-même conclu à tort que les preuves real-sidecar n'avaient pas tourné avant de vérifier ([[feedback-info-juste-impression-fausse]]). Correctif : renommer le titre du bloc et de son `it` (p. ex. `"catalog ABSENT — this block runs only without a checkout"`), **cosmétique, 10 fichiers de test, aucune spine, aucune trace, aucun run**. Décision de Guy (2026-07-29) : à replier dans le prochain lot qui touche ces tests, jamais un commit dédié. **⤵ Apparié à la dette `dependabot.yml` ci-dessous : les deux dans UN commit d'hygiène, en unité de CLÔTURE de session (le raisonnement complet est sur cette entrée-là, ne pas le dupliquer ici).** Correctif à privilégier : un **helper partagé** plutôt qu'un renommage ×10 — mesuré 2026-07-29, les 10 blocs sont structurellement identiques et ne diffèrent que par l'id du workflow, donc un renommage sur place laisserait la prochaine correction à faire 10 fois de plus. ⚠️ Ne PAS « corriger » en supprimant les blocs : ils sont ce qui distingue « sauté faute de catalogue » de « n'a pas tourné », c'est-à-dire le contraire d'un garde vert sans avoir gardé.

▫ **[HISTORIQUE, conservé pour le raisonnement — traité dans `39f938f`] FOLD INTO A NEXT LOT — `.github/dependabot.yml` comments are still in French.** Spotted 2026-07-27 while explaining the tool. The repo was translated to US English in `v0.6.0`; this file was missed, and the repo is PUBLIC so the comments are visible. Same class as the frozen README, three orders of magnitude smaller. Guy's call (2026-07-27): fold it into whatever lot next touches CI or dependencies, never a commit of its own.
  ⚠️ **TRIGGER CHANGED 2026-07-29 (Guy): fold it into the CLOSING unit of a session, not into a CI/dependencies lot.** Reason: that trigger measurably is not coming — the five remaining units (WF-006…WF-010) touch `src/dispatch/` and `test/` only, and two days had already passed. A fold-in whose condition never fires is a way of never doing it. Pair it with the sentinel-titles debt below in ONE commit ("repo hygiene: two signals that read false") — which respects "never a commit of its own": one commit for a coherent class, not one for a YAML comment. It needs no human validation and no live proof, so it belongs at the tail of a budget, while WF-006 (11 specs) deserves a cold start.
  ⛔ **THE NB THAT USED TO SIT HERE WAS FALSE, and it is corrected rather than deleted because it drove an arbitration.** It read: *"`open-pull-requests-limit: 5` means a stale backlog of 5 PRs silently suppresses new ones, **security bumps included** — that is the real argument for draining it."* Verified at source 2026-07-29 (GitHub docs, `open-pull-requests-limit`): the limit applies to **version updates only**, and *"security update pull requests are not subject to this limit and do not count toward it"*. `5` is also **exactly the default**, so that line changes no behaviour; it is kept only to immunise the repo against a future change of GitHub's default. So the security bumps were never suppressed, and the argument the NB advanced does not exist.
  ⛔ **DO NOT RE-PROPOSE raising the limit or extending the groups — both were examined and dropped 2026-07-29.** Raising it produces MORE routine PRs to qualify, when the observed bottleneck is human triage attention. Grouping was the serious candidate and it fails on a point that cannot be recovered after the fact: the four packages of the 2026-07-27 backlog (`fast-uri`, `hono`, `body-parser`, `@hono/node-server`) are all **transitive** — verified absent from `package.json`, only `ajv` is direct — and for npm it is the **security** updates that reach transitive deps by bumping the parent, which is exactly what happened. The existing `groups:` block carries no `applies-to`, so it governs version updates. Whether those PRs were security- or version-driven is **indeterminable retroactively** (PR #46's body shows only the upstream release notes, which look the same either way), so the benefit of grouping cannot be demonstrated — and grouping security fixes would COUPLE independent ones, letting a failing member block the rest, which is bad when both `high` findings sat on the execution path.
  ▶ **What DOES address the observed defect** — 7 alerts sitting untriaged for weeks was an **awareness** gap, and `dependabot.yml` governs PR creation, never alert triage: the alerts count is now part of this repo's session-start factual check, in the ▶ RESUME HERE block above. Deliberately NOT added to the global `SessionStart` hook, which has no directory scope and must carry invariants only.

✅ **CLOSED 2026-07-27 — README debt fixed, `v0.9.1` cut, released and remote-verified.** The README had been frozen at `v0.5.0` since **2026-06-19** (`d57f058`, the i18n batch) while `v0.6.0`→`v0.9.0` live-proved all ten workflows: it claimed *3 workflows proven live* and *126 tests*, left `- [ ] WF-002/003 live run end to end` unticked, and never mentioned WF-000. Every figure was **re-derived at source** before writing (not taken from this tracker): `package.json` `0.9.0`, tag `v0.9.0`, **23 traces** in `docs/audit/live-runs/`, statuses read FROM the traces = **9 `completed` + 1 `failed`** (WF-008 fail-closed by design; WF-009 carries both a `completed` and a gate-halt), `npm test` = **276 passed / 22 skipped (298)**, strict typecheck green, sidecar **v4.2.0** = 85 assets (38 agents / 37 skills / 10 workflows).
  - **Fixed:** status line (`v0.9.1`, 10 live-proven stated as **9 completed + 1 returned for rework**, 276 tests, WF-000 named as a layer **upstream** of the spines); POC scope (ten spines, dispatch as a 4th building block, `runWf001` … `runWf010`); roadmap (WF-002/003 ticked, WF-004…010 + WF-000 added, the 2 open items — dispatch V1, multi-WF chaining — listed unticked); documentation pointers to `docs/audit/live-runs/` + `docs/discovery/`; stack `Sonnet 4.6` → `Sonnet 5` with `modele_recommande` identified as a catalog artifact. Framing conventions held: WF-000 not an 11th workflow, 19/20 never smoothed.
  - **Release `v0.9.1` (docs-only, explicit approval):** CHANGELOG `[0.9.1]` → bump `package.json`+lock → commit → annotated tag → push → GitHub Release. Remote-verified: tag on origin (`8fd3e3c`), release **not draft / not prerelease**, `main` in sync, **CI green** on the release commit. Suite 276/22 + typecheck re-run green at `0.9.1`.
  - ▶ **Downstream consequence for the batched showcase pass:** the showcase currently advertises runtime **`v0.9.0`** — now one patch behind. Fold the `v0.9.1` version bump into the batched pass (do NOT open a separate showcase session for it). Nothing else there is falsified: the 10/10 and the dispatch story are unchanged.

✅ **CLOSED 2026-07-27 — Dependabot backlog qualified and cleared: 7 open alerts → 1, verified at source (`gh api …/dependabot/alerts` = 1).** The signal below had sat unqualified since it was first logged; it was resolved by mapping the 5 open PRs onto the alerts with `npm audit` + `npm ls` **before** touching anything, not by merging PRs blind.
  - **The tracker's own guess was WRONG and is the lesson.** It read `fast-uri`/`hono`/`body-parser` as "transitive dev tooling" and `#43` (SDK) as the only execution-path item. In fact **both `high` alerts were `ajv → fast-uri`** — and `ajv` validates every dispatch brief and every step handoff, so the two highs were the *most* execution-path findings of the set. `gh api` reported `scope=runtime` on all seven; **zero were dev-only.** A shape read off package names is not a diagnosis ([[feedback-diagnostic-prouve-avant-correctif]]).
  - **Fixed:** `fast-uri` 3.1.2→3.1.4 (both highs); `@anthropic-ai/claude-agent-sdk` → `^0.3.215` (lock 0.3.220), which cleared `hono` 4.12.32 + `body-parser` 2.3.0 via `@modelcontextprotocol/sdk` (2 moderate + the low); `actions/setup-node` v6→v7. Suite **276/22** + strict typecheck green, **versioned traces untouched (diff-verified)**, CI green Node 20/22. Commits `6f2a16b` + `c8e013b` pushed.
  - **1 moderate LEFT IN PLACE, deliberately — do not "fix" it on a later pass without re-reading this.** `@hono/node-server` path traversal in `serve-static` **on Windows**. The fix needs a **major** bump to `>=2.0.5` that `@modelcontextprotocol/sdk` pins, and `npm audit`'s own proposed remedy is a **semver-major DOWNGRADE of the Agent SDK to 0.2.85** — which would break the runtime and orphan the live proofs. It is also **unreachable here**: this runtime never instantiates the Hono node server nor serves static files (it calls `query()` locally), and CI runs on Linux. Revisit only when `@modelcontextprotocol/sdk` widens the range upstream.
  - The 5 PRs (#42/#43/#44/#46/#47; #45 superseded by #47) are **satisfied or exceeded** by the resolved versions and close on Dependabot's next scan — no manual merge needed.

⚠ **ORIGINAL SIGNAL, kept for the record — Dependabot reported 7 vulnerabilities (2 high, 4 moderate, 1 low) on the default branch.** Seen on 2026-07-27 as a **remote message printed by `git push`**, nothing more: **I did not open the Security tab, did not run `npm audit`, did not identify a single package.** So the severity split is GitHub's claim, not a verified finding — treat every figure above as unchecked until re-derived.
  - **Why it is worth a look:** the repo is **PUBLIC**, so the Security tab is visible to anyone the showcase sends here. Dependabot is also configured (`- [x] CI … + Dependabot` in the roadmap), which means these are alerts nobody has triaged, not a missing setup.
  - **How to qualify it (first, before any bump):** `gh api repos/guyhui01/claude-agentic-runtime/dependabot/alerts` and `npm audit`, then split **runtime dependencies vs devDependencies** — a high severity in a dev-only toolchain of a POC that ships no service is not the same risk as one in the execution path. Expect most of it to be transitive dev tooling.
  - **Guardrails:** ⛔ do **not** blind-bump — the suite is **276/22 green** and the live proofs are pinned artifacts; any dependency change must keep typecheck strict + the suite green, and **must not touch the versioned traces**. Its own unit, **after** the README fix (which is the one that actually contradicts the portfolio), or its own session.
  - **NEW FACT 2026-07-27 (verified, `gh pr list`) — this is now the NEXT UNIT, the README debt above being closed. The tracker line further down saying "No open PRs remain" is STALE (dated 2026-07-13): there are FIVE open Dependabot PRs.** `#46` `fast-uri` 3.1.2→3.1.4 · `#45` `hono` 4.12.25→4.12.31 · `#44` `body-parser` 2.2.2→2.3.0 · `#43` `@anthropic-ai/claude-agent-sdk` 0.3.207→0.3.215 · `#42` `actions/setup-node` 6→7. Note the shape: `hono`/`body-parser`/`fast-uri` smell like **transitive dev/tooling**, `#43` is the **SDK = execution path** (the one that deserves the real live-regression thinking), `#42` is CI only. **Still unqualified:** nobody has mapped these five PRs onto the 7 alerts — do that first (`gh api …/dependabot/alerts` + `npm audit`, split runtime vs dev), then merge in risk order, suite + typecheck green after each.

STATE AS OF 2026-07-13 (re-verify):
- **CURRENT UNIT — "WF-000 dispatch" discovery, STARTED 2026-07-19 (cold session as prescribed). FIRST DELIVERABLES DONE, committed LOCALLY `57249eb` (NOT pushed — push = gate):** `docs/discovery/wf-000-dispatch-brief-contract.md` (validated-brief schema + deterministic fail-closed completeness check w/ negative-sentinel rejection + blocking-floor bounds + routing output contract) and `docs/discovery/wf-000-dispatch-coverage-matrix.md` (**20** fictional prompts × 10 WF: 10/10 covered, 3 discriminating pairs P11-P13, 4 NO_MATCH miss classes, 1 REJECT_INCOMPLETE, **+ 2 role probes P19/P20 (Guy-approved 2026-07-19): `submittedBy` diversified, routing must be invariant to submitter role — role ⊥ route, falsifiable, oracle = expected-route column evaluated deterministically, never LLM-judge-LLM**). Fence parity checked per file. **Showcase note (consigned for the batched pass, separate private session): Guy flags the role probes as a strong qualitative feature to surface on the showcase** — frame as anti-persona-bias falsifiability, discovery-stage (no live run yet). **DISCOVERY STEPS 1-2 DONE SAME SESSION (2026-07-19, local commits `3adb3ac`+`45fb43a` — earlier batch `57249eb`+`1c84eec` PUSHED on Guy's order):** (1) **identity-card dry run** `wf-000-dispatch-identity-card-dry-run.md` — 15/15 routed prompts confronted with the REAL card param blocks (read live from catalog v4.2.0): every routed prompt has a param gap pre-qualification (validates the validated-brief decision); card param blocks are PROSE → V0 needs a versioned per-WF param manifest pinned to the catalog tag (WF-001 only in V0); sentinel policy is FIELD-CLASS-DEPENDENT (card-sanctioned honest unknowns "Not disclosed"/"to be defined" must be ACCEPTED by the param check while intake rejects negative sentinels); **`PARAMS_MISSING` added to the routing output contract** = 3rd returned-for-rework outcome ≠ NO_MATCH ≠ REJECT_INCOMPLETE; pilot P01→WF-001 gap = 3 params (exercises the return loop). (2) **router draft** `wf-000-dispatch-router-draft.md` — router prompt skeleton (WF table injected from pinned sidecar, role ⊥ route rule, NO_MATCH sanctioned), fail-closed validation pseudocode, offline oracle = 20 matrix fixtures string-compared (no LLM in CI), + WF-001 param manifest draft. **Convention added on Guy's order: every docs/discovery file carries a model-provenance line (drafted with Claude Fable 5) — keep for future discovery docs.** **DISCOVERY COMPLETE (5 deliverables) + V0 IMPLEMENTED OFFLINE SAME SESSION (2026-07-19, 4 local commits `cbe08c5`→`5e61d16`, NOT pushed — push = gate):** step (3) V0 plan (`wf-000-dispatch-v0-implementation-plan.md`, pushed `006482d`) then the 4 code lots: **Lot 1** intake (`src/dispatch/{types,brief-schema,completeness-check}.ts` — ajv strict, affirmativeString sentinels, word floor + versioned state-marker list); **Lot 2** routing (`validate-route.ts` + `manifests/wf-001.ts` pinned v4.2.0, tag drift hard-fails; PARAMS_MISSING named; manifest-less route = honest `paramsChecked:false`); **Lot 3** router prompt builder (sidecar-injected table, role ⊥ route) + **20-fixture matrix oracle** (proposer faked by design, no LLM in CI) + **intake fix found while fixturing: generic qualified-request marker** (else off-catalog needs die at intake and honest NO_MATCH is unreachable — contract §4.2 annotated); **Lot 4** `plan.ts` (duree_estimee/modele_recommande verbatim from card, fail-closed) + `run-dispatch.ts` (completeness short-circuit, ONE re-ask on shape-invalid only, invented id never retried, STOP before go/no-go) + live harness `test/dispatch-run-live.test.ts` (LIVE_RUN-gated, OAuth-only, maxTurns 2/$0.5 caps, score reported VERBATIM). CHANGELOG `[Unreleased]` entry (model: Fable 5). **Suite 275/21 + strict typecheck green.** NB fence-parity: CHANGELOG counts 1 fence — PRE-EXISTING (HEAD~1 and v0.8.0 = 1 too), an intentional inline triple-backtick in the FROZEN French entry line ~137; never "fix" it. **GATE 1 DONE — LIVE ROUTER ACCURACY RUN 2026-07-19 (`LIVE_MODEL=fable`, billed OAuth, explicit approval, ~2.2 min, 20 single completions): SCORE 19/20.** Role probes P19/P20 HELD live (role ⊥ route), discriminating pairs P12/P13 correct, 4/4 off-catalog → honest NO_MATCH, P18 deterministic intake reject, P01 ROUTED paramsChecked:true. **Single mismatch = FINDING (verbatim in trace): P11 pre-signature-no-RFP → NO_MATCH nearestMiss WF-006** — router only sees the sidecar description arrow ("RFP received"), narrower than the card's "Direct solicitation" scope; SAFE failure direction (under-match + nearest miss named, never force-fit). **V0 accepts; V1 candidate: router-prompt rule for equivalent pre-signature commercial states (do NOT re-spend quota re-running now).** Trace VERSIONED `docs/audit/live-runs/wf-000-router-live-result.json` (git add -f, secret-scan clean, fictional briefs, no live-model field; the `recommendedModel` inside plans = catalog artifact) + live-runs README entry + CHANGELOG updated. **GATE 2 DONE — PILOT P01 END-TO-END LIVE 2026-07-19 (`LIVE_MODEL=claude-opus-4-8`, billed OAuth, explicit approval at each gate; earlier pending commits pushed on Guy's order the same session).** Phase 1 return loop LIVE: unamended P01 (new fixture `P01_UNAMENDED`) → routed WF-001 → **`PARAMS_MISSING` exactly `[team_size, project_method, level_of_detail]`** (dry-run §2 prediction verbatim). Phase 2: amended P01 → **`ROUTED` `paramsChecked:true`** + plan verbatim from the card ("45-90 min", `claude-sonnet-5` = catalog artifact) → STOP by construction. Phase 3 on Guy's human GO: WF-001 spine, P01-derived seed via new `LIVE_BRIEF` env override (default insurer seed unchanged; seed captured in the trace payload) → **`completed` 3/3, 9/9 blocking**, advisory 2/3 (known-brittle INVEST regex, criterion artifact), ~2.7 min, content inspected P01-specific (claims-file-summarization epics, RAG over policy wordings, GDPR-by-design — not an echo of the default seed). Full V0 pipeline traversed live: intake → return loop → route → plan → human gate → spine delivery. New hermetic regression locks the deterministic return loop (`PARAMS_MISSING` on `P01_UNAMENDED`, zero LLM). Traces versioned `wf-000-pilot-p01-dispatch-live-result.json` + `wf-000-pilot-p01-wf001-live-result.json` (secret-scan clean, fictional, no live-model field) + live-runs README (2 entries) + CHANGELOG `[Unreleased]`. Suite 276/22 + strict typecheck green. **RELEASE v0.9.0 CUT & RELEASED 2026-07-19 (explicit approval): "WF-000 dispatch V0 live-proven end-to-end"** — `[Unreleased]` (the 2 WF-000 bullets) frozen as `[0.9.0]`, empty `[Unreleased]` recreated, package.json+lock 0.8.0→0.9.0. Release commit `6d33535`, annotated tag `v0.9.0` (=`92a530a` on origin), GitHub Release all pushed and remote-verified (tag on origin, release not draft/prerelease). The 3 pilot commits were pushed first on Guy's order. `main` in sync with `origin/main`. **▶ NEXT SESSION RESUMES HERE. Factual check first: `git status -sb`, `gh pr list`, suite 276/22 + typecheck, last tag = **v0.9.1** (docs-only patch, 2026-07-27; `v0.9.0` was the last CODE release). WF-000 V0 is COMPLETE and RELEASED (discovery + code + 2 live proofs + pilot end-to-end). Remaining, lead with a reco: (1) showcase consign (batched pass, separate private session: dispatch story = honest NO_MATCH + role ⊥ route probes held live 19/20 + the PARAMS_MISSING return loop proven live on P01 + the P11 finding told honestly + first spine delivery reached THROUGH the dispatch); (2) V1 backlog (do not start unprompted): router-prompt rule for pre-signature commercial states (P11), the nine remaining param manifests, assisted param filling + cost estimate. (3) **MULTI-WF ENGAGEMENT ARC — REQUALIFIED AND DOWNGRADED 2026-07-27, do NOT re-promote it without reading this.** Previously logged here as the "strongest narrative jump left". Three things were re-examined with Guy and it does not hold:
    - **The narrative value is already banked.** The claim it was meant to buy — "the stack is a SYSTEM, not a collection" — is what the **WF-000 P01 pilot already proves live**: a validated need traverses intake → `PARAMS_MISSING` return loop → route → plan → human gate → WF-001 `completed` deliverable. Chaining three workflows extends that claim quantitatively; it is not a new category of proof.
    - **The price is the one thing fenced off three times.** Inter-WF context accumulation = an orchestrator rewrite (`src/orchestrator/run-spine.ts:162` is `input = output`, mono-upstream, verified 2026-07-27). That is exactly what put the WF-008 full-cycle narrative out of scope and forced WF-009's `candidatePool` passthrough. It would stake **10 live proofs** — the hardest-won asset here — on a story. Barred by [[feedback-ne-pas-modifier-workflow-prouve-live]].
    - **The "wall" is a PRODUCT BOUNDARY, not a technical limit — this is the correction that matters.** "Blocked on context accumulation" reads as a limitation suffered. In a consulting engagement you do **not** want an agent going from "GO on the opportunity" to "onboarding the client" to "scoping the product" unattended: those are three distinct contractual commitments, each owed an accountable human decision. The gate is the feature. Making it autonomous would **erode** the fail-closed verifiability moat, not extend it. The correct design — if ever built — is a **human-gated brief hand-off at the dispatch layer** (WF-006 output → operator derives the WF-007 brief → route → new gate), which touches zero orchestrator code and restakes zero live proof.
    - Also: a chained run is **3× the freshness liability** (each trace pinned to a catalog version) for a single narrative.
    - **Do instead, in order: (1) the nine remaining param manifests** — what turns the dispatch from a WF-001-only demo into something that covers the catalog; deterministic, offline-testable, zero quota, zero risk to the existing proofs; **(2) cost estimate at the `plan` step** (directly useful to the human holding the go/no-go); **(3) the P11 router rule** (pre-signature commercial states).
    - Unchanged bans: do NOT reopen a live-proven spine, and do NOT rerun already-proven WF "to refresh" (pure freshness liability, agreed 2026-07-19). Rederiving `claude-projects` live is explicitly OUT (repetition of WF-001/002 OR an orchestrator rewrite — it stays a frozen deliverables case study). Reco priority: the batched showcase pass (realize the existing stock) BEFORE this capability jump. NB: the batched showcase pass now also owes WF-005/007/010/WF-009 surfaces (see below) + the licence-debt fix (catalog is PolyForm NC, not MIT) — one private session, one batched pass.** Original scoping (2026-07-18) kept below — goal: productize intake→routing→delivery on top of the stack (catalog v4.2.0 sidecar indexes all 85 assets incl. the 10 workflows with dependsOn — the machine-readable routing substrate). Decisions LOCKED with Guy:
  (a) V0 user = the accountable operator (Guy as PO/consultant). He collects, consolidates and VALIDATES the need with the stakeholder upstream; the router input is a **validated need brief, never a raw stakeholder prompt**.
  (b) **Closed profile, open contract**: V0 is single-profile, but the brief contract (schema: need, domain, expected deliverable, constraints, context, optional submittedBy role) carries NO operator-specific assumption — multi-profile later = productize the upstream QUALIFY step (agent interviews the stakeholder, emits the same contract), the router never changes. The go/no-go gate before any billed run stays HUMAN whatever the profile.
  (c) Pipeline V0: validated brief → deterministic completeness check (fail-closed) → ROUTE (LLM proposes a WF, DETERMINISTIC validation against sidecar.json: WF exists, agents_core resolvable, identity-card client-context params fillable — never LLM-judge-LLM) → execution plan + duration/model from the WF YAML card → human go/no-go → run the spine. Honest coverage: if no WF matches, answer "no workflow for this" (returned-for-rework pattern), never force-fit. Pilot WF target: WF-001.
  (d) Slicing: V0 = dispatch spine, single target WF; V1 = assisted client-context param filling + cost estimate; V2 = ad-hoc agent composition when no WF matches + multi-WF chaining (WF-006 → 007 → 001 pre-sales→scoping arc).
  (e) **Placement: this PUBLIC repo (PolyForm NC protects commercial use — both runtime AND catalog are PolyForm NC, catalog switched from MIT on 2026-06-12), NO private repo for the code.** Private = per-engagement client instances only (filled briefs with real client context), per the established governance; the routing matrix in discovery uses FICTIONAL prompts only (public-trace rule). First discovery deliverable: brief contract draft + a 15-20 fictional stakeholder-prompts × 10 WF coverage matrix.
- **MILESTONE — ALL 10 CATALOG WORKFLOWS ARE NOW LIVE-PROVEN (spine + billed live run).** WF-001…006/008/009 done previously; WF-005 merged (PR #39); **WF-007 + WF-010 live runs DONE this session (2026-07-13, `LIVE_MODEL=claude-opus-4-8`, billed OAuth, explicit approval) and MERGED to main — `all 10 done`.**
  ▶ **WF-007 "Client Engagement Onboarding D1-D5" — DONE + MERGED (PR #40, squash `8738e64`, CI green Node 20/22).** Backbone CHEF-PROJET-IA → BUSINESS-ANALYST → CHANGE-MANAGER → REDACTEUR-IA (STEP-01→02→03→05), linear twin of WF-004. Live run `completed` 4/4, **10/10 blocking + 8/8 advisory**, ~4.1 min, $4 cap; substantive content; **clean audit trace, no plan file written** (Opus). Trace versioned `docs/audit/live-runs/wf-007-live-result.json`, README + CHANGELOG updated.
  ▶ **WF-010 "Project Post-mortem / Lessons Learned" — DONE + MERGED (PR #41, squash `dd41178`, CI green Node 20/22).** Backbone CHEF-PROJET-IA → QA-AGILE → CHANGE-MANAGER → REDACTEUR-IA (STEP-01→02→03→06); AUDIT-METHODO-IA counter-review OPTIONAL here (vs mandatory WF-008), outside the backbone; linear twin of WF-004. Branch was rebased onto main (post WF-007) and CHANGELOG `[Unreleased]` reconciled (both bullets). Live run `completed` 4/4, **10/10 blocking + 11/11 advisory**, ~5.5 min, $4 cap; substantive content (5-Whys REX). ⚠️ **HONEST HYGIENE FINDING: this Opus run DID write a plan file** to `~/.claude/plans/` — **falsifies the WF-005 hypothesis** that only Sonnet writes plan files and Opus does not (behavior NOT cleanly model-determined). Artifact carried no PII/secret (synthetic REX task plan), was DELETED; versioned trace clean (no model field, no `internalNote` on this WF). Trace versioned `docs/audit/live-runs/wf-010-live-result.json`, README + CHANGELOG updated.
- **REMAINING (this repo): NOTHING — WF-007/010 merged AND release cut.** **`v0.8.0` CUT & RELEASED 2026-07-13 (explicit approval): "All 10 catalog workflows live-proven"** — froze `[Unreleased]` (WF-005/007/010 spines+live proofs, WF-009 candidatePool + sentinel hardening + Fixed) as `[0.8.0]`, recreated an empty `[Unreleased]`, bumped package.json+lock 0.7.0→0.8.0. Commit `f34cb68` + annotated tag `v0.8.0` (= `676f6cf`) + GitHub Release all pushed and remote-verified (tag on origin, release not draft/prerelease). Suite 222/20, typecheck OK. `main` in sync with `origin/main`.
- SHOWCASE downstream (separate private session) — **AGREED SEQUENCING (Guy, 2026-07-13): do NOT surface piecemeal.** The precondition is now MET (WF-007 + WF-010 live-proven), so the SINGLE batched showcase pass can proceed: surface WF-009 (2 traces: gate-halt + completed) + WF-005 + WF-007 + WF-010 together — one pass, not repeated partial passes. Guardrail for WF-010's showcase copy: frame it as a `completed` all-green run, and if the process-hygiene plan-file finding is surfaced, state it honestly (deliverables clean, disk artifact deleted).
- ORIGINAL STATE AS OF 2026-07-12 (re-verify):
- SHOWCASE STATUS CORRECTED (verified live on the public site 2026-07-12, no private repo opened): WF-004 (✅ Live 6/6), WF-006 (✅ Live 6/6) and WF-008 (⟲ Returned) are ALL ALREADY SURFACED on the showcase (its v1.1.0 + v1.2.0). The earlier "remaining showcase = WF-004/006/008" here was STALE (it was this repo's view at their merge time; the showcase session had already done them). ONLY WF-009 remains to surface — its framing is now SETTLED: it has TWO live proofs, a `completed` 6/6 end-to-end (HR & Talent domain, via the synthetic candidate-pool passthrough) AND the gate-says-no halt on the SAME gateway — surface both (the discriminating gateway is the story); currently the showcase still reads "Available" for it. The showcase items 0-BIS/0/1 in the RESUME block below are therefore DONE — do not redo.
- WF-009 "IT/AI Recruitment" (fifth domain HR & Talent) — SPINE MERGED to main (PR #34 squash `3e7b37f`, CI green). Linear twin of WF-004, orchestrator UNCHANGED; the two BPMN loop-back gateways modeled as blocking eval gates. **First billed live run (Sonnet 5) DONE 2026-07-12 → `completed` 6/6 BUT a CONTENT inspection exposed a HOLLOW PASS**: the seed had no candidate CVs, so the agent honestly returned a placeholder shortlist + `selectedCandidate="None — no candidate can be selected"`, and the two decision gateways (`rh-shortlist-validated` minArrayLen≥3, `sel-candidate-selected` nonEmptyString) PASSED on the exact NO branch they must halt on — a real correctness bug. **Hardened (offline, NOT billed): `affirmativeString`/`countAffirmativeField` helpers reject negative sentinels; `sel-candidate-selected`→affirmativeString, `rh-shortlist-validated`→≥3 REAL candidates; regression tests reproduce the live outputs; new `test/spine-helpers.test.ts` guards the false-positive edge (names "Noah"/"Nadia" affirmative).** The hollow live trace is NOT versioned (theater). Hardening MERGED to main (PR #35 squash `f2afce2`, CI green). Suite 182/14 + strict typecheck. **SECOND live run 2026-07-12 (Sonnet 5, billed) DONE → `failed` fail-closed at STEP-04 BY DESIGN**: same no-CV seed, the agent honestly reported "No candidate data available" (no fabrication), and the HARDENED `rh-shortlist-validated` HALTED (vs the first run where the identical refusal wrongly passed) — a "gate says no" proof on the candidate-selection decision that VALIDATES the fix live. STEP-01→03 pass (recruiter-side deliverables, no CVs needed). Anonymized trace VERSIONED `docs/audit/live-runs/wf-009-live-result.json` (git add -f, secret-scan clean, no model field); live-runs README + CHANGELOG updated. Structural note: STEP-05/06 not reached — the linear mono-upstream handoff never feeds candidate CVs to STEP-04 sourcing, so a `completed` WF-009 live run is NOT one seed-tweak away (would need context accumulation = orchestrator change, barred, or artificial candidate passthrough). **Showcase framing: WF-009 = defined backbone opening HR & Talent (offline-proven) + a live "gate says no" trace on the candidate gateway — NOT a completed domain proof.** Doc+trace commit `cf1e7b3` PUSHED to origin/main (2026-07-12). Lesson persisted: a DECISION gate must reject negative sentinels, not just check presence.
  ▶ **DONE + MERGED (2026-07-13): WF-009 `completed` via candidate passthrough — BILLED LIVE RUN PROVEN.** MERGED to main via PR #36 (squash `23ca4e7`, CI green Node 20/22). Was branch `feat/wf-009-candidate-passthrough`. (1) Added a `candidatePool` passthrough (synthetic/fictional, 5 profiles) threaded UNCHANGED through STEP-01/02A/03 output+input contracts to reach STEP-04 sourcing — orchestrator UNCHANGED, a deliberate one-field-per-hop deviation, RGPD note in-code (synthetic by design; prod would inject at STEP-04 via ATS side-channel). (2) Offline green: hermetic + real-sidecar (v4.1.0) `completed` 6/6 with real prose + a new regression test (dropped pool → fail-closed at handoff). (3) Hardened the live test itself with a substance guard (a `completed` must carry ≥3 affirmative shortlist + real selected candidate — gate-guaranteed, so no false-negative). (4) Billed live run (Sonnet 5, OAuth): **`completed` 6/6, ~6.5 min, content inspected** — pool flowed unchanged 01→03, STEP-04 scored with real discrimination (seeded weak CAND-04 → 20/100, excluded), shortlisted 4 real, selected CAND-05 (91/100, a DIFFERENT pick than the fixture = genuine reasoning). **Both WF-009 traces preserved: `wf-009-live-result.json` (completed) + `wf-009-live-result-gate-halt.json` (the earlier gate-says-no, restored from git under a new name) — the SAME `rh-shortlist-validated` gateway saying *no* there and *yes* here proves it DISCRIMINATES (anti-theater centerpiece).** README live-runs (2 entries) + CHANGELOG updated. ⛔ Frame the completed HONESTLY: `completed` on a SYNTHETIC scenario WITH a provided candidate pool, NEVER "autonomous sourcing".
  ▶ **REMAINING (this repo: NOTHING — merged).** Only downstream: SHOWCASE surfaces for WF-009 in the separate (private) showcase repo — a distinct session (one product repo in write per session; this repo is PUBLIC, never name the private repo here). WF-009 now has TWO live proofs (gate-halt + completed) — surface both: the discriminating gateway is the story. Honest framing guardrail: `completed` on a SYNTHETIC scenario WITH a provided candidate pool, never "autonomous sourcing".
- **WF-005 "Strategic Intelligence & Growth" (2026-07-13) — SPINE + LIVE TRACE MERGED to main via PR #39 (squash `8d5b7b4`, CI green Node 20/22).** Lightest backbone (3 agents: VEILLE-STRATEGIQUE → GROWTH-IA → REDACTEUR-IA), THIRD backbone in Management & Consulting (with WF-004/006 — adds a backbone, NOT a new domain). Linear twin of WF-004, orchestrator UNCHANGED; STEP-01 `condition_passage` ("signals qualified AND ranked") = ordinary blocking gate (`veille-highlights-ranked`); both source gateways optional/outside backbone; handoff `highlights → topics`. **LIVE-RUN HARDENING (anti-false-KO): output-schema numeric bounds pinned to the BLOCKING FLOOR, not the advisory ideal** — `validate-handoff.ts` ajv-strict-validates the OUTPUT at handoff, so `minItems:5`/`maxItems:3` would hard-fail a modest-but-valid run the gate passes; fixed to `minItems`=floor (highlights ≥3), NO `maxItems` (topics/posts), no `minItems` on optional `toolsToWatch`; regression test locks it (3-highlight/4-topic/4-post run completes). ⚠️ NOTE: WF-004/006 carry the SAME latent `min:5`-hard issue but are LIVE-PROVEN/FROZEN (`feedback-ne-pas-modifier-workflow-prouve-live`) — did not bite (Opus complied) — DO NOT touch. **LIVE RUN 2026-07-13, TWO runs:** first `LIVE_MODEL=claude-sonnet-5` (catalog `modele_recommande`) → `completed` 3/3 with substantive content BUT process-hygiene defects: (a) wrote a plan file to `~/.claude/plans/` despite forced `permissionMode:"plan"`, (b) leaked plan-mode meta-commentary (real name + local path) into the optional non-gated `internalNote`. Re-routed `LIVE_MODEL=claude-opus-4-8` → **`completed` 3/3 CLEAN** (no disk write, legitimate factual-check `internalNote`), ~3.1 min, $4 cap. **Behavior is MODEL-DEPENDENT (proven on disk): the 2 Opus runs WF-004/006 left NO plan file; both Sonnet runs WF-005/009 DID.** Verbatim trace versioned `docs/audit/live-runs/wf-005-live-result.json` (Guy's call: KEEP "Guy" ×2 in editorial angles — his own public identity, not third-party PII — no redaction; secret-scan clean, no model field). README live-runs + CHANGELOG updated. Both Sonnet plan-file artifacts DELETED (`topics-…-deep-quiche.md` = WF-005, `hrinterviewreport-…-lazy-fern.md` = WF-009), confirmed absent from `~/.claude/plans/`. **REMAINING (this repo): NOTHING — merged.** Only downstream: WF-005 showcase surface (separate private session). NB: `docs/NEXT_STEPS.md` snapshot §1 is one WF behind (lists WF-004/006/008, not WF-009/005) — narrative roadmap, not the session SSOT; refresh opportunistically.
- **MODEL-ROUTING DECISION (Guy, 2026-07-13): remaining LIVE runs → Opus 4.8.** Sonnet 5 relegated for live AUDIT traces (plan-mode disk write + internalNote leak = degraded trace HYGIENE, not degraded deliverables). Catalog `modele_recommande` per workflow is UNCHANGED (a catalog artifact); the live-proof routing is a runtime decision. Any pre-human verifier STAYS deterministic (never LLM-judge-LLM).
- MAINTENANCE 2026-07-13: dependabot PRs #38 (SDK `claude-agent-sdk` 0.3.201→0.3.207) and #37 (dev-dependencies group, 3 updates) both MERGED squash to main (CI green Node 20/22), local `npm ci` + typecheck + suite re-verified green. No open PRs remain **[STALE — as of 2026-07-27 there are 5 open Dependabot PRs #42-#46, see the ⚠ signal at the top]**. Bumps land in `[Unreleased]` (no release cut for them).
- main pushed, clean tree, in sync with origin/main. Last tag **v0.7.0** — CUT & RELEASED 2026-07-12 (explicit approval): `[Unreleased]` frozen = WF-006 + WF-004 + WF-008 spines + ultrareview follow-ups + INVEST determiner-agnostic fix + the previously-undocumented SDK bump #30 (0.3.195→0.3.201) folded in; a fresh empty `[Unreleased]` recreated; `package.json` 0.6.0→0.7.0. Commit + annotated tag + GitHub Release all pushed and remote-verified (tag on origin, release not draft/prerelease). Suite 165 passed / 12 skipped, strict typecheck OK (2026-07-12). Never pin HEAD here (the commit that updates this line becomes the new HEAD, stale on arrival); anchor on the last real code change instead.
- WF-006 SPINE SHIPPED + PROVEN LIVE ON OPUS 4.8 — MERGED to main via PR #33 (squash `9595b40`, CI green Node 20/22). New deterministic backbone "Pre-sales / Commercial proposal" (STEP-01 CONSULTANT-IA → 02 BUSINESS-ANALYST → 03A AI-ARCHITECT → 04 CHEF-PROJET-IA → 05 FINANCIAL-ANALYST → 07 REDACTEUR-IA), sourced from the catalog workflow, on the UNCHANGED linear orchestrator. SAME Management & Consulting domain as WF-004 (does NOT open a new domain). Its value = the GO/NO-GO qualification gateway on STEP-01, the first OPENING business decision gate in the runtime. Billed live run 2026-07-11 23:46 (`LIVE_MODEL=claude-opus-4-8`, explicit approval): `completed`, 6/6 steps pass, 16/16 blocking + 14/14 advisory — the hardened `presales-go` gate (`verdictCode`/`rationale` split, normalized — after a first run's verbosely-phrased conditional-GO would have been false-negated by an exact-match gate) returned GO on a freelance-fit seed (≈€45k advisory/design scope, ~30-40 person-days via portage) and the whole pre-sales chain ran through to the commercial proposal. ADDITIVE proof to WF-008: WF-008 banked "a gate can say no" (returned), WF-006 banks the hardened OPENING gate saying yes on a legitimate GO + the chain completing to the deliverable. Trace versioned `docs/audit/live-runs/wf-006-live-result.json` (secret-scan clean, no model field). CHANGELOG [Unreleased] + live-runs README + docs/NEXT_STEPS.md snapshot updated. The stale WF-008 CHANGELOG bullet ("live run not yet performed") was aligned in the same PR to the already-versioned failed-by-design run. ONLY REMAINING = the five downstream showcase surfaces (separate private repo, distinct session).
- WF-004 SPINE SHIPPED + PROVEN LIVE ON OPUS 4.8 — MERGED to main via PR #32 (squash `9c343f3`, CI green Node 20/22). New deterministic backbone "AI Consulting Engagement" (6 core agents STEP-01→05,07: CONSULTANT-IA → FINANCIAL-ANALYST → CDO-DIRECTEUR-IA → CHANGE-MANAGER → FORMATEUR-IA → REDACTEUR-IA), sourced from the catalog workflow, on the UNCHANGED linear orchestrator. Opens the FOURTH live-proven domain, Management & Consulting (untouched by WF-001/002 Agile & Product, WF-003 Dev & Eng, WF-008 Compliance & Governance). Structurally a LINEAR TWIN OF WF-003 — no fail-closed structural construct (STEP-01 condition_passage = ordinary blocking eval gate; GDPR/AI-Act gateway + benchmark fork stay optional, outside the backbone). Files: src/spines/wf-004-conseil.ts + run-wf-004.ts; test/spine-wf-004.test.ts + run-wf-004.test.ts + run-wf-004-real-sidecar.test.ts + wf-004-run-live.test.ts + fixtures/wf-004-outputs.ts; CHANGELOG [Unreleased]. Real-sidecar test green against catalog v4.1.0 (6 agents resolve with real prose).
- WF-004 LIVE RUN DONE 2026-07-11 (Opus 4.8, billed, OAuth — FIRST billed run routed to Opus 4.8; earlier runs were Fable/default). LIVE_MODEL=claude-opus-4-8. Outcome: `completed`, 6/6 steps pass, ~8.1 min, $4 cap respected. All blocking AND advisory checks pass on a synthetic mid-cap-industrial engagement. Trace versioned `docs/audit/live-runs/wf-004-live-result.json` (git add -f, secret-scan clean, no model field). live-runs README + docs/NEXT_STEPS.md snapshot + CHANGELOG updated.
- WF-008 SPINE SHIPPED — MERGED to main via PR #31 (squash, CI green Node 20/22), surface (b) DONE. New deterministic backbone "AI Act / GDPR Compliance Audit": the 7 core agents + the counter-review gate (AUDIT-METHODO-IA), sourced from the catalog workflow. Two new fail-closed constructs on the EXISTING linear orchestrator (UNCHANGED): counter-review CLEARANCE gate on STEP-06C (verdict must be "cleared" — a "returned" verdict halts the spine before the report STEP-07, the first mid-spine return-for-rework gate) and the "Unacceptable" AI Act tier GATEWAY on STEP-01 (fail-closed). Files: src/spines/wf-008-audit.ts + run-wf-008.ts; test/spine-wf-008.test.ts + run-wf-008-real-sidecar.test.ts + wf-008-run-live.test.ts + fixtures/wf-008-outputs.ts; CHANGELOG [Unreleased]. Real-sidecar test green against catalog v4.1.0 (8 agents resolve with real prose, 8 pass verdicts).
- Task B (catalog sidecar widening) is DONE — the old WF-008 "sidecar blocker" is LIFTED. Verified live 2026-07-11: claude-agents v4.1.0 pushed (tag on origin b1a1551), sidecar = 75 assets / 38 agents / 37 skills, all 8 WF-008 agents present incl. AGENT-AUDIT-METHODO-IA.
- WF-008 LIVE RUN DONE 2026-07-11 (Fable, billed, OAuth). Outcome: `failed` fail-closed at STEP-06C BY DESIGN — STEP-01→06 all pass, the counter-review gate AUDIT-METHODO-IA returned the audit (`verdict="returned"`, 5 reservations + bias log + ISTQB exit criteria), STEP-07 halted. First live traversal of the counter-review gate + first trace showing the fail-closed halt BLOCKING a deliverable — a stronger proof than an all-green run. Trace versioned (secret-scan clean, no model field), README live-runs + docs/NEXT_STEPS.md updated. ~11.6 min, $4 cap respected, Fable weekly ~9%.
- Fable model routing SHIPPED + PROVEN LIVE (unchanged): RunWf00X.model + LIVE_MODEL env; WF-001 LIVE_MODEL=fable completed in 145 s (separate quota). INVEST criterion fix (determiner-agnostic) + P3 Fable trace closed (unchanged).

REMAINING (nothing urgent — lead with a recommendation):
0-BIS. WF-006 SHOWCASE UPDATES — the spine + live trace are MERGED to main (PR #33, squash `9595b40`, CI green Node 20/22); push/PR/merge is DONE. What remains = the FIVE downstream showcase surfaces in the separate (private) showcase repo — a distinct session (one product repo in write per session; this repo is PUBLIC, never name the private repo here). Frame WF-006 as a straightforward `completed` all-green run — the GO gate saying YES on a legitimate opportunity, the chain running to the commercial proposal — NOT the "returned for rework" framing (that is WF-008-specific). WF-006 shares WF-004's Management & Consulting domain, so it adds a BACKBONE proven live, not a new domain — let the showcase repo (which owns its own counts) do the exact arithmetic. Surfaces: workflows (live-count sentence + WF-006 status cell + WF-006 gallery Proof line), live-proofs (runs table + a WF-006 section), home (proof card count + ✓ list).
0. WF-004 SHOWCASE UPDATES — the spine + live trace are MERGED to main (PR #32, squash `9c343f3`, CI green Node 20/22); gate (a) push/PR/merge is DONE. What remains = the FIVE downstream showcase surfaces in the separate (private) showcase repo — a distinct session (one product repo in write per session; this repo is PUBLIC, never name the private repo here). For WF-004 frame it as a straightforward `completed` all-green run opening the fourth domain (Management & Consulting) on Opus 4.8 — NOT the "returned for rework" framing that is specific to WF-008. Surfaces: workflows (live-count sentence + WF-004 status cell + WF-004 gallery Proof line), live-proofs (runs table + a WF-004 section), home (proof card count + ✓ list).
1. WF-008 SHOWCASE UPDATES — the ONLY WF-008 work left. Prerequisite (a, Task B), the spine (b) AND the billed live
   run are all DONE. The billed live run ran 2026-07-11 on Fable: `failed` fail-closed at STEP-06C BY DESIGN — the
   counter-review gate AUDIT-METHODO-IA returned the audit (`verdict = "returned"`, 5 documented reservations + bias log
   + ISTQB exit criteria; STEP-01→06 all pass, STEP-07 halted). First live traversal of the counter-review gate, and the
   first trace showing the fail-closed halt BLOCKING a deliverable. Trace versioned (`docs/audit/live-runs/wf-008-live-result.json`,
   git add -f, secret-scan clean — no model field, 16 "secret" hits all audit vocabulary), live-runs README +
   docs/NEXT_STEPS.md snapshot updated.
   What remains = the FIVE downstream showcase surfaces, IN THE SEPARATE (private) SHOWCASE REPO — a distinct repo, so a
   distinct session (one product repo in write per session; this repo is PUBLIC, never name the private repo here):
   workflows (live-count sentence + WF-008 status cell + WF-008 gallery Proof line), live-proofs (runs table + a WF-008
   section), home (proof card count + ✓ list). Frame it HONESTLY as a "returned for rework" live proof (the gate can say
   no), NOT as a failed run. ⛔ Do NOT run the other seven workflows (each is a freshness liability pinned to a catalog
   version; "backbones proven live + the counter-review gate proven to block" beats an all-green 10/10 that reads as theater).
   DECIDED (do NOT re-propose) — the "full-cycle" narrative (returned -> cleared -> report in one run) is OUT OF SCOPE: it
   would require a bounded resume loop + context accumulation in the ORCHESTRATOR, i.e. reopening a live-proven spine for an
   OPTIONAL pedagogical gain — barred by feedback-ne-pas-modifier-workflow-prouve-live. The `returned` fail-closed halt IS the proof.
2. Scoped option, only if re-runs become frequent: SEMI-automated improvement loop (GateReport traces -> tightening proposals as PRs by Fable -> human gate by Guy). NEVER autonomous (Goodhart risk, would contradict deterministic gates ADR-0007 + fail-closed budget rule).
   MODEL ROUTING (best practice): design/scoping = Opus 4.8 (architecture, Goodhart/fail-closed stakes); in-loop proposer = Fable (generative + human-gated + separate quota); any pre-human verifier STAYS deterministic (regex/tests/schema) — never an LLM judging another LLM.

GUARDRAILS: push/merge/live-run = gates on explicit approval; live run = subscription OAuth only (never ANTHROPIC_API_KEY); artifacts/commits/PRs in US English, chat in French. Detail: memory project-runtime-wf003-live-proof-pending.

ADJACENT (other repo — do NOT open it in the same session): catalog claude-agents is at v4.1.0; Task B is CLOSED (sidecar widened to 38 agents + 37 skills, dependsOn populated, released v4.1.0 and pushed). Issue #16 (Q3 maintenance) CLOSED, actions/checkout already @v7 — do not redo. Still true: no dependabot.yml. See claude-agents/next_steps.md for its own state.
```

---

## Verified state — 2026-07-11

- **Repo/branch:** `guyhui01/claude-agentic-runtime` / `main`, in sync with `origin/main`, clean tree.
- **Release:** `v0.7.0` (last tag, cut & released 2026-07-12 — three live-proven backbones WF-004/006/008 + INVEST fix + SDK #30). HEAD is deliberately not pinned here — anchor on the last real code change, see the note in the resume block.
- **Tests:** 139 passed / 8 skipped (billed live harnesses + `runIf` markers), strict `typecheck` OK — re-run 2026-07-11.
- **Last delivery — WF-008 spine, merged to main (PR #31, squash, CI green Node 20/22); surface (b) done.** New deterministic backbone "AI Act / GDPR Compliance Audit" = the 7 core agents + the counter-review gate (`AGENT-AUDIT-METHODO-IA`), sourced from `claude-agents/workflows/WF-008-audit-conformite-ia-act-rgpd.md` (v1.0). Two constructs new to the runtime, both on the **unchanged** linear fail-closed orchestrator: (1) a **counter-review clearance gate** on STEP-06C (`audit-verdict-cleared` requires `verdict === "cleared"` — a `"returned"` verdict halts the spine before the report, the first mid-spine return-for-rework gate); (2) an **"Unacceptable" AI Act tier gateway** on STEP-01 (`jur-tier-not-unacceptable`, fail-closed). Optional STEP-04B/06B stay outside the backbone; the 02/03/04 parallel fork is linearized (WF-003 precedent). Files: `src/spines/wf-008-audit.ts` + `run-wf-008.ts`; `test/spine-wf-008.test.ts` + `run-wf-008-real-sidecar.test.ts` + `wf-008-run-live.test.ts` + `fixtures/wf-008-outputs.ts`; CHANGELOG `[Unreleased]`. Real-sidecar test green against catalog **v4.1.0** (8 agents resolve with real prose, 8 pass verdicts).
- **Live run — WF-008 proven live 2026-07-11 (Fable, billed, OAuth).** `failed` **fail-closed at STEP-06C by design**: STEP-01→06 all `pass`, then the counter-review gate `AUDIT-METHODO-IA` **returned the audit** (`verdict="returned"`, 5 documented reservations + 4-entry bias log + 6 ISTQB exit criteria; only blocking `audit-verdict-cleared` did not clear), so STEP-07 (report) was halted. **First live traversal of the counter-review gate, first trace showing the fail-closed halt blocking a deliverable.** Anonymized trace versioned `docs/audit/live-runs/wf-008-live-result.json` (git add -f, secret-scan clean, no model field); live-runs README + `docs/NEXT_STEPS.md` snapshot updated. ~11.6 min, $4 cap respected.
- **Prerequisite lifted — Task B done.** `claude-agents` `v4.1.0` pushed; sidecar widened to 75 assets (38 agents + 37 skills), all 8 WF-008 agents present (verified live 2026-07-11).
- **Earlier — SDK bump (dependabot #30, 2026-07-11):** `@anthropic-ai/claude-agent-sdk` 0.3.195 → 0.3.201, merged; suite green.
- **Earlier — INVEST fix + P3 closed (unchanged):** `po-us-format-invest` made determiner-agnostic; anonymized WF-001 Fable trace versioned; per-run model routing proven live (`LIVE_MODEL=fable` → `completed` in 145 s).

## Remaining (none urgent)

1. **Scoped option** — SEMI-automated improvement loop (traces → PR proposals by Fable → human gate). Never autonomous. Only if re-runs justify it.
   - **Model routing (best practice):** design/scoping on **Opus 4.8** (architecture, Goodhart / fail-closed stakes); the in-loop **proposer** runs on **Fable** (generative, human-gated, separate quota); any pre-human **verifier stays deterministic** (regex / tests / schema) — never an LLM judging another LLM (would reintroduce the non-determinism we refuse).

2. **Backlog — LIVE RUN of WF-008 (AI Act / GDPR Compliance Audit). Surface (c) — the only WF-008 work left. One run, not the remaining seven.**
   - **Why this one.** It opens a **third domain**: the three proven runs cover Agile & Product (WF-001, WF-002) and Dev & Engineering (WF-003) only, while Compliance & Governance / Management & Consulting had no live trace at all. It also exercises the **counter-review gate** (AI Methodology Auditor), never traversed by any run — the showcase describes this mechanism while no execution trace of it exists. So: a new capability, not a repetition.
   - **(a) and (b) are DONE.** (a) Task B — the sidecar now carries WF-008's 8 agents (catalog `v4.1.0`, verified live). (b) The WF-008 spine + eval criteria + **counter-review clearance gate** + live harness are shipped locally this session (see the "Last delivery" bullet above), on the **unchanged** linear orchestrator. Real-sidecar offline proof is green.
   - **(c) — billed live run DONE (2026-07-11, Fable); only the showcase surfaces remain.** The run ended `failed` **fail-closed at STEP-06C by design**: the counter-review gate `AUDIT-METHODO-IA` **returned the audit** (`verdict="returned"`, 5 documented reservations + bias log + ISTQB exit criteria; STEP-01→06 all pass, STEP-07 halted). First live traversal of the gate, first trace showing the fail-closed halt **blocking** a deliverable. Trace versioned (`docs/audit/live-runs/wf-008-live-result.json`, secret-scan clean, no model field), live-runs README + `docs/NEXT_STEPS.md` snapshot updated. What is left = the five downstream showcase surfaces below, in the separate (private) showcase repo — **frame it as a "returned for rework" proof (the gate can say no), NOT a failed run**.
   - **⛔ Do not run the other seven.** Each trace is a freshness liability pinned to a catalog version; ten traces triple a surface that expires on its own and proves nothing new. "Three/four proven live, the rest defined and available" is a stronger credibility signal than an all-green 10/10, which reads as theater.
   - **Gate.** A live run is a billed, outward-facing action: **explicit approval from Guy**, never inferred.
   - **Mandatory last step — update the downstream showcase site.** Five points, stated functionally (exact files and lines live in that repo's own tracker, which owns its paths):
     - workflows page — the live-run **count sentence** ("three backbones are proven live" → four).
     - workflows page — WF-008 **status cell** in the overview table (`Available` → live, n/n).
     - workflows page — WF-008 **gallery entry**, its `Proof` line.
     - live-proofs page — the **runs table**, plus a detailed WF-008 section.
     - home page — the **proof card**: its live-run count and its ✓ list.

## Guardrails

- push / merge / live-run = gates on **explicit approval**.
- Live run = **subscription OAuth only** (never `ANTHROPIC_API_KEY` — runner guard).
- **Cross-cutting — every live workflow run, and more generally any task whose result the downstream showcase site displays, carries "update the showcase" as its explicit last step**, naming the surfaces to touch. Not specific to WF-008: it applies to WF-001…WF-010 alike, and to any future proof surfaced downstream. This repo is the source of truth for runs and traces; the showcase only consumes them. Tracking the same task on both sides would create two sources of truth that diverge on the first update — so the downstream update lives inside the upstream task, or it is forgotten.
- **This repo is PUBLIC.** Before writing a cross-reference to another repository, read its **visibility**, not just its name (`gh repo view --json nameWithOwner,visibility`). Never name a private repo, its paths, or its file structure here: once pushed, it cannot be taken back without rewriting history.
- Files / commits / PRs in **US English**; chat in French.
- Detailed POC roadmap: [`docs/NEXT_STEPS.md`](docs/NEXT_STEPS.md). Session memory: `project-runtime-wf003-live-proof-pending`.
