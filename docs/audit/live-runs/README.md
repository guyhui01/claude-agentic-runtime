# Live-run traces — audit observability

**P3** remediation of the [ISO quality audit v1](../audit_qualite_iso_v1.md): **persistent** capture of live-run results (status, per-step verdicts, traced outputs), **instead of the original ephemeral `/tmp`**.

## How it works

- The `test/wf-001-run-live.test.ts` harness writes its result here (`wf-001-live-result.json`) by default — overridable via `LIVE_RESULT_FILE`.
- The **raw** `.json` is **gitignored** (see the local `.gitignore`): a raw agent output must not be committed without review.
- The **anonymized** trace of a conclusive run is committed **on decision**, after review (briefs/outputs free of real client data — see the project's anonymization rule), as a dated **audit artifact**.

## State

> ✅ **Versioned traces (conclusive live runs, billed and observed on explicit approval)**, P3 review OK (synthetic cases, no real client data or secret), committed via `git add -f`:
> - **`wf-001-live-result.json`** — WF-001 "Scoping", status **`completed`**, **3/3 steps `pass`** (STEP-01 BA → STEP-03 PO → STEP-04 QA). Live proof of **per-run model routing**: executed **2026-07-02** with `LIVE_MODEL=fable` in **145 s** (Fable separate-quota counter). All `blocking` checks pass; advisory `po-us-format-invest` = non-blocking FAIL (brittle INVEST regex, criterion revisit — not an agent defect). Note: the trace schema carries no model field (same as `wf-002/003`), so the Fable attribution is contextual (run env + this note), not in the payload.
> - **`wf-003-live-result.json`** — WF-003 "AI Application Launch" (live run 2026-06-13), status **`completed`**, **7/7 steps `pass`** (STEP-00 → STEP-06), budget cap respected. Validates live the "native structured output" fix at STEP-03.
> - **`wf-002-live-result.json`** — WF-002 "Delivery" (live run 2026-06-13), status **`completed`**, **5/5 steps `pass`**. An earlier run from the same day (before the session commits, including the STEP-03 fix): a valid historical artifact, not re-played post-fix.
> - **`wf-008-live-result.json`** — WF-008 "AI Act / GDPR Compliance Audit" (live run **2026-07-11**, `LIVE_MODEL=fable`, Fable separate-quota counter), status **`failed` — fail-closed at STEP-06C, by design, not a defect**. STEP-01 → STEP-06 all **`pass`** (6/6); the independent **counter-review gate** `AGENT-AUDIT-METHODO-IA` **returned the audit for rework**: `verdict = "returned"` with **5 documented reservations** + a 4-entry bias log + 6 ISTQB exit criteria (only the blocking `audit-verdict-cleared` did not clear), so the final report (STEP-07) was **halted before production** — exactly the "never cleared by default" return-for-rework behavior this workflow was chosen to exercise. First live trace of the counter-review gate, and the first that shows the fail-closed halt actually **blocking** a deliverable (a stronger signal than an all-green run: the gate can say *no*). Budget cap ($4.0) respected, ~11.6 min. Trace schema carries no model field (Fable attribution contextual). The 5 reservations attack methodological rigor only (PROSCI ADKAR / ISTQB / ISO 19011/42001), never the legal substance — the AUDIT-METHODO-IA scope boundary held live.
> - **`wf-004-live-result.json`** — WF-004 "AI Consulting Engagement" (live run **2026-07-11**, `LIVE_MODEL=claude-opus-4-8` — explicit Opus 4.8 routing, the workflow's `modele_recommande`), status **`completed`**, **6/6 steps `pass`** (STEP-01 CONSULTANT-IA → STEP-02 FINANCIAL-ANALYST → STEP-03 CDO-DIRECTEUR-IA → STEP-04 CHANGE-MANAGER → STEP-05 FORMATEUR-IA → STEP-07 REDACTEUR-IA). Opens the **fourth domain, Management & Consulting** — untouched by WF-001/002 (Agile & Product), WF-003 (Dev & Engineering) and WF-008 (Compliance & Governance). Structurally a **linear twin of WF-003** (no fail-closed structural construct: STEP-01's `condition_passage` is an ordinary blocking eval gate; the GDPR/AI-Act gateway and the benchmark fork stay optional, outside the backbone). All blocking **and** advisory checks pass on a synthetic mid-cap-industrial engagement (maturity 3.5/10, 6 use cases, full ADKAR + 4 training levels, 15-slide ComEx deck). Budget cap ($4.0) respected, ~8.1 min. Trace schema carries no model field (Opus 4.8 attribution contextual: run env + this note).
> - **`wf-006-live-result.json`** — WF-006 "Pre-sales / Commercial proposal" (live run **2026-07-11**, `LIVE_MODEL=claude-opus-4-8` — the workflow's recommended model), status **`completed`**, **6/6 steps `pass`** (STEP-01 CONSULTANT-IA → STEP-02 BUSINESS-ANALYST → STEP-03A AI-ARCHITECT → STEP-04 CHEF-PROJET-IA → STEP-05 FINANCIAL-ANALYST → STEP-07 REDACTEUR-IA). Same **Management & Consulting** domain as WF-004 (does not open a new domain); its value is the **GO/NO-GO qualification gateway on STEP-01 proven live in the GO direction**. Seeded with a freelance-fit opportunity (≈€45k advisory/design scope, ~30-40 person-days via portage), the hardened `presales-go` gate (the `verdictCode`/`rationale` split, normalized — hardening `80d7bfc`, after a first run's verbosely-phrased conditional-GO would have been false-negated) returned **GO** and let the full pre-sales chain run through to the commercial proposal (STEP-07). All checks pass — **16/16 blocking + 14/14 advisory**. This is the **additive** proof complementing WF-008's returned-for-rework halt: WF-008 banked "a gate can say *no*"; WF-006 banks the hardened opening gate saying *yes* on a legitimate GO and the chain completing to the deliverable. Trace schema carries no model field (Opus 4.8 attribution contextual: run env + this note).
> - **`wf-009-live-result.json`** — WF-009 "IT/AI Recruitment" (live run **2026-07-13**, `LIVE_MODEL=claude-sonnet-5` — the workflow's recommended model), status **`completed`**, **6/6 steps `pass`** (STEP-01 BUSINESS-ANALYST → STEP-02A CONSULTANT-IA → STEP-03 REDACTEUR-IA → STEP-04/05/06 RH-IA). Opens the **fifth domain, HR & Talent**, end-to-end — untouched by WF-001/002 (Agile & Product), WF-003 (Dev & Engineering), WF-004/006 (Management & Consulting) and WF-008 (Compliance & Governance). Made possible by the `candidatePool` **passthrough**: candidate CVs are exogenous to the workflow and the linear orchestrator only routes data via the previous step's output, so a **synthetic, fictional** pool of 5 candidates is carried **unchanged** through STEP-01→03 to reach the RH sourcing step (STEP-04) — the orchestrator stays **unchanged** (a deliberate one-field-per-hop deviation; see `src/spines/wf-009-recrutement.ts`). ⚠️ **Synthetic by design (RGPD):** the pool is fictional — a real CV would be personal data published with no legal basis in a public, versioned trace — so this is a `completed` run **on a synthetic scenario with a provided candidate pool**, NOT autonomous sourcing. The scoring is substantive, not hollow: the agent independently scored the seeded weak profile **CAND-04 at 20/100** ("3 of 4 must-haves missing") and excluded it, shortlisted **4 real candidates**, and selected **CAND-05** (highest fit, 91/100) — a *different* pick than the fixture's, i.e. genuine reasoning rather than an echo. The same `rh-shortlist-validated` gateway that **halts** on a no-candidate refusal (see `wf-009-live-result-gate-halt.json`) here **passes on real substance** — together the two traces prove the gate **discriminates** (says *no* and *yes* correctly), the anti-theater centerpiece. Budget cap ($4.0) respected, ~6.5 min. Trace schema carries no model field (Sonnet 5 attribution contextual: run env + this note).
> - **`wf-009-live-result-gate-halt.json`** — WF-009 "IT/AI Recruitment", **companion "gate says *no*" trace** (live run **2026-07-12**, `LIVE_MODEL=claude-sonnet-5`), status **`failed` — fail-closed at STEP-04, by design**. Same spine, a seed with **no candidate CVs**: STEP-01 → STEP-03 all **`pass`** (recruiter-side deliverables needing no CVs); at STEP-04 the sourcing agent **honestly reported none** ("No candidate data available"), so the **hardened** candidate-shortlist gateway `rh-shortlist-validated` (which counts REAL candidates, ≥ 3, not placeholders) **halted**. Validates the gateway-hardening fix live: on the FIRST run (before hardening) the identical honest refusal wrongly PASSED (`nonEmptyString`/`minArrayLen` fooled by placeholder content → a hollow `completed`, not versioned); the hardened gate now fails closed. A "gate says *no*" proof on the **candidate-selection** decision (distinct from WF-008's counter-review gate). Preserved alongside the end-to-end `completed` run (`wf-009-live-result.json`): the SAME gateway saying *no* here and *yes* there is the proof it discriminates. Budget cap respected, ~3.8 min. Trace schema carries no model field (Sonnet 5 attribution contextual).
> - **`wf-005-live-result.json`** — WF-005 "Strategic Intelligence & Growth" (live run **2026-07-13**, `LIVE_MODEL=claude-opus-4-8`), status **`completed`**, **3/3 steps `pass`** (STEP-01 VEILLE-STRATEGIQUE → STEP-02 GROWTH-IA → STEP-03 REDACTEUR-IA). The runtime's **lightest backbone** (3 agents) and a **third** live-proven backbone in the **Management & Consulting** domain (with WF-004/006 — it adds a backbone, not a new domain). All blocking **and** advisory checks pass on a synthetic GenAI/agentic weekly-intelligence brief: **7 highlights** ranked by impact, 6 weak signals + 2 tools to watch, **3 topics** (LinkedIn post / article / newsletter) with editorial angles + 15 hashtags, a **3,750-char** Markdown synthesis, and **3** ready-to-publish LinkedIn posts. **Model-routing note — a deliberate deviation from the catalog's `modele_recommande` (`claude-sonnet-5`):** a first billed **Sonnet 5** run also returned `completed` 3/3 with substantive content, but it (a) **wrote a plan file** into the run environment (`~/.claude/plans/`, despite the runner's forced `permissionMode:"plan"`) and (b) leaked **plan-mode meta-commentary** into the *optional*, non-gated `internalNote` field (with a real name + a local path) — **process-hygiene** defects, not deliverable-quality ones (the deliverables passed every gate). Re-routed to **Opus 4.8** for a clean audit trace: **no disk write**, and a legitimate `internalNote` (a pre-publish factual-check note). The behavior is **model-dependent** (the two earlier Opus 4.8 runs — WF-004/006 — left no plan file; both Sonnet 5 runs did): the process cleanliness, not the deliverable content, drove the routing. Budget cap ($4.0) respected, ~3.1 min. Trace schema carries no model field (Opus 4.8 attribution contextual: run env + this note).
>
> P3 (mechanism **and** artifacts) is now handled by real executions, never a manual reconstruction. The "full-throttle" run of **2026-06-09** preceded this mechanism and its `/tmp` capture had not survived.
