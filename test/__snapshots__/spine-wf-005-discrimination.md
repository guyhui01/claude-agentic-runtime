# WF-005 eval-gate discrimination matrix

Generated by `test/spine-wf-005-discrimination.test.ts`. Read the file header for method
and the findings F1–F4. This is a MEASURED record, not a fix: it records which blocking
criteria are the sole runtime gate (UNIQUE) and which merely double the manifest output
schema enforced at the handoff, or a sibling criterion (REDUNDANT).

Runtime order (run-spine.ts): runner → **eval gate (criteria)** → **handoff (schema)**.
A criterion is REDUNDANT when, removed, the same bad output is still caught — one step
later, with a different failure `kind`.

## Load-bearing classification (measured by removal)

| Blocking criterion | Class | Removed → | Caught by |
|---|---|---|---|
| `veille-highlights-floor` | REDUNDANT | failed | handoff (output schema) |
| `veille-highlights-ranked` | UNIQUE | completed | — (nothing: real hole) |
| `veille-weak-signals` | REDUNDANT | failed | handoff (output schema) |
| `growth-topics-floor` | REDUNDANT | failed | eval-gate (sibling) |
| `growth-topics-actionable` | UNIQUE | completed | — (nothing: real hole) |
| `red-synthesis` | UNIQUE | completed | — (nothing: real hole) |
| `red-linkedin-floor` | REDUNDANT | failed | handoff (output schema) |

## Sub-clause falsification (Axis 1 — no `/.+/`)

Each clause below, mutated alone on the DoD-happy output, reddens its criterion.

| Criterion | Clause | Reddens |
|---|---|---|
| `veille-highlights-floor` | highlights:2 (below floor 3 AND schema min 3) | yes |
| `veille-highlights-ranked` | a highlight with title:'' (schema types it string, not non-empty) | yes |
| `veille-highlights-ranked` | a highlight with impact:'' | yes |
| `veille-weak-signals` | weakSignals:[] (below min 1) | yes |
| `growth-topics-floor` | topics:[] (below min 1) | yes |
| `growth-topics-actionable` | a topic with format:'' (schema types it string, not non-empty) | yes |
| `growth-topics-actionable` | a topic with angle:'' | yes |
| `red-synthesis` | synthesis:'' | yes |
| `red-linkedin-floor` | linkedinPosts:[] (below min 1) | yes |

**3 of 7 blocking criteria are UNIQUE (sole gate); 4 are REDUNDANT.**
