# WF-007 eval-gate discrimination matrix

Generated by `test/spine-wf-007-discrimination.test.ts`. Read the file header for method
and the findings F1–F4. This is a MEASURED record, not a fix: it records which blocking
criteria are the sole runtime gate (UNIQUE) and which merely double the manifest output
schema enforced at the handoff, or a sibling criterion (REDUNDANT).

Runtime order (run-spine.ts): runner → **eval gate (criteria)** → **handoff (schema)**.
A criterion is REDUNDANT when, removed, the same bad output is still caught — one step
later, with a different failure `kind`.

## Load-bearing classification (measured by removal)

| Blocking criterion | Class | Removed → | Caught by |
|---|---|---|---|
| `cp-kickoff-plan` | REDUNDANT | failed | handoff (output schema) |
| `cp-raci` | REDUNDANT | failed | handoff (output schema) |
| `ba-client-context` | UNIQUE | completed | — (nothing: real hole) |
| `ba-is-mapping` | REDUNDANT | failed | handoff (output schema) |
| `cm-stakeholder-map` | REDUNDANT | failed | handoff (output schema) |
| `cm-engagement-plan` | REDUNDANT | failed | handoff (output schema) |
| `red-d1-kit` | UNIQUE | completed | — (nothing: real hole) |
| `red-intro-email` | UNIQUE | completed | — (nothing: real hole) |
| `red-report-template` | UNIQUE | completed | — (nothing: real hole) |
| `red-d5-scoping` | UNIQUE | completed | — (nothing: real hole) |

## Sub-clause falsification (Axis 1 — no `/.+/`)

Each clause below, mutated alone on the DoD-happy output, reddens its criterion.

| Criterion | Clause | Reddens |
|---|---|---|
| `cp-kickoff-plan` | kickoffPlan:[] (below min 1) | yes |
| `cp-raci` | raci:[] (below min 1) | yes |
| `ba-client-context` | clientContext:'' | yes |
| `ba-is-mapping` | isMapping:[] (below min 1) | yes |
| `cm-stakeholder-map` | stakeholderMap:[] (below min 1) | yes |
| `cm-engagement-plan` | engagementPlan:[] (below min 1) | yes |
| `red-d1-kit` | d1Kit:'' | yes |
| `red-intro-email` | introEmail:'' | yes |
| `red-report-template` | d1ReportTemplate:'' | yes |
| `red-d5-scoping` | d5ScopingNote:'' | yes |

**5 of 10 blocking criteria are UNIQUE (sole gate); 5 are REDUNDANT.**
